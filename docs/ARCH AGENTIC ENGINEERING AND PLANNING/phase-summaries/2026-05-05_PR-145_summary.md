# PR #145 Summary - Dashboard Validation Bundle Ingestion

## Scope
- Added `/api/validation_history` to the dashboard server.
- Added validation-bundle discovery for `validation_summary.json` artifacts under `experiment_runs`.
- Extended the Campaign History tab with latest validation pass/fail state and recent bundle rows.
- Updated roadmap queue status.

## Validation
- `python -m unittest test_dashboard_server.py -v`
- `ruff check dashboard_server.py test_dashboard_server.py`
- `git diff --check`

## Decision
Reporting-only slice. No runtime defaults or promotion policy changed.
