# Phase Summary - Evidence Chain Dashboard Ingestion

- PR/branch: PR `#156` / `codex/evidence-chain-dashboard-ingestion`
- Status: complete
- Queue item: rank 27, Evidence chain dashboard ingestion

## What changed
- Added `load_evidence_chain_history()` and `/api/evidence_chain_history`.
- Added Campaign History dashboard cards and table rows for evidence-chain pass/fail, review status, artifact count, blockers, and report path.

## Validation
- `python -m unittest test_dashboard_server.py -v`
- `ruff check dashboard_server.py test_dashboard_server.py`
- `git diff --check`

## Result
- Operators can inspect latest evidence-chain summaries from the dashboard without reading `experiment_runs` JSON manually.

## Next
- Rank 28: document evidence artifact retention and regeneration policy.
