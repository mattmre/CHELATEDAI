# Phase Summary - Preflight Dashboard Ingestion

- PR/branch: PR `#150` / `codex/preflight-dashboard-ingestion`
- Status: complete
- Queue item: rank 21, Preflight dashboard ingestion

## What changed
- Added `load_preflight_history()` and `/api/preflight_history` to `dashboard_server.py`.
- Added compact normalization for default-promotion preflight reports, including blockers and artifact counts.
- Added Campaign History tab cards and a table for the latest preflight decisions.

## Validation
- `python -m unittest test_dashboard_server.py -v`
- `ruff check dashboard_server.py test_dashboard_server.py`
- `git diff --check`

## Result
- Operators can inspect latest default-promotion preflight status and blockers from the dashboard API/UI instead of reading `experiment_runs` manually.

## Next
- Rank 22: add a CI/local affordance that runs the evidence chain and preserves the fail-closed preflight status.
