# PR #142 Summary - Dashboard Campaign History

## Scope
- Added `/api/campaign_history` to `dashboard_server.py`.
- Added compact report discovery for campaign report JSON files under `experiment_runs`.
- Added a Campaign History dashboard tab with report count, promotion count, overlay-ready count, and latest report rows.
- Updated the roadmap queue to mark dashboard campaign history complete and move promotion-contract artifact-card linkage to next.

## Validation
- `python -m unittest test_dashboard_server.py -v`
- `ruff check dashboard_server.py test_dashboard_server.py`
- `git diff --check`

## Decision
Reporting-only slice. This does not change runtime defaults or promotion policy.
