# Phase Summary - Default Promotion Preflight

- PR/branch: PR `#149` / `codex/default-promotion-preflight`
- Status: complete
- Queue item: rank 20, Default-promotion evidence preflight

## What changed
- Added `default_promotion_preflight.py` and the `chelatedai-default-promotion-preflight` script entry.
- The command reads the latest validation bundle, promotion-linkage audit, and repeat-seed decision artifacts.
- The preflight is fail-closed: it can allow a review to start only when all evidence passes, but it never changes defaults.

## Validation
- `python -m unittest test_default_promotion_preflight.py -v`
- `ruff check default_promotion_preflight.py test_default_promotion_preflight.py`
- `git diff --check`
- `python run_overlay_model_scope_validation.py --output-dir experiment_runs/overlay-model-scope-validation/latest --timeout-seconds 300`
- `python audit_promotion_linkage.py --root experiment_runs --output experiment_runs/promotion-linkage-audit/latest/audit.json`
- `python attnres_repeat_seed_decision.py --output experiment_runs/attnres-repeat-seed-decision/latest/decision.json`
- `python default_promotion_preflight.py --output experiment_runs/default-promotion-preflight/latest/preflight.json` exited `1` as expected because repeat-seed evidence blocks default-promotion review.

## Result
- Validation bundle: passed.
- Promotion-linkage audit: passed.
- Repeat-seed decision: no default change.
- Default-promotion review: blocked by `repeat_seed_evidence_does_not_support_default_promotion`.

## Next
- Rank 21: surface the latest preflight decision and blockers in the dashboard campaign/history API.
