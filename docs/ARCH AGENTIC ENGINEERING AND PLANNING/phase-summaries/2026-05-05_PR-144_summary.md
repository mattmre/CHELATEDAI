# PR #144 Summary - Long-Run Validation Bundle

## Scope
- Added `run_overlay_model_scope_validation.py` as a single-command focused validation bundle.
- The bundle runs focused overlay/model-scope/promotion/AttnRes tests plus the deterministic Model-Scope overlay smoke command.
- The bundle writes `validation_summary.json` and exits nonzero if any command fails.
- Added unit coverage for summary writing and fail/pass aggregation.
- Added a console script entrypoint and updated the roadmap queue.

## Validation
- `python -m unittest test_overlay_model_scope_validation.py -v`
- `python run_overlay_model_scope_validation.py --output-dir experiment_runs/overlay-model-scope-validation/latest --timeout-seconds 300`
- `ruff check run_overlay_model_scope_validation.py test_overlay_model_scope_validation.py`
- `git diff --check`

## Decision
Validation-only slice. No runtime defaults or promotion policy changed.
