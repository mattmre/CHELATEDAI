# Day Goal — 2026-06-13 (Codex run instructions)

Mission: turn last night's mixed campaign result into a diagnosed, calibrated, fully
reported experiment. Three slices in strict order (PR-6 diagnostics, PR-7 severity
calibration, PR-8 C3 knob sweep), one stretch (PR-9). Same BHS discipline as last night:
every slice fully implemented, fresh adversarial Tier B to a genuine 100, CI green,
merged before the next slice. Same hard rules as
`goal-overnight-2026-06-12.md` (pull main first; no fabricated numbers; report what the
data says; never commit/push `docs/waypoint-research-2026-06-09/`; don't touch parked
PRs #256/#257).

## Why (context from the morning review — verify, don't assume)

Last night's table (docs/drift-recovery-results-2026-06.md) shows three suspected
problems. Your diagnostics must CONFIRM OR REFUTE each from the artifacts — these are
hypotheses, not conclusions:

- H1 (rotation too weak): all conditions including frozen C0 "recover" at mean cycle
  1.00 — the ~1.7% NDCG drop never left the 95% recovery threshold, so rotation(0.5,25°)
  cannot discriminate conditions.
- H2 (correction barely moves): C3 mean correction norm is exactly 0.010000 (looks like
  saturation at the BoundedAdapter cap); C4 unbounded is 0.000189 (near-zero learning
  signal). Determine from per-cycle artifact fields: did detection fire each cycle? Was
  correction_applied true? What are per-cycle norms?
- H3 (C2 is an oracle): C2 final NDCG 0.829212 is identical across both drift modes and
  presumably equals the pre-drift baseline — re-embedding unchanged raw text with the
  frozen model undoes synthetic vector drift by construction.

## Slice 1 — PR-6: diagnostics over existing artifacts

New `analyze_drift_recovery.py` (+ unittest) that reads
`experiment_runs/drift-recovery/*.json` and emits a diagnostics JSON + markdown:

1. Per-run baseline NDCG vs final per condition (confirm/refute H3: does C2 return
   exactly to baseline in every run?).
2. Per-cycle C3/C4 detection + correction trace: drift signal value, should_correct,
   sedimentation_attempted, correction_applied, correction norm per cycle (H2).
3. Severity analysis: baseline drop per drift mode vs the 0.95 recovery threshold (H1).
4. Plot: per-cycle NDCG trajectories per condition (mean across seeds), one per mode.

Then update `docs/drift-recovery-results-2026-06.md`: add baseline column; relabel C2
as "oracle re-embed upper bound" WITH the justification (only if H3 confirmed by the
numbers); add a Diagnostics section summarizing H1/H2 findings with file references.
No spin: if a hypothesis is refuted, say so.

## Slice 2 — PR-7: severity calibration campaign

Goal: find drift settings in the "discriminating zone" — baseline NDCG drop of roughly
8–20% (deep enough that recovery is non-trivial, shallow enough that correction is
plausible).

1. Scout (cheap): C0-only, seed 42, rotation angle {35, 50, 65}° × fraction {0.5, 0.75};
   noise sigma {0.01, 0.02, 0.035} × fraction 0.5. Record baseline drop for each.
2. Pick 1 rotation + 1 noise setting inside the zone (document the choice rule BEFORE
   looking: closest to 12% drop wins; if none lands in 8–20%, take the closest and say
   so).
3. Full matrix at the chosen settings: all 5 conditions × 3 seeds {42, 1337, 7},
   `--max-queries 100 --sample-docs 1200 --cycles 12`. ~30 runs + ~9 scout runs.
4. Artifacts under `experiment_runs/drift-recovery/calibrated/`; extend the results doc
   with a Calibrated Severity section (same column spec + baseline column). Report every
   scout cell, not just the chosen ones.

## Slice 3 — PR-8: C3 knob sweep at calibrated severity

1. Harness extension (small, tested): expose `--bound-epsilon`, `--trigger-threshold`,
   `--max-temperature`, plus an epochs-scale knob if the annealing controller supports
   it. Default behavior must remain byte-identical to current (regression test: same
   seed, no new flags → identical trajectory to pre-change harness).
2. Pre-registered grid at the calibrated ROTATION setting only, C3, seed 42:
   bound-epsilon {0.01, 0.05, 0.10} × trigger-threshold {0.05, 0.15} ×
   temperature/LR {default, hotter}. 12 cells. Report ALL cells in the results doc.
3. Take the top-2 cells by final NDCG and re-run with all 3 seeds (6 runs) alongside the
   existing C0/C2 calibrated references. The 3-seed result is the reportable number;
   the 12-cell grid is disclosed as exploratory. This is a sensitivity analysis with
   full reporting — NOT tune-until-win, because every cell is published and the
   confirmation uses held-out seeds.

## Stretch — PR-9 (only if 1–3 are merged): model-swap drift prototype

The arena where the C2 oracle trick fails by construction: drift = re-embedding half the
corpus with `all-mpnet-base-v2` (768-dim) through `DimensionProjection` while queries
stay on `all-MiniLM-L6-v2`. C2's re-embed cannot undo it (re-embedding IS the drift).
Implement as a third injector mode or a harness pre-step (whichever is less invasive),
tiny test, then a mini-matrix: {C0, C2, C3} × seed 42. This is the experiment the paper
wants most — but do not rush it; if it doesn't fit today, leave it cleanly unstarted.

## End of day (regardless of progress)

Append a "Day 2026-06-13" section to
`docs/waypoint-research-2026-06-09/review-notes-cleanup-pass.md` with: PRs merged with
hashes, H1/H2/H3 verdicts with one-line evidence each, the chosen calibrated settings
and their baseline drops, knob-sweep table summary (all 12 cells), 3-seed confirmation
numbers vs C0/C2, stretch status, and blockers.

## Success criteria

Floor: PR-6 merged + H1/H2/H3 answered from artifacts. Target: PR-7 and PR-8 merged with
the calibrated 3-seed C3 numbers (win or lose, fully reported). Stretch: PR-9 mini-matrix.
The paper consumes whichever story the data supports: "calibrated closed-loop recovery"
or "rigorous negative result with mechanism diagnosis" — both are publishable; only
honesty is non-negotiable.
