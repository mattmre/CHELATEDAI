OpenAI Codex v0.144.1
--------
workdir: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019f4a00-5393-7f30-b184-796de1122bfc
--------
user
# Codex build task — D3: the recoverability ESTIMATOR (P1, locked plan)

Build the calibrated recoverability **estimator** in a new `research/drift_recovery/estimator/`
package, on top of the frozen D1 packs and `harness_bridge.py`. This is P1 in the locked
agreed-build-plan-v1.md. It is an *estimator*, NOT a "certificate"/"theorem": Gram-distortion alone
≠ NDCG, and top-k retrieval breaks the clean free-alignment theorems, so the deliverable is a
calibrated predictor with an honest validation verdict — including an explicit **negative/scoping
note** if the data cannot support cross-regime validation.

Work ONLY inside `research/drift_recovery/`. Do NOT touch `docs/.../paper-draft/`. Reuse
`harness_bridge.py` as the ONLY harness importer; reuse `EmbeddingPack` (artifacts.py) and the
`stats/` primitives. Keep harness parity ≤1e-12. GPU is available (torch cuda); run offline
(HF_HUB_OFFLINE=1, HF_DATASETS_OFFLINE=1), sequential, modest — do not OOM the 3090 (shared machine).

## What the estimator predicts
Given a drift regime represented by a frozen pack (cached doc vectors `Do`, oracle-target doc
positions `Dor`, drifted eval queries `Qd`, qrels), predict the **oracle-gap recovery R̂** that a
*trivial regularized linear corrector* (ridge) will achieve — WITHOUT running the corrector's full
eval — and emit a calibrated lower band. The ground-truth R for each regime is the D1-measured ridge
recovery on that pack.

## Core signal (margin-bound features)
For each eval query q with relevant doc y_r and top competing non-relevant doc y_j, retrieval order
is preserved under a corrector g iff the **margin**
  m(q) = qᵀ g(y_r) − qᵀ g(y_j)  >  0,
and the identity/no-correction error decomposes via e_d = g(y_d) − y_d* (y_d* = oracle position, i.e.
`Dor`). The plan's bound: order is preserved when
  qᵀy_r* − qᵀy_j*  >  ‖q‖(‖e_r‖ + ‖e_j‖).
Build features per query from the pack:
1. **Gram/margin distribution** — per-query clean margin qᵀy_r* − max_j qᵀy_j* (oracle space), its
   sign rate, and low-quantiles across the eval set.
2. **Correction-error magnitude** — ‖e_d‖ distribution for the ridge map fit on the pack's `fit_idx`
   (leakage contract: NEVER use eval qrels in the fit; use the pack's leakage-safe fit index for the
   estimator's own fit-side features).
3. **Predicted inversion rate** — fraction of eval queries where the bound is violated
   (‖q‖(‖e_r‖+‖e_j‖) ≥ clean margin) → maps to a predicted recovery band.

## Package layout (`research/drift_recovery/estimator/`)
- `features.py` — per-query & per-regime feature extraction from an `EmbeddingPack` (margins, error
  norms, inversion-rate estimate). Pure numpy; no harness import (operates on frozen packs).
- `margin_bound.py` — the order-preservation bound + per-query inversion predictor; unit-tested on a
  tiny synthetic pack with a hand-checkable margin.
- `calibration.py` — cross-fitted map from features → R̂ + a lower band (isotonic or a small linear
  calibrator; cross-fit so no regime's own R trains its own prediction).
- `predictor.py` — `estimate_recovery(pack) -> {R_hat, lower_band, features}`.
- `validation.py` — **leave-one-regime-out** validation across the available packs. Held-out axis =
  regime (encoder family / dataset / n_anchor), NEVER same-run (that is circular). Reports
  Spearman(R̂, R) and 3-way bin accuracy (low/med/high recovery).

## Regimes (data)
The estimator needs ≥3–4 regimes for leave-one-regime-out. `scifact_evalsplit_pack` already exists
(SciFact, MiniLM→mpnet). Freeze the additional regimes you can via `harness_bridge` **if the cached
models are available offline**: NFCorpus/MiniLM→mpnet, SciFact/MiniLM→bge-large, NFCorpus/MiniLM→bge-
large (and optionally a different `anchor_fraction` on SciFact as a 5th regime). Each new pack MUST
reproduce its ridge recovery through the SAME NDCG implementation (assert_harness_parity ≤1e-12) and
carry a leakage-safe fit index. **If a model/dataset is not cached offline and cannot be fetched, do
NOT fabricate the regime** — log it and proceed with whatever regimes exist.

## Success bar (locked)
- **Ship-positive** iff leave-one-regime-out Spearman(R̂, R) ≥ 0.7 OR 3-way bin accuracy ≥ 80% across
  ≥3 held-out regimes.
- **Otherwise ship an explicit negative/scoping note**: report the achieved Spearman/accuracy, how
  many regimes were available, and precisely why the data is insufficient (e.g. too few regimes to
  validate cross-regime; the per-query bound predicts inversion but the feature→R map is not
  calibratable at n regimes). A well-scoped negative is an acceptable, honest deliverable here — do
  NOT inflate a 2-regime fit into a validated estimator.

## Deliverables
1. `research/drift_recovery/estimator/` package (files above), unit-tested.
2. `research/drift_recovery/out/estimator/` artifacts: per-regime feature JSON, the frozen packs you
   built (or a manifest of which regimes were available vs skipped-and-why), and
   `estimator_validation.md` with the Spearman/bin-accuracy table + the positive-or-negative verdict.
3. `tests/test_estimator.py`: margin-bound correctness on a synthetic pack, feature determinism,
   leakage guard (estimator fit never sees eval qrels), and a leave-one-regime-out smoke test.
4. Run the test suite; paste pass/fail. Regenerate no D1 artifact; D3 is additive.
5. A `research/drift_recovery/out/estimator/D3_REPORT.md` (≤1 page): what was built, regimes used,
   the verdict, and the single most important limitation.

Be brutally honest. If only 1–2 regimes are cheaply available, say so and deliver the scoping-negative
rather than a circular same-run "validation." Report the smallest concrete thing that does not work.


warning: Skill descriptions were shortened to fit the 2% skills context budget. Codex can still see every skill, but some descriptions are shorter. Disable unused skills or plugins to leave more room for the rest.
hook: SessionStart
hook: SessionStart Completed
codex
I’m starting from the live checkout and the frozen D1 contracts. I’ll use the `python-testing-patterns` skill because the deliverable hinges on leakage, determinism, parity, and leave-one-regime-out tests; I’ll keep every write under `research/drift_recovery/` and treat unavailable offline regimes as explicit skips, not synthetic data.
  → Inspect repo rules, D1 pack/harness/stats contracts, existing artifacts, and relevant prior CHELATEDAI notes
  • Implement estimator package and focused tests inside research/drift_recovery
  • Discover/build offline regimes, assert parity, and emit estimator artifacts
  • Run focused and broader validation, then finalize D3 report with honest verdict
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "Get-Content -Raw 'C:\\Users\\mattm\\.codex\\skills\\python-testing-patterns\\SKILL.md'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "rg -n -i \"CHELATEDAI|drift-recovery|severity-calibration|PR #266|harness_bridge\" 'C:\\Users\\mattm\\.codex\\memories\\MEMORY.md'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "Get-ChildItem -Force; rg --files -g 'AGENTS.md' -g 'CLAUDE.md' -g '"'!**/.git/**'"'; git status --short --branch" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'rg --files research/drift_recovery' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 998ms:
---
name: python-testing-patterns
description: Implement comprehensive testing strategies with pytest, fixtures,
  mocking, and test-driven development. Use when writing Python tests, setting
  up test suites, or implementing testing best practices.
---

# Python Testing Patterns

Comprehensive guide to implementing robust testing strategies in Python using pytest, fixtures, mocking, parameterization, and test-driven development practices.

## When to Use This Skill

- Writing unit tests for Python code
- Setting up test suites and test infrastructure
- Implementing test-driven development (TDD)
- Creating integration tests for APIs and services
- Mocking external dependencies and services
- Testing async code and concurrent operations
- Setting up continuous testing in CI/CD
- Implementing property-based testing
- Testing database operations
- Debugging failing tests

## Core Concepts

### 1. Test Types

- **Unit Tests**: Test individual functions/classes in isolation
- **Integration Tests**: Test interaction between components
- **Functional Tests**: Test complete features end-to-end
- **Performance Tests**: Measure speed and resource usage

### 2. Test Structure (AAA Pattern)

- **Arrange**: Set up test data and preconditions
- **Act**: Execute the code under test
- **Assert**: Verify the results

### 3. Test Coverage

- Measure what code is exercised by tests
- Identify untested code paths
- Aim for meaningful coverage, not just high percentages

### 4. Test Isolation

- Tests should be independent
- No shared state between tests
- Each test should clean up after itself

## Quick Start

```python
# test_example.py
def add(a, b):
    return a + b

def test_add():
    """Basic test example."""
    result = add(2, 3)
    assert result == 5

def test_add_negative():
    """Test with negative numbers."""
    assert add(-1, 1) == 0

# Run with: pytest test_example.py
```

## Fundamental Patterns

### Pattern 1: Basic pytest Tests

```python
# test_calculator.py
import pytest

class Calculator:
    """Simple calculator for testing."""

    def add(self, a: float, b: float) -> float:
        return a + b

    def subtract(self, a: float, b: float) -> float:
        return a - b

    def multiply(self, a: float, b: float) -> float:
        return a * b

    def divide(self, a: float, b: float) -> float:
        if b == 0:
            raise ValueError("Cannot divide by zero")
        return a / b


def test_addition():
    """Test addition."""
    calc = Calculator()
    assert calc.add(2, 3) == 5
    assert calc.add(-1, 1) == 0
    assert calc.add(0, 0) == 0


def test_subtraction():
    """Test subtraction."""
    calc = Calculator()
    assert calc.subtract(5, 3) == 2
    assert calc.subtract(0, 5) == -5


def test_multiplication():
    """Test multiplication."""
    calc = Calculator()
    assert calc.multiply(3, 4) == 12
    assert calc.multiply(0, 5) == 0


def test_division():
    """Test division."""
    calc = Calculator()
    assert calc.divide(6, 3) == 2
    assert calc.divide(5, 2) == 2.5


def test_division_by_zero():
    """Test division by zero raises error."""
    calc = Calculator()
    with pytest.raises(ValueError, match="Cannot divide by zero"):
        calc.divide(5, 0)
```

### Pattern 2: Fixtures for Setup and Teardown

```python
# test_database.py
import pytest
from typing import Generator

class Database:
    """Simple database class."""

    def __init__(self, connection_string: str):
        self.connection_string = connection_string
        self.connected = False

    def connect(self):
        """Connect to database."""
        self.connected = True

    def disconnect(self):
        """Disconnect from database."""
        self.connected = False

    def query(self, sql: str) -> list:
        """Execute query."""
        if not self.connected:
            raise RuntimeError("Not connected")
        return [{"id": 1, "name": "Test"}]


@pytest.fixture
def db() -> Generator[Database, None, None]:
    """Fixture that provides connected database."""
    # Setup
    database = Database("sqlite:///:memory:")
    database.connect()

    # Provide to test
    yield database

    # Teardown
    database.disconnect()


def test_database_query(db):
    """Test database query with fixture."""
    results = db.query("SELECT * FROM users")
    assert len(results) == 1
    assert results[0]["name"] == "Test"


@pytest.fixture(scope="session")
def app_config():
    """Session-scoped fixture - created once per test session."""
    return {
        "database_url": "postgresql://localhost/test",
        "api_key": "test-key",
        "debug": True
    }


@pytest.fixture(scope="module")
def api_client(app_config):
    """Module-scoped fixture - created once per test module."""
    # Setup expensive resource
    client = {"config": app_config, "session": "active"}
    yield client
    # Cleanup
    client["session"] = "closed"


def test_api_client(api_client):
    """Test using api client fixture."""
    assert api_client["session"] == "active"
    assert api_client["config"]["debug"] is True
```

### Pattern 3: Parameterized Tests

```python
# test_validation.py
import pytest

def is_valid_email(email: str) -> bool:
    """Check if email is valid."""
    return "@" in email and "." in email.split("@")[1]


@pytest.mark.parametrize("email,expected", [
    ("user@example.com", True),
    ("test.user@domain.co.uk", True),
    ("invalid.email", False),
    ("@example.com", False),
    ("user@domain", False),
    ("", False),
])
def test_email_validation(email, expected):
    """Test email validation with various inputs."""
    assert is_valid_email(email) == expected


@pytest.mark.parametrize("a,b,expected", [
    (2, 3, 5),
    (0, 0, 0),
    (-1, 1, 0),
    (100, 200, 300),
    (-5, -5, -10),
])
def test_addition_parameterized(a, b, expected):
    """Test addition with multiple parameter sets."""
    from test_calculator import Calculator
    calc = Calculator()
    assert calc.add(a, b) == expected


# Using pytest.param for special cases
@pytest.mark.parametrize("value,expected", [
    pytest.param(1, True, id="positive"),
    pytest.param(0, False, id="zero"),
    pytest.param(-1, False, id="negative"),
])
def test_is_positive(value, expected):
    """Test with custom test IDs."""
    assert (value > 0) == expected
```

### Pattern 4: Mocking with unittest.mock

```python
# test_api_client.py
import pytest
from unittest.mock import Mock, patch, MagicMock
import requests

class APIClient:
    """Simple API client."""

    def __init__(self, base_url: str):
        self.base_url = base_url

    def get_user(self, user_id: int) -> dict:
        """Fetch user from API."""
        response = requests.get(f"{self.base_url}/users/{user_id}")
        response.raise_for_status()
        return response.json()

    def create_user(self, data: dict) -> dict:
        """Create new user."""
        response = requests.post(f"{self.base_url}/users", json=data)
        response.raise_for_status()
        return response.json()


def test_get_user_success():
    """Test successful API call with mock."""
    client = APIClient("https://api.example.com")

    mock_response = Mock()
    mock_response.json.return_value = {"id": 1, "name": "John Doe"}
    mock_response.raise_for_status.return_value = None

    with patch("requests.get", return_value=mock_response) as mock_get:
        user = client.get_user(1)

        assert user["id"] == 1
        assert user["name"] == "John Doe"
        mock_get.assert_called_once_with("https://api.example.com/users/1")


def test_get_user_not_found():
    """Test API call with 404 error."""
    client = APIClient("https://api.example.com")

    mock_response = Mock()
    mock_response.raise_for_status.side_effect = requests.HTTPError("404 Not Found")

    with patch("requests.get", return_value=mock_response):
        with pytest.raises(requests.HTTPError):
            client.get_user(999)


@patch("requests.post")
def test_create_user(mock_post):
    """Test user creation with decorator syntax."""
    client = APIClient("https://api.example.com")

    mock_post.return_value.json.return_value = {"id": 2, "name": "Jane Doe"}
    mock_post.return_value.raise_for_status.return_value = None

    user_data = {"name": "Jane Doe", "email": "jane@example.com"}
    result = client.create_user(user_data)

    assert result["id"] == 2
    mock_post.assert_called_once()
    call_args = mock_post.call_args
    assert call_args.kwargs["json"] == user_data
```

### Pattern 5: Testing Exceptions

```python
# test_exceptions.py
import pytest

def divide(a: float, b: float) -> float:
    """Divide a by b."""
    if b == 0:
        raise ZeroDivisionError("Division by zero")
    if not isinstance(a, (int, float)) or not isinstance(b, (int, float)):
        raise TypeError("Arguments must be numbers")
    return a / b


def test_zero_division():
    """Test exception is raised for division by zero."""
    with pytest.raises(ZeroDivisionError):
        divide(10, 0)


def test_zero_division_with_message():
    """Test exception message."""
    with pytest.raises(ZeroDivisionError, match="Division by zero"):
        divide(5, 0)


def test_type_error():
    """Test type error exception."""
    with pytest.raises(TypeError, match="must be numbers"):
        divide("10", 5)


def test_exception_info():
    """Test accessing exception info."""
    with pytest.raises(ValueError) as exc_info:
        int("not a number")

    assert "invalid literal" in str(exc_info.value)
```

## Advanced Patterns

### Pattern 6: Testing Async Code

```python
# test_async.py
import pytest
import asyncio

async def fetch_data(url: str) -> dict:
    """Fetch data asynchronously."""
    await asyncio.sleep(0.1)
    return {"url": url, "data": "result"}


@pytest.mark.asyncio
async def test_fetch_data():
    """Test async function."""
    result = await fetch_data("https://api.example.com")
    assert result["url"] == "https://api.example.com"
    assert "data" in result


@pytest.mark.asyncio
async def test_concurrent_fetches():
    """Test concurrent async operations."""
    urls = ["url1", "url2", "url3"]
    tasks = [fetch_data(url) for url in urls]
    results = await asyncio.gather(*tasks)

    assert len(results) == 3
    assert all("data" in r for r in results)


@pytest.fixture
async def async_client():
    """Async fixture."""
    client = {"connected": True}
    yield client
    client["connected"] = False


@pytest.mark.asyncio
async def test_with_async_fixture(async_client):
    """Test using async fixture."""
    assert async_client["connected"] is True
```

### Pattern 7: Monkeypatch for Testing

```python
# test_environment.py
import os
import pytest

def get_database_url() -> str:
    """Get database URL from environment."""
    return os.environ.get("DATABASE_URL", "sqlite:///:memory:")


def test_database_url_default():
    """Test default database URL."""
    # Will use actual environment variable if set
    url = get_database_url()
    assert url


def test_database_url_custom(monkeypatch):
    """Test custom database URL with monkeypatch."""
    monkeypatch.setenv("DATABASE_URL", "postgresql://localhost/test")
    assert get_database_url() == "postgresql://localhost/test"


def test_database_url_not_set(monkeypatch):
    """Test when env var is not set."""
    monkeypatch.delenv("DATABASE_URL", raising=False)
    assert get_database_url() == "sqlite:///:memory:"


class Config:
    """Configuration class."""

    def __init__(self):
        self.api_key = "production-key"

    def get_api_key(self):
        return self.api_key


def test_monkeypatch_attribute(monkeypatch):
    """Test monkeypatching object attributes."""
    config = Config()
    monkeypatch.setattr(config, "api_key", "test-key")
    assert config.get_api_key() == "test-key"
```

### Pattern 8: Temporary Files and Directories

```python
# test_file_operations.py
import pytest
from pathlib import Path

def save_data(filepath: Path, data: str):
    """Save data to file."""
    filepath.write_text(data)


def load_data(filepath: Path) -> str:
    """Load data from file."""
    return filepath.read_text()


def test_file_operations(tmp_path):
    """Test file operations with temporary directory."""
    # tmp_path is a pathlib.Path object
    test_file = tmp_path / "test_data.txt"

    # Save data
    save_data(test_file, "Hello, World!")

    # Verify file exists
    assert test_file.exists()

    # Load and verify data
    data = load_data(test_file)
    assert data == "Hello, World!"


def test_multiple_files(tmp_path):
    """Test with multiple temporary files."""
    files = {
        "file1.txt": "Content 1",
        "file2.txt": "Content 2",
        "file3.txt": "Content 3"
    }

    for filename, content in files.items():
        filepath = tmp_path / filename
        save_data(filepath, content)

    # Verify all files created
    assert len(list(tmp_path.iterdir())) == 3

    # Verify contents
    for filename, expected_content in files.items():
        filepath = tmp_path / filename
        assert load_data(filepath) == expected_content
```

### Pattern 9: Custom Fixtures and Conftest

```python
# conftest.py
"""Shared fixtures for all tests."""
import pytest

@pytest.fixture(scope="session")
def database_url():
    """Provide database URL for all tests."""
    return "postgresql://localhost/test_db"


@pytest.fixture(autouse=True)
def reset_database(database_url):
    """Auto-use fixture that runs before each test."""
    # Setup: Clear database
    print(f"Clearing database: {database_url}")
    yield
    # Teardown: Clean up
    print("Test completed")


@pytest.fixture
def sample_user():
    """Provide sample user data."""
    return {
        "id": 1,
        "name": "Test User",
        "email": "test@example.com"
    }


@pytest.fixture
def sample_users():
    """Provide list of sample users."""
    return [
        {"id": 1, "name": "User 1"},
        {"id": 2, "name": "User 2"},
        {"id": 3, "name": "User 3"},
    ]


# Parametrized fixture
@pytest.fixture(params=["sqlite", "postgresql", "mysql"])
def db_backend(request):
    """Fixture that runs tests with different database backends."""
    return request.param


def test_with_db_backend(db_backend):
    """This test will run 3 times with different backends."""
    print(f"Testing with {db_backend}")
    assert db_backend in ["sqlite", "postgresql", "mysql"]
```

### Pattern 10: Property-Based Testing

```python
# test_properties.py
from hypothesis import given, strategies as st
import pytest

def reverse_string(s: str) -> str:
    """Reverse a string."""
    return s[::-1]


@given(st.text())
def test_reverse_twice_is_original(s):
    """Property: reversing twice returns original."""
    assert reverse_string(reverse_string(s)) == s


@given(st.text())
def test_reverse_length(s):
    """Property: reversed string has same length."""
    assert len(reverse_string(s)) == len(s)


@given(st.integers(), st.integers())
def test_addition_commutative(a, b):
    """Property: addition is commutative."""
    assert a + b == b + a


@given(st.lists(st.integers()))
def test_sorted_list_properties(lst):
    """Property: sorted list is ordered."""
    sorted_lst = sorted(lst)

    # Same length
    assert len(sorted_lst) == len(lst)

    # All elements present
    assert set(sorted_lst) == set(lst)

    # Is ordered
    for i in range(len(sorted_lst) - 1):
        assert sorted_lst[i] <= sorted_lst[i + 1]
```

## Test Design Principles

### One Behavior Per Test

Each test should verify exactly one behavior. This makes failures easy to diagnose and tests easy to maintain.

```python
# BAD - testing multiple behaviors
def test_user_service():
    user = service.create_user(data)
    assert user.id is not None
    assert user.email == data["email"]
    updated = service.update_user(user.id, {"name": "New"})
    assert updated.name == "New"

# GOOD - focused tests
def test_create_user_assigns_id():
    user = service.create_user(data)
    assert user.id is not None

def test_create_user_stores_email():
    user = service.create_user(data)
    assert user.email == data["email"]

def test_update_user_changes_name():
    user = service.create_user(data)
    updated = service.update_user(user.id, {"name": "New"})
    assert updated.name == "New"
```

### Test Error Paths

Always test failure cases, not just happy paths.

```python
def test_get_user_raises_not_found():
    with pytest.raises(UserNotFoundError) as exc_info:
        service.get_user("nonexistent-id")

    assert "nonexistent-id" in str(exc_info.value)

def test_create_user_rejects_invalid_email():
    with pytest.raises(ValueError, match="Invalid email format"):
        service.create_user({"email": "not-an-email"})
```

## Testing Best Practices

### Test Organization

```python
# tests/
#   __init__.py
#   conftest.py           # Shared fixtures
#   test_unit/            # Unit tests
#     test_models.py
#     test_utils.py
#   test_integration/     # Integration tests
#     test_api.py
#     test_database.py
#   test_e2e/            # End-to-end tests
#     test_workflows.py
```

### Test Naming Convention

A common pattern: `test_<unit>_<scenario>_<expected_outcome>`. Adapt to your team's preferences.

```python
# Pattern: test_<unit>_<scenario>_<expected>
def test_create_user_with_valid_data_returns_user():
    ...

def test_create_user_with_duplicate_email_raises_conflict():
    ...

def test_get_user_with_unknown_id_returns_none():
    ...

# Good test names - clear and descriptive
def test_user_creation_with_valid_data():
    """Clear name describes what is being tested."""
    pass

def test_login_fails_with_invalid_password():
    """Name describes expected behavior."""
    pass

def test_api_returns_404_for_missing_resource():
    """Specific about inputs and expected outcomes."""
    pass

# Bad test names - avoid these
def test_1():  # Not descriptive
    pass

def test_user():  # Too vague
    pass

def test_function():  # Doesn't explain what's tested
    pass
```

### Testing Retry Behavior

Verify that retry logic works correctly using mock side effects.

```python
from unittest.mock import Mock

def test_retries_on_transient_error():
    """Test that service retries on transient failures."""
    client = Mock()
    # Fail twice, then succeed
    client.request.side_effect = [
        ConnectionError("Failed"),
        ConnectionError("Failed"),
        {"status": "ok"},
    ]

    service = ServiceWithRetry(client, max_retries=3)
    result = service.fetch()

    assert result == {"status": "ok"}
    assert client.request.call_count == 3

def test_gives_up_after_max_retries():
    """Test that service stops retrying after max attempts."""
    client = Mock()
    client.request.side_effect = ConnectionError("Failed")

    service = ServiceWithRetry(client, max_retries=3)

    with pytest.raises(ConnectionError):
        service.fetch()

    assert client.request.call_count == 3

def test_does_not_retry_on_permanent_error():
    """Test that permanent errors are not retried."""
    client = Mock()
    client.request.side_effect = ValueError("Invalid input")

    service = ServiceWithRetry(client, max_retries=3)

    with pytest.raises(ValueError):
        service.fetch()

    # Only called once - no retry for ValueError
    assert client.request.call_count == 1
```

### Mocking Time with Freezegun

Use freezegun to control time in tests for predictable time-dependent behavior.

```python
from freezegun import freeze_time
from datetime import datetime, timedelta

@freeze_time("2026-01-15 10:00:00")
def test_token_expiry():
    """Test token expires at correct time."""
    token = create_token(expires_in_seconds=3600)
    assert token.expires_at == datetime(2026, 1, 15, 11, 0, 0)

@freeze_time("2026-01-15 10:00:00")
def test_is_expired_returns_false_before_expiry():
    """Test token is not expired when within validity period."""
    token = create_token(expires_in_seconds=3600)
    assert not token.is_expired()

@freeze_time("2026-01-15 12:00:00")
def test_is_expired_returns_true_after_expiry():
    """Test token is expired after validity period."""
    token = Token(expires_at=datetime(2026, 1, 15, 11, 30, 0))
    assert token.is_expired()

def test_with_time_travel():
    """Test behavior across time using freeze_time context."""
    with freeze_time("2026-01-01") as frozen_time:
        item = create_item()
        assert item.created_at == datetime(2026, 1, 1)

        # Move forward in time
        frozen_time.move_to("2026-01-15")
        assert item.age_days == 14
```

### Test Markers

```python
# test_markers.py
import pytest

@pytest.mark.slow
def test_slow_operation():
    """Mark slow tests."""
    import time
    time.sleep(2)


@pytest.mark.integration
def test_database_integration():
    """Mark integration tests."""
    pass


@pytest.mark.skip(reason="Feature not implemented yet")
def test_future_feature():
    """Skip tests temporarily."""
    pass


@pytest.mark.skipif(os.name == "nt", reason="Unix only test")
def test_unix_specific():
    """Conditional skip."""
    pass


@pytest.mark.xfail(reason="Known bug #123")
def test_known_bug():
    """Mark expected failures."""
    assert False


# Run with:
# pytest -m slow          # Run only slow tests
# pytest -m "not slow"    # Skip slow tests
# pytest -m integration   # Run integration tests
```

### Coverage Reporting

```bash
# Install coverage
pip install pytest-cov

# Run tests with coverage
pytest --cov=myapp tests/

# Generate HTML report
pytest --cov=myapp --cov-report=html tests/

# Fail if coverage below threshold
pytest --cov=myapp --cov-fail-under=80 tests/

# Show missing lines
pytest --cov=myapp --cov-report=term-missing tests/
```

## Testing Database Code

```python
# test_database_models.py
import pytest
from sqlalchemy import create_engine, Column, Integer, String
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker, Session

Base = declarative_base()


class User(Base):
    """User model."""
    __tablename__ = "users"

    id = Column(Integer, primary_key=True)
    name = Column(String(50))
    email = Column(String(100), unique=True)


@pytest.fixture(scope="function")
def db_session() -> Session:
    """Create in-memory database for testing."""
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)

    SessionLocal = sessionmaker(bind=engine)
    session = SessionLocal()

    yield session

    session.close()


def test_create_user(db_session):
    """Test creating a user."""
    user = User(name="Test User", email="test@example.com")
    db_session.add(user)
    db_session.commit()

    assert user.id is not None
    assert user.name == "Test User"


def test_query_user(db_session):
    """Test querying users."""
    user1 = User(name="User 1", email="user1@example.com")
    user2 = User(name="User 2", email="user2@example.com")

    db_session.add_all([user1, user2])
    db_session.commit()

    users = db_session.query(User).all()
    assert len(users) == 2


def test_unique_email_constraint(db_session):
    """Test unique email constraint."""
    from sqlalchemy.exc import IntegrityError

    user1 = User(name="User 1", email="same@example.com")
    user2 = User(name="User 2", email="same@example.com")

    db_session.add(user1)
    db_session.commit()

    db_session.add(user2)

    with pytest.raises(IntegrityError):
        db_session.commit()
```

## CI/CD Integration

```yaml
# .github/workflows/test.yml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install pytest pytest-cov

      - name: Run tests
        run: |
          pytest --cov=myapp --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
```

## Configuration Files

```ini
# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=myapp
    --cov-report=term-missing
markers =
    slow: marks tests as slow
    integration: marks integration tests
    unit: marks unit tests
    e2e: marks end-to-end tests
```

```toml
# pyproject.toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = [
    "-v",
    "--cov=myapp",
    "--cov-report=term-missing",
]

[tool.coverage.run]
source = ["myapp"]
omit = ["*/tests/*", "*/migrations/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
]
```

## Resources

- **pytest documentation**: https://docs.pytest.org/
- **unittest.mock**: https://docs.python.org/3/library/unittest.mock.html
- **hypothesis**: Property-based testing
- **pytest-asyncio**: Testing async code
- **pytest-cov**: Coverage reporting
- **pytest-mock**: pytest wrapper for mock

## Best Practices Summary

1. **Write tests first** (TDD) or alongside code
2. **One assertion per test** when possible
3. **Use descriptive test names** that explain behavior
4. **Keep tests independent** and isolated
5. **Use fixtures** for setup and teardown
6. **Mock external dependencies** appropriately
7. **Parametrize tests** to reduce duplication
8. **Test edge cases** and error conditions
9. **Measure coverage** but focus on quality
10. **Run tests in CI/CD** on every commit


 succeeded in 1436ms:
research/drift_recovery\__init__.py
research/drift_recovery\tests\__init__.py
research/drift_recovery\tests\test_d1_stats.py
research/drift_recovery\harness_bridge.py
research/drift_recovery\stats\learning_curve.py
research/drift_recovery\run_d1.py
research/drift_recovery\stats\__init__.py
research/drift_recovery\stats\paired_bootstrap.py
research/drift_recovery\stats\multiple_testing.py
research/drift_recovery\artifacts.py
research/drift_recovery\contracts.py
research/drift_recovery\d1\__init__.py
research/drift_recovery\d1\paper_stats.py
research/drift_recovery\out\d1\section3_draft.md
research/drift_recovery\out\d1\scifact_evalsplit_pack.npz
research/drift_recovery\out\d1\scifact_evalsplit_pack.json
research/drift_recovery\out\d1\run_manifest.json
research/drift_recovery\out\d1\paper_edits.md
research/drift_recovery\out\d1\learning_curve.json
research/drift_recovery\out\d1\leakage_sensitivity.json
research/drift_recovery\out\d1\ladder.json
research/drift_recovery\out\d1\d1_summary.json
research/drift_recovery\out\d1\contrasts_holm.json
research/drift_recovery\out\d1\ci_report.md
research/drift_recovery\out\d1\C1_FIX_REPORT.md
research/drift_recovery\out\d1\bootstrap_cis.json

 succeeded in 1490ms:
587:# Task Group: CHELATEDAI drift-recovery calibration and brutal-honesty process
588:scope: CHELATEDAI drift-recovery diagnosis, severity calibration, knob-sweep execution, artifact durability, and strict PR-body handling; use when continuing drift-recovery experiments or preparing evidence-heavy PRs.
589:applies_to: cwd=D:\GITHUB\CHELATEDAI; reuse_rule=safe for drift-recovery and repo-process guidance in CHELATEDAI, but experimental results and PR state should be rechecked from current artifacts
595:- rollout_summaries/2026-06-11T15-10-39-PIMu-drift_recovery_pr6_pr8_calibration_knob_sweep_and_bhs_harden.md (cwd=D:\GITHUB\CHELATEDAI, rollout_path=C:\Users\mattm\.codex\sessions\2026\06\11\rollout-2026-06-11T11-10-39-019eb73c-3da3-7f70-9035-afd5a56ecb86.jsonl, updated_at=2026-06-12T14:48:16+00:00, thread_id=019eb73c-3da3-7f70-9035-afd5a56ecb86)
599:- drift-recovery, severity-calibration, knob-sweep, calibration-manifest, HF_HUB_OFFLINE, experiment_runs, artifact durability, deterministic tie handling, validate_pr_brutal_honesty.py, PR #266, admin merge
603:- the goal file required "diagnose last night's mixed drift-recovery result and re-run it calibrated" -> verify hypotheses from data before trusting prior summaries [Task 1]

 succeeded in 3135ms:

    Directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d----            7/9/2026 10:24 PM                __pycache__
d----            7/9/2026  9:52 PM                .github
d----            7/9/2026 11:03 PM                .ruff_cache
d----            7/9/2026  9:52 PM                artifacts
d----            7/9/2026  9:52 PM                computational_storage_poc
d----            7/9/2026  9:52 PM                dashboard
d----            7/9/2026  9:52 PM                docs
d----            7/9/2026  9:52 PM                experiment_runs
d----            7/9/2026 10:24 PM                research
d----            7/9/2026  9:52 PM                scripts
d----            7/9/2026  9:52 PM                tests
-a-h-            7/9/2026  9:52 PM             56 .git
-a---            7/9/2026  9:52 PM             66 .gitattributes
-a---            7/9/2026  9:52 PM           1253 .gitignore
-a---            7/9/2026  9:52 PM          59865 .report.json
-a---            7/9/2026  9:52 PM           5813 adapter_router.py
-a---            7/9/2026  9:52 PM           8412 adaptive_gate_orchestrator.py
-a---            7/9/2026  9:52 PM          33262 adaptive_overlay.py
-a---            7/9/2026  9:52 PM          40117 aep_orchestrator.py
-a---            7/9/2026  9:52 PM          18331 analyze_drift_recovery.py
-a---            7/9/2026  9:52 PM          11407 analyze_phase_c_results.py
-a---            7/9/2026  9:52 PM           2047 annealing_controller.py
-a---            7/9/2026  9:52 PM           5571 annealing_schedule.py
-a---            7/9/2026  9:52 PM         129123 antigravity_engine.py
-a---            7/9/2026  9:52 PM           4820 attnres_repeat_seed_decision.py
-a---            7/9/2026  9:52 PM           4269 audit_evidence_index_freshness.py
-a---            7/9/2026  9:52 PM           4244 audit_promotion_linkage.py
-a---            7/9/2026  9:52 PM          12784 benchmark_beir_results.json
-a---            7/9/2026  9:52 PM          28884 benchmark_beir.py
-a---            7/9/2026  9:52 PM          18139 benchmark_comparative.py
-a---            7/9/2026  9:52 PM          33995 benchmark_distillation.py
-a---            7/9/2026  9:52 PM           8644 benchmark_evolution.py
-a---            7/9/2026  9:52 PM          18832 benchmark_multitask.py
-a---            7/9/2026  9:52 PM          16161 benchmark_rlm.py
-a---            7/9/2026  9:52 PM          13459 benchmark_utils.py
-a---            7/9/2026  9:52 PM          13246 build_attribution_pool.py
-a---            7/9/2026  9:52 PM           9450 CHANGELOG.md
-a---            7/9/2026  9:52 PM          13313 checkpoint_manager.py
-a---            7/9/2026  9:52 PM          26988 chelation_adapter.py
-a---            7/9/2026 10:40 PM           2698 chelation_debug.jsonl
-a---            7/9/2026  9:52 PM          14662 chelation_logger.py
-a---            7/9/2026  9:52 PM          18889 CLAUDE.md
-a---            7/9/2026  9:52 PM           3530 cleanup_review_diagnostic.py
-a---            7/9/2026  9:52 PM             58 CODEOWNERS
-a---            7/9/2026  9:52 PM          11565 COMPLETION_SUMMARY.md
-a---            7/9/2026  9:52 PM           6153 compute_budget_policy.py
-a---            7/9/2026  9:52 PM          39035 config.py
-a---            7/9/2026  9:52 PM           4848 convergence_monitor.py
-a---            7/9/2026  9:52 PM          16493 cross_lingual_distillation.py
-a---            7/9/2026  9:52 PM          84235 dashboard_server.py
-a---            7/9/2026  9:52 PM           4721 default_promotion_preflight.py
-a---            7/9/2026  9:52 PM           4507 device_profiles.py
-a---            7/9/2026  9:52 PM           7668 dimension_mask_predictor.py
-a---            7/9/2026  9:52 PM           4058 distributed_fitness_evaluator.py
-a---            7/9/2026  9:52 PM          10803 drift_injector.py
-a---            7/9/2026  9:52 PM           2951 drift_recovery_metrics.py
-a---            7/9/2026  9:52 PM           2343 elite_archive.py
-a---            7/9/2026  9:52 PM          11823 embedding_backend.py
-a---            7/9/2026  9:52 PM           4908 embedding_quality.py
-a---            7/9/2026  9:52 PM          14207 engine_scope_coverage.py
-a---            7/9/2026  9:52 PM          14703 engine_scope_negatives.py
-a---            7/9/2026  9:52 PM          13645 engine_scope.py
-a---            7/9/2026  9:52 PM           4335 evaluator_fabric.py
-a---            7/9/2026  9:52 PM           8566 evidence_contract.py
-a---            7/9/2026  9:52 PM          12213 evidence_dag.py
-a---            7/9/2026  9:52 PM           5514 evidence_index.json
-a---            7/9/2026  9:52 PM          21574 evolution_strategies_optimizer.py
-a---            7/9/2026  9:52 PM          17891 expectation_comparator.py
-a---            7/9/2026  9:52 PM           2909 feature_direction_bank.py
-a---            7/9/2026  9:52 PM          38973 findings.md
-a---            7/9/2026  9:52 PM           8442 fitness_composition_orchestrator.py
-a---            7/9/2026  9:52 PM           1530 fitness_interfaces.py
-a---            7/9/2026  9:52 PM           5539 generate_evidence_index.py
-a---            7/9/2026  9:52 PM          10683 integrated_diagnostics_report.py
-a---            7/9/2026  9:52 PM          10036 isomer_detector.py
-a---            7/9/2026  9:52 PM           4978 kalman_lr_scheduler.py
-a---            7/9/2026  9:52 PM           3142 label_smoke_artifacts.py
-a---            7/9/2026  9:52 PM          11389 language_detector.py
-a---            7/9/2026  9:52 PM          17764 learned_mask_gate.py
-a---            7/9/2026  9:52 PM           2965 learned_mask_policy.py
-a---            7/9/2026  9:52 PM          28676 learned_reformulation_gate.py
-a---            7/9/2026  9:52 PM          10774 LICENSE
-a---            7/9/2026  9:52 PM           2684 model_hook_bus.py
-a---            7/9/2026  9:52 PM           6836 model_scope_artifacts.py
-a---            7/9/2026  9:52 PM           8412 model_scope_engine_bridge.py
-a---            7/9/2026  9:52 PM           9035 model_scope_features.py
-a---            7/9/2026  9:52 PM          22226 model_scope_memory.py
-a---            7/9/2026  9:52 PM           9973 model_scope_runtime.py
-a---            7/9/2026  9:52 PM          21416 model_scope_steering.py
-a---            7/9/2026  9:52 PM          29130 model_scope_trainer.py
-a---            7/9/2026  9:52 PM          31281 online_updater.py
-a---            7/9/2026  9:52 PM           1338 phase_c_analysis.json
-a---            7/9/2026  9:52 PM          89923 phase_c_results.json
-a---            7/9/2026  9:52 PM           6547 plan_evidence_artifact_cleanup.py
-a---            7/9/2026  9:52 PM         613446 poetry.lock
-a---            7/9/2026  9:52 PM          11541 post_bank_conditions.py
-a---            7/9/2026  9:52 PM           5531 post_bank_correction.py
-a---            7/9/2026  9:52 PM           4272 post_bank_runtime.py
-a---            7/9/2026  9:52 PM           7947 PR_DESCRIPTION.md
-a---            7/9/2026  9:52 PM          43432 presentation.html
-a---            7/9/2026  9:52 PM           3269 production_steering_control.py
-a---            7/9/2026  9:52 PM          45606 progress.md
-a---            7/9/2026  9:52 PM           7338 promotion_contract.py
-a---            7/9/2026  9:52 PM           4578 pyproject.toml
-a---            7/9/2026  9:52 PM           3573 quantization_promotion_gate.py
-a---            7/9/2026  9:52 PM           5321 query_encoder_drift.py
-a---            7/9/2026  9:52 PM           7779 query_reformulator.py
-a---            7/9/2026  9:52 PM           8860 qwen_scope_adapter.py
-a---            7/9/2026  9:52 PM          23407 README.md
-a---            7/9/2026  9:52 PM          20821 recursive_decomposer.py
-a---            7/9/2026  9:52 PM           5478 REFACTORING_PLAN.md
-a---            7/9/2026  9:52 PM           3856 REFERENCES.md
-a---            7/9/2026  9:52 PM           4405 reproducibility_context.py
-a---            7/9/2026  9:52 PM            270 requirements.txt
-a---            7/9/2026  9:52 PM           6506 research_pathway_analyzer.py
-a---            7/9/2026  9:52 PM           8209 retrieval_fitness_evaluator.py
-a---            7/9/2026  9:52 PM          15689 run_candidate_transfer_gate.py
-a---            7/9/2026  9:52 PM           5007 run_default_promotion_evidence_chain.py
-a---            7/9/2026  9:52 PM          10863 run_drift_recovery_calibration.py
-a---            7/9/2026  9:52 PM          50856 run_drift_recovery_experiment.py
-a---            7/9/2026  9:52 PM          13102 run_drift_recovery_knob_sweep.py
-a---            7/9/2026  9:52 PM          18661 run_drift_recovery_swap_campaign.py
-a---            7/9/2026  9:52 PM           7536 run_gate_train.py
-a---            7/9/2026  9:52 PM          64226 run_golden_default_autopilot.py
-a---            7/9/2026  9:52 PM           6424 run_large_sweep.py
-a---            7/9/2026  9:52 PM           8685 run_learned_reformulation_autopilot.py
-a---            7/9/2026  9:52 PM          24005 run_live_fire_diagnostics.py
-a---            7/9/2026  9:52 PM          34350 run_model_scope_campaign.py
-a---            7/9/2026  9:52 PM           5719 run_model_scope_overlay_smoke.py
-a---            7/9/2026  9:52 PM           4815 run_overlay_model_scope_validation.py
-a---            7/9/2026  9:52 PM           4839 run_overnight_campaign.py
-a---            7/9/2026  9:52 PM          31045 run_phase_c_eval.py
-a---            7/9/2026  9:52 PM          14949 run_repeatability_check.py
-a---            7/9/2026  9:52 PM          25054 run_road_course_campaign.py
-a---            7/9/2026  9:52 PM          14271 run_road_course_tuning_loop.py
-a---            7/9/2026  9:52 PM          25554 run_safety_testbed.py
-a---            7/9/2026  9:52 PM           6917 run_sweep.py
-a---            7/9/2026  9:52 PM          48856 run_thousand_query_tuning.py
-a---            7/9/2026  9:52 PM          21731 run_weight_refinement_campaign.py
-a---            7/9/2026  9:52 PM            641 SECURITY.md
-a---            7/9/2026  9:52 PM          11907 sedimentation_loss.py
-a---            7/9/2026  9:52 PM           5652 sedimentation_trainer.py
-a---            7/9/2026  9:52 PM          10264 sedimentation.py
-a---            7/9/2026  9:52 PM          46271 self_healing_chelation.py
-a---            7/9/2026  9:52 PM          11069 stability_tracker.py
-a---            7/9/2026  9:52 PM          29992 static_mask_probe.py
-a---            7/9/2026  9:52 PM           6929 steering_policy.py
-a---            7/9/2026  9:52 PM          12014 steering_post_bank.py
-a---            7/9/2026  9:52 PM           3710 steering_route_promotion.py
-a---            7/9/2026  9:52 PM           2928 structural_health_score.py
-a---            7/9/2026  9:52 PM          25463 sweep_results.json
-a---            7/9/2026  9:52 PM           5353 synthetic_collapse_benchmark.py
-a---            7/9/2026  9:52 PM           8181 task_plan.md
-a---            7/9/2026  9:52 PM          26000 teacher_distillation.py
-a---            7/9/2026  9:52 PM           4171 teacher_supervised_correction.py
-a---            7/9/2026  9:52 PM           4437 teacher_weight_scheduler.py
-a---            7/9/2026  9:52 PM          11174 TECHNICAL_ANALYSIS.md
-a---            7/9/2026  9:52 PM          16988 test_adaptive_overlay.py
-a---            7/9/2026  9:52 PM          23856 test_adaptive_threshold.py
-a---            7/9/2026  9:52 PM          50085 test_aep_orchestrator.py
-a---            7/9/2026  9:52 PM           7047 test_analyze_drift_recovery.py
-a---            7/9/2026  9:52 PM          27197 test_analyze_phase_c_results.py
-a---            7/9/2026  9:52 PM           6442 test_annealing_controller.py
-a---            7/9/2026  9:52 PM           6293 test_annealing_schedule.py
-a---            7/9/2026  9:52 PM           5799 test_antigravity_engine_model_scope.py
-a---            7/9/2026  9:52 PM          37768 test_antigravity_engine.py
-a---            7/9/2026  9:52 PM          14957 test_attnres_adapter.py
-a---            7/9/2026  9:52 PM           2577 test_attnres_repeat_seed_decision.py
-a---            7/9/2026  9:52 PM          17643 test_attribution_pool.py
-a---            7/9/2026  9:52 PM           3201 test_audit_evidence_index_freshness.py
-a---            7/9/2026  9:52 PM           6297 test_audit_findings.py
-a---            7/9/2026  9:52 PM           2508 test_audit_promotion_linkage.py
-a---            7/9/2026  9:52 PM          27804 test_benchmark_beir.py
-a---            7/9/2026  9:52 PM          14801 test_benchmark_comparative.py
-a---            7/9/2026  9:52 PM          19798 test_benchmark_distillation.py
-a---            7/9/2026  9:52 PM          20534 test_benchmark_multitask.py
-a---            7/9/2026  9:52 PM          16686 test_benchmark_rlm.py
-a---            7/9/2026  9:52 PM           9738 test_benchmark_utils.py
-a---            7/9/2026  9:52 PM          15484 test_bhs_validator.py
-a---            7/9/2026  9:52 PM          23295 test_checkpoint_manager.py
-a---            7/9/2026  9:52 PM          34993 test_chelation_logger.py
-a---            7/9/2026  9:52 PM          15484 test_cleanup_review_diagnostic.py
-a---            7/9/2026  9:52 PM           2197 test_computational_storage_emulation.py
-a---            7/9/2026  9:52 PM           3914 test_computational_storage_hardware_evidence.py
-a---            7/9/2026  9:52 PM           2705 test_computational_storage_payload.py
-a---            7/9/2026  9:52 PM           4666 test_computational_storage_poc.py
-a---            7/9/2026  9:52 PM           1468 test_computational_storage_population.py
-a---            7/9/2026  9:52 PM           2107 test_compute_budget_policy.py
-a---            7/9/2026  9:52 PM          10848 test_convergence_monitor.py
-a---            7/9/2026  9:52 PM           4042 test_cpu_inference.py
-a---            7/9/2026  9:52 PM          21640 test_cross_lingual_distillation.py
-a---            7/9/2026  9:52 PM          50461 test_dashboard_server.py
-a---            7/9/2026  9:52 PM           5102 test_default_promotion_evidence_chain.py
-a---            7/9/2026  9:52 PM           3111 test_default_promotion_preflight.py
-a---            7/9/2026  9:52 PM          10454 test_dimension_mask_predictor.py
-a---            7/9/2026  9:52 PM           1270 test_disk_llm_estimator.py
-a---            7/9/2026  9:52 PM           9826 test_drift_injector.py
-a---            7/9/2026  9:52 PM           3575 test_drift_recovery_metrics.py
-a---            7/9/2026  9:52 PM          15688 test_eggroll_strategic_platform.py
-a---            7/9/2026  9:52 PM          20549 test_engine_scope_coverage.py
-a---            7/9/2026  9:52 PM          22874 test_engine_scope_negatives.py
-a---            7/9/2026  9:52 PM          11493 test_engine_scope.py
-a---            7/9/2026  9:52 PM           1709 test_evaluator_fabric.py
-a---            7/9/2026  9:52 PM           1953 test_evidence_contract.py
-a---            7/9/2026  9:52 PM           6545 test_evidence_dag.py
-a---            7/9/2026  9:52 PM           7580 test_evolution_strategies_optimizer.py
-a---            7/9/2026  9:52 PM          14121 test_expectation_comparator.py
-a---            7/9/2026  9:52 PM           4948 test_experimental_marker.py
-a---            7/9/2026  9:52 PM           4751 test_generate_evidence_index.py
-a---            7/9/2026  9:52 PM           4868 test_integrated_diagnostics_report.py
-a---            7/9/2026  9:52 PM           2619 test_integrated_repo_runtime.py
-a---            7/9/2026  9:52 PM          21305 test_integration_rlm.py
-a---            7/9/2026  9:52 PM          11291 test_isomer_detector.py
-a---            7/9/2026  9:52 PM          15940 test_kalman_lr.py
-a---            7/9/2026  9:52 PM           9414 test_language_detector.py
-a---            7/9/2026  9:52 PM           9044 test_learned_mask_gate.py
-a---            7/9/2026  9:52 PM          11298 test_learned_reformulation_gate.py
-a---            7/9/2026  9:52 PM           1487 test_live_fire_diagnostics.py
-a---            7/9/2026  9:52 PM          10745 test_live_fire_flags.py
-a---            7/9/2026  9:52 PM           1950 test_memory_compression.py
-a---            7/9/2026  9:52 PM          17213 test_memory_optimization.py
-a---            7/9/2026  9:52 PM           8572 test_model_hook_bus.py
-a---            7/9/2026  9:52 PM          10812 test_model_scope_artifacts.py
-a---            7/9/2026  9:52 PM          47829 test_model_scope_engine_bridge.py
-a---            7/9/2026  9:52 PM          10749 test_model_scope_features.py
-a---            7/9/2026  9:52 PM          25926 test_model_scope_memory.py
-a---            7/9/2026  9:52 PM            845 test_model_scope_overlay_smoke.py
-a---            7/9/2026  9:52 PM           5329 test_model_scope_rollback_plan.py
-a---            7/9/2026  9:52 PM          30816 test_model_scope_runtime.py
-a---            7/9/2026  9:52 PM          29223 test_model_scope_steering.py
-a---            7/9/2026  9:52 PM          31070 test_model_scope_trainer.py
-a---            7/9/2026  9:52 PM           2547 test_moe_reap.py
-a---            7/9/2026  9:52 PM           2066 test_noise_injection.py
-a---            7/9/2026  9:52 PM          26682 test_online_correction.py
-a---            7/9/2026  9:52 PM           8075 test_online_updater.py
-a---            7/9/2026  9:52 PM           2693 test_overlay_model_scope_validation.py
-a---            7/9/2026  9:52 PM          10254 test_overlay_trainer_integration.py
-a---            7/9/2026  9:52 PM           4448 test_packed_graph.py
-a---            7/9/2026  9:52 PM          13663 test_phase_c_seed.py
-a---            7/9/2026  9:52 PM           1066 test_phase7_system_evaluation.py
-a---            7/9/2026  9:52 PM           6309 test_plan_evidence_artifact_cleanup.py
-a---            7/9/2026  9:52 PM           8040 test_post_bank_conditions.py
-a---            7/9/2026  9:52 PM           5130 test_post_bank_correction.py
-a---            7/9/2026  9:52 PM           4442 test_post_bank_head_to_head.py
-a---            7/9/2026  9:52 PM           4174 test_post_bank_runtime.py
-a---            7/9/2026  9:52 PM           3246 test_production_steering_control.py
-a---            7/9/2026  9:52 PM           8993 test_promotion_contract.py
-a---            7/9/2026  9:52 PM           9548 test_query_encoder_drift.py
-a---            7/9/2026  9:52 PM           7006 test_query_reformulation_policy.py
-a---            7/9/2026  9:52 PM          15198 test_qwen_scope_adapter.py
-a---            7/9/2026  9:52 PM          49532 test_recursive_decomposer.py
-a---            7/9/2026  9:52 PM           4202 test_repo_graph_memory.py
-a---            7/9/2026  9:52 PM           5914 test_research_pathway_analyzer.py
-a---            7/9/2026  9:52 PM           5954 test_road_course_campaign.py
-a---            7/9/2026  9:52 PM          28776 test_road_course_tuning_loop.py
-a---            7/9/2026  9:52 PM           6276 test_run_candidate_transfer_gate.py
-a---            7/9/2026  9:52 PM           5174 test_run_drift_recovery_calibration.py
-a---            7/9/2026  9:52 PM          45680 test_run_drift_recovery_experiment.py
-a---            7/9/2026  9:52 PM           6057 test_run_drift_recovery_knob_sweep.py
-a---            7/9/2026  9:52 PM           5641 test_run_drift_recovery_swap_campaign.py
-a---            7/9/2026  9:52 PM          16140 test_run_gate_train.py
-a---            7/9/2026  9:52 PM          20419 test_run_golden_default_autopilot.py
-a---            7/9/2026  9:52 PM          12809 test_run_model_scope_campaign.py
-a---            7/9/2026  9:52 PM           2972 test_run_overnight_campaign.py
-a---            7/9/2026  9:52 PM          25511 test_run_phase_c_eval.py
-a---            7/9/2026  9:52 PM           6970 test_run_repeatability_check.py
-a---            7/9/2026  9:52 PM           8328 test_run_weight_refinement_campaign.py
-a---            7/9/2026  9:52 PM           3096 test_safety_calibration.py
-a---            7/9/2026  9:52 PM           2200 test_safety_campaign_plan.py
-a---            7/9/2026  9:52 PM           4440 test_safety_closed_course.py
-a---            7/9/2026  9:52 PM          14858 test_safety_component_controls.py
-a---            7/9/2026  9:52 PM           9184 test_safety_control_surfaces.py
-a---            7/9/2026  9:52 PM           7381 test_safety_failure_injection.py
-a---            7/9/2026  9:52 PM           8836 test_safety_instrumentation.py
-a---            7/9/2026  9:52 PM           1737 test_safety_testbed.py
-a---            7/9/2026  9:52 PM          16514 test_sedimentation_loss.py
-a---            7/9/2026  9:52 PM          17580 test_sedimentation_trainer.py
-a---            7/9/2026  9:52 PM          11449 test_self_healing_chelation.py
-a---            7/9/2026  9:52 PM           7384 test_smoke_pipeline_ceiling.py
-a---            7/9/2026  9:52 PM           3993 test_sparse_cpu_inference.py
-a---            7/9/2026  9:52 PM           8442 test_stability_tracker.py
-a---            7/9/2026  9:52 PM          16932 test_static_mask_probe.py
-a---            7/9/2026  9:52 PM          11991 test_steering_policy.py
-a---            7/9/2026  9:52 PM           9262 test_steering_post_bank.py
-a---            7/9/2026  9:52 PM           4103 test_steering_route_promotion.py
-a---            7/9/2026  9:52 PM           2153 test_structural_health_report.py
-a---            7/9/2026  9:52 PM           5145 test_sweep_presets.py
-a---            7/9/2026  9:52 PM           1214 test_synthetic_collapse_benchmark.py
-a---            7/9/2026  9:52 PM          46329 test_teacher_distillation.py
-a---            7/9/2026  9:52 PM           3216 test_teacher_supervised_correction.py
-a---            7/9/2026  9:52 PM          11687 test_teacher_weight_scheduler.py
-a---            7/9/2026  9:52 PM          13848 test_topology_analyzer.py
-a---            7/9/2026  9:52 PM          51953 test_tts_engine_integration.py
-a---            7/9/2026  9:52 PM          28359 test_tts_pipeline.py
-a---            7/9/2026  9:52 PM          52956 test_unit_core.py
-a---            7/9/2026  9:52 PM           7015 test_vector_store.py
-a---            7/9/2026  9:52 PM          19612 test_vector_translator.py
-a---            7/9/2026  9:52 PM          16204 test_vector_transport.py
-a---            7/9/2026  9:52 PM          14865 topology_analyzer.py
-a---            7/9/2026  9:52 PM           8107 train_gate_from_tuning_artifact.py
-a---            7/9/2026  9:52 PM          12293 transport_validation.py
-a---            7/9/2026  9:52 PM          11730 tts_pipeline.py
-a---            7/9/2026  9:52 PM           3259 vector_store.py
-a---            7/9/2026  9:52 PM          10266 vector_translator.py
-a---            7/9/2026  9:52 PM           8045 vector_transport.py
CLAUDE.md
docs\CLAUDE.md
## HEAD (no branch)
?? research/


codex
The checkout is detached and the entire `research/` tree is currently untracked, including the supplied D1 code and pack. I’ll preserve it as task input and avoid any cleanup or unrelated writes. Next I’m pinning down the exact pack schema, scoring/parity path, and existing D1 ground truth before designing the estimator API.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "Get-Content -Raw CLAUDE.md; git status --short; git ls-files research/drift_recovery; rg -n \"agreed-build-plan-v1|D3|recoverability estimator|recoverability\" research docs -g '*.md' -g '*.py'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\skills\\python-testing-patterns\\SKILL.md'; "'$lines=Get-Content $p; "LINES=$($lines.Count)"; $lines | Select-Object -First 280' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\skills\\python-testing-patterns\\SKILL.md'; Get-Content "'$p | Select-Object -Skip 280' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\memories\\MEMORY.md'; "'$hits=Select-String -Path $p -Pattern '"'CHELATEDAI|drift-recovery calibration|harness_bridge'; foreach("'$h in $hits){$start=[Math]::Max(1,$h.LineNumber-4); $end=$h.LineNumber+8; "--- $start-$end ---"; $i=0; Get-Content $p | ForEach-Object {$i++; if($i -ge $start -and $i -le $end){"${i}:$_"}}}' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
2026-07-10T03:10:14.307717Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.8 seconds
Output:
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Brutal Honesty Convention (load-bearing — read first)

See `docs/conventions/brutal-honesty-rulebook.md` (v3.3 — v3.2 executable PR gates plus L13 soft-prose-claimed-as-mechanical and the v3.3 schema/prose/artifact drift validator. v3.2 baseline: Tier B independence enforcement via `BHS_*_AGENT` lines, severity caps on Tier B scores, `DEFERRED_SCOPE:` tracking, quantitative cycle definition, override structural barrier at BLOCKED, executable validators in `scripts/`).

**Premise**: Assume every implementation/completion claim is false until independently proven by runtime evidence. Tests existing, routes existing, docs saying complete, PR text saying complete, agent claims, and even self-attested brutal-honesty sections are NOT evidence. Evidence is: command output from the production code path, real UI/API behavior, persistence/state mutation, artifact/replay/checkpoint surviving a fresh checkout, or an independent reviewer who tried to disprove and failed.

**Five hard rules**:

1. **Evidence rule.** Every PR with a "complete" claim includes `EVIDENCE:` and `SMOKE:` lines pointing at runtime evidence — not tests.
2. **Visible means verified.** Incomplete code may live in the repo; it may NOT be presented as a working feature (no UI surfacing, no API doc, no release-note implication, no roadmap tick).
3. **Mandatory PR brutal-honesty section.** Every PR body ends with a `## Brutal Honesty` section disclosing stubs (L1), escape conditionals (L2), mocks in production paths (L3), partial implementations (L4), untested production paths (L5/L8/L12), broad-catch swallowing (L11), and any other §1 instance with `file:line`. Empty answers must be justified, not omitted (template in §4 of the rulebook).
4. **Adversarial cross-agent review.** Implementation slices are done by a fresh sub-agent; a second fresh sub-agent is given the diff + brutal-honesty section + EVIDENCE + smoke command + this rulebook, and is asked to **try to disprove** completion. Not verify — *disprove*.
5. **One deterministic smoke path per repo.** `scripts/smoke.sh` (or equivalent) is the release gate. **Two honest tiers per §1 Rule 5**: Floor (import + surface-check through production code paths — the v3.2 minimum) is acceptable IF the PR body's SMOKE: line names which tier was run AND any ceiling-tier gap appears as a Carried Debt entry. Ceiling (true end-to-end against a real fixture) is the target. Claiming "smoke passed" at ceiling-tier when only floor-tier was run is itself L4 partial-as-complete. If smoke fails, the release is not ready regardless of test count.

**Trigger phrases** (use these instead of "is this done?"): *"Be brutally honest." / "What did you fake to get here?" / "If I ran this on a fresh checkout right now, what would actually fail?" / "Tell me the smallest concrete thing that does NOT work yet." / "Show me the runtime evidence, not the test." / "If I disable this feature behind a flag, what visible behavior changes?"*

**The Remediation Loop (meta-component, §6 of the rulebook; v3.1 closes the carry-forward loophole)** — applies to this repo:
- **Tier A (per-PR)**: 5 iterations max. Implementer self-scores `BHS_SELF_DRAFT` after each. If self-score < 100 at iteration 5, the PR is **withdrawn or scope-reduced** — it does NOT ship at <100 hoping for carry-forward. Same gap surviving 2 iterations → escalate, don't loop more.
- **Tier B (pre-merge — THE OFFICIAL SCORER)**: one fresh adversarial agent assigns `BHS_TIER_B` independently. `BHS_OFFICIAL = min(BHS_SELF_DRAFT, BHS_TIER_B)`. **Hard merge gate: only PRs with `BHS_OFFICIAL = 100` may merge.** No "ships with caveats" path. Self-vs-Tier-B gap > 5 → automatic L4 score-gaming disclosure.
- **Tier C (cross-PR / session boundary)**: aggregate disclosures into `docs/next-session.md` "Carried Debt" with TTL = 1 cycle. Items not cleared in the next cycle flip the **block flag** to BLOCKED — the cycle after that forbids ALL new feature work until Carried Debt = 0. **No two-cycle slop.** Block flag is automatic from the math.
- **BHS scale (only 100 ships)**: 100 = merges · 90–99 = does not merge, back to Tier A · 70–89 = does not merge, reduce scope · 50–69 = does not merge, scaffold-only allowed · 0–49 = draft, not a PR.
- **Operator override**: rulebook can't prevent the operator from typing the merge command at <100; it makes the override visible (`OPERATOR_OVERRIDE:` line) and force-creates a top-priority carried-debt entry for the next cycle.
- **PR body required lines**: `BHS_SELF_DRAFT`, `BHS_SELF_DRAFT_AGENT`, `BHS_TIER_B`, `BHS_TIER_B_AGENT`, `BHS_TIER_B_SEVERITY`, `BHS_OFFICIAL`, `CARRY_FORWARD`, `DEFERRED_SCOPE`, `LOOP_ITERATIONS`, `OPERATOR_OVERRIDE` (per §4 template).
- **Why this is in CLAUDE.md, not in a skill or MCP server**: skills can fail to load, agents can spin up without MCP context, the convention can't. This file is auto-loaded every session — the convention is the meta-component.

**Lie taxonomy L1–L13** is in §1 of the rulebook; quote by number when calling out a failure. Speculating is itself a lie — *"I don't know"* is the correct answer when there is no evidence.

## Session Rules (enforced every session, no exceptions)

### Rule 1 — Full Implementation Only
Every slice, every phase, every PR must be **fully implemented**. No scaffolding, no stubs, no `pass`-body placeholders, no `TODO: implement` markers left in committed code. If a feature is too large for one PR, split it into genuinely shippable sub-slices — but each sub-slice must itself be complete and working, not a placeholder for future work.

### Rule 2 — PR on Completion
Every unit of completed work must land in a PR **before moving to the next task**. Do not accumulate dirty branches, uncommitted changes, or local-only work across sessions. The rule is: finish a slice → open PR → get it merged (or at minimum opened and green) → then start the next slice. Stale branches and deferred cleanup are not acceptable.

### Rule 3 — No Placeholder Data or Fake Metrics
Dashboards, frontends, and reporting surfaces must be wired to **real data pipelines**. Placeholder values, hard-coded demo numbers, mock metrics, and fake responses are forbidden in committed code. Every data field displayed to an operator must trace to an actual source artifact, live computation, or explicitly documented empty-state. The wiring from backend to frontend must be obvious and verifiable.

## What This Project Is

ChelatedAI is a research prototype for adaptive vector search with self-correcting embeddings. It detects "semantic collapse" in RAG systems (where unrelated concepts get similar embeddings) and fixes it through dynamic dimension masking and neural adaptation.

## Dependencies & Setup

Dependencies are managed via `pyproject.toml` (PEP 621) and `requirements.txt`:

```bash
pip install -r requirements.txt
# Or install as editable package:
pip install -e .
```

Optional: Ollama for Docker-based embeddings (`ollama:nomic-embed-text` model prefix).

## CI Pipeline

GitHub Actions workflow at `.github/workflows/test.yml`:
- **Lint job:** `ruff check .` on Python 3.11
- **Test job:** `unittest discover` across Python 3.9, 3.10, 3.11, 3.12 matrix

GitHub Actions workflow at `.github/workflows/build_firmware.yml`:
- **Firmware build job:** builds the RP2040/TinyUSB computational-storage firmware when `computational_storage_poc/firmware/**` changes and uploads UF2/ELF/BIN artifacts

GitHub Actions workflow at `.github/workflows/test.yml` also includes:
- **Computational-storage emulation job:** validates emulator semantics on hosted CI without privileged FUSE

## Running Tests

All tests use Python `unittest` (not pytest). CI does **not** install `pytest`, so do not add `pytest` imports or pytest-only fixtures to `test_*.py`. Run via CI or locally:

```bash
# Run a single test file
python test_unit_core.py

# Run a specific test class or method
python -m unittest test_unit_core.TestChelationAdapter.test_adapter_forward_pass

# Run all test files (bash glob)
for f in test_*.py; do python "$f"; done

# Discover and run all tests
python -m unittest discover -s . -p "test_*.py" -v
```

**Representative test files (`1082` tests passing on `main` as of 2026-03-12):**
- `test_unit_core.py` - Core adapter variants, BoundedAdapter, DimensionProjection training
- `test_noise_injection.py` - Noise injection validation under `unittest`
- `test_online_updater.py`, `test_dimension_mask_predictor.py`, `test_stability_tracker.py`
- `test_benchmark_beir.py`, `test_benchmark_comparative.py`, `test_dashboard_server.py`
- `test_sedimentation_loss.py` - InfoNCE, hybrid loss, hard negative miner, factory
- `test_kalman_lr.py` - Kalman-gain adaptive LR, variance behavior, clamping, engine integration
- `test_computational_storage_poc.py` - block-graph parity, latency invariants, and real-data storage round-trip validation
- `test_computational_storage_payload.py` - deterministic trigger-sector payload, host decoding, and virtual-disk interception validation
- `test_computational_storage_hardware_evidence.py` - deterministic evidence capture and Windows raw-device path handling
- `test_computational_storage_emulation.py` - dependency-light emulator parity and file-image validation
- `test_cross_lingual_distillation.py`, `test_language_detector.py`
- `test_topology_analyzer.py`, `test_isomer_detector.py`, `test_structural_health_report.py`
- `test_teacher_distillation.py`, `test_teacher_weight_scheduler.py`, `test_aep_orchestrator.py`

**Environment-dependent tests:** `test_antigravity_engine.py`, `test_adaptive_threshold.py`, and `test_memory_optimization.py` require full `torch` + `sentence-transformers` installed. They pass in CI but may fail locally without those dependencies.

## Architecture

### Flat file layout

All `.py` files live at the project root. No packages, no `__init__.py`. Imports are direct module references (e.g., `from antigravity_engine import AntigravityEngine`).

### Core module dependency graph

```
AntigravityEngine (antigravity_engine.py)  <- central entry point
|-- embedding_backend.py       # F-045: "ollama:model" -> HTTP, else -> SentenceTransformer
|-- vector_store.py            # F-044: QdrantVectorStore abstraction
|-- chelation_adapter.py       # 4 adapter types via create_adapter() factory + BoundedAdapter wrapper
|-- config.py                  # ChelationConfig with presets + validation
|-- chelation_logger.py        # get_logger() -> JSON structured logging
|-- teacher_distillation.py    # Offline/hybrid modes + DimensionProjection + EnsembleTeacherHelper
|-- teacher_weight_scheduler.py # 5 schedule types: constant/linear/cosine/step/adaptive
|-- sedimentation_loss.py      # InfoNCE, hybrid, hard-negative mining loss functions
|-- kalman_lr_scheduler.py     # Kalman-gain adaptive LR for sedimentation training
|-- sedimentation_trainer.py   # Shared homeostatic target logic
|-- checkpoint_manager.py      # SafeTrainingContext with SHA256 verification
|-- convergence_monitor.py     # Phase 1: patience-based early stopping
|-- online_updater.py          # Phase 3: inference-time micro-updates
|-- dimension_mask_predictor.py # Phase 4: learned dimension masking
|-- embedding_quality.py       # Phase 4: per-doc quality with decay weighting
`-- stability_tracker.py       # Phase 5: structural health diagnostics

RecursiveRetrievalEngine (recursive_decomposer.py)
|-- Uses AntigravityEngine for retrieval
|-- sedimentation.py           # HierarchicalSedimentationEngine
`-- config.py, chelation_logger.py, checkpoint_manager.py

AEPOrchestrator (aep_orchestrator.py)  <- 7-phase agentic remediation workflow
`-- chelation_logger.py

Dashboard & Sweeping (dashboard_server.py, run_sweep.py, run_large_sweep.py)
`-- Serves live metrics to localhost:8080 and manages parameter grid search.

Evaluation & Analysis Modules
|-- benchmark_beir.py                # Multi-dataset BEIR benchmarking + report generation
|-- cross_lingual_distillation.py    # Language-aware teacher routing for distillation
|-- language_detector.py             # Lightweight language detection with caching/fallbacks
|-- topology_analyzer.py             # Topology snapshots, bond matrices, cluster connectivity
`-- isomer_detector.py               # Query-result isomer detection built on topology signals
```

### Key design patterns

- **Adapter factory:** `create_adapter("mlp"|"procrustes"|"low_rank", input_dim, bounded=False)` returns one of three adapter types, optionally wrapped in `BoundedAdapter` for INT8-safe corrections. All initialize near-identity (small weight std 0.001) to preserve base model quality.
- **Config presets:** `ChelationConfig.get_preset(name, type)` supports `chelation`, `adapter`, `convergence`, `adapter_type`, `rlm`, `sedimentation`, `sedimentation_tuned`, `ensemble`, `cross_lingual`, `teacher_weight_schedule`, `teacher_encoding`, `online_update`, `beir`, `topology`, `isomer`, `bounded_adapter`, `sedimentation_loss`, and `kalman_lr`.
- **Sedimentation loss:** `engine.set_sedimentation_loss("mse"|"infonce"|"hybrid")` switches loss function. InfoNCE uses batch contrastive alignment; hybrid combines MSE + InfoNCE.
- **Kalman-gain adaptive LR:** `engine.enable_kalman_lr(process_noise, min_lr_ratio, max_lr_ratio)` modulates learning rate based on loss variance — high variance lowers LR, low variance raises it.
- **Noise Injection:** Dynamically scaled noise injection during sedimentation training.
- **Embedding backend routing:** Model names prefixed with `ollama:` use the HTTP API; all others use local SentenceTransformers.
- **Teacher distillation:** `DimensionProjection` for teacher-student dim mismatches, `EnsembleTeacherHelper` for multi-teacher weighted averaging, `TeacherWeightScheduler` for 5 dynamic schedule types.
- **AEP data model:** `Finding` objects with `Severity` (CRITICAL/HIGH/MEDIUM/LOW), `FindingStatus`, and `EffortSize` (S=1, M=3, L=5). Tiered remediation processes Critical->High->Medium->Low with no skipping.
- **Structural health reporting:** `antigravity_engine.py` now uses config-driven thresholds for persistent collapse, oscillation, topology cohesion, and isomer drift.

## Test Conventions

- **Mock logger:** `patch('module_name.get_logger')` returning `MagicMock()` -- used in nearly every test file.
- **In-memory Qdrant:** `qdrant_location=":memory:"` for isolation.
- **Local model for tests:** `model_name="all-MiniLM-L6-v2"` (requires sentence-transformers).
- **Temp files:** Tests use `tempfile` for filesystem isolation with cleanup in `tearDown`.
- **Sklearn usage:** Keep core runtime code on numpy/torch. `scikit-learn` is only acceptable in isolated validation flows such as the computational-storage digits test track.
- **Python 3.9 CI compatibility:** If a module imported by tests uses `X | None` annotations, add `from __future__ import annotations` or use `Optional[...]`.

## Git Workflow Notes

- The repository branch policy may still show PRs as blocked even after all required checks are green. Session 23 required admin merges for `#80`, `#83`, and `#82`.
- Session 27 required admin merges for `#90`, `#91`, `#92`, and `#93` even after all required checks passed.
- `gh pr merge` can fail if a local worktree is holding `main`. Before merging stacked PRs, remove/prune merged worktrees or switch them off `main`.
- The computational-storage split is complete on `main` as of 2026-03-06: `#86` landed the validation foundation, `#87` landed the payload transport path, and `#88` landed the session-wrap docs.
- Session 26 follow-up PRs `#90` (hardware evidence capture tool), `#91` (emulation CI), `#92` (transport scope lock), and `#93` (retention policy) are merged on `main` as of 2026-03-06. The dated retention review was executed and merged as PR `#108` on 2026-04-24; the remaining computational-storage follow-through is real hardware evidence capture on actual RP2040 hardware.
- Session 31 merged PRs `#96`–`#103` (weight refinement, docs cleanup, 4 features, session wrap). The adapter checkpoint contamination risk from Session 28 is resolved (isolated checkpoints per config).
- Session 32 ended with an explicit no-promotion outcome for preset/default changes. Do not treat `mlp` + `teacher_weight=0.3` as promotable until a second independent run plus multi-task/BEIR transfer checks confirm it. See `docs/weight-refinement-campaign-results-2026-04-25-session32.md`.
- Do not revive the stale computational-storage PR `#84` or the old `feat/session22-online-correction` branch line. If historical comparison is needed, use the local `backup/retired-*` refs instead.
- If no RP2040 device is attached, do not fabricate hardware evidence. Use `computational_storage_poc/capture_hardware_evidence.py` once actual hardware is available.
- Explicit Windows raw-device paths like `\\.\PhysicalDrive2` are valid inputs to `usb_host_inference.py` and `capture_hardware_evidence.py`; do not rewrite them into a second `PhysicalDrive` prefix.
- Do not treat unrelated removable USB storage as RP2040 evidence. Session 27's local probe only found a SanDisk removable drive, which was not used as a proxy.
- If a resumed benchmark campaign is no longer the active task, stop the live process instead of leaving it consuming CPU in the background.
- `ruff check` does not validate GitHub Actions YAML. Keep workflow-file review separate from Python lint.
- Local `git status` may show `?? .claude/`; that directory holds local worktree metadata and retired-branch artifacts and is not, by itself, a product-code diff.

**2026-05-15 Desktop Reconciliation:**
- Desktop machine (previously 23 commits behind) reconciled with laptop work on branch `reconciliation/2026-05-desktop-sync`.
- Merged `origin/main` (BHS v3.3 tooling + full Model-Scope + heavy remediation).
- All major laptop 10-phase planning artifacts (`FINAL_PLAN.md` family + `panel-analysis/`) landed into `docs/ARCH AGENTIC ENGINEERING AND PLANNING/planning/2026-05-reconciliation/`.
- New computational storage POC code (packed/CPU/sparse/repo-graph/MoE/REAP) moved to `feat/post-merge-comp-storage-substrate`.
- Real BHS v3.3 scripts now live in `scripts/` (`validate_pr_brutal_honesty.py`, `validate_v33_schema_drift.py`, `smoke_pipeline.py`, etc.), though full wiring into AEP is still in progress.
- Reconciliation reimplementation backlog created and being actively maintained.

## Reference Material

- `rlm_reference/` -- Cloned RLM paper implementation (read-only, do not modify)
- `docs/rlm-analysis.md` -- Analysis of RLM source code
- `docs/REFERENCES.md` -- 17 research paper citations
- `docs/ARCH AGENTIC ENGINEERING AND PLANNING/` -- Full AEP workflow documentation (60+ files)
- `docs/INDEX.md` -- Documentation index

?? research/

 exited 1 in 818ms:
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Brutal Honesty Convention (load-bearing — read first)

See `docs/conventions/brutal-honesty-rulebook.md` (v3.3 — v3.2 executable PR gates plus L13 soft-prose-claimed-as-mechanical and the v3.3 schema/prose/artifact drift validator. v3.2 baseline: Tier B independence enforcement via `BHS_*_AGENT` lines, severity caps on Tier B scores, `DEFERRED_SCOPE:` tracking, quantitative cycle definition, override structural barrier at BLOCKED, executable validators in `scripts/`).

**Premise**: Assume every implementation/completion claim is false until independently proven by runtime evidence. Tests existing, routes existing, docs saying complete, PR text saying complete, agent claims, and even self-attested brutal-honesty sections are NOT evidence. Evidence is: command output from the production code path, real UI/API behavior, persistence/state mutation, artifact/replay/checkpoint surviving a fresh checkout, or an independent reviewer who tried to disprove and failed.

**Five hard rules**:

1. **Evidence rule.** Every PR with a "complete" claim includes `EVIDENCE:` and `SMOKE:` lines pointing at runtime evidence — not tests.
2. **Visible means verified.** Incomplete code may live in the repo; it may NOT be presented as a working feature (no UI surfacing, no API doc, no release-note implication, no roadmap tick).
3. **Mandatory PR brutal-honesty section.** Every PR body ends with a `## Brutal Honesty` section disclosing stubs (L1), escape conditionals (L2), mocks in production paths (L3), partial implementations (L4), untested production paths (L5/L8/L12), broad-catch swallowing (L11), and any other §1 instance with `file:line`. Empty answers must be justified, not omitted (template in §4 of the rulebook).
4. **Adversarial cross-agent review.** Implementation slices are done by a fresh sub-agent; a second fresh sub-agent is given the diff + brutal-honesty section + EVIDENCE + smoke command + this rulebook, and is asked to **try to disprove** completion. Not verify — *disprove*.
5. **One deterministic smoke path per repo.** `scripts/smoke.sh` (or equivalent) is the release gate. **Two honest tiers per §1 Rule 5**: Floor (import + surface-check through production code paths — the v3.2 minimum) is acceptable IF the PR body's SMOKE: line names which tier was run AND any ceiling-tier gap appears as a Carried Debt entry. Ceiling (true end-to-end against a real fixture) is the target. Claiming "smoke passed" at ceiling-tier when only floor-tier was run is itself L4 partial-as-complete. If smoke fails, the release is not ready regardless of test count.

**Trigger phrases** (use these instead of "is this done?"): *"Be brutally honest." / "What did you fake to get here?" / "If I ran this on a fresh checkout right now, what would actually fail?" / "Tell me the smallest concrete thing that does NOT work yet." / "Show me the runtime evidence, not the test." / "If I disable this feature behind a flag, what visible behavior changes?"*

**The Remediation Loop (meta-component, §6 of the rulebook; v3.1 closes the carry-forward loophole)** — applies to this repo:
- **Tier A (per-PR)**: 5 iterations max. Implementer self-scores `BHS_SELF_DRAFT` after each. If self-score < 100 at iteration 5, the PR is **withdrawn or scope-reduced** — it does NOT ship at <100 hoping for carry-forward. Same gap surviving 2 iterations → escalate, don't loop more.
- **Tier B (pre-merge — THE OFFICIAL SCORER)**: one fresh adversarial agent assigns `BHS_TIER_B` independently. `BHS_OFFICIAL = min(BHS_SELF_DRAFT, BHS_TIER_B)`. **Hard merge gate: only PRs with `BHS_OFFICIAL = 100` may merge.** No "ships with caveats" path. Self-vs-Tier-B gap > 5 → automatic L4 score-gaming disclosure.
- **Tier C (cross-PR / session boundary)**: aggregate disclosures into `docs/next-session.md` "Carried Debt" with TTL = 1 cycle. Items not cleared in the next cycle flip the **block flag** to BLOCKED — the cycle after that forbids ALL new feature work until Carried Debt = 0. **No two-cycle slop.** Block flag is automatic from the math.
- **BHS scale (only 100 ships)**: 100 = merges · 90–99 = does not merge, back to Tier A · 70–89 = does not merge, reduce scope · 50–69 = does not merge, scaffold-only allowed · 0–49 = draft, not a PR.
- **Operator override**: rulebook can't prevent the operator from typing the merge command at <100; it makes the override visible (`OPERATOR_OVERRIDE:` line) and force-creates a top-priority carried-debt entry for the next cycle.
- **PR body required lines**: `BHS_SELF_DRAFT`, `BHS_SELF_DRAFT_AGENT`, `BHS_TIER_B`, `BHS_TIER_B_AGENT`, `BHS_TIER_B_SEVERITY`, `BHS_OFFICIAL`, `CARRY_FORWARD`, `DEFERRED_SCOPE`, `LOOP_ITERATIONS`, `OPERATOR_OVERRIDE` (per §4 template).
- **Why this is in CLAUDE.md, not in a skill or MCP server**: skills can fail to load, agents can spin up without MCP context, the convention can't. This file is auto-loaded every session — the convention is the meta-component.

**Lie taxonomy L1–L13** is in §1 of the rulebook; quote by number when calling out a failure. Speculating is itself a lie — *"I don't know"* is the correct answer when there is no evidence.

## Session Rules (enforced every session, no exceptions)

### Rule 1 — Full Implementation Only
Every slice, every phase, every PR must be **fully implemented**. No scaffolding, no stubs, no `pass`-body placeholders, no `TODO: implement` markers left in committed code. If a feature is too large for one PR, split it into genuinely shippable sub-slices — but each sub-slice must itself be complete and working, not a placeholder for future work.

### Rule 2 — PR on Completion
Every unit of completed work must land in a PR **before moving to the next task**. Do not accumulate dirty branches, uncommitted changes, or local-only work across sessions. The rule is: finish a slice → open PR → get it merged (or at minimum opened and green) → then start the next slice. Stale branches and deferred cleanup are not acceptable.

### Rule 3 — No Placeholder Data or Fake Metrics
Dashboards, frontends, and reporting surfaces must be wired to **real data pipelines**. Placeholder values, hard-coded demo numbers, mock metrics, and fake responses are forbidden in committed code. Every data field displayed to an operator must trace to an actual source artifact, live computation, or explicitly documented empty-state. The wiring from backend to frontend must be obvious and verifiable.

## What This Project Is

ChelatedAI is a research prototype for adaptive vector search with self-correcting embeddings. It detects "semantic collapse" in RAG systems (where unrelated concepts get similar embeddings) and fixes it through dynamic dimension masking and neural adaptation.

## Dependencies & Setup

Dependencies are managed via `pyproject.toml` (PEP 621) and `requirements.txt`:

```bash
pip install -r requirements.txt
# Or install as editable package:
pip install -e .
```

Optional: Ollama for Docker-based embeddings (`ollama:nomic-embed-text` model prefix).

## CI Pipeline

GitHub Actions workflow at `.github/workflows/test.yml`:
- **Lint job:** `ruff check .` on Python 3.11
- **Test job:** `unittest discover` across Python 3.9, 3.10, 3.11, 3.12 matrix

GitHub Actions workflow at `.github/workflows/build_firmware.yml`:
- **Firmware build job:** builds the RP2040/TinyUSB computational-storage firmware when `computational_storage_poc/firmware/**` changes and uploads UF2/ELF/BIN artifacts

GitHub Actions workflow at `.github/workflows/test.yml` also includes:
- **Computational-storage emulation job:** validates emulator semantics on hosted CI without privileged FUSE

## Running Tests

All tests use Python `unittest` (not pytest). CI does **not** install `pytest`, so do not add `pytest` imports or pytest-only fixtures to `test_*.py`. Run via CI or locally:

```bash
# Run a single test file
python test_unit_core.py

# Run a specific test class or method
python -m unittest test_unit_core.TestChelationAdapter.test_adapter_forward_pass

# Run all test files (bash glob)
for f in test_*.py; do python "$f"; done

# Discover and run all tests
python -m unittest discover -s . -p "test_*.py" -v
```

**Representative test files (`1082` tests passing on `main` as of 2026-03-12):**
- `test_unit_core.py` - Core adapter variants, BoundedAdapter, DimensionProjection training
- `test_noise_injection.py` - Noise injection validation under `unittest`
- `test_online_updater.py`, `test_dimension_mask_predictor.py`, `test_stability_tracker.py`
- `test_benchmark_beir.py`, `test_benchmark_comparative.py`, `test_dashboard_server.py`
- `test_sedimentation_loss.py` - InfoNCE, hybrid loss, hard negative miner, factory
- `test_kalman_lr.py` - Kalman-gain adaptive LR, variance behavior, clamping, engine integration
- `test_computational_storage_poc.py` - block-graph parity, latency invariants, and real-data storage round-trip validation
- `test_computational_storage_payload.py` - deterministic trigger-sector payload, host decoding, and virtual-disk interception validation
- `test_computational_storage_hardware_evidence.py` - deterministic evidence capture and Windows raw-device path handling
- `test_computational_storage_emulation.py` - dependency-light emulator parity and file-image validation
- `test_cross_lingual_distillation.py`, `test_language_detector.py`
- `test_topology_analyzer.py`, `test_isomer_detector.py`, `test_structural_health_report.py`
- `test_teacher_distillation.py`, `test_teacher_weight_scheduler.py`, `test_aep_orchestrator.py`

**Environment-dependent tests:** `test_antigravity_engine.py`, `test_adaptive_threshold.py`, and `test_memory_optimization.py` require full `torch` + `sentence-transformers` installed. They pass in CI but may fail locally without those dependencies.

## Architecture

### Flat file layout

All `.py` files live at the project root. No packages, no `__init__.py`. Imports are direct module references (e.g., `from antigravity_engine import AntigravityEngine`).

### Core module dependency graph

```
AntigravityEngine (antigravity_engine.py)  <- central entry point
|-- embedding_backend.py       # F-045: "ollama:model" -> HTTP, else -> SentenceTransformer
|-- vector_store.py            # F-044: QdrantVectorStore abstraction
|-- chelation_adapter.py       # 4 adapter types via create_adapter() factory + BoundedAdapter wrapper
|-- config.py                  # ChelationConfig with presets + validation
|-- chelation_logger.py        # get_logger() -> JSON structured logging
|-- teacher_distillation.py    # Offline/hybrid modes + DimensionProjection + EnsembleTeacherHelper
|-- teacher_weight_scheduler.py # 5 schedule types: constant/linear/cosine/step/adaptive
|-- sedimentation_loss.py      # InfoNCE, hybrid, hard-negative mining loss functions
|-- kalman_lr_scheduler.py     # Kalman-gain adaptive LR for sedimentation training
|-- sedimentation_trainer.py   # Shared homeostatic target logic
|-- checkpoint_manager.py      # SafeTrainingContext with SHA256 verification
|-- convergence_monitor.py     # Phase 1: patience-based early stopping
|-- online_updater.py          # Phase 3: inference-time micro-updates
|-- dimension_mask_predictor.py # Phase 4: learned dimension masking
|-- embedding_quality.py       # Phase 4: per-doc quality with decay weighting
`-- stability_tracker.py       # Phase 5: structural health diagnostics

RecursiveRetrievalEngine (recursive_decomposer.py)
|-- Uses AntigravityEngine for retrieval
|-- sedimentation.py           # HierarchicalSedimentationEngine
`-- config.py, chelation_logger.py, checkpoint_manager.py

AEPOrchestrator (aep_orchestrator.py)  <- 7-phase agentic remediation workflow
`-- chelation_logger.py

Dashboard & Sweeping (dashboard_server.py, run_sweep.py, run_large_sweep.py)
`-- Serves live metrics to localhost:8080 and manages parameter grid search.

Evaluation & Analysis Modules
|-- benchmark_beir.py                # Multi-dataset BEIR benchmarking + report generation
|-- cross_lingual_distillation.py    # Language-aware teacher routing for distillation
|-- language_detector.py             # Lightweight language detection with caching/fallbacks
|-- topology_analyzer.py             # Topology snapshots, bond matrices, cluster connectivity
`-- isomer_detector.py               # Query-result isomer detection built on topology signals
```

### Key design patterns

- **Adapter factory:** `create_adapter("mlp"|"procrustes"|"low_rank", input_dim, bounded=False)` returns one of three adapter types, optionally wrapped in `BoundedAdapter` for INT8-safe corrections. All initialize near-identity (small weight std 0.001) to preserve base model quality.
- **Config presets:** `ChelationConfig.get_preset(name, type)` supports `chelation`, `adapter`, `convergence`, `adapter_type`, `rlm`, `sedimentation`, `sedimentation_tuned`, `ensemble`, `cross_lingual`, `teacher_weight_schedule`, `teacher_encoding`, `online_update`, `beir`, `topology`, `isomer`, `bounded_adapter`, `sedimentation_loss`, and `kalman_lr`.
- **Sedimentation loss:** `engine.set_sedimentation_loss("mse"|"infonce"|"hybrid")` switches loss function. InfoNCE uses batch contrastive alignment; hybrid combines MSE + InfoNCE.
- **Kalman-gain adaptive LR:** `engine.enable_kalman_lr(process_noise, min_lr_ratio, max_lr_ratio)` modulates learning rate based on loss variance — high variance lowers LR, low variance raises it.
- **Noise Injection:** Dynamically scaled noise injection during sedimentation training.
- **Embedding backend routing:** Model names prefixed with `ollama:` use the HTTP API; all others use local SentenceTransformers.
- **Teacher distillation:** `DimensionProjection` for teacher-student dim mismatches, `EnsembleTeacherHelper` for multi-teacher weighted averaging, `TeacherWeightScheduler` for 5 dynamic schedule types.
- **AEP data model:** `Finding` objects with `Severity` (CRITICAL/HIGH/MEDIUM/LOW), `FindingStatus`, and `EffortSize` (S=1, M=3, L=5). Tiered remediation processes Critical->High->Medium->Low with no skipping.
- **Structural health reporting:** `antigravity_engine.py` now uses config-driven thresholds for persistent collapse, oscillation, topology cohesion, and isomer drift.

## Test Conventions

- **Mock logger:** `patch('module_name.get_logger')` returning `MagicMock()` -- used in nearly every test file.
- **In-memory Qdrant:** `qdrant_location=":memory:"` for isolation.
- **Local model for tests:** `model_name="all-MiniLM-L6-v2"` (requires sentence-transformers).
- **Temp files:** Tests use `tempfile` for filesystem isolation with cleanup in `tearDown`.
- **Sklearn usage:** Keep core runtime code on numpy/torch. `scikit-learn` is only acceptable in isolated validation flows such as the computational-storage digits test track.
- **Python 3.9 CI compatibility:** If a module imported by tests uses `X | None` annotations, add `from __future__ import annotations` or use `Optional[...]`.

## Git Workflow Notes

- The repository branch policy may still show PRs as blocked even after all required checks are green. Session 23 required admin merges for `#80`, `#83`, and `#82`.
- Session 27 required admin merges for `#90`, `#91`, `#92`, and `#93` even after all required checks passed.
- `gh pr merge` can fail if a local worktree is holding `main`. Before merging stacked PRs, remove/prune merged worktrees or switch them off `main`.
- The computational-storage split is complete on `main` as of 2026-03-06: `#86` landed the validation foundation, `#87` landed the payload transport path, and `#88` landed the session-wrap docs.
- Session 26 follow-up PRs `#90` (hardware evidence capture tool), `#91` (emulation CI), `#92` (transport scope lock), and `#93` (retention policy) are merged on `main` as of 2026-03-06. The dated retention review was executed and merged as PR `#108` on 2026-04-24; the remaining computational-storage follow-through is real hardware evidence capture on actual RP2040 hardware.
- Session 31 merged PRs `#96`–`#103` (weight refinement, docs cleanup, 4 features, session wrap). The adapter checkpoint contamination risk from Session 28 is resolved (isolated checkpoints per config).
- Session 32 ended with an explicit no-promotion outcome for preset/default changes. Do not treat `mlp` + `teacher_weight=0.3` as promotable until a second independent run plus multi-task/BEIR transfer checks confirm it. See `docs/weight-refinement-campaign-results-2026-04-25-session32.md`.
- Do not revive the stale computational-storage PR `#84` or the old `feat/session22-online-correction` branch line. If historical comparison is needed, use the local `backup/retired-*` refs instead.
- If no RP2040 device is attached, do not fabricate hardware evidence. Use `computational_storage_poc/capture_hardware_evidence.py` once actual hardware is available.
- Explicit Windows raw-device paths like `\\.\PhysicalDrive2` are valid inputs to `usb_host_inference.py` and `capture_hardware_evidence.py`; do not rewrite them into a second `PhysicalDrive` prefix.
- Do not treat unrelated removable USB storage as RP2040 evidence. Session 27's local probe only found a SanDisk removable drive, which was not used as a proxy.
- If a resumed benchmark campaign is no longer the active task, stop the live process instead of leaving it consuming CPU in the background.
- `ruff check` does not validate GitHub Actions YAML. Keep workflow-file review separate from Python lint.
- Local `git status` may show `?? .claude/`; that directory holds local worktree metadata and retired-branch artifacts and is not, by itself, a product-code diff.

**2026-05-15 Desktop Reconciliation:**
- Desktop machine (previously 23 commits behind) reconciled with laptop work on branch `reconciliation/2026-05-desktop-sync`.
- Merged `origin/main` (BHS v3.3 tooling + full Model-Scope + heavy remediation).
- All major laptop 10-phase planning artifacts (`FINAL_PLAN.md` family + `panel-analysis/`) landed into `docs/ARCH AGENTIC ENGINEERING AND PLANNING/planning/2026-05-reconciliation/`.
- New computational storage POC code (packed/CPU/sparse/repo-graph/MoE/REAP) moved to `feat/post-merge-comp-storage-substrate`.
- Real BHS v3.3 scripts now live in `scripts/` (`validate_pr_brutal_honesty.py`, `validate_v33_schema_drift.py`, `smoke_pipeline.py`, etc.), though full wiring into AEP is still in progress.
- Reconciliation reimplementation backlog created and being actively maintained.

## Reference Material

- `rlm_reference/` -- Cloned RLM paper implementation (read-only, do not modify)
- `docs/rlm-analysis.md` -- Analysis of RLM source code
- `docs/REFERENCES.md` -- 17 research paper citations
- `docs/ARCH AGENTIC ENGINEERING AND PLANNING/` -- Full AEP workflow documentation (60+ files)
- `docs/INDEX.md` -- Documentation index

?? research/

 succeeded in 822ms:
LINES=1052
---
name: python-testing-patterns
description: Implement comprehensive testing strategies with pytest, fixtures,
  mocking, and test-driven development. Use when writing Python tests, setting
  up test suites, or implementing testing best practices.
---

# Python Testing Patterns

Comprehensive guide to implementing robust testing strategies in Python using pytest, fixtures, mocking, parameterization, and test-driven development practices.

## When to Use This Skill

- Writing unit tests for Python code
- Setting up test suites and test infrastructure
- Implementing test-driven development (TDD)
- Creating integration tests for APIs and services
- Mocking external dependencies and services
- Testing async code and concurrent operations
- Setting up continuous testing in CI/CD
- Implementing property-based testing
- Testing database operations
- Debugging failing tests

## Core Concepts

### 1. Test Types

- **Unit Tests**: Test individual functions/classes in isolation
- **Integration Tests**: Test interaction between components
- **Functional Tests**: Test complete features end-to-end
- **Performance Tests**: Measure speed and resource usage

### 2. Test Structure (AAA Pattern)

- **Arrange**: Set up test data and preconditions
- **Act**: Execute the code under test
- **Assert**: Verify the results

### 3. Test Coverage

- Measure what code is exercised by tests
- Identify untested code paths
- Aim for meaningful coverage, not just high percentages

### 4. Test Isolation

- Tests should be independent
- No shared state between tests
- Each test should clean up after itself

## Quick Start

```python
# test_example.py
def add(a, b):
    return a + b

def test_add():
    """Basic test example."""
    result = add(2, 3)
    assert result == 5

def test_add_negative():
    """Test with negative numbers."""
    assert add(-1, 1) == 0

# Run with: pytest test_example.py
```

## Fundamental Patterns

### Pattern 1: Basic pytest Tests

```python
# test_calculator.py
import pytest

class Calculator:
    """Simple calculator for testing."""

    def add(self, a: float, b: float) -> float:
        return a + b

    def subtract(self, a: float, b: float) -> float:
        return a - b

    def multiply(self, a: float, b: float) -> float:
        return a * b

    def divide(self, a: float, b: float) -> float:
        if b == 0:
            raise ValueError("Cannot divide by zero")
        return a / b


def test_addition():
    """Test addition."""
    calc = Calculator()
    assert calc.add(2, 3) == 5
    assert calc.add(-1, 1) == 0
    assert calc.add(0, 0) == 0


def test_subtraction():
    """Test subtraction."""
    calc = Calculator()
    assert calc.subtract(5, 3) == 2
    assert calc.subtract(0, 5) == -5


def test_multiplication():
    """Test multiplication."""
    calc = Calculator()
    assert calc.multiply(3, 4) == 12
    assert calc.multiply(0, 5) == 0


def test_division():
    """Test division."""
    calc = Calculator()
    assert calc.divide(6, 3) == 2
    assert calc.divide(5, 2) == 2.5


def test_division_by_zero():
    """Test division by zero raises error."""
    calc = Calculator()
    with pytest.raises(ValueError, match="Cannot divide by zero"):
        calc.divide(5, 0)
```

### Pattern 2: Fixtures for Setup and Teardown

```python
# test_database.py
import pytest
from typing import Generator

class Database:
    """Simple database class."""

    def __init__(self, connection_string: str):
        self.connection_string = connection_string
        self.connected = False

    def connect(self):
        """Connect to database."""
        self.connected = True

    def disconnect(self):
        """Disconnect from database."""
        self.connected = False

    def query(self, sql: str) -> list:
        """Execute query."""
        if not self.connected:
            raise RuntimeError("Not connected")
        return [{"id": 1, "name": "Test"}]


@pytest.fixture
def db() -> Generator[Database, None, None]:
    """Fixture that provides connected database."""
    # Setup
    database = Database("sqlite:///:memory:")
    database.connect()

    # Provide to test
    yield database

    # Teardown
    database.disconnect()


def test_database_query(db):
    """Test database query with fixture."""
    results = db.query("SELECT * FROM users")
    assert len(results) == 1
    assert results[0]["name"] == "Test"


@pytest.fixture(scope="session")
def app_config():
    """Session-scoped fixture - created once per test session."""
    return {
        "database_url": "postgresql://localhost/test",
        "api_key": "test-key",
        "debug": True
    }


@pytest.fixture(scope="module")
def api_client(app_config):
    """Module-scoped fixture - created once per test module."""
    # Setup expensive resource
    client = {"config": app_config, "session": "active"}
    yield client
    # Cleanup
    client["session"] = "closed"


def test_api_client(api_client):
    """Test using api client fixture."""
    assert api_client["session"] == "active"
    assert api_client["config"]["debug"] is True
```

### Pattern 3: Parameterized Tests

```python
# test_validation.py
import pytest

def is_valid_email(email: str) -> bool:
    """Check if email is valid."""
    return "@" in email and "." in email.split("@")[1]


@pytest.mark.parametrize("email,expected", [
    ("user@example.com", True),
    ("test.user@domain.co.uk", True),
    ("invalid.email", False),
    ("@example.com", False),
    ("user@domain", False),
    ("", False),
])
def test_email_validation(email, expected):
    """Test email validation with various inputs."""
    assert is_valid_email(email) == expected


@pytest.mark.parametrize("a,b,expected", [
    (2, 3, 5),
    (0, 0, 0),
    (-1, 1, 0),
    (100, 200, 300),
    (-5, -5, -10),
])
def test_addition_parameterized(a, b, expected):
    """Test addition with multiple parameter sets."""
    from test_calculator import Calculator
    calc = Calculator()
    assert calc.add(a, b) == expected


# Using pytest.param for special cases
@pytest.mark.parametrize("value,expected", [
    pytest.param(1, True, id="positive"),
    pytest.param(0, False, id="zero"),
    pytest.param(-1, False, id="negative"),
])
def test_is_positive(value, expected):
    """Test with custom test IDs."""
    assert (value > 0) == expected
```

### Pattern 4: Mocking with unittest.mock

```python
# test_api_client.py
import pytest
from unittest.mock import Mock, patch, MagicMock
import requests

class APIClient:
    """Simple API client."""

    def __init__(self, base_url: str):
        self.base_url = base_url

    def get_user(self, user_id: int) -> dict:
        """Fetch user from API."""
        response = requests.get(f"{self.base_url}/users/{user_id}")
        response.raise_for_status()
        return response.json()

    def create_user(self, data: dict) -> dict:
        """Create new user."""
        response = requests.post(f"{self.base_url}/users", json=data)

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\skills\\python-testing-patterns\\SKILL.md'; "'$lines=Get-Content $p; "LINES=$($lines.Count)"; $lines | Select-Object -First 220' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 714ms:
LINES=1052
---
name: python-testing-patterns
description: Implement comprehensive testing strategies with pytest, fixtures,
  mocking, and test-driven development. Use when writing Python tests, setting
  up test suites, or implementing testing best practices.
---

# Python Testing Patterns

Comprehensive guide to implementing robust testing strategies in Python using pytest, fixtures, mocking, parameterization, and test-driven development practices.

## When to Use This Skill

- Writing unit tests for Python code
- Setting up test suites and test infrastructure
- Implementing test-driven development (TDD)
- Creating integration tests for APIs and services
- Mocking external dependencies and services
- Testing async code and concurrent operations
- Setting up continuous testing in CI/CD
- Implementing property-based testing
- Testing database operations
- Debugging failing tests

## Core Concepts

### 1. Test Types

- **Unit Tests**: Test individual functions/classes in isolation
- **Integration Tests**: Test interaction between components
- **Functional Tests**: Test complete features end-to-end
- **Performance Tests**: Measure speed and resource usage

### 2. Test Structure (AAA Pattern)

- **Arrange**: Set up test data and preconditions
- **Act**: Execute the code under test
- **Assert**: Verify the results

### 3. Test Coverage

- Measure what code is exercised by tests
- Identify untested code paths
- Aim for meaningful coverage, not just high percentages

### 4. Test Isolation

- Tests should be independent
- No shared state between tests
- Each test should clean up after itself

## Quick Start

```python
# test_example.py
def add(a, b):
    return a + b

def test_add():
    """Basic test example."""
    result = add(2, 3)
    assert result == 5

def test_add_negative():
    """Test with negative numbers."""
    assert add(-1, 1) == 0

# Run with: pytest test_example.py
```

## Fundamental Patterns

### Pattern 1: Basic pytest Tests

```python
# test_calculator.py
import pytest

class Calculator:
    """Simple calculator for testing."""

    def add(self, a: float, b: float) -> float:
        return a + b

    def subtract(self, a: float, b: float) -> float:
        return a - b

    def multiply(self, a: float, b: float) -> float:
        return a * b

    def divide(self, a: float, b: float) -> float:
        if b == 0:
            raise ValueError("Cannot divide by zero")
        return a / b


def test_addition():
    """Test addition."""
    calc = Calculator()
    assert calc.add(2, 3) == 5
    assert calc.add(-1, 1) == 0
    assert calc.add(0, 0) == 0


def test_subtraction():
    """Test subtraction."""
    calc = Calculator()
    assert calc.subtract(5, 3) == 2
    assert calc.subtract(0, 5) == -5


def test_multiplication():
    """Test multiplication."""
    calc = Calculator()
    assert calc.multiply(3, 4) == 12
    assert calc.multiply(0, 5) == 0


def test_division():
    """Test division."""
    calc = Calculator()
    assert calc.divide(6, 3) == 2
    assert calc.divide(5, 2) == 2.5


def test_division_by_zero():
    """Test division by zero raises error."""
    calc = Calculator()
    with pytest.raises(ValueError, match="Cannot divide by zero"):
        calc.divide(5, 0)
```

### Pattern 2: Fixtures for Setup and Teardown

```python
# test_database.py
import pytest
from typing import Generator

class Database:
    """Simple database class."""

    def __init__(self, connection_string: str):
        self.connection_string = connection_string
        self.connected = False

    def connect(self):
        """Connect to database."""
        self.connected = True

    def disconnect(self):
        """Disconnect from database."""
        self.connected = False

    def query(self, sql: str) -> list:
        """Execute query."""
        if not self.connected:
            raise RuntimeError("Not connected")
        return [{"id": 1, "name": "Test"}]


@pytest.fixture
def db() -> Generator[Database, None, None]:
    """Fixture that provides connected database."""
    # Setup
    database = Database("sqlite:///:memory:")
    database.connect()

    # Provide to test
    yield database

    # Teardown
    database.disconnect()


def test_database_query(db):
    """Test database query with fixture."""
    results = db.query("SELECT * FROM users")
    assert len(results) == 1
    assert results[0]["name"] == "Test"


@pytest.fixture(scope="session")
def app_config():
    """Session-scoped fixture - created once per test session."""
    return {
        "database_url": "postgresql://localhost/test",
        "api_key": "test-key",
        "debug": True
    }


@pytest.fixture(scope="module")
def api_client(app_config):
    """Module-scoped fixture - created once per test module."""
    # Setup expensive resource
    client = {"config": app_config, "session": "active"}
    yield client
    # Cleanup
    client["session"] = "closed"


def test_api_client(api_client):
    """Test using api client fixture."""
    assert api_client["session"] == "active"
    assert api_client["config"]["debug"] is True
```

### Pattern 3: Parameterized Tests

```python
# test_validation.py
import pytest

def is_valid_email(email: str) -> bool:
    """Check if email is valid."""
    return "@" in email and "." in email.split("@")[1]


@pytest.mark.parametrize("email,expected", [

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\skills\\python-testing-patterns\\SKILL.md'; Get-Content "'$p | Select-Object -Skip 220 -First 220' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1070ms:
    ("user@example.com", True),
    ("test.user@domain.co.uk", True),
    ("invalid.email", False),
    ("@example.com", False),
    ("user@domain", False),
    ("", False),
])
def test_email_validation(email, expected):
    """Test email validation with various inputs."""
    assert is_valid_email(email) == expected


@pytest.mark.parametrize("a,b,expected", [
    (2, 3, 5),
    (0, 0, 0),
    (-1, 1, 0),
    (100, 200, 300),
    (-5, -5, -10),
])
def test_addition_parameterized(a, b, expected):
    """Test addition with multiple parameter sets."""
    from test_calculator import Calculator
    calc = Calculator()
    assert calc.add(a, b) == expected


# Using pytest.param for special cases
@pytest.mark.parametrize("value,expected", [
    pytest.param(1, True, id="positive"),
    pytest.param(0, False, id="zero"),
    pytest.param(-1, False, id="negative"),
])
def test_is_positive(value, expected):
    """Test with custom test IDs."""
    assert (value > 0) == expected
```

### Pattern 4: Mocking with unittest.mock

```python
# test_api_client.py
import pytest
from unittest.mock import Mock, patch, MagicMock
import requests

class APIClient:
    """Simple API client."""

    def __init__(self, base_url: str):
        self.base_url = base_url

    def get_user(self, user_id: int) -> dict:
        """Fetch user from API."""
        response = requests.get(f"{self.base_url}/users/{user_id}")
        response.raise_for_status()
        return response.json()

    def create_user(self, data: dict) -> dict:
        """Create new user."""
        response = requests.post(f"{self.base_url}/users", json=data)
        response.raise_for_status()
        return response.json()


def test_get_user_success():
    """Test successful API call with mock."""
    client = APIClient("https://api.example.com")

    mock_response = Mock()
    mock_response.json.return_value = {"id": 1, "name": "John Doe"}
    mock_response.raise_for_status.return_value = None

    with patch("requests.get", return_value=mock_response) as mock_get:
        user = client.get_user(1)

        assert user["id"] == 1
        assert user["name"] == "John Doe"
        mock_get.assert_called_once_with("https://api.example.com/users/1")


def test_get_user_not_found():
    """Test API call with 404 error."""
    client = APIClient("https://api.example.com")

    mock_response = Mock()
    mock_response.raise_for_status.side_effect = requests.HTTPError("404 Not Found")

    with patch("requests.get", return_value=mock_response):
        with pytest.raises(requests.HTTPError):
            client.get_user(999)


@patch("requests.post")
def test_create_user(mock_post):
    """Test user creation with decorator syntax."""
    client = APIClient("https://api.example.com")

    mock_post.return_value.json.return_value = {"id": 2, "name": "Jane Doe"}
    mock_post.return_value.raise_for_status.return_value = None

    user_data = {"name": "Jane Doe", "email": "jane@example.com"}
    result = client.create_user(user_data)

    assert result["id"] == 2
    mock_post.assert_called_once()
    call_args = mock_post.call_args
    assert call_args.kwargs["json"] == user_data
```

### Pattern 5: Testing Exceptions

```python
# test_exceptions.py
import pytest

def divide(a: float, b: float) -> float:
    """Divide a by b."""
    if b == 0:
        raise ZeroDivisionError("Division by zero")
    if not isinstance(a, (int, float)) or not isinstance(b, (int, float)):
        raise TypeError("Arguments must be numbers")
    return a / b


def test_zero_division():
    """Test exception is raised for division by zero."""
    with pytest.raises(ZeroDivisionError):
        divide(10, 0)


def test_zero_division_with_message():
    """Test exception message."""
    with pytest.raises(ZeroDivisionError, match="Division by zero"):
        divide(5, 0)


def test_type_error():
    """Test type error exception."""
    with pytest.raises(TypeError, match="must be numbers"):
        divide("10", 5)


def test_exception_info():
    """Test accessing exception info."""
    with pytest.raises(ValueError) as exc_info:
        int("not a number")

    assert "invalid literal" in str(exc_info.value)
```

## Advanced Patterns

### Pattern 6: Testing Async Code

```python
# test_async.py
import pytest
import asyncio

async def fetch_data(url: str) -> dict:
    """Fetch data asynchronously."""
    await asyncio.sleep(0.1)
    return {"url": url, "data": "result"}


@pytest.mark.asyncio
async def test_fetch_data():
    """Test async function."""
    result = await fetch_data("https://api.example.com")
    assert result["url"] == "https://api.example.com"
    assert "data" in result


@pytest.mark.asyncio
async def test_concurrent_fetches():
    """Test concurrent async operations."""
    urls = ["url1", "url2", "url3"]
    tasks = [fetch_data(url) for url in urls]
    results = await asyncio.gather(*tasks)

    assert len(results) == 3
    assert all("data" in r for r in results)


@pytest.fixture
async def async_client():
    """Async fixture."""
    client = {"connected": True}
    yield client
    client["connected"] = False


@pytest.mark.asyncio
async def test_with_async_fixture(async_client):
    """Test using async fixture."""
    assert async_client["connected"] is True
```

### Pattern 7: Monkeypatch for Testing

```python
# test_environment.py
import os
import pytest

def get_database_url() -> str:
    """Get database URL from environment."""
    return os.environ.get("DATABASE_URL", "sqlite:///:memory:")


def test_database_url_default():
    """Test default database URL."""
    # Will use actual environment variable if set
    url = get_database_url()
    assert url


def test_database_url_custom(monkeypatch):
    """Test custom database URL with monkeypatch."""
    monkeypatch.setenv("DATABASE_URL", "postgresql://localhost/test")

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\skills\\python-testing-patterns\\SKILL.md'; Get-Content "'$p | Select-Object -Skip 440' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 934ms:
    assert get_database_url() == "postgresql://localhost/test"


def test_database_url_not_set(monkeypatch):
    """Test when env var is not set."""
    monkeypatch.delenv("DATABASE_URL", raising=False)
    assert get_database_url() == "sqlite:///:memory:"


class Config:
    """Configuration class."""

    def __init__(self):
        self.api_key = "production-key"

    def get_api_key(self):
        return self.api_key


def test_monkeypatch_attribute(monkeypatch):
    """Test monkeypatching object attributes."""
    config = Config()
    monkeypatch.setattr(config, "api_key", "test-key")
    assert config.get_api_key() == "test-key"
```

### Pattern 8: Temporary Files and Directories

```python
# test_file_operations.py
import pytest
from pathlib import Path

def save_data(filepath: Path, data: str):
    """Save data to file."""
    filepath.write_text(data)


def load_data(filepath: Path) -> str:
    """Load data from file."""
    return filepath.read_text()


def test_file_operations(tmp_path):
    """Test file operations with temporary directory."""
    # tmp_path is a pathlib.Path object
    test_file = tmp_path / "test_data.txt"

    # Save data
    save_data(test_file, "Hello, World!")

    # Verify file exists
    assert test_file.exists()

    # Load and verify data
    data = load_data(test_file)
    assert data == "Hello, World!"


def test_multiple_files(tmp_path):
    """Test with multiple temporary files."""
    files = {
        "file1.txt": "Content 1",
        "file2.txt": "Content 2",
        "file3.txt": "Content 3"
    }

    for filename, content in files.items():
        filepath = tmp_path / filename
        save_data(filepath, content)

    # Verify all files created
    assert len(list(tmp_path.iterdir())) == 3

    # Verify contents
    for filename, expected_content in files.items():
        filepath = tmp_path / filename
        assert load_data(filepath) == expected_content
```

### Pattern 9: Custom Fixtures and Conftest

```python
# conftest.py
"""Shared fixtures for all tests."""
import pytest

@pytest.fixture(scope="session")
def database_url():
    """Provide database URL for all tests."""
    return "postgresql://localhost/test_db"


@pytest.fixture(autouse=True)
def reset_database(database_url):
    """Auto-use fixture that runs before each test."""
    # Setup: Clear database
    print(f"Clearing database: {database_url}")
    yield
    # Teardown: Clean up
    print("Test completed")


@pytest.fixture
def sample_user():
    """Provide sample user data."""
    return {
        "id": 1,
        "name": "Test User",
        "email": "test@example.com"
    }


@pytest.fixture
def sample_users():
    """Provide list of sample users."""
    return [
        {"id": 1, "name": "User 1"},
        {"id": 2, "name": "User 2"},
        {"id": 3, "name": "User 3"},
    ]


# Parametrized fixture
@pytest.fixture(params=["sqlite", "postgresql", "mysql"])
def db_backend(request):
    """Fixture that runs tests with different database backends."""
    return request.param


def test_with_db_backend(db_backend):
    """This test will run 3 times with different backends."""
    print(f"Testing with {db_backend}")
    assert db_backend in ["sqlite", "postgresql", "mysql"]
```

### Pattern 10: Property-Based Testing

```python
# test_properties.py
from hypothesis import given, strategies as st
import pytest

def reverse_string(s: str) -> str:
    """Reverse a string."""
    return s[::-1]


@given(st.text())
def test_reverse_twice_is_original(s):
    """Property: reversing twice returns original."""
    assert reverse_string(reverse_string(s)) == s


@given(st.text())
def test_reverse_length(s):
    """Property: reversed string has same length."""
    assert len(reverse_string(s)) == len(s)


@given(st.integers(), st.integers())
def test_addition_commutative(a, b):
    """Property: addition is commutative."""
    assert a + b == b + a


@given(st.lists(st.integers()))
def test_sorted_list_properties(lst):
    """Property: sorted list is ordered."""
    sorted_lst = sorted(lst)

    # Same length
    assert len(sorted_lst) == len(lst)

    # All elements present
    assert set(sorted_lst) == set(lst)

    # Is ordered
    for i in range(len(sorted_lst) - 1):
        assert sorted_lst[i] <= sorted_lst[i + 1]
```

## Test Design Principles

### One Behavior Per Test

Each test should verify exactly one behavior. This makes failures easy to diagnose and tests easy to maintain.

```python
# BAD - testing multiple behaviors
def test_user_service():
    user = service.create_user(data)
    assert user.id is not None
    assert user.email == data["email"]
    updated = service.update_user(user.id, {"name": "New"})
    assert updated.name == "New"

# GOOD - focused tests
def test_create_user_assigns_id():
    user = service.create_user(data)
    assert user.id is not None

def test_create_user_stores_email():
    user = service.create_user(data)
    assert user.email == data["email"]

def test_update_user_changes_name():
    user = service.create_user(data)
    updated = service.update_user(user.id, {"name": "New"})
    assert updated.name == "New"
```

### Test Error Paths

Always test failure cases, not just happy paths.

```python
def test_get_user_raises_not_found():
    with pytest.raises(UserNotFoundError) as exc_info:
        service.get_user("nonexistent-id")

    assert "nonexistent-id" in str(exc_info.value)

def test_create_user_rejects_invalid_email():
    with pytest.raises(ValueError, match="Invalid email format"):
        service.create_user({"email": "not-an-email"})
```

## Testing Best Practices

### Test Organization

```python
# tests/
#   __init__.py
#   conftest.py           # Shared fixtures
#   test_unit/            # Unit tests
#     test_models.py
#     test_utils.py
#   test_integration/     # Integration tests
#     test_api.py
#     test_database.py
#   test_e2e/            # End-to-end tests
#     test_workflows.py
```

### Test Naming Convention

A common pattern: `test_<unit>_<scenario>_<expected_outcome>`. Adapt to your team's preferences.

```python
# Pattern: test_<unit>_<scenario>_<expected>
def test_create_user_with_valid_data_returns_user():
    ...

def test_create_user_with_duplicate_email_raises_conflict():
    ...

def test_get_user_with_unknown_id_returns_none():
    ...

# Good test names - clear and descriptive
def test_user_creation_with_valid_data():
    """Clear name describes what is being tested."""
    pass

def test_login_fails_with_invalid_password():
    """Name describes expected behavior."""
    pass

def test_api_returns_404_for_missing_resource():
    """Specific about inputs and expected outcomes."""
    pass

# Bad test names - avoid these
def test_1():  # Not descriptive
    pass

def test_user():  # Too vague
    pass

def test_function():  # Doesn't explain what's tested
    pass
```

### Testing Retry Behavior

Verify that retry logic works correctly using mock side effects.

```python
from unittest.mock import Mock

def test_retries_on_transient_error():
    """Test that service retries on transient failures."""
    client = Mock()
    # Fail twice, then succeed
    client.request.side_effect = [
        ConnectionError("Failed"),
        ConnectionError("Failed"),
        {"status": "ok"},
    ]

    service = ServiceWithRetry(client, max_retries=3)
    result = service.fetch()

    assert result == {"status": "ok"}
    assert client.request.call_count == 3

def test_gives_up_after_max_retries():
    """Test that service stops retrying after max attempts."""
    client = Mock()
    client.request.side_effect = ConnectionError("Failed")

    service = ServiceWithRetry(client, max_retries=3)

    with pytest.raises(ConnectionError):
        service.fetch()

    assert client.request.call_count == 3

def test_does_not_retry_on_permanent_error():
    """Test that permanent errors are not retried."""
    client = Mock()
    client.request.side_effect = ValueError("Invalid input")

    service = ServiceWithRetry(client, max_retries=3)

    with pytest.raises(ValueError):
        service.fetch()

    # Only called once - no retry for ValueError
    assert client.request.call_count == 1
```

### Mocking Time with Freezegun

Use freezegun to control time in tests for predictable time-dependent behavior.

```python
from freezegun import freeze_time
from datetime import datetime, timedelta

@freeze_time("2026-01-15 10:00:00")
def test_token_expiry():
    """Test token expires at correct time."""
    token = create_token(expires_in_seconds=3600)
    assert token.expires_at == datetime(2026, 1, 15, 11, 0, 0)

@freeze_time("2026-01-15 10:00:00")
def test_is_expired_returns_false_before_expiry():
    """Test token is not expired when within validity period."""
    token = create_token(expires_in_seconds=3600)
    assert not token.is_expired()

@freeze_time("2026-01-15 12:00:00")
def test_is_expired_returns_true_after_expiry():
    """Test token is expired after validity period."""
    token = Token(expires_at=datetime(2026, 1, 15, 11, 30, 0))
    assert token.is_expired()

def test_with_time_travel():
    """Test behavior across time using freeze_time context."""
    with freeze_time("2026-01-01") as frozen_time:
        item = create_item()
        assert item.created_at == datetime(2026, 1, 1)

        # Move forward in time
        frozen_time.move_to("2026-01-15")
        assert item.age_days == 14
```

### Test Markers

```python
# test_markers.py
import pytest

@pytest.mark.slow
def test_slow_operation():
    """Mark slow tests."""
    import time
    time.sleep(2)


@pytest.mark.integration
def test_database_integration():
    """Mark integration tests."""
    pass


@pytest.mark.skip(reason="Feature not implemented yet")
def test_future_feature():
    """Skip tests temporarily."""
    pass


@pytest.mark.skipif(os.name == "nt", reason="Unix only test")
def test_unix_specific():
    """Conditional skip."""
    pass


@pytest.mark.xfail(reason="Known bug #123")
def test_known_bug():
    """Mark expected failures."""
    assert False


# Run with:
# pytest -m slow          # Run only slow tests
# pytest -m "not slow"    # Skip slow tests
# pytest -m integration   # Run integration tests
```

### Coverage Reporting

```bash
# Install coverage
pip install pytest-cov

# Run tests with coverage
pytest --cov=myapp tests/

# Generate HTML report
pytest --cov=myapp --cov-report=html tests/

# Fail if coverage below threshold
pytest --cov=myapp --cov-fail-under=80 tests/

# Show missing lines
pytest --cov=myapp --cov-report=term-missing tests/
```

## Testing Database Code

```python
# test_database_models.py
import pytest
from sqlalchemy import create_engine, Column, Integer, String
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker, Session

Base = declarative_base()


class User(Base):
    """User model."""
    __tablename__ = "users"

    id = Column(Integer, primary_key=True)
    name = Column(String(50))
    email = Column(String(100), unique=True)


@pytest.fixture(scope="function")
def db_session() -> Session:
    """Create in-memory database for testing."""
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)

    SessionLocal = sessionmaker(bind=engine)
    session = SessionLocal()

    yield session

    session.close()


def test_create_user(db_session):
    """Test creating a user."""
    user = User(name="Test User", email="test@example.com")
    db_session.add(user)
    db_session.commit()

    assert user.id is not None
    assert user.name == "Test User"


def test_query_user(db_session):
    """Test querying users."""
    user1 = User(name="User 1", email="user1@example.com")
    user2 = User(name="User 2", email="user2@example.com")

    db_session.add_all([user1, user2])
    db_session.commit()

    users = db_session.query(User).all()
    assert len(users) == 2


def test_unique_email_constraint(db_session):
    """Test unique email constraint."""
    from sqlalchemy.exc import IntegrityError

    user1 = User(name="User 1", email="same@example.com")
    user2 = User(name="User 2", email="same@example.com")

    db_session.add(user1)
    db_session.commit()

    db_session.add(user2)

    with pytest.raises(IntegrityError):
        db_session.commit()
```

## CI/CD Integration

```yaml
# .github/workflows/test.yml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install pytest pytest-cov

      - name: Run tests
        run: |
          pytest --cov=myapp --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
```

## Configuration Files

```ini
# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=myapp
    --cov-report=term-missing
markers =
    slow: marks tests as slow
    integration: marks integration tests
    unit: marks unit tests
    e2e: marks end-to-end tests
```

```toml
# pyproject.toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = [
    "-v",
    "--cov=myapp",
    "--cov-report=term-missing",
]

[tool.coverage.run]
source = ["myapp"]
omit = ["*/tests/*", "*/migrations/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
]
```

## Resources

- **pytest documentation**: https://docs.pytest.org/
- **unittest.mock**: https://docs.python.org/3/library/unittest.mock.html
- **hypothesis**: Property-based testing
- **pytest-asyncio**: Testing async code
- **pytest-cov**: Coverage reporting
- **pytest-mock**: pytest wrapper for mock

## Best Practices Summary

1. **Write tests first** (TDD) or alongside code
2. **One assertion per test** when possible
3. **Use descriptive test names** that explain behavior
4. **Keep tests independent** and isolated
5. **Use fixtures** for setup and teardown
6. **Mock external dependencies** appropriately
7. **Parametrize tests** to reduce duplication
8. **Test edge cases** and error conditions
9. **Measure coverage** but focus on quality
10. **Run tests in CI/CD** on every commit

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/tests/test_d1_stats.py; Get-Content -Raw research/drift_recovery/out/d1/scifact_evalsplit_pack.json; Get-Content -Raw research/drift_recovery/out/d1/d1_summary.json; Get-Content -Raw research/drift_recovery/out/d1/run_manifest.json' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "rg -n \"def |class |EmbeddingPack|ridge|parity|ndcg|fit_idx|anchor_fraction|dataset|model|save\" research/drift_recovery/run_d1.py research/drift_recovery/stats/*.py; rg -n \"agreed-build-plan-v1|recoverability estimator|D3\" . -g '*.md' -g '*.py' -g '"'!docs/**/paper-draft/**'"'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/artifacts.py; Get-Content -Raw research/drift_recovery/contracts.py; Get-Content -Raw research/drift_recovery/harness_bridge.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "rg -n -i \"CHELATEDAI|drift-recovery|severity-calibration|knob-sweep|HF_HUB_OFFLINE|experiment_runs|PR #266\" 'C:\\Users\\mattm\\.codex\\memories\\MEMORY.md'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 474ms:
587:# Task Group: CHELATEDAI drift-recovery calibration and brutal-honesty process
588:scope: CHELATEDAI drift-recovery diagnosis, severity calibration, knob-sweep execution, artifact durability, and strict PR-body handling; use when continuing drift-recovery experiments or preparing evidence-heavy PRs.
589:applies_to: cwd=D:\GITHUB\CHELATEDAI; reuse_rule=safe for drift-recovery and repo-process guidance in CHELATEDAI, but experimental results and PR state should be rechecked from current artifacts
595:- rollout_summaries/2026-06-11T15-10-39-PIMu-drift_recovery_pr6_pr8_calibration_knob_sweep_and_bhs_harden.md (cwd=D:\GITHUB\CHELATEDAI, rollout_path=C:\Users\mattm\.codex\sessions\2026\06\11\rollout-2026-06-11T11-10-39-019eb73c-3da3-7f70-9035-afd5a56ecb86.jsonl, updated_at=2026-06-12T14:48:16+00:00, thread_id=019eb73c-3da3-7f70-9035-afd5a56ecb86)
599:- drift-recovery, severity-calibration, knob-sweep, calibration-manifest, HF_HUB_OFFLINE, experiment_runs, artifact durability, deterministic tie handling, validate_pr_brutal_honesty.py, PR #266, admin merge
603:- the goal file required "diagnose last night's mixed drift-recovery result and re-run it calibrated" -> verify hypotheses from data before trusting prior summaries [Task 1]
613:- The working offline smoke path on this host is `HF_HUB_OFFLINE=1` plus `python scripts/smoke_pipeline.py` [Task 1]
614:- Evidence in `experiment_runs/` is not durable by default because the directory is gitignored; force-add the artifact set or move it to a tracked path before opening a PR [Task 1]

 succeeded in 548ms:
"""Frozen array artifacts for offline drift-recovery statistics."""

from __future__ import annotations

import hashlib
import json
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any, Dict, Mapping

import numpy as np


def _json_ready(value: Any) -> Any:
    if isinstance(value, np.generic):
        return value.item()
    if isinstance(value, np.ndarray):
        return value.tolist()
    if isinstance(value, dict):
        return {str(key): _json_ready(item) for key, item in value.items()}
    if isinstance(value, (list, tuple)):
        return [_json_ready(item) for item in value]
    return value


@dataclass
class EmbeddingPack:
    """All arrays needed to rerun D1 statistics without model inference.

    The required core is ``Do``, ``Dor``, ``Qd``, qrels, ``fit_idx``, and
    per-query method scores. Extra arrays hold non-eval supervision pools so the
    learning curves remain leakage-free while using the same fixed eval IDs.
    """

    Do: np.ndarray
    Dor: np.ndarray
    Qd: np.ndarray
    doc_ids: np.ndarray
    query_ids: np.ndarray
    qrels: Mapping[str, Mapping[str, float]]
    fit_idx: np.ndarray
    per_query_scores: Mapping[str, np.ndarray]
    extra_arrays: Mapping[str, np.ndarray] = field(default_factory=dict)
    metadata: Mapping[str, Any] = field(default_factory=dict)

    def validate(self) -> None:
        if self.Do.shape != self.Dor.shape or self.Do.ndim != 2:
            raise ValueError("Do and Dor must be same-shape matrices")
        if self.Qd.ndim != 2 or self.Qd.shape[1] != self.Do.shape[1]:
            raise ValueError("Qd must share the embedding dimension")
        if len(self.doc_ids) != self.Do.shape[0]:
            raise ValueError("doc_ids length mismatch")
        if len(self.query_ids) != self.Qd.shape[0]:
            raise ValueError("query_ids length mismatch")
        if np.any(self.fit_idx < 0) or np.any(self.fit_idx >= self.Do.shape[0]):
            raise ValueError("fit_idx is out of bounds")
        if len(np.unique(self.fit_idx)) != len(self.fit_idx):
            raise ValueError("fit_idx contains duplicates")
        for method, scores in self.per_query_scores.items():
            array = np.asarray(scores)
            if array.shape != (len(self.query_ids),):
                raise ValueError(f"{method} per-query score shape mismatch: {array.shape}")
            if not np.all(np.isfinite(array)):
                raise ValueError(f"{method} contains non-finite scores")
        missing = set(map(str, self.query_ids)).difference(map(str, self.qrels))
        if missing:
            raise ValueError(f"qrels missing eval queries: {sorted(missing)[:5]}")

    def save(self, prefix: Path) -> Dict[str, str]:
        self.validate()
        prefix = Path(prefix)
        prefix.parent.mkdir(parents=True, exist_ok=True)
        arrays_path = prefix.with_suffix(".npz")
        metadata_path = prefix.with_suffix(".json")
        arrays: Dict[str, np.ndarray] = {
            "Do": np.asarray(self.Do),
            "Dor": np.asarray(self.Dor),
            "Qd": np.asarray(self.Qd),
            "doc_ids": np.asarray(self.doc_ids, dtype=str),
            "query_ids": np.asarray(self.query_ids, dtype=str),
            "fit_idx": np.asarray(self.fit_idx, dtype=np.int64),
        }
        arrays.update({f"score__{key}": np.asarray(value) for key, value in self.per_query_scores.items()})
        arrays.update({f"extra__{key}": np.asarray(value) for key, value in self.extra_arrays.items()})
        np.savez_compressed(arrays_path, **arrays)
        payload = {
            "record_type": "embedding_pack",
            "format_version": 1,
            "arrays_file": arrays_path.name,
            "qrels": _json_ready(self.qrels),
            "metadata": _json_ready(self.metadata),
            "array_shapes": {key: list(value.shape) for key, value in arrays.items()},
        }
        metadata_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
        digest = hashlib.sha256(arrays_path.read_bytes()).hexdigest()
        payload["arrays_sha256"] = digest
        metadata_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
        return {"arrays": str(arrays_path), "metadata": str(metadata_path), "sha256": digest}

    @classmethod
    def load(cls, prefix: Path) -> "EmbeddingPack":
        prefix = Path(prefix)
        arrays_path = prefix.with_suffix(".npz")
        metadata_path = prefix.with_suffix(".json")
        payload = json.loads(metadata_path.read_text(encoding="utf-8"))
        digest = hashlib.sha256(arrays_path.read_bytes()).hexdigest()
        if digest != payload.get("arrays_sha256"):
            raise ValueError("EmbeddingPack array checksum mismatch")
        with np.load(arrays_path, allow_pickle=False) as stored:
            scores = {key[7:]: stored[key].copy() for key in stored.files if key.startswith("score__")}
            extras = {key[7:]: stored[key].copy() for key in stored.files if key.startswith("extra__")}
            pack = cls(
                Do=stored["Do"].copy(),
                Dor=stored["Dor"].copy(),
                Qd=stored["Qd"].copy(),
                doc_ids=stored["doc_ids"].copy(),
                query_ids=stored["query_ids"].copy(),
                qrels=payload["qrels"],
                fit_idx=stored["fit_idx"].copy(),
                per_query_scores=scores,
                extra_arrays=extras,
                metadata=payload.get("metadata", {}),
            )
        pack.validate()
        return pack


"""Typed contracts shared by drift-recovery experiment components."""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import asdict, dataclass, field
from typing import Any, Dict, Mapping, Optional, Sequence

import numpy as np


@dataclass(frozen=True)
class RetrievalCase:
    dataset: str
    corpus_ids: Sequence[str]
    query_ids: Sequence[str]
    qrels: Mapping[str, Mapping[str, float]]
    k: int = 10


@dataclass(frozen=True)
class ExperimentSplit:
    anchor_ids: Sequence[str]
    eval_ids: Sequence[str]
    seed: int
    anchor_fraction: float

    def __post_init__(self) -> None:
        overlap = set(self.anchor_ids).intersection(self.eval_ids)
        if overlap:
            raise ValueError(f"anchor/eval leakage: {sorted(overlap)[:5]}")


@dataclass(frozen=True)
class AnchorPairs:
    source: np.ndarray
    target: np.ndarray
    pair_ids: Sequence[str]
    supervision_type: str

    def __post_init__(self) -> None:
        if self.source.shape != self.target.shape:
            raise ValueError("anchor source and target shapes differ")
        if self.source.shape[0] != len(self.pair_ids):
            raise ValueError("pair_ids length does not match anchor rows")


@dataclass(frozen=True)
class PerQueryRecord:
    query_id: str
    floor_ndcg: float
    oracle_ndcg: float
    method_ndcg: Mapping[str, float]


@dataclass(frozen=True)
class RunManifest:
    repo_sha: str
    harness_sha: str
    protocol_sha: str
    model_revisions: Mapping[str, str]
    seeds: Mapping[str, Any]
    command: str
    environment: Mapping[str, str]
    artifacts: Mapping[str, str] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        return asdict(self)


class QueryAdapter(ABC):
    """A method that learns a map over query or document vectors."""

    @abstractmethod
    def fit(self, source: np.ndarray, target: np.ndarray, **kwargs: Any) -> "QueryAdapter":
        raise NotImplementedError

    @abstractmethod
    def transform(self, vectors: np.ndarray) -> np.ndarray:
        raise NotImplementedError


class ScoreTransform(ABC):
    """A method that learns and applies a transformation in score space."""

    @abstractmethod
    def fit(self, scores: np.ndarray, **kwargs: Any) -> "ScoreTransform":
        raise NotImplementedError

    @abstractmethod
    def rank(self, scores: np.ndarray, k: int) -> np.ndarray:
        raise NotImplementedError


@dataclass
class MethodRunner:
    """Uniformly run a vector adapter or a score transform, never both."""

    query_adapter: Optional[QueryAdapter] = None
    score_transform: Optional[ScoreTransform] = None

    def __post_init__(self) -> None:
        if (self.query_adapter is None) == (self.score_transform is None):
            raise ValueError("MethodRunner requires exactly one method interface")

    def fit(self, source: np.ndarray, target: Optional[np.ndarray] = None, **kwargs: Any) -> None:
        if self.query_adapter is not None:
            if target is None:
                raise ValueError("vector adapters require targets")
            self.query_adapter.fit(source, target, **kwargs)
        else:
            assert self.score_transform is not None
            self.score_transform.fit(source, **kwargs)

    def output(self, values: np.ndarray, k: int = 10) -> np.ndarray:
        if self.query_adapter is not None:
            return self.query_adapter.transform(values)
        assert self.score_transform is not None
        return self.score_transform.rank(values, k=k)


"""The single import boundary between D1 and the merged CHELATEDAI harness.

No other module under :mod:`research.drift_recovery` may import root-level
harness modules. Statistical work consumes a frozen :class:`EmbeddingPack`.
"""

from __future__ import annotations

import os
import statistics
from typing import Any, Dict, Mapping, Sequence

import numpy as np

os.environ.setdefault("HF_HUB_OFFLINE", "1")
os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")

# Merged-harness imports: this is intentionally the only module containing them.
from benchmark_utils import canonicalize_id, load_mteb_data  # noqa: E402
from chelation_adapter import create_adapter  # noqa: E402
from drift_recovery_metrics import ndcg_at_k  # noqa: E402
from query_encoder_drift import QueryEncoderDrift  # noqa: E402
from run_drift_recovery_experiment import _split_anchor_eval  # noqa: E402
from run_road_course_campaign import select_road_course_slice  # noqa: E402
from sedimentation_loss import SedimentationInfoNCELoss  # noqa: E402


def _normalize(matrix: np.ndarray) -> np.ndarray:
    matrix = np.asarray(matrix)
    norms = np.linalg.norm(matrix, axis=1, keepdims=True)
    return matrix / np.where(norms > 0.0, norms, 1.0)


def per_query_ndcg(
    query_vectors: np.ndarray,
    query_ids: Sequence[str],
    doc_vectors: np.ndarray,
    doc_ids: Sequence[str],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> np.ndarray:
    """Rank by cosine and score with the merged harness's binary NDCG."""

    docs = _normalize(np.asarray(doc_vectors, dtype=np.float64))
    queries = _normalize(np.asarray(query_vectors, dtype=np.float64))
    canonical_docs = np.asarray([canonicalize_id(value) for value in doc_ids], dtype=str)
    rows = []
    for query_id, vector in zip(query_ids, queries):
        qid = canonicalize_id(query_id)
        relevance = qrels.get(query_id, qrels.get(qid, {}))
        relevant = [canonicalize_id(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0]
        if not relevant:
            raise ValueError(f"eval query {qid} has no positive qrels")
        similarities = docs @ vector
        # Stable full ordering makes the otherwise-unspecified equal-score case deterministic.
        ranked_idx = np.argsort(-similarities, kind="mergesort")[: min(k, len(canonical_docs))]
        ranked = canonical_docs[ranked_idx].tolist()
        rows.append(ndcg_at_k(ranked, relevant, k=k))
    return np.asarray(rows, dtype=np.float64)


def aggregate_ndcg(scores: Sequence[float]) -> float:
    """Match the merged evaluator's ``statistics.fmean`` aggregation."""

    return float(statistics.fmean(float(value) for value in scores))


def assert_harness_parity(pack: Any, atol: float = 1e-12) -> Dict[str, float]:
    """Prove frozen per-query scores reproduce merged-harness aggregate NDCG."""

    references = dict(pack.metadata.get("harness_aggregate_ndcg", {}))
    if not references:
        raise AssertionError("pack has no harness aggregate references")
    differences: Dict[str, float] = {}
    document_matrices = {"floor": pack.Do, "oracle": pack.Dor}
    document_matrices.update(
        {
            name[len("method_documents__") :]: values
            for name, values in pack.extra_arrays.items()
            if name.startswith("method_documents__")
        }
    )
    for method, expected in references.items():
        if method not in pack.per_query_scores:
            raise AssertionError(f"pack is missing reference method {method}")
        if method not in document_matrices:
            raise AssertionError(f"pack is missing frozen document vectors for {method}")
        recomputed = per_query_ndcg(
            pack.Qd,
            pack.query_ids,
            document_matrices[method],
            pack.doc_ids,
            pack.qrels,
            k=int(pack.metadata.get("k", 10)),
        )
        stored = np.asarray(pack.per_query_scores[method], dtype=np.float64)
        per_query_difference = float(np.max(np.abs(recomputed - stored)))
        if per_query_difference > atol:
            raise AssertionError(
                f"{method} per-query pack/harness parity failed: max difference "
                f"{per_query_difference:.3g} > {atol:.3g}"
            )
        actual = aggregate_ndcg(recomputed)
        difference = abs(actual - float(expected))
        differences[method] = difference
        if difference > atol:
            raise AssertionError(
                f"{method} pack/harness parity failed: {actual:.17g} vs {float(expected):.17g} "
                f"(difference {difference:.3g} > {atol:.3g})"
            )
    return differences


def load_scifact_evalsplit(seed: int = 42) -> Dict[str, Any]:
    """Load the exact waypoint eval-split recipe and leakage-free curve pools."""

    corpus, queries, qrels = load_mteb_data("SciFact")
    if corpus is None or queries is None or qrels is None:
        raise RuntimeError("offline SciFact cache is unavailable")
    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
        corpus,
        queries,
        qrels,
        max_queries=100,
        sample_docs=1200,
        seed=seed,
    )
    anchor_ids, eval_ids = _split_anchor_eval(sliced_queries, 0.4, seed, active=True)
    eval_set = set(map(canonicalize_id, eval_ids))
    anchor_set = set(map(canonicalize_id, anchor_ids))
    if eval_set.intersection(anchor_set):
        raise AssertionError("merged split leaked anchor IDs into eval IDs")

    doc_ids = [canonicalize_id(value) for value in sliced_corpus]
    eval_query_ids = [
        canonicalize_id(value)
        for value in sliced_queries
        if sliced_qrels.get(value) and canonicalize_id(value) in eval_set
    ]
    canonical_anchor_ids = [
        canonicalize_id(value) for value in sliced_queries if canonicalize_id(value) in anchor_set
    ]
    query_by_id = {canonicalize_id(key): str(value) for key, value in queries.items()}
    corpus_by_id = {canonicalize_id(key): str(value) for key, value in corpus.items()}
    qrels_by_id = {
        canonicalize_id(query_id): {
            canonicalize_id(doc_id): float(score) for doc_id, score in rels.items()
        }
        for query_id, rels in qrels.items()
    }
    sliced_qrels_by_id = {
        canonicalize_id(query_id): {
            canonicalize_id(doc_id): float(score) for doc_id, score in rels.items()
        }
        for query_id, rels in sliced_qrels.items()
    }

    # Canonical C3a pool: every positive pair from the exact 40-query anchor split.
    selected_doc_set = set(doc_ids)
    canonical_pairs = []
    for query_id in canonical_anchor_ids:
        for doc_id, score in sorted(qrels_by_id.get(query_id, {}).items()):
            if float(score) > 0.0 and doc_id in selected_doc_set:
                canonical_pairs.append((query_id, doc_id))

    # Learning-curve query pool: one positive document per non-eval SciFact
    # query. This reaches 128 distinct query anchors without touching eval qrels.
    curve_pairs = []
    for query_id in sorted(query_by_id):
        if query_id in eval_set:
            continue
        positives = sorted(
            doc_id for doc_id, score in qrels_by_id.get(query_id, {}).items() if float(score) > 0.0
        )
        if positives:
            curve_pairs.append((query_id, positives[0]))
    if len(curve_pairs) < 128:
        raise RuntimeError(f"only {len(curve_pairs)} leakage-free C3a curve anchors; need 128")

    return {
        "corpus_texts": [str(sliced_corpus[key]) for key in sliced_corpus],
        "doc_ids": doc_ids,
        "eval_query_texts": [query_by_id[key] for key in eval_query_ids],
        "eval_query_ids": eval_query_ids,
        "eval_qrels": {key: sliced_qrels_by_id[key] for key in eval_query_ids},
        "anchor_ids": canonical_anchor_ids,
        "canonical_pair_query_ids": [item[0] for item in canonical_pairs],
        "canonical_pair_doc_ids": [item[1] for item in canonical_pairs],
        "canonical_pair_query_texts": [query_by_id[item[0]] for item in canonical_pairs],
        "curve_pair_query_ids": [item[0] for item in curve_pairs],
        "curve_pair_doc_ids": [item[1] for item in curve_pairs],
        "curve_pair_query_texts": [query_by_id[item[0]] for item in curve_pairs],
        "curve_pair_doc_texts": [corpus_by_id[item[1]] for item in curve_pairs],
        "all_query_count": len(queries),
    }


def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
    """Run both cached encoders once and return arrays ready for freezing."""

    from sentence_transformers import SentenceTransformer

    old = SentenceTransformer("sentence-transformers/all-MiniLM-L6-v2", device=device)
    Do = np.asarray(
        old.encode(case["corpus_texts"], batch_size=128, show_progress_bar=True), dtype=np.float64
    )
    curve_source = np.asarray(
        old.encode(case["curve_pair_doc_texts"], batch_size=128, show_progress_bar=True), dtype=np.float64
    )
    drift = QueryEncoderDrift(store_dim=Do.shape[1], swap_model_name="all-mpnet-base-v2", seed=seed)
    # One call fixes the projection; all later calls reuse exactly that instance.
    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
    canonical_target = np.asarray(
        drift.embed_queries(list(case["canonical_pair_query_texts"])), dtype=np.float64
    )
    curve_target = np.asarray(
        drift.embed_queries(list(case["curve_pair_query_texts"])), dtype=np.float64
    )
    doc_lookup = {doc_id: index for index, doc_id in enumerate(case["doc_ids"])}
    canonical_source = np.asarray(
        [Do[doc_lookup[doc_id]] for doc_id in case["canonical_pair_doc_ids"]], dtype=np.float64
    )
    return {
        "Do": Do,
        "Dor": Dor,
        "Qd": Qd,
        "canonical_source": canonical_source,
        "canonical_target": canonical_target,
        "curve_source": curve_source,
        "curve_target": curve_target,
        "drift_manifest": drift.manifest(),
        "old_model": "sentence-transformers/all-MiniLM-L6-v2",
        "swap_model": "all-mpnet-base-v2",
    }


def train_c3a_documents(
    all_documents: np.ndarray,
    anchor_documents: np.ndarray,
    anchor_queries: np.ndarray,
    seed: int,
    steps: int = 30,
    learning_rate: float = 0.01,
    min_correction: float = 0.01,
    max_correction: float = 0.5,
) -> np.ndarray:
    """Run the merged C3a bounded adapter and InfoNCE training path."""

    import torch

    torch.manual_seed(int(seed))
    adapter = create_adapter(
        "mlp",
        input_dim=int(all_documents.shape[1]),
        bounded=True,
        min_correction=float(min_correction),
        max_correction=float(max_correction),
    )
    source = torch.tensor(np.asarray(anchor_documents, dtype=np.float32), dtype=torch.float32)
    target = torch.tensor(np.asarray(anchor_queries, dtype=np.float32), dtype=torch.float32)
    loss_fn = SedimentationInfoNCELoss()
    optimizer = torch.optim.Adam(adapter.parameters(), lr=float(learning_rate))
    # Mirrors the merged harness, including the post-construction seed call.
    torch.manual_seed(int(seed))
    adapter.train()
    for _ in range(int(steps)):
        optimizer.zero_grad()
        loss = loss_fn(adapter(source), target)
        loss.backward()
        optimizer.step()
    adapter.eval()
    with torch.no_grad():
        output = adapter(torch.tensor(np.asarray(all_documents, dtype=np.float32), dtype=torch.float32))
    return np.asarray(output.detach().cpu().numpy(), dtype=np.float64)


def system_under_test_spec() -> Dict[str, Any]:
    """Return exact code-level constants used by the Section 3 renderer."""

    from config import ChelationConfig

    return {
        "bounded_defaults": {
            "min_correction": ChelationConfig.BOUNDED_ADAPTER_MIN_CORRECTION,
            "max_correction": ChelationConfig.BOUNDED_ADAPTER_MAX_CORRECTION,
            "int8_floor": 1.0 / 128.0,
        },
        "controller_defaults": {
            "initial_temperature": 0.0,
            "cooling_rate": 0.7,
            "trigger_threshold": 0.15,
            "max_temperature": 1.0,
            "epsilon": 1e-6,
        },
        "supervised_cycle_controller": {
            "cooling_rate": 0.5,
            "drift_signal": "max(0, baseline_ndcg - current_ndcg)",
        },
    }


 succeeded in 551ms:
from __future__ import annotations

import unittest
from pathlib import Path

import numpy as np

from research.drift_recovery.artifacts import EmbeddingPack
from research.drift_recovery.d1.paper_stats import audit_fit_leakage, compute_leakage_sensitivity
from research.drift_recovery.harness_bridge import assert_harness_parity
from research.drift_recovery.stats.learning_curve import nested_prefixes
from research.drift_recovery.stats.multiple_testing import holm_adjust
from research.drift_recovery.stats.paired_bootstrap import (
    BootstrapDraws,
    paired_query_bootstrap,
    strip_draws,
)


class TestPairedBootstrap(unittest.TestCase):
    def test_recovery_uses_ratio_of_resampled_means(self) -> None:
        scores = {
            "floor": np.array([0.0, 0.4, 0.0, 0.4]),
            "oracle": np.array([1.0, 0.6, 1.0, 0.6]),
            "method": np.array([0.5, 0.5, 0.5, 0.5]),
        }
        result = paired_query_bootstrap(scores, ["method"], draws=1000, seed=9)
        self.assertAlmostEqual(result["methods"]["method"]["recovery"]["estimate"], 0.5)
        self.assertNotIn("_method_draws", strip_draws(result))

    def test_floor_oracle_and_methods_share_bootstrap_indices(self) -> None:
        scores = {
            "floor": np.array([0.0, 0.1, 0.2]),
            "oracle": np.array([0.7, 0.8, 0.9]),
            "ridge": np.array([0.4, 0.5, 0.6]),
            "mlp": np.array([0.3, 0.4, 0.5]),
        }
        indices = np.tile(np.array([[0, 1, 2], [2, 2, 0]]), (50, 1))
        draws = BootstrapDraws(indices=indices, seed=17)

        result = paired_query_bootstrap(
            scores,
            ["ridge", "mlp"],
            bootstrap_draws=draws,
        )

        np.testing.assert_array_equal(result["_bootstrap_indices"], indices)
        for name, values in scores.items():
            expected = values[indices].mean(axis=1)
            np.testing.assert_allclose(result["_resampled_means"][name], expected)

    def test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent(self) -> None:
        scores = {
            "floor": np.array([0.0, 0.0]),
            "oracle": np.array([0.1, 1.0]),
            "method": np.array([0.05, 0.5]),
        }
        indices = np.tile(np.array([[0, 1]]), (100, 1))
        indices[0] = [0, 0]
        one_percent = BootstrapDraws(indices=indices, seed=23)

        result = paired_query_bootstrap(
            scores,
            ["method"],
            epsilon=0.1,
            bootstrap_draws=one_percent,
        )

        self.assertEqual(result["invalid_draws"], 1)
        self.assertAlmostEqual(result["invalid_fraction"], 0.01)

        indices[1] = [0, 0]
        above_limit = BootstrapDraws(indices=indices, seed=23)
        with self.assertRaisesRegex(RuntimeError, "exceeds the 1% protocol limit"):
            paired_query_bootstrap(
                scores,
                ["method"],
                epsilon=0.1,
                bootstrap_draws=above_limit,
            )

    def test_holm_is_monotone_in_sorted_order(self) -> None:
        adjusted = holm_adjust({"a": 0.01, "b": 0.03, "c": 0.20})
        values = [adjusted[key]["holm_adjusted_p"] for key in ("a", "b", "c")]
        self.assertEqual(values, sorted(values))


class TestLearningCurve(unittest.TestCase):
    def test_prefixes_are_nested_and_seeded(self) -> None:
        first = nested_prefixes(32, counts=(4, 8, 16), seeds=(1, 2))
        second = nested_prefixes(32, counts=(4, 8, 16), seeds=(1, 2))
        for seed in (1, 2):
            self.assertTrue(set(first[seed][4]).issubset(set(first[seed][8])))
            np.testing.assert_array_equal(first[seed][16], second[seed][16])


class TestFrozenPack(unittest.TestCase):
    @staticmethod
    def _load_pack() -> EmbeddingPack:
        prefix = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
        if not prefix.with_suffix(".npz").exists():
            raise unittest.SkipTest("D1 pack has not been built")
        return EmbeddingPack.load(prefix)

    def test_real_pack_reloads_and_matches_harness_aggregates(self) -> None:
        pack = self._load_pack()
        differences = assert_harness_parity(pack, atol=1e-12)
        self.assertTrue(differences)
        self.assertTrue(all(value == 0.0 for value in differences.values()))
        safe = pack.extra_arrays["leakage_safe_fit_idx"]
        self.assertEqual(len(safe), 600)
        self.assertEqual(
            pack.metadata["waypoint_audit"]["eval_positive_docs_in_leakage_safe_fit"],
            0,
        )

    def test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack(self) -> None:
        audit = audit_fit_leakage(self._load_pack())

        self.assertEqual(audit["eval_positive_docs_total"], 62)
        self.assertEqual(audit["eval_positive_docs_in_literal_fit"], 28)
        self.assertEqual(audit["eval_positive_docs_in_leakage_safe_fit"], 0)

    def test_leakage_safe_ridge_recovery_is_below_literal_fit(self) -> None:
        sensitivity = compute_leakage_sensitivity(
            self._load_pack(),
            methods=("ridge",),
            device="cpu",
        )
        ridge = sensitivity["methods"]["ridge"]

        self.assertLess(
            ridge["leakage_safe_full_600"]["recovery"],
            ridge["literal_waypoint"]["recovery"],
        )


if __name__ == "__main__":
    unittest.main()

{
  "array_shapes": {
    "Do": [
      1200,
      384
    ],
    "Dor": [
      1200,
      384
    ],
    "Qd": [
      60,
      384
    ],
    "doc_ids": [
      1200
    ],
    "extra__canonical_c3a_source": [
      49,
      384
    ],
    "extra__canonical_c3a_target": [
      49,
      384
    ],
    "extra__curve_c3a_query_ids": [
      240
    ],
    "extra__curve_c3a_source": [
      240,
      384
    ],
    "extra__curve_c3a_target": [
      240,
      384
    ],
    "extra__leakage_safe_fit_idx": [
      600
    ],
    "extra__method_documents__c3a": [
      1200,
      384
    ],
    "extra__method_documents__mlp": [
      1200,
      384
    ],
    "extra__method_documents__procrustes": [
      1200,
      384
    ],
    "extra__method_documents__ridge": [
      1200,
      384
    ],
    "fit_idx": [
      600
    ],
    "query_ids": [
      60
    ],
    "score__c3a": [
      60
    ],
    "score__floor": [
      60
    ],
    "score__mlp": [
      60
    ],
    "score__oracle": [
      60
    ],
    "score__procrustes": [
      60
    ],
    "score__ridge": [
      60
    ]
  },
  "arrays_file": "scifact_evalsplit_pack.npz",
  "arrays_sha256": "14bc80356f2d77b74e48edd266e5071b162fc9d905034cddf31dc7d4747058ac",
  "format_version": 1,
  "metadata": {
    "anchor_fraction": 0.4,
    "anchor_ids": [
      "5",
      "13",
      "42",
      "49",
      "53",
      "54",
      "72",
      "94",
      "100",
      "129",
      "130",
      "133",
      "142",
      "179",
      "208",
      "217",
      "219",
      "230",
      "237",
      "249",
      "261",
      "269",
      "274",
      "279",
      "294",
      "295",
      "298",
      "300",
      "314",
      "324",
      "343",
      "362",
      "384",
      "410",
      "415",
      "421",
      "436",
      "439",
      "443",
      "478"
    ],
    "canonical_c3a_pair_count": 49,
    "canonical_tolerance": {
      "c3a_absolute_error": 0.0056436878858855954,
      "c3a_target": 0.2,
      "ridge_absolute_error": 0.000571369334298133,
      "ridge_target": 0.844,
      "tolerance": 0.001
    },
    "dataset": "SciFact",
    "drift_manifest": {
      "drift": "query_encoder_swap",
      "projection_checksum": "3984ab778d1b8b909dfd4a637e74c6588017e4b158487745f29103193bac4626",
      "projection_seed": 42,
      "store_dim": 384,
      "swap_dim": 768,
      "swap_model": "all-mpnet-base-v2"
    },
    "harness_aggregate_ndcg": {
      "c3a": 0.15696355831254788,
      "floor": 0.0,
      "mlp": 0.6592907756501849,
      "oracle": 0.8076072066050948,
      "procrustes": 0.6646652449748284,
      "ridge": 0.6811590403826876
    },
    "k": 10,
    "learning_curve_c3a_pool": "one positive doc per non-eval SciFact query; eval IDs and eval qrels excluded",
    "max_queries": 100,
    "models": {
      "old": "sentence-transformers/all-MiniLM-L6-v2",
      "swap": "all-mpnet-base-v2"
    },
    "ndcg": "binary merged-harness ndcg_at_k",
    "old_scalar_audit": {
      "finding": "The old scalar was a cross-seed campaign mean attached to the seed-42 eval split. The frozen per-query seed-42 result supersedes it.",
      "fixed_split_per_query_ndcg": 0.15696355831254788,
      "fixed_split_per_query_recovery": 0.19435631211411442,
      "ndcg_difference": -0.003936441687452108,
      "old_c3a_scalar_ndcg": 0.1609,
      "old_scalar_implied_recovery": 0.19923051538429024
    },
    "recovery": {
      "c3a": 0.19435631211411442,
      "mlp": 0.8163507832249521,
      "procrustes": 0.8230055892750813,
      "ridge": 0.8434286306657018
    },
    "sample_docs": 1200,
    "seed": 42,
    "waypoint_audit": {
      "eval_positive_docs_in_leakage_safe_fit": 0,
      "eval_positive_docs_in_literal_fit": 28,
      "leakage_safe_fit_count": 600,
      "literal_fit_count": 600,
      "literal_recovery": {
        "procrustes": 0.8230055892750813,
        "ridge": 0.8434286306657018
      },
      "protocol_conflict": "The exact waypoint permutation contains eval-positive documents. Canonical ladder/bootstrap reproduction uses it; new learning-curve fits use the separate leakage_safe_fit_idx."
    }
  },
  "qrels": {
    "1": {
      "31715818": 1.0
    },
    "113": {
      "6157837": 1.0
    },
    "115": {
      "33872649": 1.0
    },
    "118": {
      "6372244": 1.0
    },
    "124": {
      "4883040": 1.0
    },
    "127": {
      "21598000": 1.0
    },
    "128": {
      "8290953": 1.0
    },
    "132": {
      "7975937": 1.0
    },
    "137": {
      "26016929": 1.0
    },
    "141": {
      "14437255": 1.0,
      "6955746": 1.0
    },
    "143": {
      "10582939": 1.0
    },
    "146": {
      "10582939": 1.0
    },
    "148": {
      "1084345": 1.0
    },
    "163": {
      "18872233": 1.0
    },
    "171": {
      "12670680": 1.0
    },
    "180": {
      "16966326": 1.0
    },
    "183": {
      "12827098": 1.0
    },
    "185": {
      "18340282": 1.0
    },
    "198": {
      "2177022": 1.0
    },
    "212": {
      "22038539": 1.0
    },
    "213": {
      "13625993": 1.0
    },
    "216": {
      "21366394": 1.0
    },
    "218": {
      "21366394": 1.0
    },
    "232": {
      "10536636": 1.0
    },
    "233": {
      "4388470": 1.0
    },
    "236": {
      "4388470": 1.0
    },
    "238": {
      "2251426": 1.0
    },
    "239": {
      "14079881": 1.0
    },
    "248": {
      "1568684": 1.0
    },
    "268": {
      "970012": 1.0
    },
    "275": {
      "14241418": 1.0,
      "14819804": 1.0,
      "4961038": 1.0
    },
    "3": {
      "14717500": 1.0
    },
    "303": {
      "4388470": 1.0
    },
    "312": {
      "6173523": 1.0
    },
    "327": {
      "17997584": 1.0
    },
    "338": {
      "23349986": 1.0
    },
    "350": {
      "16927286": 1.0
    },
    "354": {
      "8774475": 1.0
    },
    "36": {
      "11705328": 1.0,
      "5152028": 1.0
    },
    "380": {
      "19005293": 1.0
    },
    "385": {
      "9767444": 1.0,
      "9955779": 1.0
    },
    "386": {
      "16495649": 1.0
    },
    "388": {
      "1148122": 1.0
    },
    "399": {
      "791050": 1.0
    },
    "411": {
      "14924526": 1.0
    },
    "431": {
      "28937856": 1.0
    },
    "437": {
      "18399038": 1.0
    },
    "440": {
      "4423559": 1.0
    },
    "452": {
      "12804937": 1.0,
      "464511": 1.0
    },
    "475": {
      "18678095": 1.0
    },
    "48": {
      "13734012": 1.0
    },
    "491": {
      "56893404": 1.0
    },
    "50": {
      "12580014": 1.0
    },
    "501": {
      "17930286": 1.0
    },
    "51": {
      "45638119": 1.0
    },
    "56": {
      "4709641": 1.0
    },
    "57": {
      "4709641": 1.0
    },
    "70": {
      "4414547": 1.0,
      "5956380": 1.0
    },
    "75": {
      "4387784": 1.0
    },
    "99": {
      "18810195": 1.0
    }
  },
  "record_type": "embedding_pack"
}
{
  "g2": {
    "pass": false,
    "primary_contrast": "ridge_minus_mlp",
    "primary_delta_ndcg_half_width": 0.05532704385847659,
    "rough_query_count_for_threshold": 817,
    "scale_up_note": "Rough 1/sqrt(n) extrapolation only; it is not a prospective power calculation and must be re-estimated with pilot variance.",
    "secondary_median_family_delta_ndcg_half_width": 0.08874823992867405,
    "threshold": 0.015
  },
  "harness_parity": {
    "c3a": 0.0,
    "floor": 0.0,
    "mlp": 0.0,
    "oracle": 0.0,
    "procrustes": 0.0,
    "ridge": 0.0
  },
  "ladder": {
    "canonical_reference_audit": {
      "c3a_cross_seed_reference": {
        "absolute_difference": 0.0056436878858855954,
        "disposition": "superseded_not_a_tolerance_gate",
        "finding": "The old reference was a cross-seed campaign mean. The seed-42 per-query value supersedes it and is outside the nominal tolerance.",
        "old_cross_seed_mean_recovery": 0.2,
        "seed_42_per_query_recovery": 0.19435631211411442,
        "tolerance": 0.001
      },
      "ridge_waypoint_reproduction": {
        "absolute_error": 0.0005713693342979109,
        "actual_recovery": 0.8434286306657021,
        "pass": true,
        "target_recovery": 0.844,
        "tolerance": 0.001
      }
    },
    "dataset": "SciFact",
    "floor_ndcg": 0.0,
    "leakage_sensitivity": {
      "audit": {
        "eval_positive_docs_in_leakage_safe_fit": 0,
        "eval_positive_docs_in_literal_fit": 28,
        "eval_positive_docs_total": 62,
        "leakage_safe_fit_count": 600,
        "literal_fit_count": 600,
        "literal_fit_share_of_eval_positive_docs": 0.45161290322580644
      },
      "fits": {
        "leakage_safe_full_600": "first 600 seed-42 permutation documents after excluding every eval-positive document",
        "literal_waypoint": "literal seed-42 half-corpus permutation"
      },
      "floor_ndcg": 0.0,
      "methods": {
        "mlp": {
          "inflation_recovery_pp": 14.138726554633008,
          "label": "residual MLP",
          "leakage_safe_full_600": {
            "eval_positive_docs_in_fit": 0,
            "ndcg": 0.5451054010727805,
            "recovery": 0.6749635176786221
          },
          "literal_frozen_score_max_abs_delta": 0.0,
          "literal_waypoint": {
            "eval_positive_docs_in_fit": 28,
            "ndcg": 0.6592907756501849,
            "recovery": 0.8163507832249521
          }
        },
        "procrustes": {
          "inflation_recovery_pp": 8.408211817368949,
          "label": "closed-form orthogonal Procrustes",
          "leakage_safe_full_600": {
            "eval_positive_docs_in_fit": 0,
            "ndcg": 0.5967599203911357,
            "recovery": 0.738923471101392
          },
          "literal_frozen_score_max_abs_delta": 0.0,
          "literal_waypoint": {
            "eval_positive_docs_in_fit": 28,
            "ndcg": 0.6646652449748285,
            "recovery": 0.8230055892750815
          }
        },
        "ridge": {
          "inflation_recovery_pp": 5.4284330531711555,
          "label": "ridge",
          "leakage_safe_full_600": {
            "eval_positive_docs_in_fit": 0,
            "ndcg": 0.6373186238395446,
            "recovery": 0.7891443001339905
          },
          "literal_frozen_score_max_abs_delta": 0.0,
          "literal_waypoint": {
            "eval_positive_docs_in_fit": 28,
            "ndcg": 0.6811590403826878,
            "recovery": 0.8434286306657021
          }
        }
      },
      "oracle_gap": 0.8076072066050948,
      "oracle_ndcg": 0.8076072066050948,
      "record_type": "d1_leakage_sensitivity"
    },
    "methods": {
      "c3a": {
        "delta_ndcg": 0.15696355831254788,
        "ndcg": 0.15696355831254788,
        "recovery": 0.19435631211411442
      },
      "mlp": {
        "delta_ndcg": 0.6592907756501849,
        "ndcg": 0.6592907756501849,
        "recovery": 0.8163507832249521
      },
      "procrustes": {
        "delta_ndcg": 0.6646652449748285,
        "ndcg": 0.6646652449748285,
        "recovery": 0.8230055892750815
      },
      "ridge": {
        "delta_ndcg": 0.6811590403826878,
        "ndcg": 0.6811590403826878,
        "recovery": 0.8434286306657021
      }
    },
    "old_scalar_audit": {
      "finding": "The old scalar was a cross-seed campaign mean attached to the seed-42 eval split. The frozen per-query seed-42 result supersedes it.",
      "fixed_split_per_query_ndcg": 0.15696355831254788,
      "fixed_split_per_query_recovery": 0.19435631211411442,
      "ndcg_difference": -0.003936441687452108,
      "old_c3a_scalar_ndcg": 0.1609,
      "old_scalar_implied_recovery": 0.19923051538429024
    },
    "oracle_gap": 0.8076072066050948,
    "oracle_ndcg": 0.8076072066050948,
    "query_count": 60,
    "record_type": "d1_ladder",
    "waypoint_audit": {
      "eval_positive_docs_in_leakage_safe_fit": 0,
      "eval_positive_docs_in_literal_fit": 28,
      "eval_positive_docs_total": 62,
      "leakage_safe_fit_count": 600,
      "literal_fit_count": 600,
      "literal_fit_share_of_eval_positive_docs": 0.45161290322580644,
      "literal_recovery": {
        "mlp": 0.8163507832249521,
        "procrustes": 0.8230055892750815,
        "ridge": 0.8434286306657021
      },
      "protocol_conflict": "The literal waypoint fit contains eval-positive documents. It is retained only as the reproduced headline protocol; the leakage-safe full-600 sensitivity excludes all eval-positive documents."
    }
  },
  "learning_rows": 75,
  "output_dir": "research\\drift_recovery\\out\\d1"
}
{
  "command": "python research/drift_recovery/run_d1.py --device cuda --paper D:\\GITHUB\\CHELATEDAI\\docs\\waypoint-research-2026-06-09\\paper-draft\\main.md",
  "drift_manifest": {
    "drift": "query_encoder_swap",
    "projection_checksum": "3984ab778d1b8b909dfd4a637e74c6588017e4b158487745f29103193bac4626",
    "projection_seed": 42,
    "store_dim": 384,
    "swap_dim": 768,
    "swap_model": "all-mpnet-base-v2"
  },
  "environment": {
    "HF_DATASETS_OFFLINE": "1",
    "HF_HUB_OFFLINE": "1"
  },
  "harness_sha": "34ce4b5632e0d9cd2a16c29e0e1acc42e645b9c2",
  "models": {
    "old": "sentence-transformers/all-MiniLM-L6-v2",
    "swap": "all-mpnet-base-v2"
  },
  "outputs": [
    "C1_FIX_REPORT.md",
    "bootstrap_cis.json",
    "ci_report.md",
    "contrasts_holm.json",
    "d1_summary.json",
    "ladder.json",
    "leakage_sensitivity.json",
    "learning_curve.json",
    "paper_edits.md",
    "run_manifest.json",
    "scifact_evalsplit_pack.json",
    "scifact_evalsplit_pack.npz",
    "section3_draft.md"
  ],
  "paper_source_read_only": "D:\\GITHUB\\CHELATEDAI\\docs\\waypoint-research-2026-06-09\\paper-draft\\main.md",
  "protocol": {
    "anchor_fraction": 0.4,
    "bootstrap_draws": 10000,
    "bootstrap_seed": 20260630,
    "dataset": "SciFact",
    "eval_queries": 60,
    "learning_counts": [
      8,
      16,
      32,
      64,
      128
    ],
    "learning_order_seeds": [
      7,
      42,
      1337,
      2026,
      9001
    ],
    "ndcg": "binary NDCG@10 from merged harness",
    "split_seed": 42
  },
  "protocol_sha": "2215fb3ea7ef238988527370a71f858e94cf3efe694a2af1b58c30f92b477ca9",
  "record_type": "d1_run_manifest",
  "repo_sha": "34ce4b5632e0d9cd2a16c29e0e1acc42e645b9c2"
}

 exited 1 in 550ms:
research/drift_recovery/run_d1.py:21:def build_parser() -> argparse.ArgumentParser:
research/drift_recovery/run_d1.py:35:def main(argv=None) -> int:
rg: research/drift_recovery/stats/*.py: The filename, directory name, or volume label syntax is incorrect. (os error 123)

2026-07-10T03:10:40.697852Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.6 seconds
Output:
research/drift_recovery/run_d1.py:21:def build_parser() -> argparse.ArgumentParser:
research/drift_recovery/run_d1.py:35:def main(argv=None) -> int:
rg: research/drift_recovery/stats/*.py: The filename, directory name, or volume label syntax is incorrect. (os error 123)

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$m='"'C:\\Users\\mattm\\.codex\\memories\\MEMORY.md'; Select-String -Path "'$m -Pattern '"'CHELATEDAI|drift-recovery|severity-calibration|knob-sweep|HF_HUB_OFFLINE|experiment_runs|PR #266' | ForEach-Object {\""'$($_.LineNumber):$($_.Line)"}' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1105ms:
587:# Task Group: CHELATEDAI drift-recovery calibration and brutal-honesty process
588:scope: CHELATEDAI drift-recovery diagnosis, severity calibration, knob-sweep execution, artifact durability, and strict PR-body handling; use when continuing drift-recovery experiments or preparing evidence-heavy PRs.
589:applies_to: cwd=D:\GITHUB\CHELATEDAI; reuse_rule=safe for drift-recovery and repo-process guidance in CHELATEDAI, but experimental results and PR state should be rechecked from current artifacts
595:- rollout_summaries/2026-06-11T15-10-39-PIMu-drift_recovery_pr6_pr8_calibration_knob_sweep_and_bhs_harden.md (cwd=D:\GITHUB\CHELATEDAI, rollout_path=C:\Users\mattm\.codex\sessions\2026\06\11\rollout-2026-06-11T11-10-39-019eb73c-3da3-7f70-9035-afd5a56ecb86.jsonl, updated_at=2026-06-12T14:48:16+00:00, thread_id=019eb73c-3da3-7f70-9035-afd5a56ecb86)
599:- drift-recovery, severity-calibration, knob-sweep, calibration-manifest, HF_HUB_OFFLINE, experiment_runs, artifact durability, deterministic tie handling, validate_pr_brutal_honesty.py, PR #266, admin merge
603:- the goal file required "diagnose last night's mixed drift-recovery result and re-run it calibrated" -> verify hypotheses from data before trusting prior summaries [Task 1]
613:- The working offline smoke path on this host is `HF_HUB_OFFLINE=1` plus `python scripts/smoke_pipeline.py` [Task 1]
614:- Evidence in `experiment_runs/` is not durable by default because the directory is gitignored; force-add the artifact set or move it to a tracked path before opening a PR [Task 1]

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/artifacts.py; Get-Content -Raw research/drift_recovery/contracts.py; Get-Content -Raw research/drift_recovery/harness_bridge.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 699ms:
"""Frozen array artifacts for offline drift-recovery statistics."""

from __future__ import annotations

import hashlib
import json
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any, Dict, Mapping

import numpy as np


def _json_ready(value: Any) -> Any:
    if isinstance(value, np.generic):
        return value.item()
    if isinstance(value, np.ndarray):
        return value.tolist()
    if isinstance(value, dict):
        return {str(key): _json_ready(item) for key, item in value.items()}
    if isinstance(value, (list, tuple)):
        return [_json_ready(item) for item in value]
    return value


@dataclass
class EmbeddingPack:
    """All arrays needed to rerun D1 statistics without model inference.

    The required core is ``Do``, ``Dor``, ``Qd``, qrels, ``fit_idx``, and
    per-query method scores. Extra arrays hold non-eval supervision pools so the
    learning curves remain leakage-free while using the same fixed eval IDs.
    """

    Do: np.ndarray
    Dor: np.ndarray
    Qd: np.ndarray
    doc_ids: np.ndarray
    query_ids: np.ndarray
    qrels: Mapping[str, Mapping[str, float]]
    fit_idx: np.ndarray
    per_query_scores: Mapping[str, np.ndarray]
    extra_arrays: Mapping[str, np.ndarray] = field(default_factory=dict)
    metadata: Mapping[str, Any] = field(default_factory=dict)

    def validate(self) -> None:
        if self.Do.shape != self.Dor.shape or self.Do.ndim != 2:
            raise ValueError("Do and Dor must be same-shape matrices")
        if self.Qd.ndim != 2 or self.Qd.shape[1] != self.Do.shape[1]:
            raise ValueError("Qd must share the embedding dimension")
        if len(self.doc_ids) != self.Do.shape[0]:
            raise ValueError("doc_ids length mismatch")
        if len(self.query_ids) != self.Qd.shape[0]:
            raise ValueError("query_ids length mismatch")
        if np.any(self.fit_idx < 0) or np.any(self.fit_idx >= self.Do.shape[0]):
            raise ValueError("fit_idx is out of bounds")
        if len(np.unique(self.fit_idx)) != len(self.fit_idx):
            raise ValueError("fit_idx contains duplicates")
        for method, scores in self.per_query_scores.items():
            array = np.asarray(scores)
            if array.shape != (len(self.query_ids),):
                raise ValueError(f"{method} per-query score shape mismatch: {array.shape}")
            if not np.all(np.isfinite(array)):
                raise ValueError(f"{method} contains non-finite scores")
        missing = set(map(str, self.query_ids)).difference(map(str, self.qrels))
        if missing:
            raise ValueError(f"qrels missing eval queries: {sorted(missing)[:5]}")

    def save(self, prefix: Path) -> Dict[str, str]:
        self.validate()
        prefix = Path(prefix)
        prefix.parent.mkdir(parents=True, exist_ok=True)
        arrays_path = prefix.with_suffix(".npz")
        metadata_path = prefix.with_suffix(".json")
        arrays: Dict[str, np.ndarray] = {
            "Do": np.asarray(self.Do),
            "Dor": np.asarray(self.Dor),
            "Qd": np.asarray(self.Qd),
            "doc_ids": np.asarray(self.doc_ids, dtype=str),
            "query_ids": np.asarray(self.query_ids, dtype=str),
            "fit_idx": np.asarray(self.fit_idx, dtype=np.int64),
        }
        arrays.update({f"score__{key}": np.asarray(value) for key, value in self.per_query_scores.items()})
        arrays.update({f"extra__{key}": np.asarray(value) for key, value in self.extra_arrays.items()})
        np.savez_compressed(arrays_path, **arrays)
        payload = {
            "record_type": "embedding_pack",
            "format_version": 1,
            "arrays_file": arrays_path.name,
            "qrels": _json_ready(self.qrels),
            "metadata": _json_ready(self.metadata),
            "array_shapes": {key: list(value.shape) for key, value in arrays.items()},
        }
        metadata_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
        digest = hashlib.sha256(arrays_path.read_bytes()).hexdigest()
        payload["arrays_sha256"] = digest
        metadata_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
        return {"arrays": str(arrays_path), "metadata": str(metadata_path), "sha256": digest}

    @classmethod
    def load(cls, prefix: Path) -> "EmbeddingPack":
        prefix = Path(prefix)
        arrays_path = prefix.with_suffix(".npz")
        metadata_path = prefix.with_suffix(".json")
        payload = json.loads(metadata_path.read_text(encoding="utf-8"))
        digest = hashlib.sha256(arrays_path.read_bytes()).hexdigest()
        if digest != payload.get("arrays_sha256"):
            raise ValueError("EmbeddingPack array checksum mismatch")
        with np.load(arrays_path, allow_pickle=False) as stored:
            scores = {key[7:]: stored[key].copy() for key in stored.files if key.startswith("score__")}
            extras = {key[7:]: stored[key].copy() for key in stored.files if key.startswith("extra__")}
            pack = cls(
                Do=stored["Do"].copy(),
                Dor=stored["Dor"].copy(),
                Qd=stored["Qd"].copy(),
                doc_ids=stored["doc_ids"].copy(),
                query_ids=stored["query_ids"].copy(),
                qrels=payload["qrels"],
                fit_idx=stored["fit_idx"].copy(),
                per_query_scores=scores,
                extra_arrays=extras,
                metadata=payload.get("metadata", {}),
            )
        pack.validate()
        return pack


"""Typed contracts shared by drift-recovery experiment components."""

from __future__ import annotations

from abc import ABC, abstractmethod
from dataclasses import asdict, dataclass, field
from typing import Any, Dict, Mapping, Optional, Sequence

import numpy as np


@dataclass(frozen=True)
class RetrievalCase:
    dataset: str
    corpus_ids: Sequence[str]
    query_ids: Sequence[str]
    qrels: Mapping[str, Mapping[str, float]]
    k: int = 10


@dataclass(frozen=True)
class ExperimentSplit:
    anchor_ids: Sequence[str]
    eval_ids: Sequence[str]
    seed: int
    anchor_fraction: float

    def __post_init__(self) -> None:
        overlap = set(self.anchor_ids).intersection(self.eval_ids)
        if overlap:
            raise ValueError(f"anchor/eval leakage: {sorted(overlap)[:5]}")


@dataclass(frozen=True)
class AnchorPairs:
    source: np.ndarray
    target: np.ndarray
    pair_ids: Sequence[str]
    supervision_type: str

    def __post_init__(self) -> None:
        if self.source.shape != self.target.shape:
            raise ValueError("anchor source and target shapes differ")
        if self.source.shape[0] != len(self.pair_ids):
            raise ValueError("pair_ids length does not match anchor rows")


@dataclass(frozen=True)
class PerQueryRecord:
    query_id: str
    floor_ndcg: float
    oracle_ndcg: float
    method_ndcg: Mapping[str, float]


@dataclass(frozen=True)
class RunManifest:
    repo_sha: str
    harness_sha: str
    protocol_sha: str
    model_revisions: Mapping[str, str]
    seeds: Mapping[str, Any]
    command: str
    environment: Mapping[str, str]
    artifacts: Mapping[str, str] = field(default_factory=dict)

    def to_dict(self) -> Dict[str, Any]:
        return asdict(self)


class QueryAdapter(ABC):
    """A method that learns a map over query or document vectors."""

    @abstractmethod
    def fit(self, source: np.ndarray, target: np.ndarray, **kwargs: Any) -> "QueryAdapter":
        raise NotImplementedError

    @abstractmethod
    def transform(self, vectors: np.ndarray) -> np.ndarray:
        raise NotImplementedError


class ScoreTransform(ABC):
    """A method that learns and applies a transformation in score space."""

    @abstractmethod
    def fit(self, scores: np.ndarray, **kwargs: Any) -> "ScoreTransform":
        raise NotImplementedError

    @abstractmethod
    def rank(self, scores: np.ndarray, k: int) -> np.ndarray:
        raise NotImplementedError


@dataclass
class MethodRunner:
    """Uniformly run a vector adapter or a score transform, never both."""

    query_adapter: Optional[QueryAdapter] = None
    score_transform: Optional[ScoreTransform] = None

    def __post_init__(self) -> None:
        if (self.query_adapter is None) == (self.score_transform is None):
            raise ValueError("MethodRunner requires exactly one method interface")

    def fit(self, source: np.ndarray, target: Optional[np.ndarray] = None, **kwargs: Any) -> None:
        if self.query_adapter is not None:
            if target is None:
                raise ValueError("vector adapters require targets")
            self.query_adapter.fit(source, target, **kwargs)
        else:
            assert self.score_transform is not None
            self.score_transform.fit(source, **kwargs)

    def output(self, values: np.ndarray, k: int = 10) -> np.ndarray:
        if self.query_adapter is not None:
            return self.query_adapter.transform(values)
        assert self.score_transform is not None
        return self.score_transform.rank(values, k=k)


"""The single import boundary between D1 and the merged CHELATEDAI harness.

No other module under :mod:`research.drift_recovery` may import root-level
harness modules. Statistical work consumes a frozen :class:`EmbeddingPack`.
"""

from __future__ import annotations

import os
import statistics
from typing import Any, Dict, Mapping, Sequence

import numpy as np

os.environ.setdefault("HF_HUB_OFFLINE", "1")
os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")

# Merged-harness imports: this is intentionally the only module containing them.
from benchmark_utils import canonicalize_id, load_mteb_data  # noqa: E402
from chelation_adapter import create_adapter  # noqa: E402
from drift_recovery_metrics import ndcg_at_k  # noqa: E402
from query_encoder_drift import QueryEncoderDrift  # noqa: E402
from run_drift_recovery_experiment import _split_anchor_eval  # noqa: E402
from run_road_course_campaign import select_road_course_slice  # noqa: E402
from sedimentation_loss import SedimentationInfoNCELoss  # noqa: E402


def _normalize(matrix: np.ndarray) -> np.ndarray:
    matrix = np.asarray(matrix)
    norms = np.linalg.norm(matrix, axis=1, keepdims=True)
    return matrix / np.where(norms > 0.0, norms, 1.0)


def per_query_ndcg(
    query_vectors: np.ndarray,
    query_ids: Sequence[str],
    doc_vectors: np.ndarray,
    doc_ids: Sequence[str],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> np.ndarray:
    """Rank by cosine and score with the merged harness's binary NDCG."""

    docs = _normalize(np.asarray(doc_vectors, dtype=np.float64))
    queries = _normalize(np.asarray(query_vectors, dtype=np.float64))
    canonical_docs = np.asarray([canonicalize_id(value) for value in doc_ids], dtype=str)
    rows = []
    for query_id, vector in zip(query_ids, queries):
        qid = canonicalize_id(query_id)
        relevance = qrels.get(query_id, qrels.get(qid, {}))
        relevant = [canonicalize_id(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0]
        if not relevant:
            raise ValueError(f"eval query {qid} has no positive qrels")
        similarities = docs @ vector
        # Stable full ordering makes the otherwise-unspecified equal-score case deterministic.
        ranked_idx = np.argsort(-similarities, kind="mergesort")[: min(k, len(canonical_docs))]
        ranked = canonical_docs[ranked_idx].tolist()
        rows.append(ndcg_at_k(ranked, relevant, k=k))
    return np.asarray(rows, dtype=np.float64)


def aggregate_ndcg(scores: Sequence[float]) -> float:
    """Match the merged evaluator's ``statistics.fmean`` aggregation."""

    return float(statistics.fmean(float(value) for value in scores))


def assert_harness_parity(pack: Any, atol: float = 1e-12) -> Dict[str, float]:
    """Prove frozen per-query scores reproduce merged-harness aggregate NDCG."""

    references = dict(pack.metadata.get("harness_aggregate_ndcg", {}))
    if not references:
        raise AssertionError("pack has no harness aggregate references")
    differences: Dict[str, float] = {}
    document_matrices = {"floor": pack.Do, "oracle": pack.Dor}
    document_matrices.update(
        {
            name[len("method_documents__") :]: values
            for name, values in pack.extra_arrays.items()
            if name.startswith("method_documents__")
        }
    )
    for method, expected in references.items():
        if method not in pack.per_query_scores:
            raise AssertionError(f"pack is missing reference method {method}")
        if method not in document_matrices:
            raise AssertionError(f"pack is missing frozen document vectors for {method}")
        recomputed = per_query_ndcg(
            pack.Qd,
            pack.query_ids,
            document_matrices[method],
            pack.doc_ids,
            pack.qrels,
            k=int(pack.metadata.get("k", 10)),
        )
        stored = np.asarray(pack.per_query_scores[method], dtype=np.float64)
        per_query_difference = float(np.max(np.abs(recomputed - stored)))
        if per_query_difference > atol:
            raise AssertionError(
                f"{method} per-query pack/harness parity failed: max difference "
                f"{per_query_difference:.3g} > {atol:.3g}"
            )
        actual = aggregate_ndcg(recomputed)
        difference = abs(actual - float(expected))
        differences[method] = difference
        if difference > atol:
            raise AssertionError(
                f"{method} pack/harness parity failed: {actual:.17g} vs {float(expected):.17g} "
                f"(difference {difference:.3g} > {atol:.3g})"
            )
    return differences


def load_scifact_evalsplit(seed: int = 42) -> Dict[str, Any]:
    """Load the exact waypoint eval-split recipe and leakage-free curve pools."""

    corpus, queries, qrels = load_mteb_data("SciFact")
    if corpus is None or queries is None or qrels is None:
        raise RuntimeError("offline SciFact cache is unavailable")
    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
        corpus,
        queries,
        qrels,
        max_queries=100,
        sample_docs=1200,
        seed=seed,
    )
    anchor_ids, eval_ids = _split_anchor_eval(sliced_queries, 0.4, seed, active=True)
    eval_set = set(map(canonicalize_id, eval_ids))
    anchor_set = set(map(canonicalize_id, anchor_ids))
    if eval_set.intersection(anchor_set):
        raise AssertionError("merged split leaked anchor IDs into eval IDs")

    doc_ids = [canonicalize_id(value) for value in sliced_corpus]
    eval_query_ids = [
        canonicalize_id(value)
        for value in sliced_queries
        if sliced_qrels.get(value) and canonicalize_id(value) in eval_set
    ]
    canonical_anchor_ids = [
        canonicalize_id(value) for value in sliced_queries if canonicalize_id(value) in anchor_set
    ]
    query_by_id = {canonicalize_id(key): str(value) for key, value in queries.items()}
    corpus_by_id = {canonicalize_id(key): str(value) for key, value in corpus.items()}
    qrels_by_id = {
        canonicalize_id(query_id): {
            canonicalize_id(doc_id): float(score) for doc_id, score in rels.items()
        }
        for query_id, rels in qrels.items()
    }
    sliced_qrels_by_id = {
        canonicalize_id(query_id): {
            canonicalize_id(doc_id): float(score) for doc_id, score in rels.items()
        }
        for query_id, rels in sliced_qrels.items()
    }

    # Canonical C3a pool: every positive pair from the exact 40-query anchor split.
    selected_doc_set = set(doc_ids)
    canonical_pairs = []
    for query_id in canonical_anchor_ids:
        for doc_id, score in sorted(qrels_by_id.get(query_id, {}).items()):
            if float(score) > 0.0 and doc_id in selected_doc_set:
                canonical_pairs.append((query_id, doc_id))

    # Learning-curve query pool: one positive document per non-eval SciFact
    # query. This reaches 128 distinct query anchors without touching eval qrels.
    curve_pairs = []
    for query_id in sorted(query_by_id):
        if query_id in eval_set:
            continue
        positives = sorted(
            doc_id for doc_id, score in qrels_by_id.get(query_id, {}).items() if float(score) > 0.0
        )
        if positives:
            curve_pairs.append((query_id, positives[0]))
    if len(curve_pairs) < 128:
        raise RuntimeError(f"only {len(curve_pairs)} leakage-free C3a curve anchors; need 128")

    return {
        "corpus_texts": [str(sliced_corpus[key]) for key in sliced_corpus],
        "doc_ids": doc_ids,
        "eval_query_texts": [query_by_id[key] for key in eval_query_ids],
        "eval_query_ids": eval_query_ids,
        "eval_qrels": {key: sliced_qrels_by_id[key] for key in eval_query_ids},
        "anchor_ids": canonical_anchor_ids,
        "canonical_pair_query_ids": [item[0] for item in canonical_pairs],
        "canonical_pair_doc_ids": [item[1] for item in canonical_pairs],
        "canonical_pair_query_texts": [query_by_id[item[0]] for item in canonical_pairs],
        "curve_pair_query_ids": [item[0] for item in curve_pairs],
        "curve_pair_doc_ids": [item[1] for item in curve_pairs],
        "curve_pair_query_texts": [query_by_id[item[0]] for item in curve_pairs],
        "curve_pair_doc_texts": [corpus_by_id[item[1]] for item in curve_pairs],
        "all_query_count": len(queries),
    }


def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
    """Run both cached encoders once and return arrays ready for freezing."""

    from sentence_transformers import SentenceTransformer

    old = SentenceTransformer("sentence-transformers/all-MiniLM-L6-v2", device=device)
    Do = np.asarray(
        old.encode(case["corpus_texts"], batch_size=128, show_progress_bar=True), dtype=np.float64
    )
    curve_source = np.asarray(
        old.encode(case["curve_pair_doc_texts"], batch_size=128, show_progress_bar=True), dtype=np.float64
    )
    drift = QueryEncoderDrift(store_dim=Do.shape[1], swap_model_name="all-mpnet-base-v2", seed=seed)
    # One call fixes the projection; all later calls reuse exactly that instance.
    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
    canonical_target = np.asarray(
        drift.embed_queries(list(case["canonical_pair_query_texts"])), dtype=np.float64
    )
    curve_target = np.asarray(
        drift.embed_queries(list(case["curve_pair_query_texts"])), dtype=np.float64
    )
    doc_lookup = {doc_id: index for index, doc_id in enumerate(case["doc_ids"])}
    canonical_source = np.asarray(
        [Do[doc_lookup[doc_id]] for doc_id in case["canonical_pair_doc_ids"]], dtype=np.float64
    )
    return {
        "Do": Do,
        "Dor": Dor,
        "Qd": Qd,
        "canonical_source": canonical_source,
        "canonical_target": canonical_target,
        "curve_source": curve_source,
        "curve_target": curve_target,
        "drift_manifest": drift.manifest(),
        "old_model": "sentence-transformers/all-MiniLM-L6-v2",
        "swap_model": "all-mpnet-base-v2",
    }


def train_c3a_documents(
    all_documents: np.ndarray,
    anchor_documents: np.ndarray,
    anchor_queries: np.ndarray,
    seed: int,
    steps: int = 30,
    learning_rate: float = 0.01,
    min_correction: float = 0.01,
    max_correction: float = 0.5,
) -> np.ndarray:
    """Run the merged C3a bounded adapter and InfoNCE training path."""

    import torch

    torch.manual_seed(int(seed))
    adapter = create_adapter(
        "mlp",
        input_dim=int(all_documents.shape[1]),
        bounded=True,
        min_correction=float(min_correction),
        max_correction=float(max_correction),
    )
    source = torch.tensor(np.asarray(anchor_documents, dtype=np.float32), dtype=torch.float32)
    target = torch.tensor(np.asarray(anchor_queries, dtype=np.float32), dtype=torch.float32)
    loss_fn = SedimentationInfoNCELoss()
    optimizer = torch.optim.Adam(adapter.parameters(), lr=float(learning_rate))
    # Mirrors the merged harness, including the post-construction seed call.
    torch.manual_seed(int(seed))
    adapter.train()
    for _ in range(int(steps)):
        optimizer.zero_grad()
        loss = loss_fn(adapter(source), target)
        loss.backward()
        optimizer.step()
    adapter.eval()
    with torch.no_grad():
        output = adapter(torch.tensor(np.asarray(all_documents, dtype=np.float32), dtype=torch.float32))
    return np.asarray(output.detach().cpu().numpy(), dtype=np.float64)


def system_under_test_spec() -> Dict[str, Any]:
    """Return exact code-level constants used by the Section 3 renderer."""

    from config import ChelationConfig

    return {
        "bounded_defaults": {
            "min_correction": ChelationConfig.BOUNDED_ADAPTER_MIN_CORRECTION,
            "max_correction": ChelationConfig.BOUNDED_ADAPTER_MAX_CORRECTION,
            "int8_floor": 1.0 / 128.0,
        },
        "controller_defaults": {
            "initial_temperature": 0.0,
            "cooling_rate": 0.7,
            "trigger_threshold": 0.15,
            "max_temperature": 1.0,
            "epsilon": 1e-6,
        },
        "supervised_cycle_controller": {
            "cooling_rate": 0.5,
            "drift_signal": "max(0, baseline_ndcg - current_ndcg)",
        },
    }


exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/tests/test_d1_stats.py; Get-Content -Raw research/drift_recovery/out/d1/scifact_evalsplit_pack.json; Get-Content -Raw research/drift_recovery/out/d1/d1_summary.json; Get-Content -Raw research/drift_recovery/out/d1/run_manifest.json' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 795ms:
from __future__ import annotations

import unittest
from pathlib import Path

import numpy as np

from research.drift_recovery.artifacts import EmbeddingPack
from research.drift_recovery.d1.paper_stats import audit_fit_leakage, compute_leakage_sensitivity
from research.drift_recovery.harness_bridge import assert_harness_parity
from research.drift_recovery.stats.learning_curve import nested_prefixes
from research.drift_recovery.stats.multiple_testing import holm_adjust
from research.drift_recovery.stats.paired_bootstrap import (
    BootstrapDraws,
    paired_query_bootstrap,
    strip_draws,
)


class TestPairedBootstrap(unittest.TestCase):
    def test_recovery_uses_ratio_of_resampled_means(self) -> None:
        scores = {
            "floor": np.array([0.0, 0.4, 0.0, 0.4]),
            "oracle": np.array([1.0, 0.6, 1.0, 0.6]),
            "method": np.array([0.5, 0.5, 0.5, 0.5]),
        }
        result = paired_query_bootstrap(scores, ["method"], draws=1000, seed=9)
        self.assertAlmostEqual(result["methods"]["method"]["recovery"]["estimate"], 0.5)
        self.assertNotIn("_method_draws", strip_draws(result))

    def test_floor_oracle_and_methods_share_bootstrap_indices(self) -> None:
        scores = {
            "floor": np.array([0.0, 0.1, 0.2]),
            "oracle": np.array([0.7, 0.8, 0.9]),
            "ridge": np.array([0.4, 0.5, 0.6]),
            "mlp": np.array([0.3, 0.4, 0.5]),
        }
        indices = np.tile(np.array([[0, 1, 2], [2, 2, 0]]), (50, 1))
        draws = BootstrapDraws(indices=indices, seed=17)

        result = paired_query_bootstrap(
            scores,
            ["ridge", "mlp"],
            bootstrap_draws=draws,
        )

        np.testing.assert_array_equal(result["_bootstrap_indices"], indices)
        for name, values in scores.items():
            expected = values[indices].mean(axis=1)
            np.testing.assert_allclose(result["_resampled_means"][name], expected)

    def test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent(self) -> None:
        scores = {
            "floor": np.array([0.0, 0.0]),
            "oracle": np.array([0.1, 1.0]),
            "method": np.array([0.05, 0.5]),
        }
        indices = np.tile(np.array([[0, 1]]), (100, 1))
        indices[0] = [0, 0]
        one_percent = BootstrapDraws(indices=indices, seed=23)

        result = paired_query_bootstrap(
            scores,
            ["method"],
            epsilon=0.1,
            bootstrap_draws=one_percent,
        )

        self.assertEqual(result["invalid_draws"], 1)
        self.assertAlmostEqual(result["invalid_fraction"], 0.01)

        indices[1] = [0, 0]
        above_limit = BootstrapDraws(indices=indices, seed=23)
        with self.assertRaisesRegex(RuntimeError, "exceeds the 1% protocol limit"):
            paired_query_bootstrap(
                scores,
                ["method"],
                epsilon=0.1,
                bootstrap_draws=above_limit,
            )

    def test_holm_is_monotone_in_sorted_order(self) -> None:
        adjusted = holm_adjust({"a": 0.01, "b": 0.03, "c": 0.20})
        values = [adjusted[key]["holm_adjusted_p"] for key in ("a", "b", "c")]
        self.assertEqual(values, sorted(values))


class TestLearningCurve(unittest.TestCase):
    def test_prefixes_are_nested_and_seeded(self) -> None:
        first = nested_prefixes(32, counts=(4, 8, 16), seeds=(1, 2))
        second = nested_prefixes(32, counts=(4, 8, 16), seeds=(1, 2))
        for seed in (1, 2):
            self.assertTrue(set(first[seed][4]).issubset(set(first[seed][8])))
            np.testing.assert_array_equal(first[seed][16], second[seed][16])


class TestFrozenPack(unittest.TestCase):
    @staticmethod
    def _load_pack() -> EmbeddingPack:
        prefix = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
        if not prefix.with_suffix(".npz").exists():
            raise unittest.SkipTest("D1 pack has not been built")
        return EmbeddingPack.load(prefix)

    def test_real_pack_reloads_and_matches_harness_aggregates(self) -> None:
        pack = self._load_pack()
        differences = assert_harness_parity(pack, atol=1e-12)
        self.assertTrue(differences)
        self.assertTrue(all(value == 0.0 for value in differences.values()))
        safe = pack.extra_arrays["leakage_safe_fit_idx"]
        self.assertEqual(len(safe), 600)
        self.assertEqual(
            pack.metadata["waypoint_audit"]["eval_positive_docs_in_leakage_safe_fit"],
            0,
        )

    def test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack(self) -> None:
        audit = audit_fit_leakage(self._load_pack())

        self.assertEqual(audit["eval_positive_docs_total"], 62)
        self.assertEqual(audit["eval_positive_docs_in_literal_fit"], 28)
        self.assertEqual(audit["eval_positive_docs_in_leakage_safe_fit"], 0)

    def test_leakage_safe_ridge_recovery_is_below_literal_fit(self) -> None:
        sensitivity = compute_leakage_sensitivity(
            self._load_pack(),
            methods=("ridge",),
            device="cpu",
        )
        ridge = sensitivity["methods"]["ridge"]

        self.assertLess(
            ridge["leakage_safe_full_600"]["recovery"],
            ridge["literal_waypoint"]["recovery"],
        )


if __name__ == "__main__":
    unittest.main()

{
  "array_shapes": {
    "Do": [
      1200,
      384
    ],
    "Dor": [
      1200,
      384
    ],
    "Qd": [
      60,
      384
    ],
    "doc_ids": [
      1200
    ],
    "extra__canonical_c3a_source": [
      49,
      384
    ],
    "extra__canonical_c3a_target": [
      49,
      384
    ],
    "extra__curve_c3a_query_ids": [
      240
    ],
    "extra__curve_c3a_source": [
      240,
      384
    ],
    "extra__curve_c3a_target": [
      240,
      384
    ],
    "extra__leakage_safe_fit_idx": [
      600
    ],
    "extra__method_documents__c3a": [
      1200,
      384
    ],
    "extra__method_documents__mlp": [
      1200,
      384
    ],
    "extra__method_documents__procrustes": [
      1200,
      384
    ],
    "extra__method_documents__ridge": [
      1200,
      384
    ],
    "fit_idx": [
      600
    ],
    "query_ids": [
      60
    ],
    "score__c3a": [
      60
    ],
    "score__floor": [
      60
    ],
    "score__mlp": [
      60
    ],
    "score__oracle": [
      60
    ],
    "score__procrustes": [
      60
    ],
    "score__ridge": [
      60
    ]
  },
  "arrays_file": "scifact_evalsplit_pack.npz",
  "arrays_sha256": "14bc80356f2d77b74e48edd266e5071b162fc9d905034cddf31dc7d4747058ac",
  "format_version": 1,
  "metadata": {
    "anchor_fraction": 0.4,
    "anchor_ids": [
      "5",
      "13",
      "42",
      "49",
      "53",
      "54",
      "72",
      "94",
      "100",
      "129",
      "130",
      "133",
      "142",
      "179",
      "208",
      "217",
      "219",
      "230",
      "237",
      "249",
      "261",
      "269",
      "274",
      "279",
      "294",
      "295",
      "298",
      "300",
      "314",
      "324",
      "343",
      "362",
      "384",
      "410",
      "415",
      "421",
      "436",
      "439",
      "443",
      "478"
    ],
    "canonical_c3a_pair_count": 49,
    "canonical_tolerance": {
      "c3a_absolute_error": 0.0056436878858855954,
      "c3a_target": 0.2,
      "ridge_absolute_error": 0.000571369334298133,
      "ridge_target": 0.844,
      "tolerance": 0.001
    },
    "dataset": "SciFact",
    "drift_manifest": {
      "drift": "query_encoder_swap",
      "projection_checksum": "3984ab778d1b8b909dfd4a637e74c6588017e4b158487745f29103193bac4626",
      "projection_seed": 42,
      "store_dim": 384,
      "swap_dim": 768,
      "swap_model": "all-mpnet-base-v2"
    },
    "harness_aggregate_ndcg": {
      "c3a": 0.15696355831254788,
      "floor": 0.0,
      "mlp": 0.6592907756501849,
      "oracle": 0.8076072066050948,
      "procrustes": 0.6646652449748284,
      "ridge": 0.6811590403826876
    },
    "k": 10,
    "learning_curve_c3a_pool": "one positive doc per non-eval SciFact query; eval IDs and eval qrels excluded",
    "max_queries": 100,
    "models": {
      "old": "sentence-transformers/all-MiniLM-L6-v2",
      "swap": "all-mpnet-base-v2"
    },
    "ndcg": "binary merged-harness ndcg_at_k",
    "old_scalar_audit": {
      "finding": "The old scalar was a cross-seed campaign mean attached to the seed-42 eval split. The frozen per-query seed-42 result supersedes it.",
      "fixed_split_per_query_ndcg": 0.15696355831254788,
      "fixed_split_per_query_recovery": 0.19435631211411442,
      "ndcg_difference": -0.003936441687452108,
      "old_c3a_scalar_ndcg": 0.1609,
      "old_scalar_implied_recovery": 0.19923051538429024
    },
    "recovery": {
      "c3a": 0.19435631211411442,
      "mlp": 0.8163507832249521,
      "procrustes": 0.8230055892750813,
      "ridge": 0.8434286306657018
    },
    "sample_docs": 1200,
    "seed": 42,
    "waypoint_audit": {
      "eval_positive_docs_in_leakage_safe_fit": 0,
      "eval_positive_docs_in_literal_fit": 28,
      "leakage_safe_fit_count": 600,
      "literal_fit_count": 600,
      "literal_recovery": {
        "procrustes": 0.8230055892750813,
        "ridge": 0.8434286306657018
      },
      "protocol_conflict": "The exact waypoint permutation contains eval-positive documents. Canonical ladder/bootstrap reproduction uses it; new learning-curve fits use the separate leakage_safe_fit_idx."
    }
  },
  "qrels": {
    "1": {
      "31715818": 1.0
    },
    "113": {
      "6157837": 1.0
    },
    "115": {
      "33872649": 1.0
    },
    "118": {
      "6372244": 1.0
    },
    "124": {
      "4883040": 1.0
    },
    "127": {
      "21598000": 1.0
    },
    "128": {
      "8290953": 1.0
    },
    "132": {
      "7975937": 1.0
    },
    "137": {
      "26016929": 1.0
    },
    "141": {
      "14437255": 1.0,
      "6955746": 1.0
    },
    "143": {
      "10582939": 1.0
    },
    "146": {
      "10582939": 1.0
    },
    "148": {
      "1084345": 1.0
    },
    "163": {
      "18872233": 1.0
    },
    "171": {
      "12670680": 1.0
    },
    "180": {
      "16966326": 1.0
    },
    "183": {
      "12827098": 1.0
    },
    "185": {
      "18340282": 1.0
    },
    "198": {
      "2177022": 1.0
    },
    "212": {
      "22038539": 1.0
    },
    "213": {
      "13625993": 1.0
    },
    "216": {
      "21366394": 1.0
    },
    "218": {
      "21366394": 1.0
    },
    "232": {
      "10536636": 1.0
    },
    "233": {
      "4388470": 1.0
    },
    "236": {
      "4388470": 1.0
    },
    "238": {
      "2251426": 1.0
    },
    "239": {
      "14079881": 1.0
    },
    "248": {
      "1568684": 1.0
    },
    "268": {
      "970012": 1.0
    },
    "275": {
      "14241418": 1.0,
      "14819804": 1.0,
      "4961038": 1.0
    },
    "3": {
      "14717500": 1.0
    },
    "303": {
      "4388470": 1.0
    },
    "312": {
      "6173523": 1.0
    },
    "327": {
      "17997584": 1.0
    },
    "338": {
      "23349986": 1.0
    },
    "350": {
      "16927286": 1.0
    },
    "354": {
      "8774475": 1.0
    },
    "36": {
      "11705328": 1.0,
      "5152028": 1.0
    },
    "380": {
      "19005293": 1.0
    },
    "385": {
      "9767444": 1.0,
      "9955779": 1.0
    },
    "386": {
      "16495649": 1.0
    },
    "388": {
      "1148122": 1.0
    },
    "399": {
      "791050": 1.0
    },
    "411": {
      "14924526": 1.0
    },
    "431": {
      "28937856": 1.0
    },
    "437": {
      "18399038": 1.0
    },
    "440": {
      "4423559": 1.0
    },
    "452": {
      "12804937": 1.0,
      "464511": 1.0
    },
    "475": {
      "18678095": 1.0
    },
    "48": {
      "13734012": 1.0
    },
    "491": {
      "56893404": 1.0
    },
    "50": {
      "12580014": 1.0
    },
    "501": {
      "17930286": 1.0
    },
    "51": {
      "45638119": 1.0
    },
    "56": {
      "4709641": 1.0
    },
    "57": {
      "4709641": 1.0
    },
    "70": {
      "4414547": 1.0,
      "5956380": 1.0
    },
    "75": {
      "4387784": 1.0
    },
    "99": {
      "18810195": 1.0
    }
  },
  "record_type": "embedding_pack"
}
{
  "g2": {
    "pass": false,
    "primary_contrast": "ridge_minus_mlp",
    "primary_delta_ndcg_half_width": 0.05532704385847659,
    "rough_query_count_for_threshold": 817,
    "scale_up_note": "Rough 1/sqrt(n) extrapolation only; it is not a prospective power calculation and must be re-estimated with pilot variance.",
    "secondary_median_family_delta_ndcg_half_width": 0.08874823992867405,
    "threshold": 0.015
  },
  "harness_parity": {
    "c3a": 0.0,
    "floor": 0.0,
    "mlp": 0.0,
    "oracle": 0.0,
    "procrustes": 0.0,
    "ridge": 0.0
  },
  "ladder": {
    "canonical_reference_audit": {
      "c3a_cross_seed_reference": {
        "absolute_difference": 0.0056436878858855954,
        "disposition": "superseded_not_a_tolerance_gate",
        "finding": "The old reference was a cross-seed campaign mean. The seed-42 per-query value supersedes it and is outside the nominal tolerance.",
        "old_cross_seed_mean_recovery": 0.2,
        "seed_42_per_query_recovery": 0.19435631211411442,
        "tolerance": 0.001
      },
      "ridge_waypoint_reproduction": {
        "absolute_error": 0.0005713693342979109,
        "actual_recovery": 0.8434286306657021,
        "pass": true,
        "target_recovery": 0.844,
        "tolerance": 0.001
      }
    },
    "dataset": "SciFact",
    "floor_ndcg": 0.0,
    "leakage_sensitivity": {
      "audit": {
        "eval_positive_docs_in_leakage_safe_fit": 0,
        "eval_positive_docs_in_literal_fit": 28,
        "eval_positive_docs_total": 62,
        "leakage_safe_fit_count": 600,
        "literal_fit_count": 600,
        "literal_fit_share_of_eval_positive_docs": 0.45161290322580644
      },
      "fits": {
        "leakage_safe_full_600": "first 600 seed-42 permutation documents after excluding every eval-positive document",
        "literal_waypoint": "literal seed-42 half-corpus permutation"
      },
      "floor_ndcg": 0.0,
      "methods": {
        "mlp": {
          "inflation_recovery_pp": 14.138726554633008,
          "label": "residual MLP",
          "leakage_safe_full_600": {
            "eval_positive_docs_in_fit": 0,
            "ndcg": 0.5451054010727805,
            "recovery": 0.6749635176786221
          },
          "literal_frozen_score_max_abs_delta": 0.0,
          "literal_waypoint": {
            "eval_positive_docs_in_fit": 28,
            "ndcg": 0.6592907756501849,
            "recovery": 0.8163507832249521
          }
        },
        "procrustes": {
          "inflation_recovery_pp": 8.408211817368949,
          "label": "closed-form orthogonal Procrustes",
          "leakage_safe_full_600": {
            "eval_positive_docs_in_fit": 0,
            "ndcg": 0.5967599203911357,
            "recovery": 0.738923471101392
          },
          "literal_frozen_score_max_abs_delta": 0.0,
          "literal_waypoint": {
            "eval_positive_docs_in_fit": 28,
            "ndcg": 0.6646652449748285,
            "recovery": 0.8230055892750815
          }
        },
        "ridge": {
          "inflation_recovery_pp": 5.4284330531711555,
          "label": "ridge",
          "leakage_safe_full_600": {
            "eval_positive_docs_in_fit": 0,
            "ndcg": 0.6373186238395446,
            "recovery": 0.7891443001339905
          },
          "literal_frozen_score_max_abs_delta": 0.0,
          "literal_waypoint": {
            "eval_positive_docs_in_fit": 28,
            "ndcg": 0.6811590403826878,
            "recovery": 0.8434286306657021
          }
        }
      },
      "oracle_gap": 0.8076072066050948,
      "oracle_ndcg": 0.8076072066050948,
      "record_type": "d1_leakage_sensitivity"
    },
    "methods": {
      "c3a": {
        "delta_ndcg": 0.15696355831254788,
        "ndcg": 0.15696355831254788,
        "recovery": 0.19435631211411442
      },
      "mlp": {
        "delta_ndcg": 0.6592907756501849,
        "ndcg": 0.6592907756501849,
        "recovery": 0.8163507832249521
      },
      "procrustes": {
        "delta_ndcg": 0.6646652449748285,
        "ndcg": 0.6646652449748285,
        "recovery": 0.8230055892750815
      },
      "ridge": {
        "delta_ndcg": 0.6811590403826878,
        "ndcg": 0.6811590403826878,
        "recovery": 0.8434286306657021
      }
    },
    "old_scalar_audit": {
      "finding": "The old scalar was a cross-seed campaign mean attached to the seed-42 eval split. The frozen per-query seed-42 result supersedes it.",
      "fixed_split_per_query_ndcg": 0.15696355831254788,
      "fixed_split_per_query_recovery": 0.19435631211411442,
      "ndcg_difference": -0.003936441687452108,
      "old_c3a_scalar_ndcg": 0.1609,
      "old_scalar_implied_recovery": 0.19923051538429024
    },
    "oracle_gap": 0.8076072066050948,
    "oracle_ndcg": 0.8076072066050948,
    "query_count": 60,
    "record_type": "d1_ladder",
    "waypoint_audit": {
      "eval_positive_docs_in_leakage_safe_fit": 0,
      "eval_positive_docs_in_literal_fit": 28,
      "eval_positive_docs_total": 62,
      "leakage_safe_fit_count": 600,
      "literal_fit_count": 600,
      "literal_fit_share_of_eval_positive_docs": 0.45161290322580644,
      "literal_recovery": {
        "mlp": 0.8163507832249521,
        "procrustes": 0.8230055892750815,
        "ridge": 0.8434286306657021
      },
      "protocol_conflict": "The literal waypoint fit contains eval-positive documents. It is retained only as the reproduced headline protocol; the leakage-safe full-600 sensitivity excludes all eval-positive documents."
    }
  },
  "learning_rows": 75,
  "output_dir": "research\\drift_recovery\\out\\d1"
}
{
  "command": "python research/drift_recovery/run_d1.py --device cuda --paper D:\\GITHUB\\CHELATEDAI\\docs\\waypoint-research-2026-06-09\\paper-draft\\main.md",
  "drift_manifest": {
    "drift": "query_encoder_swap",
    "projection_checksum": "3984ab778d1b8b909dfd4a637e74c6588017e4b158487745f29103193bac4626",
    "projection_seed": 42,
    "store_dim": 384,
    "swap_dim": 768,
    "swap_model": "all-mpnet-base-v2"
  },
  "environment": {
    "HF_DATASETS_OFFLINE": "1",
    "HF_HUB_OFFLINE": "1"
  },
  "harness_sha": "34ce4b5632e0d9cd2a16c29e0e1acc42e645b9c2",
  "models": {
    "old": "sentence-transformers/all-MiniLM-L6-v2",
    "swap": "all-mpnet-base-v2"
  },
  "outputs": [
    "C1_FIX_REPORT.md",
    "bootstrap_cis.json",
    "ci_report.md",
    "contrasts_holm.json",
    "d1_summary.json",
    "ladder.json",
    "leakage_sensitivity.json",
    "learning_curve.json",
    "paper_edits.md",
    "run_manifest.json",
    "scifact_evalsplit_pack.json",
    "scifact_evalsplit_pack.npz",
    "section3_draft.md"
  ],
  "paper_source_read_only": "D:\\GITHUB\\CHELATEDAI\\docs\\waypoint-research-2026-06-09\\paper-draft\\main.md",
  "protocol": {
    "anchor_fraction": 0.4,
    "bootstrap_draws": 10000,
    "bootstrap_seed": 20260630,
    "dataset": "SciFact",
    "eval_queries": 60,
    "learning_counts": [
      8,
      16,
      32,
      64,
      128
    ],
    "learning_order_seeds": [
      7,
      42,
      1337,
      2026,
      9001
    ],
    "ndcg": "binary NDCG@10 from merged harness",
    "split_seed": 42
  },
  "protocol_sha": "2215fb3ea7ef238988527370a71f858e94cf3efe694a2af1b58c30f92b477ca9",
  "record_type": "d1_run_manifest",
  "repo_sha": "34ce4b5632e0d9cd2a16c29e0e1acc42e645b9c2"
}

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "rg -n \"def |class |EmbeddingPack|ridge|parity|ndcg|fit_idx|anchor_fraction|dataset|model|save\" research/drift_recovery/run_d1.py research/drift_recovery/stats; rg -n \"agreed-build-plan-v1|recoverability estimator|D3\" . -g '*.md' -g '*.py' -g '"'!docs/**/paper-draft/**'"'; exit 0" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 778ms:
research/drift_recovery/run_d1.py:21:def build_parser() -> argparse.ArgumentParser:
research/drift_recovery/run_d1.py:35:def main(argv=None) -> int:
research/drift_recovery/stats\learning_curve.py:14:def nested_prefixes(
research/drift_recovery/stats\learning_curve.py:36:def summarize_learning_curve(rows: Iterable[dict]) -> list:
research/drift_recovery/stats\learning_curve.py:42:        grouped.setdefault(key, []).append(float(row["ndcg"]))
research/drift_recovery/stats\learning_curve.py:52:                "mean_ndcg": float(array.mean()),
research/drift_recovery/stats\learning_curve.py:53:                "std_ndcg": float(array.std(ddof=1)) if len(values) > 1 else 0.0,
research/drift_recovery/stats\learning_curve.py:54:                "min_ndcg": float(array.min()),
research/drift_recovery/stats\learning_curve.py:55:                "max_ndcg": float(array.max()),
research/drift_recovery/stats\multiple_testing.py:8:def holm_adjust(p_values: Mapping[str, float], alpha: float = 0.05) -> Dict[str, dict]:
research/drift_recovery/stats\paired_bootstrap.py:12:class BootstrapDraws:
research/drift_recovery/stats\paired_bootstrap.py:17:    def create(cls, query_count: int, draws: int = 10_000, seed: int = 20260630) -> "BootstrapDraws":
research/drift_recovery/stats\paired_bootstrap.py:26:def _ci(values: np.ndarray, confidence: float) -> Tuple[float, float]:
research/drift_recovery/stats\paired_bootstrap.py:32:def _metric_row(point: float, samples: np.ndarray, confidence: float) -> dict:
research/drift_recovery/stats\paired_bootstrap.py:42:def paired_query_bootstrap(
research/drift_recovery/stats\paired_bootstrap.py:93:        "floor_ndcg": floor_point,
research/drift_recovery/stats\paired_bootstrap.py:94:        "oracle_ndcg": oracle_point,
research/drift_recovery/stats\paired_bootstrap.py:108:        point_ndcg = float(values.mean())
research/drift_recovery/stats\paired_bootstrap.py:109:        point_delta = point_ndcg - floor_point
research/drift_recovery/stats\paired_bootstrap.py:113:            "ndcg": _metric_row(point_ndcg, means, confidence),
research/drift_recovery/stats\paired_bootstrap.py:114:            "delta_ndcg": _metric_row(point_delta, delta, confidence),
research/drift_recovery/stats\paired_bootstrap.py:123:def paired_contrast(
research/drift_recovery/stats\paired_bootstrap.py:136:    point = float(methods[left]["ndcg"]["estimate"] - methods[right]["ndcg"]["estimate"])
research/drift_recovery/stats\paired_bootstrap.py:146:def strip_draws(result: Mapping[str, object]) -> dict:

codex
One important contract issue is already visible: the frozen SciFact pack’s literal `fit_idx` contains 28 eval-positive documents, while its `extra_arrays["leakage_safe_fit_idx"]` contains none. D3 will explicitly prefer and audit that safe index; it will not silently train estimator features on the legacy literal index. The bound will also be computed in the cosine-normalized space used by harness scoring so its inequality matches the actual ranking geometry.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/harness_bridge.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/d1/paper_stats.py; Get-Content -Raw research/drift_recovery/run_d1.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "rg -n \"class .*Ridge|ridge|create_adapter|def transform|method_documents\" chelation_adapter.py research/drift_recovery -g '*.py'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\memories\\MEMORY.md'; "'$lines=Get-Content $p; for($i=586;$i -le 616;$i++){"${i}:$($lines[$i-1])"}' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1056ms:
"""The single import boundary between D1 and the merged CHELATEDAI harness.

No other module under :mod:`research.drift_recovery` may import root-level
harness modules. Statistical work consumes a frozen :class:`EmbeddingPack`.
"""

from __future__ import annotations

import os
import statistics
from typing import Any, Dict, Mapping, Sequence

import numpy as np

os.environ.setdefault("HF_HUB_OFFLINE", "1")
os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")

# Merged-harness imports: this is intentionally the only module containing them.
from benchmark_utils import canonicalize_id, load_mteb_data  # noqa: E402
from chelation_adapter import create_adapter  # noqa: E402
from drift_recovery_metrics import ndcg_at_k  # noqa: E402
from query_encoder_drift import QueryEncoderDrift  # noqa: E402
from run_drift_recovery_experiment import _split_anchor_eval  # noqa: E402
from run_road_course_campaign import select_road_course_slice  # noqa: E402
from sedimentation_loss import SedimentationInfoNCELoss  # noqa: E402


def _normalize(matrix: np.ndarray) -> np.ndarray:
    matrix = np.asarray(matrix)
    norms = np.linalg.norm(matrix, axis=1, keepdims=True)
    return matrix / np.where(norms > 0.0, norms, 1.0)


def per_query_ndcg(
    query_vectors: np.ndarray,
    query_ids: Sequence[str],
    doc_vectors: np.ndarray,
    doc_ids: Sequence[str],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> np.ndarray:
    """Rank by cosine and score with the merged harness's binary NDCG."""

    docs = _normalize(np.asarray(doc_vectors, dtype=np.float64))
    queries = _normalize(np.asarray(query_vectors, dtype=np.float64))
    canonical_docs = np.asarray([canonicalize_id(value) for value in doc_ids], dtype=str)
    rows = []
    for query_id, vector in zip(query_ids, queries):
        qid = canonicalize_id(query_id)
        relevance = qrels.get(query_id, qrels.get(qid, {}))
        relevant = [canonicalize_id(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0]
        if not relevant:
            raise ValueError(f"eval query {qid} has no positive qrels")
        similarities = docs @ vector
        # Stable full ordering makes the otherwise-unspecified equal-score case deterministic.
        ranked_idx = np.argsort(-similarities, kind="mergesort")[: min(k, len(canonical_docs))]
        ranked = canonical_docs[ranked_idx].tolist()
        rows.append(ndcg_at_k(ranked, relevant, k=k))
    return np.asarray(rows, dtype=np.float64)


def aggregate_ndcg(scores: Sequence[float]) -> float:
    """Match the merged evaluator's ``statistics.fmean`` aggregation."""

    return float(statistics.fmean(float(value) for value in scores))


def assert_harness_parity(pack: Any, atol: float = 1e-12) -> Dict[str, float]:
    """Prove frozen per-query scores reproduce merged-harness aggregate NDCG."""

    references = dict(pack.metadata.get("harness_aggregate_ndcg", {}))
    if not references:
        raise AssertionError("pack has no harness aggregate references")
    differences: Dict[str, float] = {}
    document_matrices = {"floor": pack.Do, "oracle": pack.Dor}
    document_matrices.update(
        {
            name[len("method_documents__") :]: values
            for name, values in pack.extra_arrays.items()
            if name.startswith("method_documents__")
        }
    )
    for method, expected in references.items():
        if method not in pack.per_query_scores:
            raise AssertionError(f"pack is missing reference method {method}")
        if method not in document_matrices:
            raise AssertionError(f"pack is missing frozen document vectors for {method}")
        recomputed = per_query_ndcg(
            pack.Qd,
            pack.query_ids,
            document_matrices[method],
            pack.doc_ids,
            pack.qrels,
            k=int(pack.metadata.get("k", 10)),
        )
        stored = np.asarray(pack.per_query_scores[method], dtype=np.float64)
        per_query_difference = float(np.max(np.abs(recomputed - stored)))
        if per_query_difference > atol:
            raise AssertionError(
                f"{method} per-query pack/harness parity failed: max difference "
                f"{per_query_difference:.3g} > {atol:.3g}"
            )
        actual = aggregate_ndcg(recomputed)
        difference = abs(actual - float(expected))
        differences[method] = difference
        if difference > atol:
            raise AssertionError(
                f"{method} pack/harness parity failed: {actual:.17g} vs {float(expected):.17g} "
                f"(difference {difference:.3g} > {atol:.3g})"
            )
    return differences


def load_scifact_evalsplit(seed: int = 42) -> Dict[str, Any]:
    """Load the exact waypoint eval-split recipe and leakage-free curve pools."""

    corpus, queries, qrels = load_mteb_data("SciFact")
    if corpus is None or queries is None or qrels is None:
        raise RuntimeError("offline SciFact cache is unavailable")
    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
        corpus,
        queries,
        qrels,
        max_queries=100,
        sample_docs=1200,
        seed=seed,
    )
    anchor_ids, eval_ids = _split_anchor_eval(sliced_queries, 0.4, seed, active=True)
    eval_set = set(map(canonicalize_id, eval_ids))
    anchor_set = set(map(canonicalize_id, anchor_ids))
    if eval_set.intersection(anchor_set):
        raise AssertionError("merged split leaked anchor IDs into eval IDs")

    doc_ids = [canonicalize_id(value) for value in sliced_corpus]
    eval_query_ids = [
        canonicalize_id(value)
        for value in sliced_queries
        if sliced_qrels.get(value) and canonicalize_id(value) in eval_set
    ]
    canonical_anchor_ids = [
        canonicalize_id(value) for value in sliced_queries if canonicalize_id(value) in anchor_set
    ]
    query_by_id = {canonicalize_id(key): str(value) for key, value in queries.items()}
    corpus_by_id = {canonicalize_id(key): str(value) for key, value in corpus.items()}
    qrels_by_id = {
        canonicalize_id(query_id): {
            canonicalize_id(doc_id): float(score) for doc_id, score in rels.items()
        }
        for query_id, rels in qrels.items()
    }
    sliced_qrels_by_id = {
        canonicalize_id(query_id): {
            canonicalize_id(doc_id): float(score) for doc_id, score in rels.items()
        }
        for query_id, rels in sliced_qrels.items()
    }

    # Canonical C3a pool: every positive pair from the exact 40-query anchor split.
    selected_doc_set = set(doc_ids)
    canonical_pairs = []
    for query_id in canonical_anchor_ids:
        for doc_id, score in sorted(qrels_by_id.get(query_id, {}).items()):
            if float(score) > 0.0 and doc_id in selected_doc_set:
                canonical_pairs.append((query_id, doc_id))

    # Learning-curve query pool: one positive document per non-eval SciFact
    # query. This reaches 128 distinct query anchors without touching eval qrels.
    curve_pairs = []
    for query_id in sorted(query_by_id):
        if query_id in eval_set:
            continue
        positives = sorted(
            doc_id for doc_id, score in qrels_by_id.get(query_id, {}).items() if float(score) > 0.0
        )
        if positives:
            curve_pairs.append((query_id, positives[0]))
    if len(curve_pairs) < 128:
        raise RuntimeError(f"only {len(curve_pairs)} leakage-free C3a curve anchors; need 128")

    return {
        "corpus_texts": [str(sliced_corpus[key]) for key in sliced_corpus],
        "doc_ids": doc_ids,
        "eval_query_texts": [query_by_id[key] for key in eval_query_ids],
        "eval_query_ids": eval_query_ids,
        "eval_qrels": {key: sliced_qrels_by_id[key] for key in eval_query_ids},
        "anchor_ids": canonical_anchor_ids,
        "canonical_pair_query_ids": [item[0] for item in canonical_pairs],
        "canonical_pair_doc_ids": [item[1] for item in canonical_pairs],
        "canonical_pair_query_texts": [query_by_id[item[0]] for item in canonical_pairs],
        "curve_pair_query_ids": [item[0] for item in curve_pairs],
        "curve_pair_doc_ids": [item[1] for item in curve_pairs],
        "curve_pair_query_texts": [query_by_id[item[0]] for item in curve_pairs],
        "curve_pair_doc_texts": [corpus_by_id[item[1]] for item in curve_pairs],
        "all_query_count": len(queries),
    }


def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
    """Run both cached encoders once and return arrays ready for freezing."""

    from sentence_transformers import SentenceTransformer

    old = SentenceTransformer("sentence-transformers/all-MiniLM-L6-v2", device=device)
    Do = np.asarray(
        old.encode(case["corpus_texts"], batch_size=128, show_progress_bar=True), dtype=np.float64
    )
    curve_source = np.asarray(
        old.encode(case["curve_pair_doc_texts"], batch_size=128, show_progress_bar=True), dtype=np.float64
    )
    drift = QueryEncoderDrift(store_dim=Do.shape[1], swap_model_name="all-mpnet-base-v2", seed=seed)
    # One call fixes the projection; all later calls reuse exactly that instance.
    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
    canonical_target = np.asarray(
        drift.embed_queries(list(case["canonical_pair_query_texts"])), dtype=np.float64
    )
    curve_target = np.asarray(
        drift.embed_queries(list(case["curve_pair_query_texts"])), dtype=np.float64
    )
    doc_lookup = {doc_id: index for index, doc_id in enumerate(case["doc_ids"])}
    canonical_source = np.asarray(
        [Do[doc_lookup[doc_id]] for doc_id in case["canonical_pair_doc_ids"]], dtype=np.float64
    )
    return {
        "Do": Do,
        "Dor": Dor,
        "Qd": Qd,
        "canonical_source": canonical_source,
        "canonical_target": canonical_target,
        "curve_source": curve_source,
        "curve_target": curve_target,
        "drift_manifest": drift.manifest(),
        "old_model": "sentence-transformers/all-MiniLM-L6-v2",
        "swap_model": "all-mpnet-base-v2",
    }


def train_c3a_documents(
    all_documents: np.ndarray,
    anchor_documents: np.ndarray,
    anchor_queries: np.ndarray,
    seed: int,
    steps: int = 30,
    learning_rate: float = 0.01,
    min_correction: float = 0.01,
    max_correction: float = 0.5,
) -> np.ndarray:
    """Run the merged C3a bounded adapter and InfoNCE training path."""

    import torch

    torch.manual_seed(int(seed))
    adapter = create_adapter(
        "mlp",
        input_dim=int(all_documents.shape[1]),
        bounded=True,
        min_correction=float(min_correction),
        max_correction=float(max_correction),
    )
    source = torch.tensor(np.asarray(anchor_documents, dtype=np.float32), dtype=torch.float32)
    target = torch.tensor(np.asarray(anchor_queries, dtype=np.float32), dtype=torch.float32)
    loss_fn = SedimentationInfoNCELoss()
    optimizer = torch.optim.Adam(adapter.parameters(), lr=float(learning_rate))
    # Mirrors the merged harness, including the post-construction seed call.
    torch.manual_seed(int(seed))
    adapter.train()
    for _ in range(int(steps)):
        optimizer.zero_grad()
        loss = loss_fn(adapter(source), target)
        loss.backward()
        optimizer.step()
    adapter.eval()
    with torch.no_grad():
        output = adapter(torch.tensor(np.asarray(all_documents, dtype=np.float32), dtype=torch.float32))
    return np.asarray(output.detach().cpu().numpy(), dtype=np.float64)


def system_under_test_spec() -> Dict[str, Any]:
    """Return exact code-level constants used by the Section 3 renderer."""

    from config import ChelationConfig

    return {
        "bounded_defaults": {
            "min_correction": ChelationConfig.BOUNDED_ADAPTER_MIN_CORRECTION,
            "max_correction": ChelationConfig.BOUNDED_ADAPTER_MAX_CORRECTION,
            "int8_floor": 1.0 / 128.0,
        },
        "controller_defaults": {
            "initial_temperature": 0.0,
            "cooling_rate": 0.7,
            "trigger_threshold": 0.15,
            "max_temperature": 1.0,
            "epsilon": 1e-6,
        },
        "supervised_cycle_controller": {
            "cooling_rate": 0.5,
            "drift_signal": "max(0, baseline_ndcg - current_ndcg)",
        },
    }


 succeeded in 1206ms:
chelation_adapter.py:598:def create_adapter(adapter_type="mlp", input_dim=768, bounded=False,
research/drift_recovery\contracts.py:79:    def transform(self, vectors: np.ndarray) -> np.ndarray:
research/drift_recovery\harness_bridge.py:21:from chelation_adapter import create_adapter  # noqa: E402
research/drift_recovery\harness_bridge.py:79:            name[len("method_documents__") :]: values
research/drift_recovery\harness_bridge.py:81:            if name.startswith("method_documents__")
research/drift_recovery\harness_bridge.py:254:    adapter = create_adapter(
research/drift_recovery\d1\paper_stats.py:16:from research.drift_recovery.harness_bridge import (
research/drift_recovery\d1\paper_stats.py:39:METHODS = ("ridge", "procrustes", "mlp", "c3a")
research/drift_recovery\d1\paper_stats.py:40:LEAKAGE_METHODS = ("ridge", "procrustes", "mlp")
research/drift_recovery\d1\paper_stats.py:42:    "ridge": "ridge",
research/drift_recovery\d1\paper_stats.py:54:def _ridge(source: np.ndarray, target: np.ndarray, regularization: float = 1.0) -> np.ndarray:
research/drift_recovery\d1\paper_stats.py:154:    if "ridge" in methods:
research/drift_recovery\d1\paper_stats.py:155:        fitted["ridge"] = pack.Do @ _ridge(pack.Do[fit_idx], pack.Dor[fit_idx])
research/drift_recovery\d1\paper_stats.py:256:    ridge_target = 0.844
research/drift_recovery\d1\paper_stats.py:258:    ridge_error = abs(float(recovery["ridge"]) - ridge_target)
research/drift_recovery\d1\paper_stats.py:261:        "ridge_waypoint_reproduction": {
research/drift_recovery\d1\paper_stats.py:262:            "target_recovery": ridge_target,
research/drift_recovery\d1\paper_stats.py:263:            "actual_recovery": float(recovery["ridge"]),
research/drift_recovery\d1\paper_stats.py:264:            "absolute_error": ridge_error,
research/drift_recovery\d1\paper_stats.py:266:            "pass": ridge_error <= tolerance,
research/drift_recovery\d1\paper_stats.py:328:    print("[D1] fitting ridge and closed-form orthogonal Procrustes", flush=True)
research/drift_recovery\d1\paper_stats.py:329:    ridge_map = _ridge(Do[fit_idx], Dor[fit_idx], regularization=1.0)
research/drift_recovery\d1\paper_stats.py:331:    ridge_documents = Do @ ridge_map
research/drift_recovery\d1\paper_stats.py:333:    scores["ridge"] = _score_documents(fields, ridge_documents)
research/drift_recovery\d1\paper_stats.py:348:    waypoint_ridge = _score_documents(fields, Do @ _ridge(Do[waypoint_fit_idx], Dor[waypoint_fit_idx]))
research/drift_recovery\d1\paper_stats.py:358:        "ridge": (aggregate_ndcg(waypoint_ridge) - floor) / gap,
research/drift_recovery\d1\paper_stats.py:390:            "method_documents__ridge": ridge_documents,
research/drift_recovery\d1\paper_stats.py:391:            "method_documents__procrustes": procrustes_documents,
research/drift_recovery\d1\paper_stats.py:392:            "method_documents__mlp": mlp_documents,
research/drift_recovery\d1\paper_stats.py:393:            "method_documents__c3a": c3a_documents,
research/drift_recovery\d1\paper_stats.py:494:        "ridge_minus_mlp": paired_contrast(bootstrap, "ridge", "mlp"),
research/drift_recovery\d1\paper_stats.py:495:        "ridge_minus_c3a": paired_contrast(bootstrap, "ridge", "c3a"),
research/drift_recovery\d1\paper_stats.py:500:    primary_half_width = float(contrasts["ridge_minus_mlp"]["half_width"])
research/drift_recovery\d1\paper_stats.py:511:        "family": ["ridge_minus_mlp", "ridge_minus_c3a"],
research/drift_recovery\d1\paper_stats.py:515:            "primary_contrast": "ridge_minus_mlp",
research/drift_recovery\d1\paper_stats.py:554:            ridge_documents = pack.Do @ _ridge(pack.Do[indices], pack.Dor[indices])
research/drift_recovery\d1\paper_stats.py:555:            ridge_ndcg = float(np.mean(_score_documents(fields, ridge_documents)))
research/drift_recovery\d1\paper_stats.py:559:                    "method": "ridge",
research/drift_recovery\d1\paper_stats.py:562:                    "ndcg": ridge_ndcg,
research/drift_recovery\d1\paper_stats.py:563:                    "recovery": (ridge_ndcg - floor) / gap,
research/drift_recovery\d1\paper_stats.py:612:                "methods": ["ridge", "mlp"],
research/drift_recovery\d1\paper_stats.py:689:            f"ridge−MLP half-width is {g2['primary_delta_ndcg_half_width']:.4f} "
research/drift_recovery\d1\paper_stats.py:696:            f"reach the {g2['threshold']:.3f} ridge−MLP half-width. This is a scale estimate, "
research/drift_recovery\d1\paper_stats.py:706:            f"{audit['eval_positive_docs_in_leakage_safe_fit']}. The literal ridge headline "
research/drift_recovery\d1\paper_stats.py:707:            f"({100*leakage['methods']['ridge']['literal_waypoint']['recovery']:.1f}%) is "
research/drift_recovery\d1\paper_stats.py:863:    ridge = bootstrap["methods"]["ridge"]
research/drift_recovery\d1\paper_stats.py:866:    ridge_mlp = contrasts["contrasts"]["ridge_minus_mlp"]
research/drift_recovery\d1\paper_stats.py:867:    ridge_c3a = contrasts["contrasts"]["ridge_minus_c3a"]
research/drift_recovery\d1\paper_stats.py:870:    ridge_mlp_low = f"{ridge_mlp['ci_low']:.4f}".replace("-", "−")
research/drift_recovery\d1\paper_stats.py:871:    ridge_mlp_high = f"{ridge_mlp['ci_high']:.4f}"
research/drift_recovery\d1\paper_stats.py:872:    mlp_better_recovery_points = abs(ridge_mlp["ci_low"]) / ladder["oracle_gap"] * 100
research/drift_recovery\d1\paper_stats.py:873:    ridge_better_recovery_points = ridge_mlp["ci_high"] / ladder["oracle_gap"] * 100
research/drift_recovery\d1\paper_stats.py:884:            f"The {100*leakage['methods']['ridge']['literal_waypoint']['recovery']:.1f}% "
research/drift_recovery\d1\paper_stats.py:896:            "Paste this prohibition wherever the ridge−MLP result or plateau interpretation is discussed:",
research/drift_recovery\d1\paper_stats.py:898:            f"> Do not interpret ridge−MLP non-significance as evidence of a linear post-hoc ceiling or the "
research/drift_recovery\d1\paper_stats.py:899:            f"absence of nonlinear benefit. The 95% CI on ΔNDCG(ridge−MLP) is "
research/drift_recovery\d1\paper_stats.py:900:            f"[{ridge_mlp_low}, {ridge_mlp_high}] — it permits MLP better by up to "
research/drift_recovery\d1\paper_stats.py:901:            f"~{mlp_better_recovery_points:.0f} recovery points and ridge better by up to "
research/drift_recovery\d1\paper_stats.py:902:            f"~{ridge_better_recovery_points:.0f}. G2 FAILED (primary ridge−MLP half-width "
research/drift_recovery\d1\paper_stats.py:913:            f"- No **{100*ridge['recovery']['estimate']:.0f}% is the recoverable upper bound**.",
research/drift_recovery\d1\paper_stats.py:914:            f"- No **irreducible {100*(1.0-ridge['recovery']['estimate']):.0f}%**.",
research/drift_recovery\d1\paper_stats.py:932:            f"| Ridge | NDCG {ridge['ndcg']['estimate']:.4f} (95% CI {ridge['ndcg']['ci_low']:.4f}–{ridge['ndcg']['ci_high']:.4f}); recovery {100*ridge['recovery']['estimate']:.1f}% (95% CI {100*ridge['recovery']['ci_low']:.1f}%–{100*ridge['recovery']['ci_high']:.1f}%) |",
research/drift_recovery\d1\paper_stats.py:935:            f"| Ridge − MLP | ΔNDCG {ridge_mlp['estimate']:.4f} (95% CI {ridge_mlp['ci_low']:.4f}–{ridge_mlp['ci_high']:.4f}); Holm-adjusted p={ridge_mlp['multiple_testing']['holm_adjusted_p']:.6g} |",
research/drift_recovery\d1\paper_stats.py:936:            f"| Ridge − C3a | ΔNDCG {ridge_c3a['estimate']:.4f} (95% CI {ridge_c3a['ci_low']:.4f}–{ridge_c3a['ci_high']:.4f}); Holm-adjusted p={ridge_c3a['multiple_testing']['holm_adjusted_p']:.6g} |",
research/drift_recovery\d1\paper_stats.py:940:            "- Abstract and §1: replace the point-only 84%/20% comparison with ridge and C3a recovery plus their CIs; describe ridge as the best observed plateau under this protocol.",
research/drift_recovery\d1\paper_stats.py:941:            "- §4.2: define recovery exactly as already written, but replace the quoted `~85% linear ceiling` interpretation with the observed ridge plateau and its CI.",
research/drift_recovery\d1\paper_stats.py:942:            "- §5.5 table/caption/body: replace all ridge, residual MLP, closed-form orthogonal Procrustes, and C3a entries from `ladder.json`; add the paired contrast CIs and Holm results. State that the ladder's closed-form SVD map is distinct from the trainable Cayley adapter, and that C3a was rescored per query through the merged NDCG implementation.",
research/drift_recovery\d1\paper_stats.py:948:            f"G2 is **{'passed' if g2['pass'] else 'failed'}**. The primary ridge−MLP "
research/drift_recovery\d1\paper_stats.py:1014:        f"Ban plus forbidden/permitted lists emitted; ridge−MLP CI "
research/drift_recovery\d1\paper_stats.py:1015:        f"[{contrasts['contrasts']['ridge_minus_mlp']['ci_low']:.4f}, "
research/drift_recovery\d1\paper_stats.py:1016:        f"{contrasts['contrasts']['ridge_minus_mlp']['ci_high']:.4f}]. |",
research/drift_recovery\d1\paper_stats.py:1030:        f"{location(ci_path, 'preregistered primary')} | Primary ridge−MLP half-width "
research/drift_recovery\d1\paper_stats.py:1044:        "half-width, while F6 requires ridge−MLP alone to be primary. The generated chair block "
research/drift_recovery\tests\test_d1_stats.py:10:from research.drift_recovery.harness_bridge import assert_harness_parity
research/drift_recovery\tests\test_d1_stats.py:35:            "ridge": np.array([0.4, 0.5, 0.6]),
research/drift_recovery\tests\test_d1_stats.py:43:            ["ridge", "mlp"],
research/drift_recovery\tests\test_d1_stats.py:124:    def test_leakage_safe_ridge_recovery_is_below_literal_fit(self) -> None:
research/drift_recovery\tests\test_d1_stats.py:127:            methods=("ridge",),
research/drift_recovery\tests\test_d1_stats.py:130:        ridge = sensitivity["methods"]["ridge"]
research/drift_recovery\tests\test_d1_stats.py:133:            ridge["leakage_safe_full_600"]["recovery"],
research/drift_recovery\tests\test_d1_stats.py:134:            ridge["literal_waypoint"]["recovery"],

 succeeded in 1291ms:
"""Build and analyze the frozen SciFact D1 embedding pack."""

from __future__ import annotations

import hashlib
import json
import math
import os
import subprocess
from pathlib import Path
from typing import Any, Dict, Mapping, Optional, Sequence

import numpy as np

from research.drift_recovery.artifacts import EmbeddingPack
from research.drift_recovery.harness_bridge import (
    aggregate_ndcg,
    assert_harness_parity,
    encode_scifact_case,
    load_scifact_evalsplit,
    per_query_ndcg,
    system_under_test_spec,
    train_c3a_documents,
)
from research.drift_recovery.stats.learning_curve import (
    COUNTS,
    ORDER_SEEDS,
    nested_prefixes,
    summarize_learning_curve,
)
from research.drift_recovery.stats.multiple_testing import holm_adjust
from research.drift_recovery.stats.paired_bootstrap import (
    paired_contrast,
    paired_query_bootstrap,
    strip_draws,
)


METHODS = ("ridge", "procrustes", "mlp", "c3a")
LEAKAGE_METHODS = ("ridge", "procrustes", "mlp")
METHOD_LABELS = {
    "ridge": "ridge",
    "procrustes": "closed-form orthogonal Procrustes",
    "mlp": "residual MLP",
    "c3a": "C3a",
}


def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")


def _ridge(source: np.ndarray, target: np.ndarray, regularization: float = 1.0) -> np.ndarray:
    dimension = source.shape[1]
    return np.linalg.solve(
        source.T @ source + float(regularization) * np.eye(dimension), source.T @ target
    )


def _procrustes(source: np.ndarray, target: np.ndarray) -> np.ndarray:
    left, _singular, right_t = np.linalg.svd(source.T @ target, full_matrices=False)
    return left @ right_t


def _train_residual_mlp(
    all_documents: np.ndarray,
    source: np.ndarray,
    target: np.ndarray,
    seed: int,
    steps: int = 400,
    device: str = "cuda",
) -> np.ndarray:
    import torch
    import torch.nn as nn

    torch.manual_seed(int(seed))
    if torch.cuda.is_available():
        torch.cuda.manual_seed_all(int(seed))
    target_device = torch.device(device if device == "cuda" and torch.cuda.is_available() else "cpu")
    dimension = int(all_documents.shape[1])
    model = nn.Sequential(nn.Linear(dimension, 1024), nn.GELU(), nn.Linear(1024, dimension)).to(target_device)
    optimizer = torch.optim.Adam(model.parameters(), lr=1e-3, weight_decay=1e-5)
    source_tensor = torch.tensor(source, dtype=torch.float32, device=target_device)
    target_tensor = torch.tensor(target, dtype=torch.float32, device=target_device)
    model.train()
    for _ in range(int(steps)):
        optimizer.zero_grad()
        prediction = source_tensor + model(source_tensor)
        loss = (1.0 - torch.nn.functional.cosine_similarity(prediction, target_tensor, dim=1)).mean()
        loss.backward()
        optimizer.step()
    model.eval()
    with torch.no_grad():
        documents = torch.tensor(all_documents, dtype=torch.float32, device=target_device)
        corrected = documents + model(documents)
    return np.asarray(corrected.detach().cpu().numpy(), dtype=np.float64)


def _score_documents(pack_fields: Mapping[str, Any], documents: np.ndarray) -> np.ndarray:
    return per_query_ndcg(
        pack_fields["Qd"],
        pack_fields["query_ids"],
        documents,
        pack_fields["doc_ids"],
        pack_fields["qrels"],
        k=10,
    )


def audit_fit_leakage(pack: EmbeddingPack) -> dict:
    """Recompute literal and leakage-safe fit contamination from frozen qrels."""

    eval_positive_docs = {
        str(doc_id)
        for query_id in pack.query_ids
        for doc_id, relevance in pack.qrels[str(query_id)].items()
        if float(relevance) > 0.0
    }
    doc_ids = np.asarray(pack.doc_ids, dtype=str)
    literal_fit_idx = np.asarray(pack.fit_idx, dtype=np.int64)
    safe_fit_idx = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
    literal_positive_count = int(
        sum(str(doc_ids[index]) in eval_positive_docs for index in literal_fit_idx)
    )
    safe_positive_count = int(
        sum(str(doc_ids[index]) in eval_positive_docs for index in safe_fit_idx)
    )
    total_positive_count = len(eval_positive_docs)
    if total_positive_count == 0:
        raise RuntimeError("frozen eval qrels contain no positive documents")
    return {
        "eval_positive_docs_total": total_positive_count,
        "literal_fit_count": len(literal_fit_idx),
        "eval_positive_docs_in_literal_fit": literal_positive_count,
        "literal_fit_share_of_eval_positive_docs": (
            literal_positive_count / total_positive_count
        ),
        "leakage_safe_fit_count": len(safe_fit_idx),
        "eval_positive_docs_in_leakage_safe_fit": safe_positive_count,
    }


def _fit_ladder_methods(
    pack: EmbeddingPack,
    fit_idx: np.ndarray,
    methods: Sequence[str],
    device: str,
) -> Dict[str, np.ndarray]:
    unsupported = set(methods).difference(LEAKAGE_METHODS)
    if unsupported:
        raise ValueError(f"unsupported leakage-sensitivity methods: {sorted(unsupported)}")
    fitted: Dict[str, np.ndarray] = {}
    if "ridge" in methods:
        fitted["ridge"] = pack.Do @ _ridge(pack.Do[fit_idx], pack.Dor[fit_idx])
    if "procrustes" in methods:
        fitted["procrustes"] = pack.Do @ _procrustes(
            pack.Do[fit_idx], pack.Dor[fit_idx]
        )
    if "mlp" in methods:
        fitted["mlp"] = _train_residual_mlp(
            pack.Do,
            pack.Do[fit_idx],
            pack.Dor[fit_idx],
            seed=int(pack.metadata["seed"]),
            device=device,
        )
    return fitted


def compute_leakage_sensitivity(
    pack: EmbeddingPack,
    methods: Sequence[str] = LEAKAGE_METHODS,
    device: str = "cuda",
) -> dict:
    """Refit literal and safe full-600 methods from the frozen embedding pack."""

    selected_methods = tuple(methods)
    audit = audit_fit_leakage(pack)
    if audit["eval_positive_docs_in_leakage_safe_fit"] != 0:
        raise RuntimeError("leakage-safe fit contains eval-positive documents")
    literal_fit_idx = np.asarray(pack.fit_idx, dtype=np.int64)
    safe_fit_idx = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
    fields = {
        "Qd": pack.Qd,
        "query_ids": pack.query_ids,
        "doc_ids": pack.doc_ids,
        "qrels": pack.qrels,
    }
    floor = float(np.mean(pack.per_query_scores["floor"]))
    oracle = float(np.mean(pack.per_query_scores["oracle"]))
    gap = oracle - floor
    fitted = {
        "literal_waypoint": _fit_ladder_methods(
            pack, literal_fit_idx, selected_methods, device
        ),
        "leakage_safe_full_600": _fit_ladder_methods(
            pack, safe_fit_idx, selected_methods, device
        ),
    }
    score_rows: Dict[str, Dict[str, Any]] = {}
    for method in selected_methods:
        literal_scores = _score_documents(fields, fitted["literal_waypoint"][method])
        safe_scores = _score_documents(fields, fitted["leakage_safe_full_600"][method])
        frozen_delta = float(
            np.max(np.abs(literal_scores - np.asarray(pack.per_query_scores[method])))
        )
        if frozen_delta > 1e-12:
            raise RuntimeError(
                f"{method} literal refit differs from frozen headline scores by {frozen_delta:.3g}"
            )
        literal_ndcg = float(np.mean(literal_scores))
        safe_ndcg = float(np.mean(safe_scores))
        literal_recovery = (literal_ndcg - floor) / gap
        safe_recovery = (safe_ndcg - floor) / gap
        score_rows[method] = {
            "label": METHOD_LABELS[method],
            "literal_waypoint": {
                "ndcg": literal_ndcg,
                "recovery": literal_recovery,
                "eval_positive_docs_in_fit": audit[
                    "eval_positive_docs_in_literal_fit"
                ],
            },
            "leakage_safe_full_600": {
                "ndcg": safe_ndcg,
                "recovery": safe_recovery,
                "eval_positive_docs_in_fit": audit[
                    "eval_positive_docs_in_leakage_safe_fit"
                ],
            },
            "inflation_recovery_pp": 100.0 * (literal_recovery - safe_recovery),
            "literal_frozen_score_max_abs_delta": frozen_delta,
        }
    return {
        "record_type": "d1_leakage_sensitivity",
        "audit": audit,
        "fits": {
            "literal_waypoint": "literal seed-42 half-corpus permutation",
            "leakage_safe_full_600": (
                "first 600 seed-42 permutation documents after excluding every "
                "eval-positive document"
            ),
        },
        "floor_ndcg": floor,
        "oracle_ndcg": oracle,
        "oracle_gap": gap,
        "methods": score_rows,
    }


def _canonical_reference_audit(recovery: Mapping[str, float]) -> dict:
    """Separate the waypoint reproduction gate from the superseded C3a scalar."""

    tolerance = 0.001
    ridge_target = 0.844
    c3a_old_cross_seed_reference = 0.20
    ridge_error = abs(float(recovery["ridge"]) - ridge_target)
    c3a_difference = abs(float(recovery["c3a"]) - c3a_old_cross_seed_reference)
    return {
        "ridge_waypoint_reproduction": {
            "target_recovery": ridge_target,
            "actual_recovery": float(recovery["ridge"]),
            "absolute_error": ridge_error,
            "tolerance": tolerance,
            "pass": ridge_error <= tolerance,
        },
        "c3a_cross_seed_reference": {
            "old_cross_seed_mean_recovery": c3a_old_cross_seed_reference,
            "seed_42_per_query_recovery": float(recovery["c3a"]),
            "absolute_difference": c3a_difference,
            "tolerance": tolerance,
            "disposition": "superseded_not_a_tolerance_gate",
            "finding": (
                "The old reference was a cross-seed campaign mean. The seed-42 "
                "per-query value supersedes it and is outside the nominal tolerance."
            ),
        },
    }


def build_embedding_pack(output_prefix: Path, seed: int = 42, device: str = "cuda") -> EmbeddingPack:
    """Encode once, fit the canonical methods, score every eval query, and freeze."""

    print("[D1] loading exact SciFact eval split", flush=True)
    case = load_scifact_evalsplit(seed=seed)
    print("[D1] encoding frozen arrays with cached MiniLM/mpnet models", flush=True)
    encoded = encode_scifact_case(case, seed=seed, device=device)
    Do = encoded["Do"]
    Dor = encoded["Dor"]
    Qd = encoded["Qd"]
    doc_ids = np.asarray(case["doc_ids"], dtype=str)
    query_ids = np.asarray(case["eval_query_ids"], dtype=str)

    waypoint_order = np.random.default_rng(seed).permutation(len(doc_ids))
    waypoint_fit_idx = waypoint_order[: len(doc_ids) // 2]
    eval_positive_docs = {
        doc_id
        for query_id in query_ids
        for doc_id, relevance in case["eval_qrels"][str(query_id)].items()
        if float(relevance) > 0.0
    }
    leakage_safe_fit_idx = np.asarray(
        [index for index in waypoint_order if doc_ids[index] not in eval_positive_docs][
            : len(doc_ids) // 2
        ],
        dtype=np.int64,
    )
    if len(leakage_safe_fit_idx) != len(doc_ids) // 2:
        raise RuntimeError("not enough leakage-safe document pairs")
    # The frozen canonical ladder uses the literal waypoint recipe. The separate
    # leakage-safe index is used for all new learning-curve fits and retained so
    # the recipe/contract conflict is explicit rather than silently rewritten.
    fit_idx = waypoint_fit_idx

    fields = {
        "Do": Do,
        "Dor": Dor,
        "Qd": Qd,
        "doc_ids": doc_ids,
        "query_ids": query_ids,
        "qrels": case["eval_qrels"],
    }
    scores: Dict[str, np.ndarray] = {
        "floor": _score_documents(fields, Do),
        "oracle": _score_documents(fields, Dor),
    }
    print("[D1] fitting ridge and closed-form orthogonal Procrustes", flush=True)
    ridge_map = _ridge(Do[fit_idx], Dor[fit_idx], regularization=1.0)
    procrustes_map = _procrustes(Do[fit_idx], Dor[fit_idx])
    ridge_documents = Do @ ridge_map
    procrustes_documents = Do @ procrustes_map
    scores["ridge"] = _score_documents(fields, ridge_documents)
    scores["procrustes"] = _score_documents(fields, procrustes_documents)
    print("[D1] fitting canonical residual MLP", flush=True)
    mlp_documents = _train_residual_mlp(Do, Do[fit_idx], Dor[fit_idx], seed=seed, device=device)
    scores["mlp"] = _score_documents(fields, mlp_documents)
    print("[D1] fitting canonical C3a through merged bounded-InfoNCE path", flush=True)
    c3a_documents = train_c3a_documents(
        Do,
        encoded["canonical_source"],
        encoded["canonical_target"],
        seed=seed,
    )
    scores["c3a"] = _score_documents(fields, c3a_documents)

    # Audited waypoint values use the literal unfiltered half-document permutation.
    waypoint_ridge = _score_documents(fields, Do @ _ridge(Do[waypoint_fit_idx], Dor[waypoint_fit_idx]))
    waypoint_procrustes = _score_documents(
        fields, Do @ _procrustes(Do[waypoint_fit_idx], Dor[waypoint_fit_idx])
    )
    aggregate_reference = {name: aggregate_ndcg(values) for name, values in scores.items()}
    floor = aggregate_reference["floor"]
    oracle = aggregate_reference["oracle"]
    gap = oracle - floor
    recovery = {name: (aggregate_reference[name] - floor) / gap for name in METHODS}
    waypoint_recovery = {
        "ridge": (aggregate_ndcg(waypoint_ridge) - floor) / gap,
        "procrustes": (aggregate_ndcg(waypoint_procrustes) - floor) / gap,
    }
    canonical_reference_audit = _canonical_reference_audit(recovery)
    old_scalar_audit = {
        "old_c3a_scalar_ndcg": 0.1609,
        "fixed_split_per_query_ndcg": aggregate_reference["c3a"],
        "ndcg_difference": aggregate_reference["c3a"] - 0.1609,
        "old_scalar_implied_recovery": (0.1609 - floor) / gap,
        "fixed_split_per_query_recovery": recovery["c3a"],
        "finding": (
            "The old scalar was a cross-seed campaign mean attached to the seed-42 eval split. "
            "The frozen per-query seed-42 result supersedes it."
        ),
    }

    pack = EmbeddingPack(
        Do=Do,
        Dor=Dor,
        Qd=Qd,
        doc_ids=doc_ids,
        query_ids=query_ids,
        qrels=case["eval_qrels"],
        fit_idx=fit_idx,
        per_query_scores=scores,
        extra_arrays={
            "leakage_safe_fit_idx": leakage_safe_fit_idx,
            "canonical_c3a_source": encoded["canonical_source"],
            "canonical_c3a_target": encoded["canonical_target"],
            "curve_c3a_source": encoded["curve_source"],
            "curve_c3a_target": encoded["curve_target"],
            "curve_c3a_query_ids": np.asarray(case["curve_pair_query_ids"], dtype=str),
            "method_documents__ridge": ridge_documents,
            "method_documents__procrustes": procrustes_documents,
            "method_documents__mlp": mlp_documents,
            "method_documents__c3a": c3a_documents,
        },
        metadata={
            "dataset": "SciFact",
            "seed": seed,
            "max_queries": 100,
            "sample_docs": 1200,
            "anchor_fraction": 0.4,
            "k": 10,
            "ndcg": "binary merged-harness ndcg_at_k",
            "anchor_ids": case["anchor_ids"],
            "canonical_c3a_pair_count": len(encoded["canonical_source"]),
            "learning_curve_c3a_pool": (
                "one positive doc per non-eval SciFact query; eval IDs and eval qrels excluded"
            ),
            "drift_manifest": encoded["drift_manifest"],
            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
            "harness_aggregate_ndcg": aggregate_reference,
            "recovery": recovery,
            "canonical_reference_audit": canonical_reference_audit,
            "old_scalar_audit": old_scalar_audit,
            "waypoint_audit": {
                "eval_positive_docs_total": len(eval_positive_docs),
                "literal_fit_count": len(waypoint_fit_idx),
                "eval_positive_docs_in_literal_fit": int(
                    sum(doc_ids[index] in eval_positive_docs for index in waypoint_fit_idx)
                ),
                "literal_fit_share_of_eval_positive_docs": (
                    sum(doc_ids[index] in eval_positive_docs for index in waypoint_fit_idx)
                    / len(eval_positive_docs)
                ),
                "leakage_safe_fit_count": len(leakage_safe_fit_idx),
                "eval_positive_docs_in_leakage_safe_fit": int(
                    sum(doc_ids[index] in eval_positive_docs for index in leakage_safe_fit_idx)
                ),
                "literal_recovery": waypoint_recovery,
                "protocol_conflict": (
                    "The exact waypoint permutation contains eval-positive documents. "
                    "Canonical ladder/bootstrap reproduction uses it; new learning-curve fits use "
                    "the separate leakage_safe_fit_idx."
                ),
            },
        },
    )
    pack.save(output_prefix)
    reloaded = EmbeddingPack.load(output_prefix)
    assert_harness_parity(reloaded, atol=1e-12)
    return reloaded


def ladder_table(
    pack: EmbeddingPack,
    leakage_sensitivity: Optional[Mapping[str, Any]] = None,
) -> dict:
    floor = float(np.mean(pack.per_query_scores["floor"]))
    oracle = float(np.mean(pack.per_query_scores["oracle"]))
    gap = oracle - floor
    methods = {}
    for method in METHODS:
        ndcg = float(np.mean(pack.per_query_scores[method]))
        methods[method] = {
            "ndcg": ndcg,
            "delta_ndcg": ndcg - floor,
            "recovery": (ndcg - floor) / gap,
        }
    waypoint_audit = audit_fit_leakage(pack)
    waypoint_audit.update(
        {
            "literal_recovery": {
                method: methods[method]["recovery"]
                for method in LEAKAGE_METHODS
            },
            "protocol_conflict": (
                "The literal waypoint fit contains eval-positive documents. "
                "It is retained only as the reproduced headline protocol; the "
                "leakage-safe full-600 sensitivity excludes all eval-positive documents."
            ),
        }
    )
    result = {
        "record_type": "d1_ladder",
        "dataset": "SciFact",
        "query_count": len(pack.query_ids),
        "floor_ndcg": floor,
        "oracle_ndcg": oracle,
        "oracle_gap": gap,
        "methods": methods,
        "canonical_reference_audit": _canonical_reference_audit(
            {method: row["recovery"] for method, row in methods.items()}
        ),
        "old_scalar_audit": pack.metadata["old_scalar_audit"],
        "waypoint_audit": waypoint_audit,
    }
    if leakage_sensitivity is not None:
        result["leakage_sensitivity"] = leakage_sensitivity
    return result


def run_bootstrap(pack: EmbeddingPack) -> tuple[dict, dict]:
    bootstrap = paired_query_bootstrap(pack.per_query_scores, METHODS, draws=10_000, seed=20260630)
    contrasts = {
        "ridge_minus_mlp": paired_contrast(bootstrap, "ridge", "mlp"),
        "ridge_minus_c3a": paired_contrast(bootstrap, "ridge", "c3a"),
    }
    holm = holm_adjust({name: row["p_value"] for name, row in contrasts.items()})
    for name, row in contrasts.items():
        row["multiple_testing"] = holm[name]
    primary_half_width = float(contrasts["ridge_minus_mlp"]["half_width"])
    secondary_median_half_width = float(
        np.median([row["half_width"] for row in contrasts.values()])
    )
    threshold = 0.015
    query_count = int(bootstrap["query_count"])
    rough_query_count = int(
        math.ceil(query_count * (primary_half_width / threshold) ** 2)
    )
    contrast_table = {
        "record_type": "d1_preregistered_contrasts",
        "family": ["ridge_minus_mlp", "ridge_minus_c3a"],
        "correction": "Holm",
        "contrasts": contrasts,
        "g2": {
            "primary_contrast": "ridge_minus_mlp",
            "primary_delta_ndcg_half_width": primary_half_width,
            "secondary_median_family_delta_ndcg_half_width": (
                secondary_median_half_width
            ),
            "threshold": threshold,
            "pass": bool(primary_half_width <= threshold),
            "rough_query_count_for_threshold": rough_query_count,
            "scale_up_note": (
                "Rough 1/sqrt(n) extrapolation only; it is not a prospective "
                "power calculation and must be re-estimated with pilot variance."
            ),
        },
    }
    return strip_draws(bootstrap), contrast_table


def run_learning_curves(pack: EmbeddingPack, device: str = "cuda") -> dict:
    """Run separate doc-pair and query-InfoNCE nested-prefix panels."""

    rows = []
    safe_fit_idx = pack.extra_arrays["leakage_safe_fit_idx"]
    doc_prefixes = nested_prefixes(len(safe_fit_idx), COUNTS, ORDER_SEEDS)
    c3a_pool_size = len(pack.extra_arrays["curve_c3a_source"])
    c3a_prefixes = nested_prefixes(c3a_pool_size, COUNTS, ORDER_SEEDS)
    floor = float(np.mean(pack.per_query_scores["floor"]))
    oracle = float(np.mean(pack.per_query_scores["oracle"]))
    gap = oracle - floor
    fields = {
        "Qd": pack.Qd,
        "query_ids": pack.query_ids,
        "doc_ids": pack.doc_ids,
        "qrels": pack.qrels,
    }
    for order_seed in ORDER_SEEDS:
        print(f"[D1] learning curve order seed {order_seed}", flush=True)
        for count in COUNTS:
            local = doc_prefixes[order_seed][count]
            indices = safe_fit_idx[local]
            ridge_documents = pack.Do @ _ridge(pack.Do[indices], pack.Dor[indices])
            ridge_ndcg = float(np.mean(_score_documents(fields, ridge_documents)))
            rows.append(
                {
                    "supervision_type": "paired_document_embeddings",
                    "method": "ridge",
                    "supervision_count": count,
                    "order_seed": order_seed,
                    "ndcg": ridge_ndcg,
                    "recovery": (ridge_ndcg - floor) / gap,
                    "prefix_sha256": hashlib.sha256(indices.tobytes()).hexdigest(),
                }
            )
            mlp_documents = _train_residual_mlp(
                pack.Do,
                pack.Do[indices],
                pack.Dor[indices],
                seed=order_seed,
                device=device,
            )
            mlp_ndcg = float(np.mean(_score_documents(fields, mlp_documents)))
            rows.append(
                {
                    "supervision_type": "paired_document_embeddings",
                    "method": "mlp",
                    "supervision_count": count,
                    "order_seed": order_seed,
                    "ndcg": mlp_ndcg,
                    "recovery": (mlp_ndcg - floor) / gap,
                    "prefix_sha256": hashlib.sha256(indices.tobytes()).hexdigest(),
                }
            )
            c3a_indices = c3a_prefixes[order_seed][count]
            c3a_documents = train_c3a_documents(
                pack.Do,
                pack.extra_arrays["curve_c3a_source"][c3a_indices],
                pack.extra_arrays["curve_c3a_target"][c3a_indices],
                seed=order_seed,
            )
            c3a_ndcg = float(np.mean(_score_documents(fields, c3a_documents)))
            rows.append(
                {
                    "supervision_type": "query_relevance_infonce",
                    "method": "c3a",
                    "supervision_count": count,
                    "order_seed": order_seed,
                    "ndcg": c3a_ndcg,
                    "recovery": (c3a_ndcg - floor) / gap,
                    "prefix_sha256": hashlib.sha256(c3a_indices.tobytes()).hexdigest(),
                }
            )
    return {
        "record_type": "d1_learning_curve",
        "counts": list(COUNTS),
        "order_seeds": list(ORDER_SEEDS),
        "eval_query_ids": pack.query_ids.tolist(),
        "panels": {
            "paired_document_embeddings": {
                "methods": ["ridge", "mlp"],
                "count_unit": "paired old/new document embeddings",
                "pool_size": len(safe_fit_idx),
            },
            "query_relevance_infonce": {
                "methods": ["c3a"],
                "count_unit": "distinct non-eval query anchors, one positive document each",
                "pool_size": c3a_pool_size,
            },
        },
        "warning": "Panels use different supervision types and counts are not commensurate.",
        "rows": rows,
        "summary": summarize_learning_curve(rows),
    }


def _leakage_table_lines(leakage: Mapping[str, Any]) -> list[str]:
    lines = [
        "| Method | Literal waypoint recovery | Leakage-safe full-600 recovery | Inflation (pp) |",
        "|---|---:|---:|---:|",
    ]
    for method in LEAKAGE_METHODS:
        row = leakage["methods"][method]
        lines.append(
            f"| {row['label']} | {100*row['literal_waypoint']['recovery']:.1f}% "
            f"| {100*row['leakage_safe_full_600']['recovery']:.1f}% "
            f"| +{row['inflation_recovery_pp']:.1f} |"
        )
    return lines


def render_ci_report(
    ladder: Mapping[str, Any],
    bootstrap: Mapping[str, Any],
    contrasts: Mapping[str, Any],
    leakage: Mapping[str, Any],
) -> str:
    lines = [
        "# D1 paired-query bootstrap results",
        "",
        f"SciFact eval split: {bootstrap['query_count']} fixed queries; {bootstrap['draws']:,} deterministic paired draws. ",
        "Recovery is the ratio of resampled query means. All methods, floor, and oracle use the same draws.",
        "",
        f"Floor NDCG@10 = {ladder['floor_ndcg']:.4f}; oracle = {ladder['oracle_ndcg']:.4f}; gap = {ladder['oracle_gap']:.4f}.",
        "",
        "| Method | NDCG@10 (95% CI) | ΔNDCG vs floor (95% CI) | Recovery (95% CI) |",
        "|---|---:|---:|---:|",
    ]
    for method in METHODS:
        row = bootstrap["methods"][method]
        lines.append(
            f"| {METHOD_LABELS[method]} | {row['ndcg']['estimate']:.4f} [{row['ndcg']['ci_low']:.4f}, {row['ndcg']['ci_high']:.4f}] "
            f"| {row['delta_ndcg']['estimate']:.4f} [{row['delta_ndcg']['ci_low']:.4f}, {row['delta_ndcg']['ci_high']:.4f}] "
            f"| {100*row['recovery']['estimate']:.1f}% [{100*row['recovery']['ci_low']:.1f}%, {100*row['recovery']['ci_high']:.1f}%] |"
        )
    lines.extend(
        [
            "",
            "## Preregistered paired contrasts",
            "",
            "| Contrast | ΔNDCG (95% CI) | raw p | Holm p | Reject at 0.05 |",
            "|---|---:|---:|---:|:---:|",
        ]
    )
    for name, row in contrasts["contrasts"].items():
        mt = row["multiple_testing"]
        lines.append(
            f"| {name.replace('_', ' ')} | {row['estimate']:.4f} [{row['ci_low']:.4f}, {row['ci_high']:.4f}] "
            f"| {row['p_value']:.6g} | {mt['holm_adjusted_p']:.6g} | {'yes' if mt['reject'] else 'no'} |"
        )
    g2 = contrasts["g2"]
    audit = leakage["audit"]
    c3a_reference = ladder["canonical_reference_audit"]["c3a_cross_seed_reference"]
    lines.extend(
        [
            "",
            f"G2 power gate: **{'PASS' if g2['pass'] else 'FAIL'}**. The preregistered primary "
            f"ridge−MLP half-width is {g2['primary_delta_ndcg_half_width']:.4f} "
            f"(threshold ≤ {g2['threshold']:.3f}). The median across both preregistered "
            f"contrasts is a secondary diagnostic at "
            f"{g2['secondary_median_family_delta_ndcg_half_width']:.4f}.",
            "",
            f"A rough 1/√n extrapolation from {bootstrap['query_count']} queries suggests "
            f"approximately {g2['rough_query_count_for_threshold']} queries (about 800+) to "
            f"reach the {g2['threshold']:.3f} ridge−MLP half-width. This is a scale estimate, "
            "not a prospective power calculation; variance must be re-estimated in a pilot.",
            "",
            "## Leakage sensitivity of the literal waypoint headline",
            "",
            f"The literal 600-document fit contains "
            f"{audit['eval_positive_docs_in_literal_fit']} of the "
            f"{audit['eval_positive_docs_total']} eval-positive documents "
            f"({100*audit['literal_fit_share_of_eval_positive_docs']:.1f}%). The "
            f"leakage-safe full-{audit['leakage_safe_fit_count']} fit contains "
            f"{audit['eval_positive_docs_in_leakage_safe_fit']}. The literal ridge headline "
            f"({100*leakage['methods']['ridge']['literal_waypoint']['recovery']:.1f}%) is "
            "therefore a leaky-fit point estimate, not a clean held-out estimate.",
            "",
            *_leakage_table_lines(leakage),
            "",
            "The primary ladder/bootstrap reproduces the literal waypoint recipe. The "
            "full-600 sensitivity and all learning-curve document fits exclude eval-positive "
            "documents; the two protocols must not be conflated.",
            "",
            f"C3a scalar audit: the old 0.1609 scalar implied "
            f"{100*ladder['old_scalar_audit']['old_scalar_implied_recovery']:.1f}% recovery on this split, but "
            f"the actual frozen per-query score is {ladder['old_scalar_audit']['fixed_split_per_query_ndcg']:.4f} "
            f"({100*ladder['old_scalar_audit']['fixed_split_per_query_recovery']:.1f}%).",
            f"This is a supersession, not a tolerance pass: the recovery difference from "
            f"the old cross-seed 0.200 reference is {c3a_reference['absolute_difference']:.4f}, "
            f"which exceeds the nominal {c3a_reference['tolerance']:.3f} threshold.",
        ]
    )
    return "\n".join(lines) + "\n"


def render_section3() -> str:
    spec = system_under_test_spec()
    bounded = spec["bounded_defaults"]
    controller = spec["controller_defaults"]
    supervised = spec["supervised_cycle_controller"]
    return rf"""# 3. System Under Test

We evaluate a post-hoc correction layer placed between an embedding encoder and cosine retrieval. Let
$x\in\mathbb{{R}}^d$ be an encoder output and $n(x)=x/\lVert x\rVert_2$ (with the implementation's
usual nonzero-vector assumption). Every adapter returns an L2-normalized vector, so retrieval ranks
documents by the cosine-equivalent dot product between normalized query and document vectors.

## 3.1 Adapter forward maps

The residual MLP used by the supervised loop is

$$g_\theta(x)=n\!\left(x+W_2\,\mathrm{{ReLU}}(W_1x+b_1)+b_2\right).$$

Its hidden width defaults to $d/2$. Both linear layers are initialized with zero bias and Gaussian
weights of standard deviation $10^{{-3}}$, making the initial map close to identity. The nonlinear
comparison in §5.5 uses the same residual form with a 1,024-wide GELU hidden layer and no bounded
wrapper.

The D1 ladder baseline is **closed-form orthogonal Procrustes**: it computes an orthogonal map by
SVD of the paired-document cross-covariance. It is not the trainable Cayley adapter described next,
and the two objects must not share an unqualified "Procrustes" label in the ladder or its caption.

The trainable Cayley adapter parameterizes $A=P-P^\top$ and computes the map

$$W=(I-A)(I+A)^{{-1}},\qquad g_{{\mathrm{{proc}}}}(x)=n\!\left((xW^\top)\odot s\right),$$

where $s$ is a learned per-dimension scale initialized to one. The low-rank affine alternative is

$$g_{{\mathrm{{lr}}}}(x)=n\!\left(x+(xU)V^\top+b\right),$$

with $U\in\mathbb{{R}}^{{d\times r}}$, $V\in\mathbb{{R}}^{{d\times r}}$, and $V$ initialized to zero,
so its initial correction is exactly zero.

## 3.2 Exact bounded-adapter map and correction floor

The bounded wrapper operates on the normalized input $\bar x=n(x)$ and the already normalized output
$z=g_\theta(x)$. With learned dimension scale $s$ (initialized to all ones), define

$$\delta=((z-\bar x)\odot s),\qquad r=\lVert\delta\rVert_2.$$

For lower and upper correction bounds $a$ and $b$, the implementation applies

$$
\gamma(r)=
\begin{{cases}}
a/r, & 10^{{-10}}<r<a,\\
b/r, & r>b,\\
1, & \text{{otherwise}},
\end{{cases}}
\qquad
g_{{[a,b]}}(x)=n\!\left(\bar x+\gamma(r)\delta\right).
$$

Thus the lower bound is a floor only for a *nonzero* correction: an exactly zero (or numerically
$\le10^{{-10}}$) correction is left at zero. C3a uses $a={bounded['min_correction']:.2f}$ and
$b={bounded['max_correction']:.1f}$. The stated INT8 scale is $1/128={bounded['int8_floor']:.7f}$,
so $a=0.01$ is intended to place nonzero corrections above that quantization-noise scale. After
clipping, the vector is normalized again. The wrapper therefore bounds the pre-renormalization
correction norm; it does not imply an identical Euclidean displacement after the final normalization.

## 3.3 Drift-triggered controller

The controller stores a scalar temperature $T$. Given a nonnegative drift magnitude $m$, observation
is a strict-threshold update:

$$
T\leftarrow
\begin{{cases}}
\max(T,\min(T_{{\max}},m)), & m>\tau,\\
T, & m\le\tau.
\end{{cases}}
$$

Correction is enabled iff $T>{controller['epsilon']:.0e}$. With
$\rho=\mathrm{{clip}}(T/T_{{\max}},0,1)$, a correction cycle receives learning-rate scale
$0.1+0.9\rho$, epoch count $1+\mathrm{{round}}(2\rho)$, and online intensity $\rho$. At cycle end,
$T\leftarrow cT$ and values at or below ${controller['epsilon']:.0e}$ are set to zero. Constructor
defaults are $T_0={controller['initial_temperature']:.1f}$, $\tau={controller['trigger_threshold']:.2f}$,
$T_{{\max}}={controller['max_temperature']:.1f}$, and $c={controller['cooling_rate']:.1f}$.

For the supervised query-encoder-swap cycle, the actual drift signal is
${supervised['drift_signal']}$, the campaign sets $\tau=0$, $T_{{\max}}=1$, and uses
$c={supervised['cooling_rate']:.1f}$. When the trigger fires and anchor pairs exist, the adapter is
reinitialized from the run seed, trained by query–document InfoNCE, applied to every cached document,
and written back. In the default non-compounding regime every cycle applies the newly trained adapter
to the same pre-correction document snapshot, making the correction a reproducible one-shot map rather
than an accumulated trajectory.
"""


def _paper_section_for_line(lines: Sequence[str], line_number: int) -> str:
    for index in range(line_number - 1, -1, -1):
        if lines[index].startswith("#"):
            return lines[index].lstrip("# ")
    return "document preamble"


def render_paper_edits(
    paper_path: Optional[Path],
    ladder: Mapping[str, Any],
    bootstrap: Mapping[str, Any],
    contrasts: Mapping[str, Any],
    leakage: Mapping[str, Any],
) -> str:
    lines = paper_path.read_text(encoding="utf-8").splitlines() if paper_path and paper_path.exists() else []
    output = [
        "# Chair edit-list for the paper",
        "",
        "This file is an application list only; the git-excluded paper was not modified.",
        "",
        "## Replace all ceiling claims",
        "",
        "Use **observed plateau under this protocol**. This wording does not claim a mathematical or universal ceiling.",
        "",
        "| Source line | Section | Current text containing `ceiling` | Required edit |",
        "|---:|---|---|---|",
    ]
    if lines:
        for line_number, text in enumerate(lines, start=1):
            if "ceiling" not in text.lower():
                continue
            escaped = text.strip().replace("|", "\\|")
            section = _paper_section_for_line(lines, line_number).replace("|", "\\|")
            output.append(
                f"| {line_number} | {section} | {escaped} | Replace each `ceiling` noun phrase with "
                "`observed plateau under this protocol`; recast grammar where needed. |"
            )
    else:
        output.append("| — | — | Paper source unavailable to the runner | Re-run with `--paper PATH`. |")

    ridge = bootstrap["methods"]["ridge"]
    mlp = bootstrap["methods"]["mlp"]
    c3a = bootstrap["methods"]["c3a"]
    ridge_mlp = contrasts["contrasts"]["ridge_minus_mlp"]
    ridge_c3a = contrasts["contrasts"]["ridge_minus_c3a"]
    g2 = contrasts["g2"]
    audit = leakage["audit"]
    ridge_mlp_low = f"{ridge_mlp['ci_low']:.4f}".replace("-", "−")
    ridge_mlp_high = f"{ridge_mlp['ci_high']:.4f}"
    mlp_better_recovery_points = abs(ridge_mlp["ci_low"]) / ladder["oracle_gap"] * 100
    ridge_better_recovery_points = ridge_mlp["ci_high"] / ladder["oracle_gap"] * 100
    output.extend(
        [
            "",
            "## Mandatory leakage-sensitivity disclosure",
            "",
            f"The literal waypoint 600-document fit contains "
            f"{audit['eval_positive_docs_in_literal_fit']} of "
            f"{audit['eval_positive_docs_total']} eval-positive documents "
            f"({100*audit['literal_fit_share_of_eval_positive_docs']:.1f}%). The "
            f"leakage-safe full-{audit['leakage_safe_fit_count']} fit excludes all of them. "
            f"The {100*leakage['methods']['ridge']['literal_waypoint']['recovery']:.1f}% "
            "headline is therefore a leaky-fit point estimate.",
            "",
            *_leakage_table_lines(leakage),
            "",
            "State explicitly that the primary ladder/bootstrap is a literal-waypoint "
            "reproduction, while the full-600 sensitivity and document learning curves are "
            "leakage-safe. Do not equate the safe n=128 learning-curve regime with the "
            "literal 600-document headline.",
            "",
            "## Mandatory location-agnostic inference-ban block",
            "",
            "Paste this prohibition wherever the ridge−MLP result or plateau interpretation is discussed:",
            "",
            f"> Do not interpret ridge−MLP non-significance as evidence of a linear post-hoc ceiling or the "
            f"absence of nonlinear benefit. The 95% CI on ΔNDCG(ridge−MLP) is "
            f"[{ridge_mlp_low}, {ridge_mlp_high}] — it permits MLP better by up to "
            f"~{mlp_better_recovery_points:.0f} recovery points and ridge better by up to "
            f"~{ridge_better_recovery_points:.0f}. G2 FAILED (primary ridge−MLP half-width "
            f"{g2['primary_delta_ndcg_half_width']:.3f} ≫ {g2['threshold']:.3f}; the secondary "
            f"median across both preregistered contrasts is "
            f"{g2['secondary_median_family_delta_ndcg_half_width']:.3f}), so this study is "
            f"underpowered for the equality contrast on {bootstrap['query_count']} queries. "
            "Non-significance here is absence of evidence, not evidence of absence.",
            "",
            "### Forbidden claims",
            "",
            "- No **linear post-hoc ceiling**.",
            "- No **no nonlinear benefit** or **capacity is linear**.",
            f"- No **{100*ridge['recovery']['estimate']:.0f}% is the recoverable upper bound**.",
            f"- No **irreducible {100*(1.0-ridge['recovery']['estimate']):.0f}%**.",
            "- No G2-powered precision on method contrasts.",
            "",
            "### Permitted claims",
            "",
            "- Report point estimates plus 95% CIs on the stated protocol.",
            "- Ridge is the highest-observed method here.",
            "- Ridge ≫ C3a is Holm-significant.",
            "- Ridge versus MLP is not distinguishable at α=0.05 under this n.",
            "- Prefer **observed plateau under this protocol**.",
            "",
            "## Replace point-only D1 numbers with uncertainty-aware values",
            "",
            "Apply these at every matching abstract, Introduction, §4.2, §5.5, §6/§7, and Limitations occurrence; "
            "keep the full precision in tables and round prose to one recovery point / three NDCG decimals.",
            "",
            "| Quantity | Chair-ready replacement |",
            "|---|---|",
            f"| Ridge | NDCG {ridge['ndcg']['estimate']:.4f} (95% CI {ridge['ndcg']['ci_low']:.4f}–{ridge['ndcg']['ci_high']:.4f}); recovery {100*ridge['recovery']['estimate']:.1f}% (95% CI {100*ridge['recovery']['ci_low']:.1f}%–{100*ridge['recovery']['ci_high']:.1f}%) |",
            f"| Residual MLP | NDCG {mlp['ndcg']['estimate']:.4f} (95% CI {mlp['ndcg']['ci_low']:.4f}–{mlp['ndcg']['ci_high']:.4f}); recovery {100*mlp['recovery']['estimate']:.1f}% (95% CI {100*mlp['recovery']['ci_low']:.1f}%–{100*mlp['recovery']['ci_high']:.1f}%) |",
            f"| C3a (per-query, not scalar) | NDCG {c3a['ndcg']['estimate']:.4f} (95% CI {c3a['ndcg']['ci_low']:.4f}–{c3a['ndcg']['ci_high']:.4f}); recovery {100*c3a['recovery']['estimate']:.1f}% (95% CI {100*c3a['recovery']['ci_low']:.1f}%–{100*c3a['recovery']['ci_high']:.1f}%) |",
            f"| Ridge − MLP | ΔNDCG {ridge_mlp['estimate']:.4f} (95% CI {ridge_mlp['ci_low']:.4f}–{ridge_mlp['ci_high']:.4f}); Holm-adjusted p={ridge_mlp['multiple_testing']['holm_adjusted_p']:.6g} |",
            f"| Ridge − C3a | ΔNDCG {ridge_c3a['estimate']:.4f} (95% CI {ridge_c3a['ci_low']:.4f}–{ridge_c3a['ci_high']:.4f}); Holm-adjusted p={ridge_c3a['multiple_testing']['holm_adjusted_p']:.6g} |",
            "",
            "## Location-specific instructions",
            "",
            "- Abstract and §1: replace the point-only 84%/20% comparison with ridge and C3a recovery plus their CIs; describe ridge as the best observed plateau under this protocol.",
            "- §4.2: define recovery exactly as already written, but replace the quoted `~85% linear ceiling` interpretation with the observed ridge plateau and its CI.",
            "- §5.5 table/caption/body: replace all ridge, residual MLP, closed-form orthogonal Procrustes, and C3a entries from `ladder.json`; add the paired contrast CIs and Holm results. State that the ladder's closed-form SVD map is distinct from the trainable Cayley adapter, and that C3a was rescored per query through the merged NDCG implementation.",
            f"- Remove the old C3a `0.1609` scalar wherever it is presented as this fixed split. The actual seed-42 per-query mean is {c3a['ndcg']['estimate']:.4f}; `0.1609` was the mean across three different seeded splits.",
            "- §6 and §7: do not call the unexplained residual an irreducible 16%; the bootstrap measures sampling uncertainty, not an information-theoretic bound.",
            "- §8: replace `fixed ... ceiling` with `observed plateau under this protocol` and add the fixed 60-query eval-split limitation.",
            "- Learning-curve text/figure: show two panels. Ridge/MLP x-axis is paired document embeddings; C3a x-axis is distinct non-eval query anchors. Do not overlay them on a shared `n anchors` axis.",
            "",
            f"G2 is **{'passed' if g2['pass'] else 'failed'}**. The primary ridge−MLP "
            f"half-width is {g2['primary_delta_ndcg_half_width']:.4f}; the secondary "
            f"median across both preregistered contrasts is "
            f"{g2['secondary_median_family_delta_ndcg_half_width']:.4f}.",
        ]
    )
    return "\n".join(output) + "\n"


def _line_number(path: Path, needle: str) -> int:
    for line_number, text in enumerate(path.read_text(encoding="utf-8").splitlines(), start=1):
        if needle in text:
            return line_number
    raise RuntimeError(f"cannot locate {needle!r} in {path}")


def _research_relative(path: Path) -> str:
    parts = path.resolve().parts
    research_index = parts.index("research")
    return Path(*parts[research_index:]).as_posix()


def render_c1_fix_report(
    ladder: Mapping[str, Any],
    contrasts: Mapping[str, Any],
    leakage: Mapping[str, Any],
    parity: Mapping[str, float],
    output_dir: Path,
) -> str:
    source = Path(__file__)
    drift_root = source.parents[1]
    tests_path = drift_root / "tests" / "test_d1_stats.py"
    bootstrap_path = drift_root / "stats" / "paired_bootstrap.py"
    ci_path = output_dir / "ci_report.md"
    edits_path = output_dir / "paper_edits.md"
    section3_path = output_dir / "section3_draft.md"
    contrast_path = output_dir / "contrasts_holm.json"
    ladder_path = output_dir / "ladder.json"

    def location(path: Path, needle: str) -> str:
        return f"`{_research_relative(path)}:{_line_number(path, needle)}`"

    audit = leakage["audit"]
    g2 = contrasts["g2"]
    c3a_reference = ladder["canonical_reference_audit"]["c3a_cross_seed_reference"]
    max_parity_delta = max(abs(float(value)) for value in parity.values())
    leakage_results = ", ".join(
        f"{leakage['methods'][method]['label']} "
        f"{100*leakage['methods'][method]['leakage_safe_full_600']['recovery']:.1f}% "
        f"safe / +{leakage['methods'][method]['inflation_recovery_pp']:.1f} pp"
        for method in LEAKAGE_METHODS
    )
    lines = [
        "# C1 adversarial-review fix report",
        "",
        "Generated from the frozen pack and regenerated D1 artifacts; result numbers are not hand-entered.",
        "",
        "| Fix | Exact evidence location | Result |",
        "|---|---|---|",
        f"| F1 | {location(source, 'def compute_leakage_sensitivity')}<br>"
        f"{location(ci_path, '## Leakage sensitivity')}<br>"
        f"{location(edits_path, '## Mandatory leakage-sensitivity')} | "
        f"Literal fit: {audit['eval_positive_docs_in_literal_fit']}/"
        f"{audit['eval_positive_docs_total']} eval-positive docs; safe fit: "
        f"{audit['eval_positive_docs_in_leakage_safe_fit']}. {leakage_results}. |",
        f"| F2 | {location(edits_path, '## Mandatory location-agnostic inference-ban')} | "
        f"Ban plus forbidden/permitted lists emitted; ridge−MLP CI "
        f"[{contrasts['contrasts']['ridge_minus_mlp']['ci_low']:.4f}, "
        f"{contrasts['contrasts']['ridge_minus_mlp']['ci_high']:.4f}]. |",
        f"| F3 | {location(edits_path, 'Paste this prohibition')} | Chair-only application guard "
        "is explicit; protected paper-draft files modified: 0. |",
        f"| F4 | {location(source, 'The D1 ladder baseline is **closed-form orthogonal Procrustes**')}<br>"
        f"{location(section3_path, 'The D1 ladder baseline is **closed-form orthogonal Procrustes**')} | "
        "Closed-form SVD ladder map is explicitly distinct from the trainable Cayley adapter. |",
        f"| F5 | {location(source, 'def _canonical_reference_audit')}<br>"
        f"{location(ladder_path, 'superseded_not_a_tolerance_gate')} | "
        f"Old cross-seed C3a scalar 0.1609 is superseded by seed-42 per-query "
        f"{ladder['old_scalar_audit']['fixed_split_per_query_ndcg']:.4f}; recovery difference "
        f"{c3a_reference['absolute_difference']:.4f} exceeds "
        f"{c3a_reference['tolerance']:.3f}. |",
        f"| F6 | {location(source, 'primary_half_width =')}<br>"
        f"{location(contrast_path, 'primary_delta_ndcg_half_width')}<br>"
        f"{location(ci_path, 'preregistered primary')} | Primary ridge−MLP half-width "
        f"{g2['primary_delta_ndcg_half_width']:.4f}; secondary family median "
        f"{g2['secondary_median_family_delta_ndcg_half_width']:.4f}; rough scale "
        f"{g2['rough_query_count_for_threshold']} queries. |",
        f"| F7 | {location(tests_path, 'test_floor_oracle_and_methods_share_bootstrap_indices')}<br>"
        f"{location(tests_path, 'test_invalid_draw_gate_includes_epsilon')}<br>"
        f"{location(tests_path, 'test_literal_and_safe_fit_leakage_counts')}<br>"
        f"{location(bootstrap_path, 'result[' + chr(34) + '_bootstrap_indices' + chr(34) + ']')} | "
        "Shared-draw audit exposed; "
        f"1% invalid allowed and >1% rejected; leakage assertions bind "
        f"{audit['eval_positive_docs_in_literal_fit']}/{audit['eval_positive_docs_total']} and safe=0; "
        f"max harness-parity delta {max_parity_delta:.1e}. |",
        "",
        "Protocol-honesty note: C1's supplied F2 sentence called 0.089 the `median primary` "
        "half-width, while F6 requires ridge−MLP alone to be primary. The generated chair block "
        f"therefore reports primary={g2['primary_delta_ndcg_half_width']:.3f} and labels "
        f"{g2['secondary_median_family_delta_ndcg_half_width']:.3f} as secondary.",
    ]
    return "\n".join(lines) + "\n"


def _git_sha() -> str:
    return subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()


def find_paper() -> Optional[Path]:
    candidates = [
        Path.cwd() / "docs" / "waypoint-research-2026-06-09" / "paper-draft" / "main.md",
        Path.cwd().parents[2] / "docs" / "waypoint-research-2026-06-09" / "paper-draft" / "main.md",
    ]
    return next((path for path in candidates if path.exists()), None)


def run_d1(
    output_dir: Path,
    rebuild_pack: bool = False,
    rerun_learning_curve: bool = False,
    device: str = "cuda",
    paper_path: Optional[Path] = None,
    command: str = "python research/drift_recovery/run_d1.py",
) -> dict:
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)
    pack_prefix = output_dir / "scifact_evalsplit_pack"
    if rebuild_pack or not pack_prefix.with_suffix(".npz").exists():
        pack = build_embedding_pack(pack_prefix, device=device)
    else:
        print("[D1] reloading frozen EmbeddingPack", flush=True)
        pack = EmbeddingPack.load(pack_prefix)
        assert_harness_parity(pack, atol=1e-12)

    print("[D1] recomputing literal and leakage-safe full-600 fits", flush=True)
    leakage = compute_leakage_sensitivity(pack, device=device)
    _write_json(output_dir / "leakage_sensitivity.json", leakage)
    ladder = ladder_table(pack, leakage_sensitivity=leakage)
    _write_json(output_dir / "ladder.json", ladder)
    bootstrap, contrasts = run_bootstrap(pack)
    _write_json(output_dir / "bootstrap_cis.json", bootstrap)
    _write_json(output_dir / "contrasts_holm.json", contrasts)
    learning_path = output_dir / "learning_curve.json"
    if rerun_learning_curve or not learning_path.exists():
        learning = run_learning_curves(pack, device=device)
        _write_json(learning_path, learning)
    else:
        learning = json.loads(learning_path.read_text(encoding="utf-8"))
        _write_json(learning_path, learning)

    (output_dir / "ci_report.md").write_text(
        render_ci_report(ladder, bootstrap, contrasts, leakage), encoding="utf-8"
    )
    (output_dir / "section3_draft.md").write_text(render_section3(), encoding="utf-8")
    selected_paper = paper_path or find_paper()
    (output_dir / "paper_edits.md").write_text(
        render_paper_edits(
            selected_paper,
            ladder,
            bootstrap,
            contrasts,
            leakage,
        ),
        encoding="utf-8",
    )
    parity = assert_harness_parity(pack, atol=1e-12)
    (output_dir / "C1_FIX_REPORT.md").write_text(
        render_c1_fix_report(ladder, contrasts, leakage, parity, output_dir),
        encoding="utf-8",
    )

    protocol = {
        "dataset": "SciFact",
        "split_seed": 42,
        "anchor_fraction": 0.4,
        "eval_queries": len(pack.query_ids),
        "bootstrap_draws": 10_000,
        "bootstrap_seed": 20260630,
        "learning_counts": list(COUNTS),
        "learning_order_seeds": list(ORDER_SEEDS),
        "ndcg": "binary NDCG@10 from merged harness",
    }
    manifest = {
        "record_type": "d1_run_manifest",
        "repo_sha": _git_sha(),
        "harness_sha": _git_sha(),
        "protocol_sha": hashlib.sha256(
            json.dumps(protocol, sort_keys=True).encode("utf-8")
        ).hexdigest(),
        "protocol": protocol,
        "models": pack.metadata["models"],
        "drift_manifest": pack.metadata["drift_manifest"],
        "environment": {
            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
        },
        "command": command,
        "paper_source_read_only": str(selected_paper) if selected_paper else None,
        "outputs": sorted(
            {
                path.name for path in output_dir.iterdir()
            }.union({"run_manifest.json", "d1_summary.json"})
        ),
    }
    _write_json(output_dir / "run_manifest.json", manifest)
    result = {
        "output_dir": str(output_dir),
        "ladder": ladder,
        "g2": contrasts["g2"],
        "harness_parity": parity,
        "learning_rows": len(learning["rows"]),
    }
    _write_json(output_dir / "d1_summary.json", result)
    return result

"""Single command for the complete Phase B1 D1 build."""

from __future__ import annotations

import argparse
import json
import os
import sys
from pathlib import Path

os.environ.setdefault("HF_HUB_OFFLINE", "1")
os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")

if __package__ in {None, ""}:
    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from research.drift_recovery.d1.paper_stats import run_d1  # noqa: E402


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Build Phase B1 D1 statistics and paper assets")
    parser.add_argument(
        "--output-dir",
        type=Path,
        default=Path("research/drift_recovery/out/d1"),
    )
    parser.add_argument("--rebuild-pack", action="store_true")
    parser.add_argument("--rerun-learning-curve", action="store_true")
    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
    parser.add_argument("--paper", type=Path, default=None, help="Read-only paper source for edit locations")
    return parser


def main(argv=None) -> int:
    args = build_parser().parse_args(argv)
    result = run_d1(
        output_dir=args.output_dir,
        rebuild_pack=args.rebuild_pack,
        rerun_learning_curve=args.rerun_learning_curve,
        device=args.device,
        paper_path=args.paper,
        command="python " + " ".join(sys.argv),
    )
    print(json.dumps(result, indent=2, sort_keys=True))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())



 succeeded in 1462ms:
586:
587:# Task Group: CHELATEDAI drift-recovery calibration and brutal-honesty process
588:scope: CHELATEDAI drift-recovery diagnosis, severity calibration, knob-sweep execution, artifact durability, and strict PR-body handling; use when continuing drift-recovery experiments or preparing evidence-heavy PRs.
589:applies_to: cwd=D:\GITHUB\CHELATEDAI; reuse_rule=safe for drift-recovery and repo-process guidance in CHELATEDAI, but experimental results and PR state should be rechecked from current artifacts
590:
591:## Task 1: PR-6 diagnostics, PR-7 calibration, PR-8 knob sweep, and merge, completed
592:
593:### rollout_summary_files
594:
595:- rollout_summaries/2026-06-11T15-10-39-PIMu-drift_recovery_pr6_pr8_calibration_knob_sweep_and_bhs_harden.md (cwd=D:\GITHUB\CHELATEDAI, rollout_path=C:\Users\mattm\.codex\sessions\2026\06\11\rollout-2026-06-11T11-10-39-019eb73c-3da3-7f70-9035-afd5a56ecb86.jsonl, updated_at=2026-06-12T14:48:16+00:00, thread_id=019eb73c-3da3-7f70-9035-afd5a56ecb86)
596:
597:### keywords
598:
599:- drift-recovery, severity-calibration, knob-sweep, calibration-manifest, HF_HUB_OFFLINE, experiment_runs, artifact durability, deterministic tie handling, validate_pr_brutal_honesty.py, PR #266, admin merge
600:
601:## User preferences
602:
603:- the goal file required "diagnose last night's mixed drift-recovery result and re-run it calibrated" -> verify hypotheses from data before trusting prior summaries [Task 1]
604:- the goal file explicitly required "ALL cells reported plus 3-seed confirmation" -> default to full-grid reporting plus confirmation, not cherry-picked cells [Task 1]
605:- repeated reviewer feedback showed the user expects the agent to fix real review findings rather than hand-wave them away [Task 1]
606:- the goal also required parked PRs #256/#257 to stay untouched -> preserve scoped no-touch boundaries exactly [Task 1]
607:
608:## Reusable knowledge
609:
610:- `run_drift_recovery_knob_sweep.py` now derives calibrated references from `Path(calibration_manifest).parent` instead of hardcoding the default artifact directory [Task 1]
611:- The deterministic top-cell tie policy is: sort by final NDCG, then lower trigger threshold, then profile name, then lower bound epsilon [Task 1]
612:- `hotter` means a lower max-temperature cap plus higher epoch scale, not a higher temperature cap [Task 1]
613:- The working offline smoke path on this host is `HF_HUB_OFFLINE=1` plus `python scripts/smoke_pipeline.py` [Task 1]
614:- Evidence in `experiment_runs/` is not durable by default because the directory is gitignored; force-add the artifact set or move it to a tracked path before opening a PR [Task 1]
615:- Related skill: skills/brutal-honesty-local-gate-and-pr-body/SKILL.md [Task 1]
616:

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$p='"'C:\\Users\\mattm\\.codex\\memories\\rollout_summaries\\2026-06-11T15-10-39-PIMu-drift_recovery_pr6_pr8_calibration_knob_sweep_and_bhs_harden.md'; (Get-Content "'$p | Measure-Object -Line).Lines; Get-Content -Raw $p' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1335ms:
108
thread_id: 019eb73c-3da3-7f70-9035-afd5a56ecb86
updated_at: 2026-06-12T14:48:16+00:00
rollout_path: C:\Users\mattm\.codex\sessions\2026\06\11\rollout-2026-06-11T11-10-39-019eb73c-3da3-7f70-9035-afd5a56ecb86.jsonl
cwd: \\?\D:\GITHUB\CHELATEDAI
git_branch: main

# Day 2026-06-13 drift-recovery calibration work: PR-6 diagnostics, PR-7 severity calibration, and PR-8 knob sweep were completed and merged, with PR-8 ultimately landing as an honestly negative sensitivity result.

Rollout context: The active objective was to follow `docs/waypoint-research-2026-06-09/goal-day-2026-06-13.md` exactly: diagnose the mixed drift-recovery result, run a severity-calibration campaign into the 8–20% drop zone, execute a pre-registered C3 knob sweep with all cells reported plus 3-seed confirmation, avoid touching parked PRs #256/#257, and finish by appending the `Day 2026-06-13` section to the local-only waypoint notes. The repo uses a strict brutal-honesty/BHS process with required PR-body fields, fresh adversarial Tier B review, and `scripts/smoke_pipeline.py` as the release gate. The waypoint folder is gitignored/local-only and must not be committed.

## Task 1: PR-6 diagnostics over existing artifacts

Outcome: success

Preference signals:
- The user’s goal file explicitly required “diagnose last night’s mixed drift-recovery result and re-run it calibrated” before starting later slices, implying the next agent should verify hypotheses from data rather than assuming the previous run was meaningful.

Key steps:
- Re-examined the existing drift-recovery artifacts and results docs.
- Confirmed the likely hypotheses from the morning review: H1 rotation(0.5,25°) was too weak, H2 correction barely moved, H3 the C2 baseline-reembed baseline was effectively an oracle upper bound.
- Appended the `Day 2026-06-13` review notes locally in the ignored waypoint folder, including PR-6/PR-7/PR-8 and PR-9 status, and left the folder uncommitted as required.

Failures and how to do differently:
- The rollout briefly discovered that local Windows console Unicode logging can emit noisy errors during large SciFact runs; these did not invalidate the artifacts because the runs still exited 0 and recorded `cycle_errors: []`.

Reusable knowledge:
- The calibration results and reviewer notes live in the ignored waypoint folder, so they can be updated locally for tracking but should not be committed.
- The day-goal review notes are the right place to record H1/H2/H3 confirmations and the eventual PR-8 result.

References:
- `docs/waypoint-research-2026-06-09/review-notes-cleanup-pass.md` (local-only Day 2026-06-13 section appended).
- H1/H2/H3 were summarized in the review notes as: weak original rotation setting, correction saturation/near-zero learning signal, and C2 acting as an oracle re-embed upper bound.

## Task 2: PR-7 severity-calibration campaign

Outcome: success

Preference signals:
- The goal file required calibrated severity work before the knob sweep, which indicates the user wants the calibrated run results preserved as evidence, not just the final sweep.

Key steps:
- Ran the severity calibration campaign and then the follow-on analysis.
- The calibrated settings selected were rotation `fraction 0.5 / 35 degrees` (closest to the 12% target among rotation scouts) and noise `fraction 0.5 / sigma 0.035` (inside the 8–20% target zone).
- Documented that C3 still did not beat the oracle C2 baseline, and that the result should be described as mixed/negative rather than as a win.

Reusable knowledge:
- The calibrated severity campaign feeds the knob-sweep report and the later PR-8 selection logic.
- The chosen settings and the negative-vs-oracle interpretation are important context for later report wording.

References:
- `docs/drift-recovery-results-2026-06.md` contains the calibrated severity summary and later PR-8 additions.
- The results doc states C3 rotation and noise remain below C2, despite sometimes slightly beating C0/C1.

## Task 3: PR-8 pre-registered C3 knob sweep

Outcome: success

Preference signals:
- The goal file explicitly required “PR-8 pre-registered C3 knob sweep with ALL cells reported plus 3-seed confirmation,” indicating that full grid reporting and confirmation are mandatory, not optional.
- The user’s day goal also implied the final report should remain honest if C3 still loses.
- The later reviewer feedback showed the user expects the agent to fix review findings rather than hand-wave them away.

Key steps:
- Implemented `run_drift_recovery_knob_sweep.py`, added tests, and exposed a CLI entry in `pyproject.toml`.
- Extended `run_drift_recovery_experiment.py` and `antigravity_engine.py` to accept/configure the extra knobs needed for the sweep.
- Added/updated tests for: default-preservation, knob metadata, invalid knob values, deterministic top-cell tie handling, calibration-manifest scoping, and the knob-sweep orchestration itself.
- Generated the actual sweep artifacts: 12 grid JSONs, 6 confirmation JSONs, plus the manifest.
- Updated `docs/drift-recovery-results-2026-06.md` and created `docs/drift-recovery-knob-sweep-2026-06.md` with the calibrated selection policy and a plain negative-result conclusion.
- Committed the artifacts, pushed the branch, opened PR #266, fixed the PR-body schema to satisfy the §4 validator, and merged via admin after the repo’s branch policy blocked a normal merge.

Preference signals:
- The reviewer feedback repeatedly required explicit honesty about artifacts, tie handling, and the fact that the result was negative. That strongly indicates future similar work should default to transparent reporting and explicit selection policies.
- The user’s instruction to leave parked PRs #256/#257 untouched mattered: they were checked later and not modified.

Failures and how to do differently:
- First PR body draft failed the repo’s §4 validator because it used markdown section headers instead of literal `FIELD: value` lines. Future PR bodies in this repo need colon-form fields that the validator can parse directly.
- The validator also requires a non-empty `OPERATOR_OVERRIDE` line to be structurally valid when applicable; it only warns, but the body format must still be precise.
- The first local/online smoke attempts surfaced a host-specific Hugging Face SSL issue; the successful verification path on this machine was `HF_HUB_OFFLINE=1` plus the cached model/dataset.
- A major blocker was that `experiment_runs/drift-recovery/knob-sweep/` was gitignored. The artifacts were real but not durable until force-added. Future evidence-bearing experiment runs in ignored directories must be force-added or moved to a tracked path before PR creation.
- The initial tie policy was under-specified; the fix was to make it deterministic and disclose it in both the manifest and report.

Reusable knowledge:
- `run_drift_recovery_knob_sweep.py` now derives calibrated references from `Path(calibration_manifest).parent`, rather than hardcoding the default artifact directory.
- The top-cell selection is now deterministic: sort by seed-42 final NDCG, then lower trigger threshold, then profile name, then lower bound epsilon.
- The PR-8 report explicitly states the chosen selection policy and that `hotter` means lower max-temperature cap plus higher epoch scale, not a higher temperature cap.
- The final PR-8 evidence set includes the tracked manifest plus 18 tracked artifact JSON files (12 grid + 6 confirmation).

References:
- PR #266: `https://github.com/mattmre/CHELATEDAI/pull/266`
- Merge commit: `7ececa1 Add drift recovery knob sweep (#266)`
- Key files: `run_drift_recovery_knob_sweep.py`, `run_drift_recovery_experiment.py`, `test_run_drift_recovery_experiment.py`, `test_run_drift_recovery_knob_sweep.py`, `docs/drift-recovery-results-2026-06.md`, `docs/drift-recovery-knob-sweep-2026-06.md`.
- Manifest: `experiment_runs/drift-recovery/knob-sweep/knob-sweep-manifest-2026-06.json`
- Final verified stats from the regenerated manifest: 12 grid rows, 6 confirmation rows, 2 confirmation summaries, no cycle errors, tracked artifacts present, and the negative result persisted after fixes.

## Task 4: PR-body/BHS process and adversarial review

Outcome: success

Preference signals:
- The rollout shows repeated corrections on PR-body shape and evidence durability, implying future agents should expect the repo’s BHS validator to be literal and to require exact field formatting and durable artifacts.
- The user’s objective asked to complete all phases and confirm BHS 100/100 as the goal. Even though the final PR body was formally set to `BHS_OFFICIAL: 92` under a session-level waiver, the practical requirement was satisfied by the adversarial review, validator pass, and merge.

Key steps:
- Spawned fresh reviewer sub-agents for adversarial BHS review: Kant, Kierkegaard, Aquinas, and Archimedes.
- Used their findings to fix substantive issues: default-preservation evidence, tie-policy disclosure, calibration reference scoping, profile wording, and artifact durability.
- Converted the PR body to the exact validator-friendly `FIELD: value` format, which allowed the §4 validator to pass.
- Ran `scripts/check_block_flag.py`, `scripts/validate_pr_brutal_honesty.py`, `gh pr checks`, and the required offline smoke path.
- After the final focused reviewer confirmed no merge blocker remained, the PR merged cleanly under admin because the repository branch policy blocks ordinary merges even when checks are green.

Failures and how to do differently:
- The first PR body was rejected because the validator does not parse markdown headings as fields.
- The reviewer chain showed that artifact durability is a first-class BHS concern in this repo; evidence files in ignored directories are not enough unless they are tracked or otherwise made durable in the PR.
- The rulebook validator is structural only; it cannot verify that Tier B was genuinely independent, so that claim should stay epistemically honest in future bodies.

Reusable knowledge:
- In this repo, the §4 validator is sensitive to exact line prefixes and will fail if required fields are not literal `FIELD: value` lines.
- `scripts/smoke_pipeline.py` should be run in offline mode (`HF_HUB_OFFLINE=1`) on this host to avoid the SSL/caching failure.
- Admin merge is often required even when all checks pass.

References:
- `scripts/validate_pr_brutal_honesty.py` and `scripts/check_block_flag.py`.
- Review findings from Kant/Kierkegaard/Aquinas/Archimedes: deterministic tie handling, calibration manifest scoping, artifact durability, and the final no-blocker confirmation.

## Task 5: end-of-day local-only note update and cleanup

Outcome: success

Preference signals:
- The goal file explicitly required appending the `Day 2026-06-13` section at the end, and the user also emphasized not touching the ignored waypoint folder beyond local notes.

Key steps:
- Appended the day section to `docs/waypoint-research-2026-06-09/review-notes-cleanup-pass.md`.
- Verified the repo was on `main` and aligned with `origin/main` after the merge.
- Confirmed parked PRs #256 and #257 remained open drafts and untouched.
- Called `update_goal(status="complete")` only after the full objective was actually satisfied and merged.

Reusable knowledge:
- The waypoint folder stays ignored/local-only; it can be updated for handoff notes without being committed.
- Final goal completion should only be claimed after the merge, the day-section append, and the explicit verification that parked PRs were left alone.

References:
- Local note file: `docs/waypoint-research-2026-06-09/review-notes-cleanup-pass.md`.
- Parked PRs checked and left alone: #256 and #257.
- Final merged PR sequence: #264, #265, #266, with #266 at merge commit `7ececa1`.



exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-ChildItem -Recurse -Force research/drift_recovery | Select-Object FullName,Length,LastWriteTime | Format-Table -AutoSize' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$roots=@("$env:USERPROFILE'"\\.cache\\huggingface\\hub\",\""'$env:USERPROFILE'"\\.cache\\huggingface\\datasets\",\""'$env:LOCALAPPDATA'"\\huggingface\\hub\"); foreach("'$r in $roots){"ROOT=$r EXISTS=$(Test-Path $r)"; if(Test-Path $r){Get-ChildItem $r -Directory -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Name | Sort-Object}}' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'rg -n "def load_mteb_data|MTEB|NFCorpus|SciFact|cache" benchmark_utils.py pyproject.toml requirements.txt; Get-Content benchmark_utils.py | Select-Object -Skip 180 -First 180' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; @'
import json
try:
 import torch
 print(json.dumps({'torch':torch.__version__,'cuda':torch.cuda.is_available(),'device':torch.cuda.get_device_name(0) if torch.cuda.is_available() else None,'free_total':torch.cuda.mem_get_info() if torch.cuda.is_available() else None}))
except Exception as e: print(type(e).__name__, str(e))
try:
 import sentence_transformers
 print('sentence_transformers', sentence_transformers.__version__)
except Exception as e: print(type(e).__name__, str(e))
'@ | python -" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 4517ms:
ROOT=C:\Users\mattm\.cache\huggingface\hub EXISTS=True
.locks
datasets--allenai--WildChat-1M
datasets--climate_fever
datasets--fever
datasets--hotpot_qa
datasets--lmsys--lmsys-chat-1m
datasets--mteb--fiqa
datasets--mteb--nfcorpus
datasets--mteb--scifact
datasets--RyokoAI--ShareGPT52K
datasets--stablediffusiontutorials--LTX_workflows
datasets--wikitext
models--BAAI--bge-large-en-v1.5
models--BAAI--bge-reranker-v2-m3
models--bytkim--Qwen3.6-27B-MTP-pi-tune-GGUF
models--colbert-ir--colbertv2.0
models--Floppyshy--sulphur-2-runpod
models--HauhauCS--Qwen3.6-27B-Uncensored-HauhauCS-Aggressive
models--microsoft--infoxlm-large
models--microsoft--layoutlmv3-base
models--mradermacher--Ministral-3-14B-Instruct-2512-absolute-heresy-i1-GGUF
models--nomic-ai--nomic-bert-2048
models--nomic-ai--nomic-embed-text-v1
models--nvidia--canary-qwen-2.5b
models--PaddlePaddle--en_PP-OCRv5_mobile_rec
models--PaddlePaddle--PP-LCNet_x1_0_doc_ori
models--PaddlePaddle--PP-LCNet_x1_0_textline_ori
models--PaddlePaddle--PP-OCRv5_server_det
models--PaddlePaddle--UVDoc
models--Qwen--Qwen2.5-0.5B-Instruct
models--Qwen--Qwen3.5-0.8B-Base
models--sentence-transformers--all-MiniLM-L6-v2
models--sentence-transformers--all-mpnet-base-v2
models--solailabs--wmt22-cometkiwi-da-int8
models--tiny-random--qwen2.5
models--unsloth--Qwen3.6-27B-GGUF
models--unsloth--Qwen3.6-35B-A3B-GGUF
models--vidore--colpali-v1.2
ROOT=C:\Users\mattm\.cache\huggingface\datasets EXISTS=True
json
mteb___fiqa
mteb___nfcorpus
mteb___scifact
wikitext
ROOT=C:\Users\mattm\AppData\Local\huggingface\hub EXISTS=False

 succeeded in 4856ms:
benchmark_utils.py:17:    - load_mteb_data: Load corpus, queries, and qrels from MTEB tasks
benchmark_utils.py:256:# MTEB Data Loading Helpers
benchmark_utils.py:302:def load_mteb_data(task_name: str) -> Tuple[Optional[Dict], Optional[Dict], Optional[Dict]]:
benchmark_utils.py:304:    Load corpus, queries, and qrels from an MTEB retrieval task.
benchmark_utils.py:306:    This function handles various MTEB task formats and provides robust
benchmark_utils.py:310:        task_name: Name of the MTEB task (e.g., "SciFact", "NFCorpus")
benchmark_utils.py:325:        print(f"ERROR: Task '{task_name}' not found in MTEB registry!")
benchmark_utils.py:328:        print(f"ERROR: Failed to load MTEB task '{task_name}': {e}")

def mean_average_precision_at_k(retrieved_ids, relevant_ids, k=10):
    """
    Average Precision at rank k for a single query.

    Args:
        retrieved_ids: List of retrieved document IDs (ordered by rank)
        relevant_ids: Set/list of relevant document IDs
        k: Rank cutoff

    Returns:
        float: AP@k score
    """
    retrieved = list(retrieved_ids)[:k]
    relevant_set = set(relevant_ids)

    if not relevant_set:
        return 0.0

    num_relevant = 0
    sum_precision = 0.0

    for i, doc_id in enumerate(retrieved):
        if doc_id in relevant_set:
            num_relevant += 1
            sum_precision += num_relevant / (i + 1)

    if num_relevant == 0:
        return 0.0

    return sum_precision / min(len(relevant_set), k)


def mean_reciprocal_rank(retrieved_ids, relevant_ids):
    """
    Reciprocal Rank for a single query.

    Args:
        retrieved_ids: List of retrieved document IDs (ordered by rank)
        relevant_ids: Set/list of relevant document IDs

    Returns:
        float: RR score (1/rank of first relevant result, or 0)
    """
    relevant_set = set(relevant_ids)

    for i, doc_id in enumerate(retrieved_ids):
        if doc_id in relevant_set:
            return 1.0 / (i + 1)

    return 0.0


def recall_at_k(retrieved_ids, relevant_ids, k=10):
    """
    Recall at rank k for a single query.

    Args:
        retrieved_ids: List of retrieved document IDs (ordered by rank)
        relevant_ids: Set/list of relevant document IDs
        k: Rank cutoff

    Returns:
        float: Recall@k score
    """
    retrieved_set = set(list(retrieved_ids)[:k])
    relevant_set = set(relevant_ids)

    if not relevant_set:
        return 0.0

    return len(retrieved_set & relevant_set) / len(relevant_set)


# =============================================================================
# MTEB Data Loading Helpers
# =============================================================================

def find_keys(obj, target_keys):
    """
    Recursively search a nested dict for a level containing all target_keys.
    
    Args:
        obj: Object to search (typically a nested dict)
        target_keys: List of keys that must all be present
        
    Returns:
        dict or None: First dict containing all target_keys, or None if not found
    """
    if not isinstance(obj, dict):
        return None
    if all(k in obj for k in target_keys):
        return obj
    for k, v in obj.items():
        found = find_keys(v, target_keys)
        if found:
            return found
    return None


def find_payload(obj, key):
    """
    Recursively search a nested dict for a specific key and return its value.
    
    Args:
        obj: Object to search (typically a nested dict)
        key: Key to find
        
    Returns:
        Value associated with key, or None if not found
    """
    if isinstance(obj, dict):
        if key in obj:
            return obj[key]
        for v in obj.values():
            res = find_payload(v, key)
            if res is not None:
                return res
    return None


def load_mteb_data(task_name: str) -> Tuple[Optional[Dict], Optional[Dict], Optional[Dict]]:
    """
    Load corpus, queries, and qrels from an MTEB retrieval task.
    
    This function handles various MTEB task formats and provides robust
    extraction of corpus documents, queries, and relevance judgments.
    
    Args:
        task_name: Name of the MTEB task (e.g., "SciFact", "NFCorpus")
        
    Returns:
        Tuple of (corpus, queries, qrels) dicts, or (None, None, None) on error
        - corpus: {doc_id: text} mapping
        - queries: {query_id: text} mapping  
        - qrels: {query_id: {doc_id: relevance_score}} mapping
    """
    if mteb is None:
        print("ERROR: mteb package not installed. Run: pip install mteb")
        return None, None, None
    
    try:
        task = mteb.get_task(task_name)
    except KeyError:
        print(f"ERROR: Task '{task_name}' not found in MTEB registry!")
        return None, None, None
    except Exception as e:
        print(f"ERROR: Failed to load MTEB task '{task_name}': {e}")
        return None, None, None

    task.load_data()

    # Try to find the data root containing corpus and queries
    targets = ['corpus', 'queries']
    data_root = find_keys(task.dataset, targets)

    if not data_root:
        c_payload = find_payload(task.dataset, 'corpus')
        q_payload = find_payload(task.dataset, 'queries')
        r_payload = find_payload(task.dataset, 'relevant_docs')
        if not r_payload:
            r_payload = find_payload(task.dataset, 'test')
    else:
        c_payload = data_root.get('corpus')
        q_payload = data_root.get('queries')
        r_payload = data_root.get('relevant_docs')

    corpus = {}
    queries = {}
    qrels = {}

    # Parse corpus
    if c_payload:
        try:
            for k, v in c_payload.items():
                corpus[k] = v['text'] + " " + v['title']
        except (AttributeError, TypeError):
            for row in c_payload:
                if '_id' in row:
                    doc_id = row['_id']

 succeeded in 4870ms:

FullName
--------                                                                                                               
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\__pycache__                                 
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\d1                                          
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out                                         
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats                                       
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests                                       
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\__init__.py                                 
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\artifacts.py                                
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\contracts.py                                
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\harness_bridge.py                           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_d1.py                                   
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\d1\__pycache__                              
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\d1\__init__.py                              
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\d1\paper_stats.py                           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\d1\__pycache__\__init__.cpython-311.pyc     
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\d1\__pycache__\paper_stats.cpython-311.pyc  
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1                                      
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\bootstrap_cis.json                   
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\C1_FIX_REPORT.md                     
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\ci_report.md                         
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\contrasts_holm.json                  
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\d1_summary.json                      
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\ladder.json                          
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\leakage_sensitivity.json             
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\learning_curve.json                  
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\paper_edits.md                       
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\run_manifest.json                    
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\scifact_evalsplit_pack.json          
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\scifact_evalsplit_pack.npz           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\d1\section3_draft.md                    
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\__pycache__                           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\__init__.py                           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\learning_curve.py                     
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\multiple_testing.py                   
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\paired_bootstrap.py                   
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\__pycache__\__init__.cpython-311.pyc  
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\__pycache__\learning_curve.cpython-31…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\__pycache__\multiple_testing.cpython-…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\stats\__pycache__\paired_bootstrap.cpython-…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\__pycache__                           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\__init__.py                           
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_d1_stats.py                      
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\__pycache__\__init__.cpython-311.pyc  
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\__pycache__\test_d1_stats.cpython-311…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\__pycache__\__init__.cpython-311.pyc        
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\__pycache__\artifacts.cpython-311.pyc       
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\__pycache__\harness_bridge.cpython-311.pyc  


2026-07-10T03:12:04.638725Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 23.8 seconds
Output:
command timed out after 23755 milliseconds

 exited 124 in 23755ms:
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'rg -n "def load_mteb_data|MTEB|NFCorpus|SciFact|cache" benchmark_utils.py pyproject.toml requirements.txt; Get-Content benchmark_utils.py | Select-Object -Skip 120 -First 220' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1273ms:
benchmark_utils.py:17:    - load_mteb_data: Load corpus, queries, and qrels from MTEB tasks
benchmark_utils.py:256:# MTEB Data Loading Helpers
benchmark_utils.py:302:def load_mteb_data(task_name: str) -> Tuple[Optional[Dict], Optional[Dict], Optional[Dict]]:
benchmark_utils.py:304:    Load corpus, queries, and qrels from an MTEB retrieval task.
benchmark_utils.py:306:    This function handles various MTEB task formats and provides robust
benchmark_utils.py:310:        task_name: Name of the MTEB task (e.g., "SciFact", "NFCorpus")
benchmark_utils.py:325:        print(f"ERROR: Task '{task_name}' not found in MTEB registry!")
benchmark_utils.py:328:        print(f"ERROR: Failed to load MTEB task '{task_name}': {e}")

    target_path.parent.mkdir(parents=True, exist_ok=True)
    backup_path = None

    if target_path.exists():
        backup_path = target_path.with_name(
            f"{target_path.stem}.benchmark-backup-{uuid4().hex}{target_path.suffix}"
        )
        shutil.copy2(target_path, backup_path)
        target_path.unlink()

    try:
        yield target_path
    finally:
        target_path.unlink(missing_ok=True)
        if backup_path is not None and backup_path.exists():
            shutil.move(str(backup_path), str(target_path))


# =============================================================================
# Metric Calculation (NDCG@k)
# =============================================================================

def dcg_at_k(r, k):
    """
    Discounted Cumulative Gain at rank k.
    
    Args:
        r: Array-like of relevance scores (binary or graded)
        k: Rank cutoff
        
    Returns:
        float: DCG score
    """
    r = np.asarray(r, dtype=float)[:k]
    if r.size:
        return np.sum(r / np.log2(np.arange(2, r.size + 2)))
    return 0.


def ndcg_at_k(r, k):
    """
    Normalized Discounted Cumulative Gain at rank k.

    Args:
        r: Array-like of relevance scores (binary or graded)
        k: Rank cutoff

    Returns:
        float: NDCG score in [0, 1]
    """
    dcg_max = dcg_at_k(sorted(r, reverse=True), k)
    if not dcg_max:
        return 0.
    return dcg_at_k(r, k) / dcg_max


# =============================================================================
# Extended Metrics (Phase 6)
# =============================================================================

def mean_average_precision_at_k(retrieved_ids, relevant_ids, k=10):
    """
    Average Precision at rank k for a single query.

    Args:
        retrieved_ids: List of retrieved document IDs (ordered by rank)
        relevant_ids: Set/list of relevant document IDs
        k: Rank cutoff

    Returns:
        float: AP@k score
    """
    retrieved = list(retrieved_ids)[:k]
    relevant_set = set(relevant_ids)

    if not relevant_set:
        return 0.0

    num_relevant = 0
    sum_precision = 0.0

    for i, doc_id in enumerate(retrieved):
        if doc_id in relevant_set:
            num_relevant += 1
            sum_precision += num_relevant / (i + 1)

    if num_relevant == 0:
        return 0.0

    return sum_precision / min(len(relevant_set), k)


def mean_reciprocal_rank(retrieved_ids, relevant_ids):
    """
    Reciprocal Rank for a single query.

    Args:
        retrieved_ids: List of retrieved document IDs (ordered by rank)
        relevant_ids: Set/list of relevant document IDs

    Returns:
        float: RR score (1/rank of first relevant result, or 0)
    """
    relevant_set = set(relevant_ids)

    for i, doc_id in enumerate(retrieved_ids):
        if doc_id in relevant_set:
            return 1.0 / (i + 1)

    return 0.0


def recall_at_k(retrieved_ids, relevant_ids, k=10):
    """
    Recall at rank k for a single query.

    Args:
        retrieved_ids: List of retrieved document IDs (ordered by rank)
        relevant_ids: Set/list of relevant document IDs
        k: Rank cutoff

    Returns:
        float: Recall@k score
    """
    retrieved_set = set(list(retrieved_ids)[:k])
    relevant_set = set(relevant_ids)

    if not relevant_set:
        return 0.0

    return len(retrieved_set & relevant_set) / len(relevant_set)


# =============================================================================
# MTEB Data Loading Helpers
# =============================================================================

def find_keys(obj, target_keys):
    """
    Recursively search a nested dict for a level containing all target_keys.
    
    Args:
        obj: Object to search (typically a nested dict)
        target_keys: List of keys that must all be present
        
    Returns:
        dict or None: First dict containing all target_keys, or None if not found
    """
    if not isinstance(obj, dict):
        return None
    if all(k in obj for k in target_keys):
        return obj
    for k, v in obj.items():
        found = find_keys(v, target_keys)
        if found:
            return found
    return None


def find_payload(obj, key):
    """
    Recursively search a nested dict for a specific key and return its value.
    
    Args:
        obj: Object to search (typically a nested dict)
        key: Key to find
        
    Returns:
        Value associated with key, or None if not found
    """
    if isinstance(obj, dict):
        if key in obj:
            return obj[key]
        for v in obj.values():
            res = find_payload(v, key)
            if res is not None:
                return res
    return None


def load_mteb_data(task_name: str) -> Tuple[Optional[Dict], Optional[Dict], Optional[Dict]]:
    """
    Load corpus, queries, and qrels from an MTEB retrieval task.
    
    This function handles various MTEB task formats and provides robust
    extraction of corpus documents, queries, and relevance judgments.
    
    Args:
        task_name: Name of the MTEB task (e.g., "SciFact", "NFCorpus")
        
    Returns:
        Tuple of (corpus, queries, qrels) dicts, or (None, None, None) on error
        - corpus: {doc_id: text} mapping
        - queries: {query_id: text} mapping  
        - qrels: {query_id: {doc_id: relevance_score}} mapping
    """
    if mteb is None:
        print("ERROR: mteb package not installed. Run: pip install mteb")
        return None, None, None
    
    try:
        task = mteb.get_task(task_name)
    except KeyError:
        print(f"ERROR: Task '{task_name}' not found in MTEB registry!")
        return None, None, None
    except Exception as e:
        print(f"ERROR: Failed to load MTEB task '{task_name}': {e}")
        return None, None, None

    task.load_data()

    # Try to find the data root containing corpus and queries
    targets = ['corpus', 'queries']
    data_root = find_keys(task.dataset, targets)

    if not data_root:
        c_payload = find_payload(task.dataset, 'corpus')
        q_payload = find_payload(task.dataset, 'queries')
        r_payload = find_payload(task.dataset, 'relevant_docs')

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$roots=@("$env:USERPROFILE'"\\.cache\\huggingface\\hub\",\""'$env:USERPROFILE'"\\.cache\\huggingface\\datasets\",\""'$env:LOCALAPPDATA'"\\huggingface\\hub\"); foreach("'$r in $roots){"ROOT=$r EXISTS=$(Test-Path $r)"; if(Test-Path $r){Get-ChildItem $r -Directory -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Name | Sort-Object}}' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 995ms:
ROOT=C:\Users\mattm\.cache\huggingface\hub EXISTS=True
.locks
datasets--allenai--WildChat-1M
datasets--climate_fever
datasets--fever
datasets--hotpot_qa
datasets--lmsys--lmsys-chat-1m
datasets--mteb--fiqa
datasets--mteb--nfcorpus
datasets--mteb--scifact
datasets--RyokoAI--ShareGPT52K
datasets--stablediffusiontutorials--LTX_workflows
datasets--wikitext
models--BAAI--bge-large-en-v1.5
models--BAAI--bge-reranker-v2-m3
models--bytkim--Qwen3.6-27B-MTP-pi-tune-GGUF
models--colbert-ir--colbertv2.0
models--Floppyshy--sulphur-2-runpod
models--HauhauCS--Qwen3.6-27B-Uncensored-HauhauCS-Aggressive
models--microsoft--infoxlm-large
models--microsoft--layoutlmv3-base
models--mradermacher--Ministral-3-14B-Instruct-2512-absolute-heresy-i1-GGUF
models--nomic-ai--nomic-bert-2048
models--nomic-ai--nomic-embed-text-v1
models--nvidia--canary-qwen-2.5b
models--PaddlePaddle--en_PP-OCRv5_mobile_rec
models--PaddlePaddle--PP-LCNet_x1_0_doc_ori
models--PaddlePaddle--PP-LCNet_x1_0_textline_ori
models--PaddlePaddle--PP-OCRv5_server_det
models--PaddlePaddle--UVDoc
models--Qwen--Qwen2.5-0.5B-Instruct
models--Qwen--Qwen3.5-0.8B-Base
models--sentence-transformers--all-MiniLM-L6-v2
models--sentence-transformers--all-mpnet-base-v2
models--solailabs--wmt22-cometkiwi-da-int8
models--tiny-random--qwen2.5
models--unsloth--Qwen3.6-27B-GGUF
models--unsloth--Qwen3.6-35B-A3B-GGUF
models--vidore--colpali-v1.2
ROOT=C:\Users\mattm\.cache\huggingface\datasets EXISTS=True
json
mteb___fiqa
mteb___nfcorpus
mteb___scifact
wikitext
ROOT=C:\Users\mattm\AppData\Local\huggingface\hub EXISTS=False

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; @'
import json, torch
print(json.dumps({'torch':torch.__version__,'cuda':torch.cuda.is_available(),'device':torch.cuda.get_device_name(0) if torch.cuda.is_available() else None,'free_total':torch.cuda.mem_get_info() if torch.cuda.is_available() else None}))
import sentence_transformers
print('sentence_transformers', sentence_transformers.__version__)
'@ | python -" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 15462ms:
{"torch": "2.5.1+cu121", "cuda": true, "device": "NVIDIA GeForce RTX 3090", "free_total": [24469569536, 25769279488]}
sentence_transformers 5.2.0

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content query_encoder_drift.py | Select-Object -First 260; Get-Content research/drift_recovery/d1/paper_stats.py | Select-Object -Skip 285 -First 135' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1289ms:
"""Query-side encoder-upgrade drift for retrieval-recovery experiments.

This models the realistic failure mode an encoder upgrade introduces: the eval
queries move to a *new* encoder while the document store remains cached in the
*original* encoder's space. Retrieval degrades because queries and documents are
no longer in the same representation.

Crucially, this drift defeats the "re-embed maintenance" oracle (condition C2).
C2 re-embeds the documents' unchanged text with the *original* frozen encoder,
which reproduces the cached document vectors exactly — but those vectors are
still in the old space and therefore still misaligned with the upgraded query
space, so C2 does not recover retrieval. Only an adapter that re-aligns the
cached document vectors toward the new query space (condition C3), or a full
re-embed of the corpus with the *new* encoder (the expensive oracle upper
bound), can recover. See
docs/waypoint-research-2026-06-09/finding-oracle-survives-document-model-swap.md.
"""

from __future__ import annotations

import hashlib
from typing import Any, Dict, List, Optional

import numpy as np


class QueryEncoderDrift:
    """Embeds query text with a swapped encoder projected into the store's space.

    The projection is a FROZEN, seeded ``DimensionProjection`` and is part of the
    drift definition — it is never trained. Outcomes are a deterministic function
    of ``(seed, swap model, query text)``.
    """

    def __init__(
        self,
        store_dim: int,
        swap_model_name: str = "all-mpnet-base-v2",
        seed: int = 0,
        swap_backend: Any = None,
    ):
        self.store_dim = int(store_dim)
        self.swap_model_name = str(swap_model_name)
        self.seed = int(seed)
        self._swap_backend = swap_backend
        self._projection = None
        self._swap_dim: Optional[int] = None

    def embed_queries(self, texts: List[str]) -> np.ndarray:
        """Return drifted query embeddings of shape (len(texts), store_dim), L2-normalized."""
        if not isinstance(texts, (list, tuple)) or len(texts) == 0:
            raise ValueError("texts must be a non-empty sequence of strings")
        backend = self._backend()
        raw = np.asarray(backend.embed_raw(list(texts)), dtype=np.float32)
        if raw.ndim != 2 or raw.shape[0] != len(texts):
            raise ValueError(
                f"swap backend returned shape {raw.shape}; expected ({len(texts)}, swap_dim)"
            )
        self._ensure_projection(int(raw.shape[1]))
        projected = self._projection.project_numpy(raw)
        return self._l2_normalize(np.asarray(projected, dtype=np.float32))

    def manifest(self) -> dict:
        """Reproducibility record. Call after at least one embed_queries()."""
        if self._projection is None or self._swap_dim is None:
            raise ValueError("manifest() requires at least one embed_queries() call first")
        return {
            "drift": "query_encoder_swap",
            "swap_model": self.swap_model_name,
            "projection_seed": self.seed,
            "projection_checksum": self._projection_checksum(self._projection),
            "swap_dim": int(self._swap_dim),
            "store_dim": int(self.store_dim),
        }

    def _backend(self) -> Any:
        if self._swap_backend is None:
            from embedding_backend import create_embedding_backend

            self._swap_backend = create_embedding_backend(self.swap_model_name)
        return self._swap_backend

    def _ensure_projection(self, swap_dim: int) -> None:
        if self._projection is not None:
            if swap_dim != self._swap_dim:
                raise ValueError(
                    f"swap backend changed dimension from {self._swap_dim} to {swap_dim}"
                )
            return
        import torch

        from teacher_distillation import DimensionProjection

        # Seed torch's global RNG so DimensionProjection's near-identity init
        # (which uses torch.randn_like) is reproducible. Frozen — never trained.
        # Caveat: reproducibility relies on the global torch RNG between this
        # seed call and the randn_like inside DimensionProjection. This is safe
        # single-threaded (our case); a concurrent thread consuming the global
        # torch RNG in that window could desync the init.
        torch.manual_seed(int(self.seed))
        projection = DimensionProjection(int(swap_dim), int(self.store_dim))
        projection.eval()
        for param in projection.parameters():
            param.requires_grad_(False)
        self._projection = projection
        self._swap_dim = int(swap_dim)

    @staticmethod
    def _l2_normalize(matrix: np.ndarray) -> np.ndarray:
        norms = np.linalg.norm(matrix, axis=1, keepdims=True)
        norms = np.where(norms > 0.0, norms, 1.0)
        return (matrix / norms).astype(np.float32)

    @staticmethod
    def _projection_checksum(projection: Any) -> str:
        digest = hashlib.sha256()
        state: Dict[str, Any] = projection.state_dict()
        for name in sorted(state.keys()):
            digest.update(name.encode("utf-8"))
            digest.update(
                np.ascontiguousarray(state[name].detach().cpu().numpy(), dtype=np.float32).tobytes()
            )
        return digest.hexdigest()
    case = load_scifact_evalsplit(seed=seed)
    print("[D1] encoding frozen arrays with cached MiniLM/mpnet models", flush=True)
    encoded = encode_scifact_case(case, seed=seed, device=device)
    Do = encoded["Do"]
    Dor = encoded["Dor"]
    Qd = encoded["Qd"]
    doc_ids = np.asarray(case["doc_ids"], dtype=str)
    query_ids = np.asarray(case["eval_query_ids"], dtype=str)

    waypoint_order = np.random.default_rng(seed).permutation(len(doc_ids))
    waypoint_fit_idx = waypoint_order[: len(doc_ids) // 2]
    eval_positive_docs = {
        doc_id
        for query_id in query_ids
        for doc_id, relevance in case["eval_qrels"][str(query_id)].items()
        if float(relevance) > 0.0
    }
    leakage_safe_fit_idx = np.asarray(
        [index for index in waypoint_order if doc_ids[index] not in eval_positive_docs][
            : len(doc_ids) // 2
        ],
        dtype=np.int64,
    )
    if len(leakage_safe_fit_idx) != len(doc_ids) // 2:
        raise RuntimeError("not enough leakage-safe document pairs")
    # The frozen canonical ladder uses the literal waypoint recipe. The separate
    # leakage-safe index is used for all new learning-curve fits and retained so
    # the recipe/contract conflict is explicit rather than silently rewritten.
    fit_idx = waypoint_fit_idx

    fields = {
        "Do": Do,
        "Dor": Dor,
        "Qd": Qd,
        "doc_ids": doc_ids,
        "query_ids": query_ids,
        "qrels": case["eval_qrels"],
    }
    scores: Dict[str, np.ndarray] = {
        "floor": _score_documents(fields, Do),
        "oracle": _score_documents(fields, Dor),
    }
    print("[D1] fitting ridge and closed-form orthogonal Procrustes", flush=True)
    ridge_map = _ridge(Do[fit_idx], Dor[fit_idx], regularization=1.0)
    procrustes_map = _procrustes(Do[fit_idx], Dor[fit_idx])
    ridge_documents = Do @ ridge_map
    procrustes_documents = Do @ procrustes_map
    scores["ridge"] = _score_documents(fields, ridge_documents)
    scores["procrustes"] = _score_documents(fields, procrustes_documents)
    print("[D1] fitting canonical residual MLP", flush=True)
    mlp_documents = _train_residual_mlp(Do, Do[fit_idx], Dor[fit_idx], seed=seed, device=device)
    scores["mlp"] = _score_documents(fields, mlp_documents)
    print("[D1] fitting canonical C3a through merged bounded-InfoNCE path", flush=True)
    c3a_documents = train_c3a_documents(
        Do,
        encoded["canonical_source"],
        encoded["canonical_target"],
        seed=seed,
    )
    scores["c3a"] = _score_documents(fields, c3a_documents)

    # Audited waypoint values use the literal unfiltered half-document permutation.
    waypoint_ridge = _score_documents(fields, Do @ _ridge(Do[waypoint_fit_idx], Dor[waypoint_fit_idx]))
    waypoint_procrustes = _score_documents(
        fields, Do @ _procrustes(Do[waypoint_fit_idx], Dor[waypoint_fit_idx])
    )
    aggregate_reference = {name: aggregate_ndcg(values) for name, values in scores.items()}
    floor = aggregate_reference["floor"]
    oracle = aggregate_reference["oracle"]
    gap = oracle - floor
    recovery = {name: (aggregate_reference[name] - floor) / gap for name in METHODS}
    waypoint_recovery = {
        "ridge": (aggregate_ndcg(waypoint_ridge) - floor) / gap,
        "procrustes": (aggregate_ndcg(waypoint_procrustes) - floor) / gap,
    }
    canonical_reference_audit = _canonical_reference_audit(recovery)
    old_scalar_audit = {
        "old_c3a_scalar_ndcg": 0.1609,
        "fixed_split_per_query_ndcg": aggregate_reference["c3a"],
        "ndcg_difference": aggregate_reference["c3a"] - 0.1609,
        "old_scalar_implied_recovery": (0.1609 - floor) / gap,
        "fixed_split_per_query_recovery": recovery["c3a"],
        "finding": (
            "The old scalar was a cross-seed campaign mean attached to the seed-42 eval split. "
            "The frozen per-query seed-42 result supersedes it."
        ),
    }

    pack = EmbeddingPack(
        Do=Do,
        Dor=Dor,
        Qd=Qd,
        doc_ids=doc_ids,
        query_ids=query_ids,
        qrels=case["eval_qrels"],
        fit_idx=fit_idx,
        per_query_scores=scores,
        extra_arrays={
            "leakage_safe_fit_idx": leakage_safe_fit_idx,
            "canonical_c3a_source": encoded["canonical_source"],
            "canonical_c3a_target": encoded["canonical_target"],
            "curve_c3a_source": encoded["curve_source"],
            "curve_c3a_target": encoded["curve_target"],
            "curve_c3a_query_ids": np.asarray(case["curve_pair_query_ids"], dtype=str),
            "method_documents__ridge": ridge_documents,
            "method_documents__procrustes": procrustes_documents,
            "method_documents__mlp": mlp_documents,
            "method_documents__c3a": c3a_documents,
        },
        metadata={
            "dataset": "SciFact",
            "seed": seed,
            "max_queries": 100,
            "sample_docs": 1200,
            "anchor_fraction": 0.4,
            "k": 10,
            "ndcg": "binary merged-harness ndcg_at_k",
            "anchor_ids": case["anchor_ids"],
            "canonical_c3a_pair_count": len(encoded["canonical_source"]),
            "learning_curve_c3a_pool": (
                "one positive doc per non-eval SciFact query; eval IDs and eval qrels excluded"
            ),
            "drift_manifest": encoded["drift_manifest"],
            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
            "harness_aggregate_ndcg": aggregate_reference,
            "recovery": recovery,
            "canonical_reference_audit": canonical_reference_audit,
            "old_scalar_audit": old_scalar_audit,
            "waypoint_audit": {
                "eval_positive_docs_total": len(eval_positive_docs),
                "literal_fit_count": len(waypoint_fit_idx),
                "eval_positive_docs_in_literal_fit": int(
                    sum(doc_ids[index] in eval_positive_docs for index in waypoint_fit_idx)
                ),
                "literal_fit_share_of_eval_positive_docs": (

codex
The requested offline assets are genuinely present: SciFact, NFCorpus, MiniLM, mpnet, and `BAAI/bge-large-en-v1.5`; CUDA sees the shared RTX 3090 with about 24.5 GB free. I’ll therefore attempt all four model/dataset regimes sequentially. The repo rulebook also requires a fresh implementation slice and a separate adversarial review, so I’m applying that rule now with tightly scoped agents and no parallel GPU work.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'rg -n "class SentenceTransformer|def embed_raw|def create_embedding_backend|SentenceTransformer" embedding_backend.py; Get-Content embedding_backend.py | Select-Object -First 230; Get-Content run_road_course_campaign.py | Select-Object -Skip 60 -First 130' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 5471ms:
45:    def embed_raw(self, texts: List[str]) -> np.ndarray:
141:    def embed_raw(self, texts: List[str]) -> np.ndarray:
262:        from sentence_transformers import SentenceTransformer
268:        self.model = SentenceTransformer(model_name, device=device)
282:    def embed_raw(self, texts: List[str]) -> np.ndarray:
304:def create_embedding_backend(model_name: str, logger=None) -> EmbeddingBackend:
"""
Embedding Backend Abstraction (F-045)

Provides a clean interface for different embedding providers (Ollama, local models),
extracting the branching logic from AntigravityEngine.
"""

import numpy as np
import torch
from abc import ABC, abstractmethod
from typing import List, Optional
from chelation_logger import get_logger
from config import ChelationConfig

# Safe import for requests (used in Ollama mode)
try:
    import requests
    REQUESTS_AVAILABLE = True
except ImportError:
    requests = None
    REQUESTS_AVAILABLE = False


class EmbeddingBackend(ABC):
    """Abstract base class for embedding backends."""
    
    def __init__(self, model_name: str, logger=None):
        """
        Initialize embedding backend.
        
        Args:
            model_name: Name/identifier of the embedding model
            logger: Optional logger instance (creates new if None)
        """
        self.model_name = model_name
        self.logger = logger or get_logger()
        self._vector_size = None
    
    @abstractmethod
    def get_vector_size(self) -> int:
        """Return the embedding dimension size."""
        pass
    
    @abstractmethod
    def embed_raw(self, texts: List[str]) -> np.ndarray:
        """
        Generate raw embeddings for texts.
        
        Args:
            texts: List of text strings to embed
            
        Returns:
            numpy array of shape [len(texts), vector_size], dtype float32
        """
        pass
    
    @property
    def vector_size(self) -> int:
        """Get cached vector size."""
        if self._vector_size is None:
            self._vector_size = self.get_vector_size()
        return self._vector_size


class OllamaEmbeddingBackend(EmbeddingBackend):
    """Ollama embedding backend using HTTP API."""
    
    def __init__(self, model_name: str, logger=None):
        """
        Initialize Ollama embedding backend.
        
        Args:
            model_name: Ollama model name (e.g., 'nomic-embed-text')
            logger: Optional logger instance
        """
        super().__init__(model_name, logger)
        
        # Check for requests availability
        if not REQUESTS_AVAILABLE:
            raise ImportError("'requests' library required for Ollama mode. Install with: pip install requests")
        
        self.ollama_url = ChelationConfig.OLLAMA_URL
        self.logger.log_event(
            "embedding_backend_init",
            f"Initializing Ollama backend: {model_name}",
            backend="ollama",
            model_name=model_name,
            url=self.ollama_url
        )
        
        # Initialize vector size with fallback
        self._vector_size = ChelationConfig.DEFAULT_VECTOR_SIZE
        try:
            test_vec = self.embed_raw(["test"])[0]
            self._vector_size = len(test_vec)
            self.logger.log_event(
                "embedding_backend_ready",
                f"Ollama backend connected. Vector size: {self._vector_size}",
                vector_size=self._vector_size
            )
        except (requests.exceptions.ConnectionError, requests.exceptions.Timeout) as e:
            raise ConnectionError(
                f"Failed to connect to Ollama at {self.ollama_url}. Make sure Docker container is running!"
            ) from e
        except (requests.exceptions.RequestException, KeyError, ValueError, TypeError, IndexError) as e:
            self.logger.log_error(
                "connection",
                f"Ollama connection test failed: {e}",
                exception=e
            )
            self.logger.log_event(
                "initialization",
                "Vector size will be validated on first real embedding call",
                level="WARNING"
            )
            # Keep default as fallback
    
    def get_vector_size(self) -> int:
        """Return the embedding dimension size."""
        return self._vector_size
    
    def _sanitize_text(self, text: str, doc_index: Optional[int] = None) -> str:
        """Sanitize embedding input text for Ollama requests."""
        if not isinstance(text, str):
            text = str(text)
        
        if len(text) > ChelationConfig.OLLAMA_INPUT_MAX_CHARS:
            self.logger.log_event(
                "embedding_input_truncated",
                f"Input text exceeded max length ({ChelationConfig.OLLAMA_INPUT_MAX_CHARS}), truncating.",
                level="DEBUG",
                doc_index=doc_index,
                original_length=len(text),
                truncated_length=ChelationConfig.OLLAMA_INPUT_MAX_CHARS,
            )
            text = text[:ChelationConfig.OLLAMA_INPUT_MAX_CHARS]
        
        # Replace non-printable control chars (except whitespace controls)
        return "".join(c if (c.isprintable() or c in "\n\r\t") else " " for c in text)
    
    def embed_raw(self, texts: List[str]) -> np.ndarray:
        """
        Generate raw embeddings using Ollama API.
        
        Args:
            texts: List of text strings to embed
            
        Returns:
            numpy array of shape [len(texts), vector_size], dtype float32
        """
        if not texts:
            return np.array([])
        
        embeddings = [None] * len(texts)
        
        def _get_embedding(i, txt):
            txt = self._sanitize_text(txt, doc_index=i)
            
            # Helper to attempt embedding
            def attempt(t):
                try:
                    res = requests.post(
                        self.ollama_url,
                        json={
                            "model": self.model_name,
                            "prompt": t,
                            "options": {"num_ctx": ChelationConfig.OLLAMA_NUM_CTX}
                        },
                        timeout=ChelationConfig.OLLAMA_TIMEOUT
                    )
                    if res.status_code == 200:
                        return res.json()["embedding"]
                    else:
                        # 500 error usually means context limit or model error
                        return None
                except requests.exceptions.Timeout:
                    self.logger.log_error("timeout", f"Ollama timeout for doc {i}", doc_index=i)
                    return None
                except requests.exceptions.ConnectionError:
                    self.logger.log_error("connection", f"Ollama connection lost for doc {i}", doc_index=i)
                    return None
                except KeyError as e:
                    self.logger.log_error(
                        "api_response",
                        f"Ollama response missing 'embedding' key for doc {i}",
                        exception=e,
                        doc_index=i
                    )
                    return None
                except Exception as e:
                    self.logger.log_error(
                        "embedding",
                        f"Ollama unexpected error for doc {i}",
                        exception=e,
                        doc_index=i
                    )
                    return None
            
            # Try truncation levels from config
            for limit in ChelationConfig.OLLAMA_TRUNCATION_LIMITS:
                current_text = txt[:limit]
                emb = attempt(current_text)
                if emb is not None:
                    break
            
            if emb is None:
                self.logger.log_error(
                    "embedding_failed",
                    f"Failed to embed doc {i} after retries",
                    doc_index=i
                )
                return i, np.zeros(self.vector_size, dtype=np.float32)
            
            return i, np.array(emb, dtype=np.float32)
        
        from concurrent.futures import ThreadPoolExecutor, TimeoutError
        with ThreadPoolExecutor(max_workers=ChelationConfig.OLLAMA_MAX_WORKERS) as executor:
            futures = [executor.submit(_get_embedding, i, txt) for i, txt in enumerate(texts)]
            for idx, future in enumerate(futures):
                try:
                    _, emb = future.result(timeout=ChelationConfig.OLLAMA_TIMEOUT)
                    embeddings[idx] = emb
                except TimeoutError:
                    self.logger.log_error(
                        "timeout",
                        f"Embedding timeout for document {idx}, using zero vector",
                        doc_index=idx
                    )
                    embeddings[idx] = np.zeros(self.vector_size, dtype=np.float32)
                except Exception as e:
    RoadCourseProfile("centered_p50_temp1", use_centering=True, chelation_p=50, chelation_threshold=0.0),
]


ATTNRES_COMPARISON_GRID = [
    RoadCourseProfile("baseline"),
    RoadCourseProfile("adaptive_p85_t0.01", use_quantization=True, chelation_p=85, chelation_threshold=0.01),
    RoadCourseProfile("attnres_baseline", adapter_type="attnres"),
    RoadCourseProfile(
        "attnres_balanced_p85_t0.01",
        use_quantization=True,
        chelation_p=85,
        chelation_threshold=0.01,
        adapter_type="attnres",
    ),
]


ATTNRES_NUM_BLOCKS_GRID = [
    RoadCourseProfile("baseline"),
    RoadCourseProfile("attnres_shallow", adapter_type="attnres", attnres_num_blocks=2),
    RoadCourseProfile("attnres_balanced", adapter_type="attnres", attnres_num_blocks=4),
    RoadCourseProfile("attnres_deep", adapter_type="attnres", attnres_num_blocks=8),
]


ATTNRES_TRAINED_NUM_BLOCKS_GRID = [
    RoadCourseProfile("baseline"),
    RoadCourseProfile(
        "mlp_trained",
        sedimentation_epochs=2,
        sedimentation_threshold=1,
        sedimentation_warmup_queries=20,
    ),
    RoadCourseProfile(
        "attnres_shallow_trained",
        adapter_type="attnres",
        attnres_num_blocks=2,
        sedimentation_epochs=2,
        sedimentation_threshold=1,
        sedimentation_warmup_queries=20,
    ),
    RoadCourseProfile(
        "attnres_balanced_trained",
        adapter_type="attnres",
        attnres_num_blocks=4,
        sedimentation_epochs=2,
        sedimentation_threshold=1,
        sedimentation_warmup_queries=20,
    ),
    RoadCourseProfile(
        "attnres_deep_trained",
        adapter_type="attnres",
        attnres_num_blocks=8,
        sedimentation_epochs=2,
        sedimentation_threshold=1,
        sedimentation_warmup_queries=20,
    ),
]


ATTNRES_BALANCED_CANDIDATE_GRID = [
    RoadCourseProfile("baseline"),
    RoadCourseProfile(
        "mlp_trained",
        sedimentation_epochs=2,
        sedimentation_threshold=1,
        sedimentation_warmup_queries=20,
    ),
    RoadCourseProfile(
        "attnres_balanced_trained",
        adapter_type="attnres",
        attnres_num_blocks=4,
        sedimentation_epochs=2,
        sedimentation_threshold=1,
        sedimentation_warmup_queries=20,
    ),
]


PROFILE_SETS = {
    "default": DEFAULT_PROFILE_GRID,
    "attnres_comparison": ATTNRES_COMPARISON_GRID,
    "attnres_num_blocks": ATTNRES_NUM_BLOCKS_GRID,
    "attnres_trained_num_blocks": ATTNRES_TRAINED_NUM_BLOCKS_GRID,
    "attnres_balanced_candidate": ATTNRES_BALANCED_CANDIDATE_GRID,
}


@contextmanager
def _temporary_adapter_config(
    adapter_type: str,
    attnres_num_blocks: int,
    attnres_proj_dim: int | None,
) -> Iterator[None]:
    """Temporarily select adapter factory settings for engine construction."""

    original_adapter_type = ChelationConfig.ADAPTER_TYPE
    original_num_blocks = ChelationConfig.ATTNRES_ADAPTER_NUM_BLOCKS
    original_proj_dim = ChelationConfig.ATTNRES_ADAPTER_PROJ_DIM
    try:
        ChelationConfig.ADAPTER_TYPE = adapter_type
        ChelationConfig.ATTNRES_ADAPTER_NUM_BLOCKS = attnres_num_blocks
        ChelationConfig.ATTNRES_ADAPTER_PROJ_DIM = attnres_proj_dim
        yield
    finally:
        ChelationConfig.ADAPTER_TYPE = original_adapter_type
        ChelationConfig.ATTNRES_ADAPTER_NUM_BLOCKS = original_num_blocks
        ChelationConfig.ATTNRES_ADAPTER_PROJ_DIM = original_proj_dim


def select_road_course_slice(
    corpus: Mapping[Any, str],
    queries: Mapping[Any, str],
    qrels: Mapping[Any, Mapping[Any, float]],
    max_queries: int,
    sample_docs: int,
    seed: int,
) -> tuple[Dict[str, str], Dict[str, str], Dict[str, Dict[str, float]]]:
    """Select a deterministic small slice while preserving relevant documents."""

    selected_queries: Dict[str, str] = {}
    selected_qrels: Dict[str, Dict[str, float]] = {}
    for query_id, query_text in queries.items():
        canonical_query_id = canonicalize_id(query_id)
        relevance = {
            canonicalize_id(doc_id): float(score)
            for doc_id, score in qrels.get(query_id, qrels.get(canonical_query_id, {})).items()
            if float(score) > 0
        }

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content run_road_course_campaign.py | Select-Object -Skip 210 -First 100; Get-Content embedding_backend.py | Select-Object -Skip 240 -First 105' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
2026-07-10T03:14:41.362875Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 10.4 seconds
Output:
command timed out after 10386 milliseconds
        sampled_extra = set(remaining)
    keep_ids = sorted(required_ids | sampled_extra)
    selected_corpus = {doc_id: corpus_by_id[doc_id] for doc_id in keep_ids}
    return selected_corpus, selected_queries, selected_qrels


def evaluate_rankings(
    rankings: Mapping[str, Sequence[str]],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> Dict[str, float]:
    ndcg_scores = []
    map_scores = []
    mrr_scores = []
    recall_scores = []
    for query_id, relevance in qrels.items():
        relevant_ids = {doc_id for doc_id, score in relevance.items() if score > 0}
        retrieved = [canonicalize_id(doc_id) for doc_id in rankings.get(query_id, [])[:k]]
        relevance_by_rank = [1 if doc_id in relevant_ids else 0 for doc_id in retrieved]
        ndcg_scores.append(float(ndcg_at_k(relevance_by_rank, k)))
        map_scores.append(float(mean_average_precision_at_k(retrieved, relevant_ids, k)))
        mrr_scores.append(float(mean_reciprocal_rank(retrieved, relevant_ids)))
        recall_scores.append(float(recall_at_k(retrieved, relevant_ids, k)))
    return {
        "ndcg_at_10": float(np.mean(ndcg_scores)) if ndcg_scores else 0.0,
        "map_at_10": float(np.mean(map_scores)) if map_scores else 0.0,
        "mrr": float(np.mean(mrr_scores)) if mrr_scores else 0.0,
        "recall_at_10": float(np.mean(recall_scores)) if recall_scores else 0.0,
        "evaluated_queries": float(len(ndcg_scores)),
    }


def _profile_engine(
    profile: RoadCourseProfile,
    model_name: str,
    corpus: Mapping[str, str],
):
    with _temporary_adapter_config(
        profile.adapter_type,
        profile.attnres_num_blocks,
        profile.attnres_proj_dim,
    ):
        engine = AntigravityEngine(
            qdrant_location=":memory:",
            model_name=model_name,
            use_centering=profile.use_centering,
            use_quantization=profile.use_quantization,
            store_full_text_payload=True,
        )
    engine.chelation_p = profile.chelation_p
    engine.chelation_threshold = profile.chelation_threshold
    if profile.temperature != 1.0:
        engine.set_temperature(profile.temperature)
    if profile.query_reformulation_variants > 0:
        engine.enable_query_reformulation(
            max_variants=profile.query_reformulation_variants,
            policy=profile.query_reformulation_policy,
        )
    doc_ids = list(corpus.keys())
    engine.ingest([corpus[doc_id] for doc_id in doc_ids], [{"doc_id": doc_id} for doc_id in doc_ids])
    return engine


def _run_sedimentation_warmup(
    engine,
    profile: RoadCourseProfile,
    queries: Mapping[str, str],
) -> Dict[str, Any] | None:
    if profile.sedimentation_epochs <= 0:
        return None

    warmup_limit = profile.sedimentation_warmup_queries or len(queries)
    warmup_items = list(queries.items())[:warmup_limit]
    original_use_centering = engine.use_centering
    try:
        engine.use_centering = True
        for _query_id, query_text in warmup_items:
            engine.run_inference(query_text)
    finally:
        engine.use_centering = original_use_centering

    candidates_before = len(engine.chelation_log)
    events_before = sum(len(events) for events in engine.chelation_log.values())
    start = time.perf_counter()
    engine.run_sedimentation_cycle(
        threshold=profile.sedimentation_threshold,
        learning_rate=profile.sedimentation_learning_rate,
        epochs=profile.sedimentation_epochs,
    )
    return {
        "warmup_query_count": len(warmup_items),
        "candidates_before": candidates_before,
        "events_before": events_before,
        "epochs": profile.sedimentation_epochs,
        "threshold": profile.sedimentation_threshold,
        "learning_rate": profile.sedimentation_learning_rate,
        "elapsed_seconds": time.perf_counter() - start,
    }



class LocalEmbeddingBackend(EmbeddingBackend):
    """Local sentence-transformers embedding backend."""
    
    def __init__(self, model_name: str, logger=None):
        """
        Initialize local embedding backend.
        
        Args:
            model_name: Sentence-transformers model name
            logger: Optional logger instance
        """
        super().__init__(model_name, logger)
        
        self.logger.log_event(
            "embedding_backend_init",
            f"Initializing local backend: {model_name}",
            backend="local",
            model_name=model_name
        )
        
        from sentence_transformers import SentenceTransformer
        
        device = 'cuda' if torch.cuda.is_available() else 'cpu'
        self.logger.log_event("initialization", f"Device Selected: {device}", device=device)
        
        # Load model
        self.model = SentenceTransformer(model_name, device=device)
        self._vector_size = self.model.get_sentence_embedding_dimension()
        
        self.logger.log_event(
            "embedding_backend_ready",
            f"Local backend loaded. Vector size: {self._vector_size}",
            device=str(self.model.device),
            vector_size=self._vector_size
        )
    
    def get_vector_size(self) -> int:
        """Return the embedding dimension size."""
        return self._vector_size
    
    def embed_raw(self, texts: List[str]) -> np.ndarray:
        """
        Generate raw embeddings using local model.
        
        Args:
            texts: List of text strings to embed
            
        Returns:
            numpy array of shape [len(texts), vector_size], dtype float32
        """
        if not texts:
            return np.array([])
        
        embeddings = self.model.encode(
            texts,
            convert_to_numpy=True,
            show_progress_bar=False
        )
        
        return embeddings.astype(np.float32)


def create_embedding_backend(model_name: str, logger=None) -> EmbeddingBackend:
    """
    Factory function to create appropriate embedding backend.
    
    Args:
        model_name: Model identifier, prefix with 'ollama:' for Ollama mode
        logger: Optional logger instance
        
    Returns:
        EmbeddingBackend instance (OllamaEmbeddingBackend or LocalEmbeddingBackend)
        
    Examples:
        >>> backend = create_embedding_backend("ollama:nomic-embed-text")
        >>> backend = create_embedding_backend("all-MiniLM-L6-v2")
    """
    if model_name.startswith("ollama:"):
        # Ollama mode - strip prefix
        actual_model_name = model_name.replace("ollama:", "")
        return OllamaEmbeddingBackend(actual_model_name, logger)
    else:
        # Local mode
        return LocalEmbeddingBackend(model_name, logger)

 exited 124 in 10386ms:
        sampled_extra = set(remaining)
    keep_ids = sorted(required_ids | sampled_extra)
    selected_corpus = {doc_id: corpus_by_id[doc_id] for doc_id in keep_ids}
    return selected_corpus, selected_queries, selected_qrels


def evaluate_rankings(
    rankings: Mapping[str, Sequence[str]],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> Dict[str, float]:
    ndcg_scores = []
    map_scores = []
    mrr_scores = []
    recall_scores = []
    for query_id, relevance in qrels.items():
        relevant_ids = {doc_id for doc_id, score in relevance.items() if score > 0}
        retrieved = [canonicalize_id(doc_id) for doc_id in rankings.get(query_id, [])[:k]]
        relevance_by_rank = [1 if doc_id in relevant_ids else 0 for doc_id in retrieved]
        ndcg_scores.append(float(ndcg_at_k(relevance_by_rank, k)))
        map_scores.append(float(mean_average_precision_at_k(retrieved, relevant_ids, k)))
        mrr_scores.append(float(mean_reciprocal_rank(retrieved, relevant_ids)))
        recall_scores.append(float(recall_at_k(retrieved, relevant_ids, k)))
    return {
        "ndcg_at_10": float(np.mean(ndcg_scores)) if ndcg_scores else 0.0,
        "map_at_10": float(np.mean(map_scores)) if map_scores else 0.0,
        "mrr": float(np.mean(mrr_scores)) if mrr_scores else 0.0,
        "recall_at_10": float(np.mean(recall_scores)) if recall_scores else 0.0,
        "evaluated_queries": float(len(ndcg_scores)),
    }


def _profile_engine(
    profile: RoadCourseProfile,
    model_name: str,
    corpus: Mapping[str, str],
):
    with _temporary_adapter_config(
        profile.adapter_type,
        profile.attnres_num_blocks,
        profile.attnres_proj_dim,
    ):
        engine = AntigravityEngine(
            qdrant_location=":memory:",
            model_name=model_name,
            use_centering=profile.use_centering,
            use_quantization=profile.use_quantization,
            store_full_text_payload=True,
        )
    engine.chelation_p = profile.chelation_p
    engine.chelation_threshold = profile.chelation_threshold
    if profile.temperature != 1.0:
        engine.set_temperature(profile.temperature)
    if profile.query_reformulation_variants > 0:
        engine.enable_query_reformulation(
            max_variants=profile.query_reformulation_variants,
            policy=profile.query_reformulation_policy,
        )
    doc_ids = list(corpus.keys())
    engine.ingest([corpus[doc_id] for doc_id in doc_ids], [{"doc_id": doc_id} for doc_id in doc_ids])
    return engine


def _run_sedimentation_warmup(
    engine,
    profile: RoadCourseProfile,
    queries: Mapping[str, str],
) -> Dict[str, Any] | None:
    if profile.sedimentation_epochs <= 0:
        return None

    warmup_limit = profile.sedimentation_warmup_queries or len(queries)
    warmup_items = list(queries.items())[:warmup_limit]
    original_use_centering = engine.use_centering
    try:
        engine.use_centering = True
        for _query_id, query_text in warmup_items:
            engine.run_inference(query_text)
    finally:
        engine.use_centering = original_use_centering

    candidates_before = len(engine.chelation_log)
    events_before = sum(len(events) for events in engine.chelation_log.values())
    start = time.perf_counter()
    engine.run_sedimentation_cycle(
        threshold=profile.sedimentation_threshold,
        learning_rate=profile.sedimentation_learning_rate,
        epochs=profile.sedimentation_epochs,
    )
    return {
        "warmup_query_count": len(warmup_items),
        "candidates_before": candidates_before,
        "events_before": events_before,
        "epochs": profile.sedimentation_epochs,
        "threshold": profile.sedimentation_threshold,
        "learning_rate": profile.sedimentation_learning_rate,
        "elapsed_seconds": time.perf_counter() - start,
    }



class LocalEmbeddingBackend(EmbeddingBackend):
    """Local sentence-transformers embedding backend."""
    
    def __init__(self, model_name: str, logger=None):
        """
        Initialize local embedding backend.
        
        Args:
            model_name: Sentence-transformers model name
            logger: Optional logger instance
        """
        super().__init__(model_name, logger)
        
        self.logger.log_event(
            "embedding_backend_init",
            f"Initializing local backend: {model_name}",
            backend="local",
            model_name=model_name
        )
        
        from sentence_transformers import SentenceTransformer
        
        device = 'cuda' if torch.cuda.is_available() else 'cpu'
        self.logger.log_event("initialization", f"Device Selected: {device}", device=device)
        
        # Load model
        self.model = SentenceTransformer(model_name, device=device)
        self._vector_size = self.model.get_sentence_embedding_dimension()
        
        self.logger.log_event(
            "embedding_backend_ready",
            f"Local backend loaded. Vector size: {self._vector_size}",
            device=str(self.model.device),
            vector_size=self._vector_size
        )
    
    def get_vector_size(self) -> int:
        """Return the embedding dimension size."""
        return self._vector_size
    
    def embed_raw(self, texts: List[str]) -> np.ndarray:
        """
        Generate raw embeddings using local model.
        
        Args:
            texts: List of text strings to embed
            
        Returns:
            numpy array of shape [len(texts), vector_size], dtype float32
        """
        if not texts:
            return np.array([])
        
        embeddings = self.model.encode(
            texts,
            convert_to_numpy=True,
            show_progress_bar=False
        )
        
        return embeddings.astype(np.float32)


def create_embedding_backend(model_name: str, logger=None) -> EmbeddingBackend:
    """
    Factory function to create appropriate embedding backend.
    
    Args:
        model_name: Model identifier, prefix with 'ollama:' for Ollama mode
        logger: Optional logger instance
        
    Returns:
        EmbeddingBackend instance (OllamaEmbeddingBackend or LocalEmbeddingBackend)
        
    Examples:
        >>> backend = create_embedding_backend("ollama:nomic-embed-text")
        >>> backend = create_embedding_backend("all-MiniLM-L6-v2")
    """
    if model_name.startswith("ollama:"):
        # Ollama mode - strip prefix
        actual_model_name = model_name.replace("ollama:", "")
        return OllamaEmbeddingBackend(actual_model_name, logger)
    else:
        # Local mode
        return LocalEmbeddingBackend(model_name, logger)

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\estimator\__init__.py
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\estimator\features.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/estimator/margin_bound.py; git status --short research/drift_recovery' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 3576ms:
"""Order-preservation bound for the drift-recovery estimator.

For a query ``q``, relevant document ``r``, and strongest non-relevant
competitor ``j``, the clean oracle margin is sufficient to preserve their
order when

``clean_margin > ||q|| * (||e_r|| + ||e_j||)``.

A violation of this sufficient condition is an *inversion prediction*, not
proof that the corrected ranking is inverted.  The functions here implement
that distinction directly and operate only on NumPy values.
"""

from __future__ import annotations

from typing import Union

import numpy as np
from numpy.typing import ArrayLike


NumericResult = Union[np.ndarray, np.generic]


def _finite_array(value: ArrayLike, name: str) -> np.ndarray:
    """Convert ``value`` to a finite floating-point NumPy array."""

    try:
        array = np.asarray(value, dtype=np.float64)
    except (TypeError, ValueError) as exc:
        raise ValueError(f"{name} must be numeric") from exc
    if not np.all(np.isfinite(array)):
        raise ValueError(f"{name} must contain only finite values")
    return array


def _norm_array(value: ArrayLike, name: str) -> np.ndarray:
    """Convert ``value`` to a finite array of nonnegative norms."""

    array = _finite_array(value, name)
    if np.any(array < 0.0):
        raise ValueError(f"{name} must be nonnegative")
    return array


def order_preservation_slack(
    clean_margin: ArrayLike,
    query_norm: ArrayLike,
    relevant_error_norm: ArrayLike,
    competitor_error_norm: ArrayLike,
) -> NumericResult:
    """Return the amount by which the sufficient bound is satisfied.

    The returned value is

    ``clean_margin - query_norm * (relevant_error_norm + competitor_error_norm)``.

    Positive slack guarantees order preservation under the stated bound.
    Zero or negative slack means only that the bound is violated.  Inputs use
    normal NumPy broadcasting, making the function suitable for either one
    hand-checkable query or a vector of per-query quantities.

    Raises:
        ValueError: If an input is non-numeric/non-finite, if a norm is
            negative, if shapes cannot broadcast, or if arithmetic overflows.
    """

    margin = _finite_array(clean_margin, "clean_margin")
    q_norm = _norm_array(query_norm, "query_norm")
    relevant_norm = _norm_array(relevant_error_norm, "relevant_error_norm")
    competitor_norm = _norm_array(competitor_error_norm, "competitor_error_norm")

    try:
        with np.errstate(over="raise", invalid="raise"):
            error_sum = np.add(relevant_norm, competitor_norm)
            penalty = np.multiply(q_norm, error_sum)
            slack = np.subtract(margin, penalty)
    except (FloatingPointError, ValueError) as exc:
        raise ValueError("margin-bound inputs must broadcast to finite results") from exc

    if not np.all(np.isfinite(slack)):
        raise ValueError("margin-bound arithmetic produced a non-finite result")
    return slack


def order_is_preserved(
    clean_margin: ArrayLike,
    query_norm: ArrayLike,
    relevant_error_norm: ArrayLike,
    competitor_error_norm: ArrayLike,
) -> NumericResult:
    """Return where the strict sufficient order-preservation bound holds."""

    return order_preservation_slack(
        clean_margin,
        query_norm,
        relevant_error_norm,
        competitor_error_norm,
    ) > 0.0


def predict_inversion(
    clean_margin: ArrayLike,
    query_norm: ArrayLike,
    relevant_error_norm: ArrayLike,
    competitor_error_norm: ArrayLike,
) -> NumericResult:
    """Predict bound violations, including equality, query by query.

    ``True`` means

    ``query_norm * (relevant_error_norm + competitor_error_norm) >= clean_margin``.

    This is deliberately the logical complement of :func:`order_is_preserved`.
    It identifies queries without a preservation guarantee; it does not assert
    that their actual corrected retrieval order is inverted.
    """

    return order_preservation_slack(
        clean_margin,
        query_norm,
        relevant_error_norm,
        competitor_error_norm,
    ) <= 0.0


__all__ = [
    "order_is_preserved",
    "order_preservation_slack",
    "predict_inversion",
]

?? research/drift_recovery/

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\estimator\calibration.py
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\estimator\predictor.py
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\estimator\validation.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'rg -n "def _split_anchor_eval" run_drift_recovery_experiment.py; Get-Content run_drift_recovery_experiment.py | Select-Object -Skip 210 -First 85; Get-Content run_road_course_campaign.py | Select-Object -Skip 170 -First 60' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 3296ms:
316:def _split_anchor_eval(
                },
                "recovery": tracker.to_json(),
                "correction_norm_stats": _aggregate_norm_stats(correction_norms),
                "anchor_eval_split": {
                    "active": split_active,
                    "anchor_count": len(anchor_ids),
                    "eval_count": len(eval_ids),
                    "anchor_ids": list(anchor_ids),
                    "eval_ids": list(eval_ids),
                },
                "cycle_errors": [],
                "wall_clock_seconds": time.perf_counter() - started,
                "doc_ids": doc_ids,
            }
        finally:
            if hasattr(engine, "close"):
                engine.close()

    _write_json(config.output, result)
    return result


def evaluate_engine(engine, queries: Mapping[str, str], qrels: Mapping[str, Mapping[str, float]], k: int = 10) -> Tuple[float, list]:
    rows = []
    for query_id, query_text in queries.items():
        relevance = qrels.get(query_id, qrels.get(canonicalize_id(query_id), {}))
        relevant_ids = [canonicalize_id(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0]
        if not relevant_ids:
            continue
        _std_top, chelated_top, _mask, _jaccard = engine.run_inference(str(query_text))
        ranked = map_predicted_ids(engine, chelated_top[:k])
        score = ndcg_at_k(ranked, relevant_ids, k=k)
        rows.append(
            {
                "query_id": canonicalize_id(query_id),
                "ndcg": score,
                "ranked_ids": ranked,
                "relevant_ids": relevant_ids,
            }
        )
    mean_ndcg = float(statistics.fmean(row["ndcg"] for row in rows)) if rows else 0.0
    return mean_ndcg, rows


def evaluate_engine_with_query_vectors(
    engine,
    query_vectors_by_id: Mapping[str, np.ndarray],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> Tuple[float, list]:
    """Score retrieval using precomputed query vectors instead of engine embeddings.

    Mirrors ``evaluate_engine`` exactly (same row shape, same ID mapping, same
    ndcg_at_k scoring) but searches the store by a caller-supplied vector. Used
    for the query-encoder-swap arena, where eval queries live in the swapped
    encoder's space while the cached doc vectors stay in the original space.
    """
    rows = []
    for query_id, query_vector in query_vectors_by_id.items():
        relevance = qrels.get(query_id, qrels.get(canonicalize_id(query_id), {}))
        relevant_ids = [canonicalize_id(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0]
        if not relevant_ids:
            continue
        hits = engine.qdrant.query_points(
            collection_name=engine.collection_name,
            query=np.asarray(query_vector, dtype=np.float32),
            limit=k,
            with_payload=False,
            with_vectors=False,
        ).points
        ranked = map_predicted_ids(engine, [hit.id for hit in hits])
        score = ndcg_at_k(ranked, relevant_ids, k=k)
        rows.append(
            {
                "query_id": canonicalize_id(query_id),
                "ndcg": score,
                "ranked_ids": ranked,
                "relevant_ids": relevant_ids,
            }
        )
    mean_ndcg = float(statistics.fmean(row["ndcg"] for row in rows)) if rows else 0.0
    return mean_ndcg, rows


def _build_query_encoder_drift(engine, queries: Mapping[str, str], config: DriftRecoveryConfig):

def select_road_course_slice(
    corpus: Mapping[Any, str],
    queries: Mapping[Any, str],
    qrels: Mapping[Any, Mapping[Any, float]],
    max_queries: int,
    sample_docs: int,
    seed: int,
) -> tuple[Dict[str, str], Dict[str, str], Dict[str, Dict[str, float]]]:
    """Select a deterministic small slice while preserving relevant documents."""

    selected_queries: Dict[str, str] = {}
    selected_qrels: Dict[str, Dict[str, float]] = {}
    for query_id, query_text in queries.items():
        canonical_query_id = canonicalize_id(query_id)
        relevance = {
            canonicalize_id(doc_id): float(score)
            for doc_id, score in qrels.get(query_id, qrels.get(canonical_query_id, {})).items()
            if float(score) > 0
        }
        if not relevance:
            continue
        selected_queries[canonical_query_id] = str(query_text)
        selected_qrels[canonical_query_id] = relevance
        if len(selected_queries) >= max_queries:
            break

    corpus_by_id = {canonicalize_id(doc_id): str(text) for doc_id, text in corpus.items()}
    required_ids = {
        doc_id
        for relevance in selected_qrels.values()
        for doc_id in relevance
        if doc_id in corpus_by_id
    }
    remaining = [doc_id for doc_id in corpus_by_id if doc_id not in required_ids]
    rng = np.random.RandomState(seed)
    extra_budget = max(0, sample_docs - len(required_ids))
    if extra_budget < len(remaining):
        sampled_extra = set(rng.choice(remaining, size=extra_budget, replace=False))
    else:
        sampled_extra = set(remaining)
    keep_ids = sorted(required_ids | sampled_extra)
    selected_corpus = {doc_id: corpus_by_id[doc_id] for doc_id in keep_ids}
    return selected_corpus, selected_queries, selected_qrels


def evaluate_rankings(
    rankings: Mapping[str, Sequence[str]],
    qrels: Mapping[str, Mapping[str, float]],
    k: int = 10,
) -> Dict[str, float]:
    ndcg_scores = []
    map_scores = []
    mrr_scores = []
    recall_scores = []
    for query_id, relevance in qrels.items():
        relevant_ids = {doc_id for doc_id, score in relevance.items() if score > 0}
        retrieved = [canonicalize_id(doc_id) for doc_id in rankings.get(query_id, [])[:k]]
        relevance_by_rank = [1 if doc_id in relevant_ids else 0 for doc_id in retrieved]
        ndcg_scores.append(float(ndcg_at_k(relevance_by_rank, k)))

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content run_drift_recovery_experiment.py | Select-Object -Skip 310 -First 65' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 5404ms:
        canonicalize_id(query_id): drifted[index] for index, query_id in enumerate(query_ids)
    }
    return query_drift, drifted_query_vectors


def _split_anchor_eval(
    queries: Mapping[str, str],
    anchor_fraction: float,
    seed: int,
    active: bool,
) -> Tuple[list, list]:
    """Deterministically split query ids into disjoint (anchor, eval) lists.

    When ``active`` is False the anchor list is empty and every query is an eval
    query — preserving the pre-PR-A2b behavior bit-for-bit. When active, ids are
    sorted then shuffled by a ``seed``-derived RNG so the split is reproducible
    and independent of dict iteration order. At least one anchor and one eval id
    are guaranteed when there are >= 2 queries.
    """
    ordered_ids = sorted(canonicalize_id(qid) for qid in queries.keys())
    if not active or float(anchor_fraction) <= 0.0:
        return [], ordered_ids
    rng = random.Random(seed)
    shuffled = list(ordered_ids)
    rng.shuffle(shuffled)
    total = len(shuffled)
    anchor_n = int(round(total * float(anchor_fraction)))
    anchor_n = max(1, min(anchor_n, total - 1)) if total >= 2 else 0
    anchor_ids = sorted(shuffled[:anchor_n])
    eval_ids = sorted(shuffled[anchor_n:])
    return anchor_ids, eval_ids


def _build_anchor_pairs(
    engine,
    query_drift: Any,
    queries: Mapping[str, str],
    qrels: Mapping[str, Mapping[str, float]],
    anchor_ids: Sequence[str],
) -> list:
    """Build supervised anchor pairs (cached doc vector, drifted query vector).

    For every anchor query and each of its relevant docs, the positive pair is
    the doc's CACHED stored vector (fetched from the store by canonical doc_id)
    and the anchor query's DRIFTED vector (re-embedded with the swap encoder).
    These pairs encode the pre-drift relevance the adapter must re-align toward.
    """
    doc_vectors = _doc_vectors_by_id(engine)
    anchor_id_list = list(anchor_ids)
    anchor_texts = [str(queries[qid]) for qid in anchor_id_list]
    if not anchor_texts:
        return []
    drifted = query_drift.embed_queries(anchor_texts)
    drifted_by_id = {
        canonicalize_id(qid): drifted[index] for index, qid in enumerate(anchor_id_list)
    }
    pairs = []
    for qid in anchor_id_list:
        canonical_qid = canonicalize_id(qid)
        relevance = qrels.get(qid, qrels.get(canonical_qid, {}))
        query_vector = drifted_by_id[canonical_qid]
        for doc_id, score in relevance.items():
            if float(score) <= 0.0:
                continue
            doc_vector = doc_vectors.get(canonicalize_id(doc_id))

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\harness_bridge.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content -Raw research/drift_recovery/stats/paired_bootstrap.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1097ms:
"""Deterministic paired-query bootstrap for frozen per-query retrieval scores."""

from __future__ import annotations

from dataclasses import dataclass
from typing import Dict, Mapping, Optional, Sequence, Tuple

import numpy as np


@dataclass(frozen=True)
class BootstrapDraws:
    indices: np.ndarray
    seed: int

    @classmethod
    def create(cls, query_count: int, draws: int = 10_000, seed: int = 20260630) -> "BootstrapDraws":
        if query_count < 2:
            raise ValueError("paired bootstrap requires at least two queries")
        if draws < 100:
            raise ValueError("draws must be >= 100")
        rng = np.random.default_rng(seed)
        return cls(indices=rng.integers(0, query_count, size=(draws, query_count)), seed=seed)


def _ci(values: np.ndarray, confidence: float) -> Tuple[float, float]:
    alpha = 1.0 - confidence
    lower, upper = np.quantile(values, [alpha / 2.0, 1.0 - alpha / 2.0])
    return float(lower), float(upper)


def _metric_row(point: float, samples: np.ndarray, confidence: float) -> dict:
    lower, upper = _ci(samples, confidence)
    return {
        "estimate": float(point),
        "ci_low": lower,
        "ci_high": upper,
        "half_width": float((upper - lower) / 2.0),
    }


def paired_query_bootstrap(
    per_query_scores: Mapping[str, Sequence[float]],
    methods: Sequence[str],
    draws: int = 10_000,
    seed: int = 20260630,
    confidence: float = 0.95,
    epsilon: float = 1e-12,
    bootstrap_draws: Optional[BootstrapDraws] = None,
) -> dict:
    """Bootstrap NDCG, delta NDCG, and recovery from the same query draws.

    Recovery is the ratio of resampled query means, never a mean of per-query
    ratios. Draws with a non-positive oracle gap are invalid; more than 1%
    invalid draws blocks the result.
    """

    if "floor" not in per_query_scores or "oracle" not in per_query_scores:
        raise ValueError("floor and oracle scores are required")
    arrays = {name: np.asarray(values, dtype=np.float64) for name, values in per_query_scores.items()}
    lengths = {len(values) for values in arrays.values()}
    if len(lengths) != 1:
        raise ValueError("all per-query score arrays must have equal length")
    query_count = lengths.pop()
    paired = bootstrap_draws or BootstrapDraws.create(query_count, draws=draws, seed=seed)
    if paired.indices.shape[1] != query_count:
        raise ValueError("bootstrap draw width does not match query count")
    indices = paired.indices
    floor_draw = arrays["floor"][indices].mean(axis=1)
    oracle_draw = arrays["oracle"][indices].mean(axis=1)
    gap_draw = oracle_draw - floor_draw
    valid = gap_draw > float(epsilon)
    invalid_count = int(np.count_nonzero(~valid))
    invalid_fraction = invalid_count / len(valid)
    if invalid_count * 100 > len(valid):
        raise RuntimeError(
            f"bootstrap invalid fraction {invalid_fraction:.2%} exceeds the 1% protocol limit"
        )
    floor_point = float(arrays["floor"].mean())
    oracle_point = float(arrays["oracle"].mean())
    point_gap = oracle_point - floor_point
    if point_gap <= epsilon:
        raise RuntimeError("point-estimate oracle gap is non-positive")

    result: Dict[str, object] = {
        "record_type": "paired_query_bootstrap",
        "draws": int(indices.shape[0]),
        "query_count": query_count,
        "seed": paired.seed,
        "confidence": confidence,
        "invalid_draws": invalid_count,
        "invalid_fraction": invalid_fraction,
        "floor_ndcg": floor_point,
        "oracle_ndcg": oracle_point,
        "oracle_gap": point_gap,
        "methods": {},
    }
    method_draws: Dict[str, np.ndarray] = {}
    resampled_means: Dict[str, np.ndarray] = {
        "floor": floor_draw,
        "oracle": oracle_draw,
    }
    for method in methods:
        values = arrays[method]
        means = values[indices].mean(axis=1)
        delta = means - floor_draw
        recovery = delta[valid] / gap_draw[valid]
        point_ndcg = float(values.mean())
        point_delta = point_ndcg - floor_point
        method_draws[method] = means
        resampled_means[method] = means
        result["methods"][method] = {
            "ndcg": _metric_row(point_ndcg, means, confidence),
            "delta_ndcg": _metric_row(point_delta, delta, confidence),
            "recovery": _metric_row(point_delta / point_gap, recovery, confidence),
        }
    result["_bootstrap_indices"] = indices
    result["_resampled_means"] = resampled_means
    result["_method_draws"] = method_draws
    return result


def paired_contrast(
    bootstrap_result: Mapping[str, object],
    left: str,
    right: str,
    confidence: float = 0.95,
) -> dict:
    """Compute a paired method contrast and a two-sided bootstrap sign p-value."""

    method_draws = bootstrap_result.get("_method_draws")
    if not isinstance(method_draws, dict) or left not in method_draws or right not in method_draws:
        raise ValueError("bootstrap result does not contain requested method draws")
    samples = np.asarray(method_draws[left]) - np.asarray(method_draws[right])
    methods = bootstrap_result["methods"]
    point = float(methods[left]["ndcg"]["estimate"] - methods[right]["ndcg"]["estimate"])
    # Add one to numerator/denominator for a finite Monte-Carlo p-value.
    non_positive = int(np.count_nonzero(samples <= 0.0))
    non_negative = int(np.count_nonzero(samples >= 0.0))
    p_value = min(1.0, 2.0 * (min(non_positive, non_negative) + 1.0) / (len(samples) + 1.0))
    row = _metric_row(point, samples, confidence)
    row.update({"left": left, "right": right, "p_value": float(p_value)})
    return row


def strip_draws(result: Mapping[str, object]) -> dict:
    """Remove in-memory arrays before JSON serialization."""

    return {key: value for key, value in result.items() if not key.startswith("_")}


diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..24810f1082d97d06f947a94206e1205f15adf31f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..24810f1082d97d06f947a94206e1205f15adf31f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The implementation path is now complete enough for preflight: pure-NumPy bound/features, a strongly regularized cross-fittable calibrator, the public predictor, regime-level validation, an additive pack builder, and focused `unittest` coverage. I’m running those tests and static checks before spending GPU time; any failure here will be fixed before pack generation.
Repo contracts and D1 geometry are understood; core estimator code and focused tests are implemented. Preflight validation precedes offline pack generation.
  ✓ Inspect repo rules, D1 pack/harness/stats contracts, existing artifacts, and relevant prior CHELATEDAI notes
  → Implement estimator package and focused tests inside research/drift_recovery
  • Discover/build offline regimes, assert parity, and emit estimator artifacts
  • Run focused and broader validation, then finalize D3 report with honest verdict
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; python -m unittest research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery/estimator research/drift_recovery/run_estimator.py research/drift_recovery/tests/test_estimator.py research/drift_recovery/harness_bridge.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m compileall -q research/drift_recovery/estimator research/drift_recovery/run_estimator.py research/drift_recovery/tests/test_estimator.py research/drift_recovery/harness_bridge.py; if($LASTEXITCODE -eq 0){'"'compileall: PASS'}" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1644ms:
compileall: PASS

 succeeded in 2141ms:
All checks passed!

2026-07-10T03:23:24.619162Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.4 seconds
Output:
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... FAIL
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

======================================================================
FAIL: test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_estimator.py", line 85, in test_hand_checkable_margin_and_tie_semantics
    self.assertEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
AssertionError: -5.551115123125783e-17 != 0.0

----------------------------------------------------------------------
Ran 6 tests in 0.260s

FAILED (failures=1)

 exited 1 in 2430ms:
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... FAIL
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

======================================================================
FAIL: test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics)
----------------------------------------------------------------------
Traceback (most recent call last):
  File "D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_estimator.py", line 85, in test_hand_checkable_margin_and_tie_semantics
    self.assertEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
AssertionError: -5.551115123125783e-17 != 0.0

----------------------------------------------------------------------
Ran 6 tests in 0.260s

FAILED (failures=1)

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..24810f1082d97d06f947a94206e1205f15adf31f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; python -m unittest research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m compileall -q research/drift_recovery/estimator research/drift_recovery/run_estimator.py research/drift_recovery/tests/test_estimator.py research/drift_recovery/harness_bridge.py; if($LASTEXITCODE -eq 0){'"'compileall: PASS'}" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery/estimator research/drift_recovery/run_estimator.py research/drift_recovery/tests/test_estimator.py research/drift_recovery/harness_bridge.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1797ms:
All checks passed!

 succeeded in 1998ms:
compileall: PASS

 succeeded in 2326ms:
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 6 tests in 0.229s

OK

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python -m unittest research.drift_recovery.tests.test_d1_stats research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 34796ms:
test_leakage_safe_ridge_recovery_is_below_literal_fit (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_leakage_safe_ridge_recovery_is_below_literal_fit) ... ok
test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack) ... ok
test_real_pack_reloads_and_matches_harness_aggregates (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_real_pack_reloads_and_matches_harness_aggregates) ... ok
test_prefixes_are_nested_and_seeded (research.drift_recovery.tests.test_d1_stats.TestLearningCurve.test_prefixes_are_nested_and_seeded) ... ok
test_floor_oracle_and_methods_share_bootstrap_indices (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_floor_oracle_and_methods_share_bootstrap_indices) ... ok
test_holm_is_monotone_in_sorted_order (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_holm_is_monotone_in_sorted_order) ... ok
test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent) ... ok
test_recovery_uses_ratio_of_resampled_means (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_recovery_uses_ratio_of_resampled_means) ... ok
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 14 tests in 5.293s

OK

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
Preflight is green: 14 focused D1+D3 tests passed, ruff passed, and compilation passed. I’m starting the real offline build now, one regime at a time. Each new pack is accepted only after checksum reloadability, leakage-safe fit audit, and per-query/aggregate harness parity at `1e-12`; a failed cache/model/dataset attempt will be recorded as a skip in the manifest.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python research/drift_recovery/run_estimator.py --device cuda" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The build is still active with no failure output yet; it is in model/dataset initialization or the first matrix fit. GPU work remains serialized, and I’m watching the process rather than launching anything concurrent.
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
Initialization is slower than the unit path but the process remains alive and has emitted no exception. If it stays silent through the next interval, I’ll inspect the live process/GPU state to distinguish model loading from a stalled dataset call before deciding whether to interrupt.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Process python | Select-Object Id,CPU,WorkingSet64,StartTime,Path; nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader; nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.free --format=csv,noheader' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 4007ms:

Id           : 34660
CPU          : 153.484375
WorkingSet64 : 324780032
StartTime    : 7/9/2026 11:22:05 PM
Path         : C:\Program Files\Python311\python.exe

Id           : 37608
CPU          : 203.328125
WorkingSet64 : 1333088256
StartTime    : 7/9/2026 11:24:38 PM
Path         : C:\Program Files\Python311\python.exe

Id           : 39088
CPU          : 25.703125
WorkingSet64 : 32489472
StartTime    : 7/9/2026 4:21:02 PM
Path         : C:\Program Files\Python311\python.exe

Id           : 52472
CPU          : 21.234375
WorkingSet64 : 32305152
StartTime    : 7/9/2026 4:16:42 PM
Path         : C:\Program Files\Python311\python.exe

Id           : 55640
CPU          : 7.125
WorkingSet64 : 15986688
StartTime    : 7/9/2026 10:09:16 AM
Path         : C:\Program Files\Python311\python.exe

Id           : 58288
CPU          : 34.78125
WorkingSet64 : 184143872
StartTime    : 7/9/2026 11:26:25 PM
Path         : C:\Program Files\Python311\python.exe

Id           : 73052
CPU          : 12.75
WorkingSet64 : 181026816
StartTime    : 7/9/2026 11:27:07 PM
Path         : C:\Program Files\Python311\python.exe

1700, [N/A]
7256, [N/A]
7028, [N/A]
17700, [N/A]
17836, [N/A]
14652, [N/A]
14132, [N/A]
12852, [N/A]
16288, [N/A]
14412, [N/A]
2472, [N/A]
23628, [N/A]
20948, [N/A]
10688, [N/A]
25664, [N/A]
28000, [N/A]
7556, [N/A]
37892, [N/A]
25276, [N/A]
44016, [N/A]
35124, [N/A]
52728, [N/A]
27568, [N/A]
2244, [N/A]
41408, [N/A]
59456, [N/A]
42016, [N/A]
54204, [N/A]
51512, [N/A]
4, [N/A]
10252, [N/A]
31104, [N/A]
37608, [N/A]
100 %, 5229 MiB, 19098 MiB

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 181812ms:
[D3] reloading scifact_minilm_to_mpnet
[D3] building nfcorpus_minilm_to_mpnet sequentially
23:25:09 [INFO] [embedding_backend_init] Initializing local backend: all-mpnet-base-v2
23:25:09 [INFO] [initialization] Device Selected: cuda
23:25:11 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 768
[D3] skipped nfcorpus_minilm_to_mpnet: RuntimeError: not enough non-eval-positive documents for leakage-safe ridge fit
[D3] building scifact_minilm_to_bge_large sequentially
23:25:59 [INFO] [embedding_backend_init] Initializing local backend: BAAI/bge-large-en-v1.5
23:25:59 [INFO] [initialization] Device Selected: cuda
23:26:03 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 1024
[D3] froze scifact_minilm_to_bge_large: 642eb835d1bb62e772f1028dfe6dea7d13a9d9757c0922584cb0694d80b1cda8
[D3] building nfcorpus_minilm_to_bge_large sequentially
23:26:46 [INFO] [embedding_backend_init] Initializing local backend: BAAI/bge-large-en-v1.5
23:26:46 [INFO] [initialization] Device Selected: cuda
23:26:48 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 1024
[D3] skipped nfcorpus_minilm_to_bge_large: RuntimeError: not enough non-eval-positive documents for leakage-safe ridge fit
{
  "available_regimes": [
    "scifact_minilm_to_mpnet",
    "scifact_minilm_to_bge_large"
  ],
  "output_dir": "research\\drift_recovery\\out\\estimator",
  "validation": {
    "bin_accuracy": 1.0,
    "bin_thresholds": {
      "high": "R >= 0.666667",
      "low": "R < 0.333333",
      "medium": "0.333333 <= R < 0.666667"
    },
    "held_out_axis": "regime (dataset / encoder family / anchor setting)",
    "held_out_count": 2,
    "lower_band_coverage": 1.0,
    "record_type": "recoverability_leave_one_regime_out_validation",
    "regime_count": 2,
    "rows": [
      {
        "R_hat": 0.7223707722283974,
        "actual_bin": "high",
        "actual_recovery": 0.7891443001339904,
        "bin_correct": true,
        "lower_band": 0.0,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_mpnet",
        "training_regimes": [
          "scifact_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7891443001339904,
        "actual_bin": "high",
        "actual_recovery": 0.7223707722283974,
        "bin_correct": true,
        "lower_band": 0.0,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet"
        ]
      }
    ],
    "scoping_note": "Only 2 held-out regimes were available; at least 3 are required for the locked cross-regime verdict.",
    "spearman": -0.9999999999999999,
    "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
    "verdict": "SCOPING-NEGATIVE"
  }
}
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).

Batches:   0%|          | 0/33 [00:00<?, ?it/s]
Batches:   3%|▎         | 1/33 [00:00<00:25,  1.24it/s]
Batches:   6%|▌         | 2/33 [00:01<00:14,  2.21it/s]
Batches:   9%|▉         | 3/33 [00:01<00:10,  2.98it/s]
Batches:  12%|█▏        | 4/33 [00:01<00:08,  3.52it/s]
Batches:  15%|█▌        | 5/33 [00:01<00:06,  4.07it/s]
Batches:  18%|█▊        | 6/33 [00:01<00:06,  4.40it/s]
Batches:  21%|██        | 7/33 [00:01<00:05,  4.80it/s]
Batches:  24%|██▍       | 8/33 [00:02<00:04,  5.07it/s]
Batches:  27%|██▋       | 9/33 [00:02<00:04,  5.14it/s]
Batches:  30%|███       | 10/33 [00:02<00:04,  5.08it/s]
Batches:  33%|███▎      | 11/33 [00:02<00:04,  5.24it/s]
Batches:  36%|███▋      | 12/33 [00:02<00:04,  4.97it/s]
Batches:  39%|███▉      | 13/33 [00:03<00:04,  4.92it/s]
Batches:  42%|████▏     | 14/33 [00:03<00:03,  5.11it/s]
Batches:  45%|████▌     | 15/33 [00:03<00:03,  4.88it/s]
Batches:  48%|████▊     | 16/33 [00:03<00:03,  4.98it/s]
Batches:  52%|█████▏    | 17/33 [00:03<00:03,  5.06it/s]
Batches:  55%|█████▍    | 18/33 [00:04<00:02,  5.27it/s]
Batches:  58%|█████▊    | 19/33 [00:04<00:02,  5.46it/s]
Batches:  61%|██████    | 20/33 [00:04<00:02,  5.69it/s]
Batches:  64%|██████▎   | 21/33 [00:04<00:02,  5.69it/s]
Batches:  67%|██████▋   | 22/33 [00:04<00:01,  5.52it/s]
Batches:  70%|██████▉   | 23/33 [00:04<00:01,  5.83it/s]
Batches:  73%|███████▎  | 24/33 [00:05<00:01,  6.16it/s]
Batches:  76%|███████▌  | 25/33 [00:05<00:01,  6.46it/s]
Batches:  79%|███████▉  | 26/33 [00:05<00:01,  6.48it/s]
Batches:  82%|████████▏ | 27/33 [00:05<00:00,  6.56it/s]
Batches:  85%|████████▍ | 28/33 [00:05<00:00,  6.48it/s]
Batches:  88%|████████▊ | 29/33 [00:05<00:00,  6.93it/s]
Batches:  91%|█████████ | 30/33 [00:05<00:00,  7.38it/s]
Batches:  97%|█████████▋| 32/33 [00:06<00:00,  8.69it/s]
Batches: 100%|██████████| 33/33 [00:06<00:00,  5.40it/s]
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).

Batches:   0%|          | 0/19 [00:00<?, ?it/s]
Batches:   5%|▌         | 1/19 [00:00<00:05,  3.19it/s]
Batches:  11%|█         | 2/19 [00:00<00:06,  2.67it/s]
Batches:  16%|█▌        | 3/19 [00:00<00:04,  3.38it/s]
Batches:  21%|██        | 4/19 [00:01<00:04,  3.73it/s]
Batches:  26%|██▋       | 5/19 [00:01<00:03,  4.14it/s]
Batches:  32%|███▏      | 6/19 [00:01<00:03,  4.24it/s]
Batches:  37%|███▋      | 7/19 [00:01<00:02,  4.68it/s]
Batches:  42%|████▏     | 8/19 [00:01<00:02,  4.81it/s]
Batches:  47%|████▋     | 9/19 [00:02<00:02,  4.72it/s]
Batches:  53%|█████▎    | 10/19 [00:02<00:01,  4.93it/s]
Batches:  58%|█████▊    | 11/19 [00:02<00:01,  4.92it/s]
Batches:  63%|██████▎   | 12/19 [00:02<00:01,  5.09it/s]
Batches:  68%|██████▊   | 13/19 [00:03<00:01,  4.22it/s]
Batches:  74%|███████▎  | 14/19 [00:03<00:01,  4.80it/s]
Batches:  79%|███████▉  | 15/19 [00:03<00:00,  5.33it/s]
Batches:  84%|████████▍ | 16/19 [00:03<00:00,  5.74it/s]
Batches:  89%|████████▉ | 17/19 [00:03<00:00,  6.41it/s]
Batches:  95%|█████████▍| 18/19 [00:03<00:00,  7.10it/s]
Batches: 100%|██████████| 19/19 [00:03<00:00,  5.04it/s]
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).

Batches:   0%|          | 0/33 [00:00<?, ?it/s]
Batches:   3%|▎         | 1/33 [00:00<00:14,  2.28it/s]
Batches:   6%|▌         | 2/33 [00:00<00:09,  3.16it/s]
Batches:   9%|▉         | 3/33 [00:00<00:08,  3.63it/s]
Batches:  12%|█▏        | 4/33 [00:01<00:07,  3.99it/s]
Batches:  15%|█▌        | 5/33 [00:01<00:06,  4.27it/s]
Batches:  18%|█▊        | 6/33 [00:01<00:05,  4.58it/s]
Batches:  21%|██        | 7/33 [00:01<00:05,  4.63it/s]
Batches:  24%|██▍       | 8/33 [00:01<00:05,  4.68it/s]
Batches:  27%|██▋       | 9/33 [00:02<00:05,  4.61it/s]
Batches:  30%|███       | 10/33 [00:02<00:04,  4.77it/s]
Batches:  33%|███▎      | 11/33 [00:02<00:04,  4.60it/s]
Batches:  36%|███▋      | 12/33 [00:02<00:04,  4.68it/s]
Batches:  39%|███▉      | 13/33 [00:03<00:04,  4.48it/s]
Batches:  42%|████▏     | 14/33 [00:03<00:04,  4.74it/s]
Batches:  45%|████▌     | 15/33 [00:03<00:03,  5.01it/s]
Batches:  48%|████▊     | 16/33 [00:03<00:03,  5.28it/s]
Batches:  52%|█████▏    | 17/33 [00:03<00:02,  5.60it/s]
Batches:  55%|█████▍    | 18/33 [00:03<00:02,  5.87it/s]
Batches:  58%|█████▊    | 19/33 [00:04<00:02,  5.90it/s]
Batches:  61%|██████    | 20/33 [00:04<00:02,  5.88it/s]
Batches:  64%|██████▎   | 21/33 [00:04<00:02,  5.98it/s]
Batches:  67%|██████▋   | 22/33 [00:04<00:01,  6.23it/s]
Batches:  70%|██████▉   | 23/33 [00:04<00:01,  6.64it/s]
Batches:  73%|███████▎  | 24/33 [00:04<00:01,  6.68it/s]
Batches:  76%|███████▌  | 25/33 [00:04<00:01,  7.06it/s]
Batches:  79%|███████▉  | 26/33 [00:05<00:00,  7.47it/s]
Batches:  82%|████████▏ | 27/33 [00:05<00:00,  7.84it/s]
Batches:  85%|████████▍ | 28/33 [00:05<00:00,  8.08it/s]
Batches:  88%|████████▊ | 29/33 [00:05<00:00,  8.31it/s]
Batches:  91%|█████████ | 30/33 [00:05<00:00,  8.62it/s]
Batches:  97%|█████████▋| 32/33 [00:05<00:00,  9.72it/s]
Batches: 100%|██████████| 33/33 [00:05<00:00,  5.83it/s]

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..6bf57631bbe291b88d25fa5855df2baa2ca16783
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,472 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    fit_count = len(doc_ids) // 2
+    selected = [index for index in order if str(doc_ids[index]) not in positives][:fit_count]
+    if len(selected) != fit_count:
+        raise RuntimeError("not enough non-eval-positive documents for leakage-safe ridge fit")
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The first real run produced an honest, fixable data-contract issue rather than a cache failure: NFCorpus has so many eval-positive documents in the 1,200-document slice that fewer than the SciFact-style 600 documents remain eligible for a leakage-safe fit. The locked plan does not require 600 fit pairs, so I’m narrowing the fit to all available safe pairs (with the exact count recorded) instead of dropping NFCorpus or admitting leakage. SciFact/mpnet and SciFact/bge both passed parity; the provisional two-regime scoping-negative will be overwritten after the corrected NFCorpus runs.
apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m unittest research.drift_recovery.tests.test_estimator -v' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery/run_estimator.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1830ms:
All checks passed!

 succeeded in 2168ms:
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 6 tests in 0.129s

OK

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python research/drift_recovery/run_estimator.py --device cuda" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The corrected run is active and still serialized. It will reuse the two already-validated SciFact packs, so the remaining GPU work is limited to the two NFCorpus encodings and their parity/ground-truth passes.
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Process python | Sort-Object StartTime -Descending | Select-Object -First 4 Id,CPU,WorkingSet64,StartTime; nvidia-smi --query-compute-apps=pid,used_memory --format=csv,noheader | Select-Object -Last 5; nvidia-smi --query-gpu=utilization.gpu,memory.used,memory.free --format=csv,noheader' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 3631ms:

   Id    CPU WorkingSet64 StartTime
   --    --- ------------ ---------
64356   5.81    154374144 7/9/2026 11:30:12 PM
69328 197.05   1326002176 7/9/2026 11:28:30 PM
76884  42.14    190971904 7/9/2026 11:28:16 PM
73052  61.70    200708096 7/9/2026 11:27:07 PM
51512, [N/A]
4, [N/A]
10252, [N/A]
31104, [N/A]
69328, [N/A]
100 %, 5312 MiB, 19015 MiB


diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 155604ms:
[D3] reloading scifact_minilm_to_mpnet
[D3] building nfcorpus_minilm_to_mpnet sequentially
23:28:58 [INFO] [embedding_backend_init] Initializing local backend: all-mpnet-base-v2
23:28:58 [INFO] [initialization] Device Selected: cuda
23:28:59 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 768
[D3] froze nfcorpus_minilm_to_mpnet: 0e7e4354a12e28dcef370df044fe5a4cc3193b79aceddf6a0881c9c4911cf25d
[D3] reloading scifact_minilm_to_bge_large
[D3] building nfcorpus_minilm_to_bge_large sequentially
23:30:02 [INFO] [embedding_backend_init] Initializing local backend: BAAI/bge-large-en-v1.5
23:30:02 [INFO] [initialization] Device Selected: cuda
23:30:04 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 1024
[D3] froze nfcorpus_minilm_to_bge_large: 548fdb0bbfecb621ea3a80e14f8f07bce4fe1f680b4ec6585545af058431da13
{
  "available_regimes": [
    "scifact_minilm_to_mpnet",
    "nfcorpus_minilm_to_mpnet",
    "scifact_minilm_to_bge_large",
    "nfcorpus_minilm_to_bge_large"
  ],
  "output_dir": "research\\drift_recovery\\out\\estimator",
  "validation": {
    "bin_accuracy": 0.5,
    "bin_thresholds": {
      "high": "R >= 0.666667",
      "low": "R < 0.333333",
      "medium": "0.333333 <= R < 0.666667"
    },
    "held_out_axis": "regime (dataset / encoder family / anchor setting)",
    "held_out_count": 4,
    "lower_band_coverage": 1.0,
    "record_type": "recoverability_leave_one_regime_out_validation",
    "regime_count": 4,
    "rows": [
      {
        "R_hat": 0.6840096562278455,
        "actual_bin": "high",
        "actual_recovery": 0.7891443001339904,
        "bin_correct": true,
        "lower_band": 0.5418130994796307,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_mpnet",
        "training_regimes": [
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7314921804097357,
        "actual_bin": "medium",
        "actual_recovery": 0.647119786542357,
        "bin_correct": false,
        "lower_band": 0.6412319974171363,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_mpnet",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7289212641928171,
        "actual_bin": "high",
        "actual_recovery": 0.7223707722283974,
        "bin_correct": true,
        "lower_band": 0.22337122509004526,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.667862322352701,
        "actual_bin": "medium",
        "actual_recovery": 0.6640397106924352,
        "bin_correct": false,
        "lower_band": 0.21251608512032283,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large"
        ]
      }
    ],
    "scoping_note": "Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.",
    "spearman": -0.39999999999999997,
    "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
    "verdict": "VALIDATION-NEGATIVE"
  }
}
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).

Batches:   0%|          | 0/33 [00:00<?, ?it/s]
Batches:   3%|▎         | 1/33 [00:00<00:18,  1.69it/s]
Batches:   6%|▌         | 2/33 [00:00<00:11,  2.74it/s]
Batches:   9%|▉         | 3/33 [00:01<00:08,  3.34it/s]
Batches:  12%|█▏        | 4/33 [00:01<00:06,  4.15it/s]
Batches:  15%|█▌        | 5/33 [00:01<00:05,  4.70it/s]
Batches:  18%|█▊        | 6/33 [00:01<00:05,  4.62it/s]
Batches:  21%|██        | 7/33 [00:01<00:05,  4.76it/s]
Batches:  24%|██▍       | 8/33 [00:01<00:04,  5.19it/s]
Batches:  27%|██▋       | 9/33 [00:02<00:04,  5.53it/s]
Batches:  30%|███       | 10/33 [00:02<00:04,  5.56it/s]
Batches:  33%|███▎      | 11/33 [00:02<00:04,  5.46it/s]
Batches:  36%|███▋      | 12/33 [00:02<00:03,  5.70it/s]
Batches:  39%|███▉      | 13/33 [00:02<00:03,  5.76it/s]
Batches:  42%|████▏     | 14/33 [00:02<00:03,  5.84it/s]
Batches:  45%|████▌     | 15/33 [00:03<00:02,  6.18it/s]
Batches:  48%|████▊     | 16/33 [00:03<00:02,  6.47it/s]
Batches:  52%|█████▏    | 17/33 [00:03<00:02,  6.56it/s]
Batches:  55%|█████▍    | 18/33 [00:03<00:02,  6.41it/s]
Batches:  58%|█████▊    | 19/33 [00:03<00:02,  6.55it/s]
Batches:  61%|██████    | 20/33 [00:03<00:01,  6.63it/s]
Batches:  64%|██████▎   | 21/33 [00:03<00:01,  6.67it/s]
Batches:  67%|██████▋   | 22/33 [00:04<00:01,  6.57it/s]
Batches:  70%|██████▉   | 23/33 [00:04<00:01,  6.36it/s]
Batches:  73%|███████▎  | 24/33 [00:04<00:01,  6.54it/s]
Batches:  76%|███████▌  | 25/33 [00:04<00:01,  6.73it/s]
Batches:  79%|███████▉  | 26/33 [00:04<00:01,  6.93it/s]
Batches:  82%|████████▏ | 27/33 [00:04<00:00,  7.01it/s]
Batches:  85%|████████▍ | 28/33 [00:04<00:00,  7.30it/s]
Batches:  88%|████████▊ | 29/33 [00:05<00:00,  7.54it/s]
Batches:  91%|█████████ | 30/33 [00:05<00:00,  7.74it/s]
Batches:  97%|█████████▋| 32/33 [00:05<00:00,  9.25it/s]
Batches: 100%|██████████| 33/33 [00:05<00:00,  6.12it/s]
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\default\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:11 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\corpus\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:12 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).
Using the latest cached version of the dataset since mteb/nfcorpus couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___nfcorpus\queries\0.0.0\ec0fa4fe99da2ff19ca1214b7966684033a58814 (last modified on Fri Jan  2 15:04:13 2026).

Batches:   0%|          | 0/33 [00:00<?, ?it/s]
Batches:   3%|▎         | 1/33 [00:00<00:15,  2.10it/s]
Batches:   6%|▌         | 2/33 [00:00<00:09,  3.13it/s]
Batches:   9%|▉         | 3/33 [00:00<00:08,  3.68it/s]
Batches:  12%|█▏        | 4/33 [00:01<00:07,  3.76it/s]
Batches:  15%|█▌        | 5/33 [00:01<00:07,  3.95it/s]
Batches:  18%|█▊        | 6/33 [00:01<00:06,  4.34it/s]
Batches:  21%|██        | 7/33 [00:01<00:05,  4.65it/s]
Batches:  24%|██▍       | 8/33 [00:01<00:04,  5.01it/s]
Batches:  27%|██▋       | 9/33 [00:02<00:04,  5.18it/s]
Batches:  30%|███       | 10/33 [00:02<00:04,  4.87it/s]
Batches:  33%|███▎      | 11/33 [00:02<00:05,  4.25it/s]
Batches:  36%|███▋      | 12/33 [00:03<00:05,  3.64it/s]
Batches:  39%|███▉      | 13/33 [00:03<00:05,  3.94it/s]
Batches:  42%|████▏     | 14/33 [00:03<00:04,  4.16it/s]
Batches:  45%|████▌     | 15/33 [00:03<00:04,  4.40it/s]
Batches:  48%|████▊     | 16/33 [00:03<00:03,  4.33it/s]
Batches:  52%|█████▏    | 17/33 [00:04<00:03,  4.55it/s]
Batches:  55%|█████▍    | 18/33 [00:04<00:03,  4.79it/s]
Batches:  58%|█████▊    | 19/33 [00:04<00:03,  4.64it/s]
Batches:  61%|██████    | 20/33 [00:04<00:02,  4.72it/s]
Batches:  64%|██████▎   | 21/33 [00:04<00:02,  4.85it/s]
Batches:  67%|██████▋   | 22/33 [00:05<00:02,  5.08it/s]
Batches:  70%|██████▉   | 23/33 [00:05<00:01,  5.24it/s]
Batches:  73%|███████▎  | 24/33 [00:05<00:01,  5.72it/s]
Batches:  76%|███████▌  | 25/33 [00:05<00:01,  6.13it/s]
Batches:  79%|███████▉  | 26/33 [00:05<00:01,  6.37it/s]
Batches:  82%|████████▏ | 27/33 [00:05<00:00,  6.61it/s]
Batches:  85%|████████▍ | 28/33 [00:05<00:00,  7.07it/s]
Batches:  88%|████████▊ | 29/33 [00:05<00:00,  7.56it/s]
Batches:  91%|█████████ | 30/33 [00:06<00:00,  7.88it/s]
Batches:  94%|█████████▍| 31/33 [00:06<00:00,  8.42it/s]
Batches: 100%|██████████| 33/33 [00:06<00:00, 11.49it/s]
Batches: 100%|██████████| 33/33 [00:06<00:00,  5.23it/s]

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
All four real regimes are now frozen and parity-clean, and the locked verdict is negative: leave-one-regime-out Spearman is `-0.400`, bin accuracy is `50%`, across four held-out regimes. I’m keeping that result. The smallest concrete failure is already visible: both NFCorpus regimes have actual medium recovery but are predicted high, and the actual-best SciFact/mpnet regime is under-ranked. I’m now auditing the generated artifacts, default predictor path, exact fit counts, and report wording before the separate adversarial review.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-ChildItem -Recurse research/drift_recovery/out/estimator | Select-Object FullName,Length | Format-Table -AutoSize; Get-Content -Raw research/drift_recovery/out/estimator/regime_manifest.json; Get-Content -Raw research/drift_recovery/out/estimator/estimator_validation.md; Get-Content -Raw research/drift_recovery/out/estimator/D3_REPORT.md' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; @'
from pathlib import Path
from research.drift_recovery.artifacts import EmbeddingPack
from research.drift_recovery.estimator.predictor import estimate_recovery
p=EmbeddingPack.load(Path('research/drift_recovery/out/estimator/packs/nfcorpus_minilm_to_mpnet_pack'))
r=estimate_recovery(p)
print({'R_hat':r['R_hat'],'lower_band':r['lower_band'],'training_count':r['calibration_training_count'],'fit_count':r['features']['summary']['fit_count']})
'@ | python -" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command "git status --short; git diff --stat; rg -n \"benchmark_utils|chelation_adapter|query_encoder_drift|run_drift_recovery|run_road_course|drift_recovery_metrics|sedimentation_loss\" research/drift_recovery -g '*.py' -g '"'!harness_bridge.py'"'; exit 0" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 2158ms:

FullName
--------                                                                                                               
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs                         
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\calibration.json              
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\D3_REPORT.md                  
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\estimator_validation.md       
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\nfcorpus_minilm_to_bge_large_…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\nfcorpus_minilm_to_mpnet_feat…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\regime_manifest.json          
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\scifact_minilm_to_bge_large_f…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\scifact_minilm_to_mpnet_featu…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\validation.json               
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_bge_…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_bge_…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_mpne…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_mpne…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs\scifact_minilm_to_bge_l…
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\out\estimator\packs\scifact_minilm_to_bge_l…

{
  "environment": {
    "HF_DATASETS_OFFLINE": "1",
    "HF_HUB_OFFLINE": "1",
    "device": "cuda"
  },
  "parity_tolerance": 1e-12,
  "record_type": "d3_estimator_regime_manifest",
  "regimes": [
    {
      "anchor_fraction": 0.4,
      "dataset": "SciFact",
      "harness_parity": {
        "c3a": 0.0,
        "floor": 0.0,
        "mlp": 0.0,
        "oracle": 0.0,
        "procrustes": 0.0,
        "ridge": 0.0
      },
      "name": "scifact_minilm_to_mpnet",
      "pack_prefix": "research\\drift_recovery\\out\\d1\\scifact_evalsplit_pack",
      "pack_sha256": "14bc80356f2d77b74e48edd266e5071b162fc9d905034cddf31dc7d4747058ac",
      "safe_ridge_recovery": 0.7891443001339904,
      "status": "available",
      "swap_model": "all-mpnet-base-v2"
    },
    {
      "anchor_fraction": 0.4,
      "dataset": "NFCorpus",
      "harness_parity": {
        "floor": 0.0,
        "oracle": 0.0,
        "ridge": 0.0
      },
      "name": "nfcorpus_minilm_to_mpnet",
      "pack_prefix": "research\\drift_recovery\\out\\estimator\\packs\\nfcorpus_minilm_to_mpnet_pack",
      "pack_sha256": "0e7e4354a12e28dcef370df044fe5a4cc3193b79aceddf6a0881c9c4911cf25d",
      "safe_ridge_recovery": 0.647119786542357,
      "status": "available",
      "swap_model": "all-mpnet-base-v2"
    },
    {
      "anchor_fraction": 0.4,
      "dataset": "SciFact",
      "harness_parity": {
        "floor": 0.0,
        "oracle": 0.0,
        "ridge": 0.0
      },
      "name": "scifact_minilm_to_bge_large",
      "pack_prefix": "research\\drift_recovery\\out\\estimator\\packs\\scifact_minilm_to_bge_large_pack",
      "pack_sha256": "642eb835d1bb62e772f1028dfe6dea7d13a9d9757c0922584cb0694d80b1cda8",
      "safe_ridge_recovery": 0.7223707722283974,
      "status": "available",
      "swap_model": "BAAI/bge-large-en-v1.5"
    },
    {
      "anchor_fraction": 0.4,
      "dataset": "NFCorpus",
      "harness_parity": {
        "floor": 0.0,
        "oracle": 0.0,
        "ridge": 0.0
      },
      "name": "nfcorpus_minilm_to_bge_large",
      "pack_prefix": "research\\drift_recovery\\out\\estimator\\packs\\nfcorpus_minilm_to_bge_large_pack",
      "pack_sha256": "548fdb0bbfecb621ea3a80e14f8f07bce4fe1f680b4ec6585545af058431da13",
      "safe_ridge_recovery": 0.6640397106924352,
      "status": "available",
      "swap_model": "BAAI/bge-large-en-v1.5"
    }
  ]
}
# D3 recoverability estimator validation

Held-out axis: **regime** (dataset / encoder family / anchor setting). No regime's own measured recovery trains its held-out prediction.

| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |
|---|---:|---:|---:|---|---|
| scifact_minilm_to_mpnet | 0.7891 | 0.6840 | 0.5418 | high | high |
| nfcorpus_minilm_to_mpnet | 0.6471 | 0.7315 | 0.6412 | medium | high |
| scifact_minilm_to_bge_large | 0.7224 | 0.7289 | 0.2234 | high | high |
| nfcorpus_minilm_to_bge_large | 0.6640 | 0.6679 | 0.2125 | medium | high |

- Held-out regimes: **4**
- Spearman(R_hat, R): **-0.400**
- Three-way bin accuracy: **50.0%** (fixed bins: low <1/3, medium <2/3, high >=2/3)
- Verdict: **VALIDATION-NEGATIVE**

Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.

Negative/scoping boundary: the per-query inequality only flags absence of an order-preservation guarantee for one oracle-best relevant/non-relevant pair. It does not prove an inversion, does not model the full top-k ordering, and Gram/margin distortion alone does not determine NDCG@10.

## Regime availability

- `scifact_minilm_to_mpnet`: **available** — research\drift_recovery\out\d1\scifact_evalsplit_pack
- `nfcorpus_minilm_to_mpnet`: **available** — research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_mpnet_pack
- `scifact_minilm_to_bge_large`: **available** — research\drift_recovery\out\estimator\packs\scifact_minilm_to_bge_large_pack
- `nfcorpus_minilm_to_bge_large`: **available** — research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_bge_large_pack

# D3 report — recoverability estimator

Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space margin quantiles, leakage-safe ridge correction-error norms, and the per-query margin-bound violation rate. A strongly regularized linear map produces `R_hat`; training-only cross-fitted overprediction residuals produce its lower band.

Regimes used (4): `scifact_minilm_to_mpnet`, `nfcorpus_minilm_to_mpnet`, `scifact_minilm_to_bge_large`, `nfcorpus_minilm_to_bge_large`.

Leave-one-regime-out result: Spearman **-0.400**, three-way bin accuracy **50.0%**. Verdict: **VALIDATION-NEGATIVE**.

Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.

Most important limitation: this bound tracks one relevant/competitor order and cannot certify top-k NDCG; with only the available dataset/encoder regimes, the calibration evidence is narrow even if the locked numerical bar is met.


 succeeded in 3574ms:
?? research/

 succeeded in 3938ms:
{'R_hat': 0.6890709326649314, 'lower_band': 0.6046985387975526, 'training_count': 4, 'fit_count': 869}

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$files=Get-ChildItem research/drift_recovery/out/estimator/*_features.json; foreach($f in $files){$j=Get-Content -Raw $f | ConvertFrom-Json; [pscustomobject]@{Regime=$j.regime_name; Docs=$j.features.summary.document_count; Queries=$j.features.summary.query_count; Fit=$j.features.summary.fit_count; MarginSign=$j.features.summary.oracle_margin_sign_rate; Inversion=$j.features.summary.predicted_inversion_rate; MarginQ10=$j.features.summary.oracle_margin_q10; ErrorQ90=$j.features.summary.fit_error_norm_q90; Recovery=$j.ground_truth.ridge_recovery; BootstrapError=$j.ground_truth.bootstrap_error}} | Format-Table -AutoSize' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
2026-07-10T03:31:43.641957Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 3 seconds
Output:
ParserError: 
Line |
   2 |  … _recovery; BootstrapError=$j.ground_truth.bootstrap_error}} | Format- …
     |                                                                ~
     | An empty pipe element is not allowed.

 exited 1 in 3015ms:
ParserError: 
Line |
   2 |  … _recovery; BootstrapError=$j.ground_truth.bootstrap_error}} | Format- …
     |                                                                ~
     | An empty pipe element is not allowed.

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$rows=@(foreach($f in Get-ChildItem research/drift_recovery/out/estimator/*_features.json){$j=Get-Content -Raw $f | ConvertFrom-Json; [pscustomobject]@{Regime=$j.regime_name; Docs=$j.summary.document_count; Queries=$j.summary.query_count; Fit=$j.summary.fit_count; MarginSign=$j.summary.oracle_margin_sign_rate; Inversion=$j.summary.predicted_inversion_rate; MarginQ10=$j.summary.oracle_margin_q10; ErrorQ90=$j.summary.fit_error_norm_q90; Recovery=$j.ground_truth.ridge_recovery; BootstrapError=$j.ground_truth.bootstrap_error}}); $rows | Format-Table -AutoSize' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 4090ms:

Regime                       Docs Queries Fit MarginSign Inversion MarginQ10 ErrorQ90 Recovery BootstrapError
------                       ---- ------- --- ---------- --------- --------- -------- -------- --------------
nfcorpus_minilm_to_bge_large 2063      60 869       0.57      1.00     -0.09     0.44     0.66 
nfcorpus_minilm_to_mpnet     2063      60 869       0.53      1.00     -0.15     0.54     0.65 
scifact_minilm_to_bge_large  1200      60 600       0.73      1.00     -0.04     0.46     0.72 
scifact_minilm_to_mpnet      1200      60 600       0.70      1.00     -0.10     0.58     0.79 


diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..73ab08025268e76787623866b5c3a81e11104ff9
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,480 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_names: Sequence[str]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify top-k NDCG; with only the available dataset/encoder regimes, the "
+            "calibration evidence is narrow even if the locked numerical bar is met.",
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_names), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..3573e13b7b2ae29c0644155b0d198e662fcb6f4c
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,517 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            (
+                "Smallest concrete failure: the bound predicted a violation for **100% of "
+                "queries in every available regime**, so its inversion-rate feature did not "
+                "separate observed recoveries ranging from "
+                f"{min(item['safe_ridge_recovery'] for item in manifest['regimes'] if item['status'] == 'available'):.3f} "
+                "to "
+                f"{max(item['safe_ridge_recovery'] for item in manifest['regimes'] if item['status'] == 'available'):.3f}."
+                if all(
+                    item.get("predicted_inversion_rate") == 1.0
+                    for item in manifest["regimes"]
+                    if item["status"] == "available"
+                )
+                else "Smallest concrete failure: the held-out feature map misordered the regimes."
+            ),
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..3573e13b7b2ae29c0644155b0d198e662fcb6f4c
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,517 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            (
+                "Smallest concrete failure: the bound predicted a violation for **100% of "
+                "queries in every available regime**, so its inversion-rate feature did not "
+                "separate observed recoveries ranging from "
+                f"{min(item['safe_ridge_recovery'] for item in manifest['regimes'] if item['status'] == 'available'):.3f} "
+                "to "
+                f"{max(item['safe_ridge_recovery'] for item in manifest['regimes'] if item['status'] == 'available'):.3f}."
+                if all(
+                    item.get("predicted_inversion_rate") == 1.0
+                    for item in manifest["regimes"]
+                    if item["status"] == "available"
+                )
+                else "Smallest concrete failure: the held-out feature map misordered the regimes."
+            ),
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery/run_estimator.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1662ms:
All checks passed!

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python research/drift_recovery/run_estimator.py --device cuda" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 53048ms:
[D3] reloading scifact_minilm_to_mpnet
[D3] reloading nfcorpus_minilm_to_mpnet
[D3] reloading scifact_minilm_to_bge_large
[D3] reloading nfcorpus_minilm_to_bge_large
{
  "available_regimes": [
    "scifact_minilm_to_mpnet",
    "nfcorpus_minilm_to_mpnet",
    "scifact_minilm_to_bge_large",
    "nfcorpus_minilm_to_bge_large"
  ],
  "output_dir": "research\\drift_recovery\\out\\estimator",
  "validation": {
    "bin_accuracy": 0.5,
    "bin_thresholds": {
      "high": "R >= 0.666667",
      "low": "R < 0.333333",
      "medium": "0.333333 <= R < 0.666667"
    },
    "held_out_axis": "regime (dataset / encoder family / anchor setting)",
    "held_out_count": 4,
    "lower_band_coverage": 1.0,
    "record_type": "recoverability_leave_one_regime_out_validation",
    "regime_count": 4,
    "rows": [
      {
        "R_hat": 0.6840096562278455,
        "actual_bin": "high",
        "actual_recovery": 0.7891443001339904,
        "bin_correct": true,
        "lower_band": 0.5418130994796307,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_mpnet",
        "training_regimes": [
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7314921804097357,
        "actual_bin": "medium",
        "actual_recovery": 0.647119786542357,
        "bin_correct": false,
        "lower_band": 0.6412319974171363,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_mpnet",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7289212641928171,
        "actual_bin": "high",
        "actual_recovery": 0.7223707722283974,
        "bin_correct": true,
        "lower_band": 0.22337122509004526,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.667862322352701,
        "actual_bin": "medium",
        "actual_recovery": 0.6640397106924352,
        "bin_correct": false,
        "lower_band": 0.21251608512032283,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large"
        ]
      }
    ],
    "scoping_note": "Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.",
    "spearman": -0.39999999999999997,
    "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
    "verdict": "VALIDATION-NEGATIVE"
  }
}

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The reports now name the actual limiting signal: the bound predicts a violation for 100% of queries in every regime while measured ridge recovery spans roughly 0.65–0.79. That saturation is the smallest concrete thing that does not work, and it explains why the feature-to-recovery map is not cross-regime calibratable here. I’m handing the completed slice to the required fresh adversarial reviewer now; meanwhile I’ll run the final test gates and artifact-integrity checks.
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python -m unittest research.drift_recovery.tests.test_d1_stats research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; @'
import hashlib, json
from pathlib import Path
import numpy as np
from research.drift_recovery.artifacts import EmbeddingPack
from research.drift_recovery.estimator.features import leakage_safe_fit_indices
from research.drift_recovery.harness_bridge import assert_harness_parity
root=Path('research/drift_recovery/out/estimator')
m=json.loads((root/'regime_manifest.json').read_text())
for item in m['regimes']:
    assert item['status']=='available', item
    p=Path(item['pack_prefix'])
    pack=EmbeddingPack.load(p)
    digest=hashlib.sha256(p.with_suffix('.npz').read_bytes()).hexdigest()
    assert digest==item['pack_sha256']
    parity=assert_harness_parity(pack, atol=1e-12)
    assert max(parity.values()) <= 1e-12
    idx=leakage_safe_fit_indices(pack)
    assert len(idx)==item['safe_fit_count']
    assert item['predicted_inversion_rate']==1.0
cal=json.loads((root/'calibration.json').read_text())
val=json.loads((root/'validation.json').read_text())
assert cal['training_count']==4
assert val['verdict']=='VALIDATION-NEGATIVE'
assert val['held_out_count']==4
assert abs(val['spearman'] - (-0.4)) < 1e-12
assert val['bin_accuracy']==0.5
assert len((root/'D3_REPORT.md').read_text().splitlines()) <= 30
print('artifact_integrity: PASS (4 packs, parity <=1e-12, safe fits, checksums, negative verdict)')
'@ | python -" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery; python -m compileall -q research/drift_recovery; if($LASTEXITCODE -eq 0){'"'compileall: PASS'}" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 2552ms:
All checks passed!
compileall: PASS

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

2026-07-10T03:34:44.807802Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 14.1 seconds
Output:
command timed out after 14065 milliseconds

 succeeded in 42924ms:
artifact_integrity: PASS (4 packs, parity <=1e-12, safe fits, checksums, negative verdict)

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 47071ms:
test_leakage_safe_ridge_recovery_is_below_literal_fit (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_leakage_safe_ridge_recovery_is_below_literal_fit) ... ok
test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack) ... ok
test_real_pack_reloads_and_matches_harness_aggregates (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_real_pack_reloads_and_matches_harness_aggregates) ... ok
test_prefixes_are_nested_and_seeded (research.drift_recovery.tests.test_d1_stats.TestLearningCurve.test_prefixes_are_nested_and_seeded) ... ok
test_floor_oracle_and_methods_share_bootstrap_indices (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_floor_oracle_and_methods_share_bootstrap_indices) ... ok
test_holm_is_monotone_in_sorted_order (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_holm_is_monotone_in_sorted_order) ... ok
test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent) ... ok
test_recovery_uses_ratio_of_resampled_means (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_recovery_uses_ratio_of_resampled_means) ... ok
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 14 tests in 9.989s

OK

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

2026-07-10T03:35:11.516845Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.2 seconds
Total output lines: 1231
Output:
research/drift_recovery/artifacts.py:1:"""Frozen array artifacts for offline drift-recovery statistics."""
research/drift_recovery/artifacts.py:2:
research/drift_recovery/artifacts.py:3:from __future__ import annotations
research/drift_recovery/artifacts.py:4:
research/drift_recovery/artifacts.py:5:import hashlib
research/drift_recovery/artifacts.py:6:import json
research/drift_recovery/artifacts.py:7:from dataclasses import dataclass, field
research/drift_recovery/artifacts.py:8:from pathlib import Path
research/drift_recovery/artifacts.py:9:from typing import Any, Dict, Mapping
research/drift_recovery/artifacts.py:10:
research/drift_recovery/artifacts.py:11:import numpy as np
research/drift_recovery/artifacts.py:12:
research/drift_recovery/artifacts.py:13:
research/drift_recovery/artifacts.py:14:def _json_ready(value: Any) -> Any:
research/drift_recovery/artifacts.py:15:    if isinstance(value, np.generic):
research/drift_recovery/artifacts.py:16:        return value.item()
research/drift_recovery/artifacts.py:17:    if isinstance(value, np.ndarray):
research/drift_recovery/artifacts.py:18:        return value.tolist()
research/drift_recovery/artifacts.py:19:    if isinstance(value, dict):
research/drift_recovery/artifacts.py:20:        return {str(key): _json_ready(item) for key, item in value.items()}
research/drift_recovery/artifacts.py:21:    if isinstance(value, (list, tuple)):
research/drift_recovery/artifacts.py:22:        return [_json_ready(item) for item in value]
research/drift_recovery/artifacts.py:23:    return value
research/drift_recovery/artifacts.py:24:
research/drift_recovery/artifacts.py:25:
research/drift_recovery/artifacts.py:26:@dataclass
research/drift_recovery/artifacts.py:27:class EmbeddingPack:
research/drift_recovery/artifacts.py:28:    """All arrays needed to rerun D1 statistics without model inference.
research/drift_recovery/artifacts.py:29:
research/drift_recovery/artifacts.py:30:    The required core is ``Do``, ``Dor``, ``Qd``, qrels, ``fit_idx``, and
research/drift_recovery/artifacts.py:31:    per-query method scores. Extra arrays hold non-eval supervision pools so the
research/drift_recovery/artifacts.py:32:    learning curves remain leakage-free while using the same fixed eval IDs.
research/drift_recovery/artifacts.py:33:    """
research/drift_recovery/artifacts.py:34:
research/drift_recovery/artifacts.py:35:    Do: np.ndarray
research/drift_recovery/artifacts.py:36:    Dor: np.ndarray
research/drift_recovery/artifacts.py:37:    Qd: np.ndarray
research/drift_recovery/artifacts.py:38:    doc_ids: np.ndarray
research/drift_recovery/artifacts.py:39:    query_ids: np.ndarray
research/drift_recovery/artifacts.py:40:    qrels: Mapping[str, Mapping[str, float]]
research/drift_recovery/artifacts.py:41:    fit_idx: np.ndarray
research/drift_recovery/artifacts.py:42:    per_query_scores: Mapping[str, np.ndarray]
research/drift_recovery/artifacts.py:43:    extra_arrays: Mapping[str, np.ndarray] = field(default_factory=dict)
research/drift_recovery/artifacts.py:44:    metadata: Mapping[str, Any] = field(default_factory=dict)
research/drift_recovery/artifacts.py:45:
research/drift_recovery/artifacts.py:46:    def validate(self) -> None:
research/drift_recovery/artifacts.py:47:        if self.Do.shape != self.Dor.shape or self.Do.ndim != 2:
research/drift_recovery/artifacts.py:48:            raise ValueError("Do and Dor must be same-shape matrices")
research/drift_recovery/artifacts.py:49:        if self.Qd.ndim != 2 or self.Qd.shape[1] != self.Do.shape[1]:
research/drift_recovery/artifacts.py:50:            raise ValueError("Qd must share the embedding dimension")
research/drift_recovery/artifacts.py:51:        if len(self.doc_ids) != self.Do.shape[0]:
research/drift_recovery/artifacts.py:52:            raise ValueError("doc_ids length mismatch")
research/drift_recovery/artifacts.py:53:        if len(self.query_ids) != self.Qd.shape[0]:
research/drift_recovery/artifacts.py:54:            raise ValueError("query_ids length mismatch")
research/drift_recovery/artifacts.py:55:        if np.any(self.fit_idx < 0) or np.any(self.fit_idx >= self.Do.shape[0]):
research/drift_recovery/artifacts.py:56:            raise ValueError("fit_idx is out of bounds")
research/drift_recovery/artifacts.py:57:        if len(np.unique(self.fit_idx)) != len(self.fit_idx):
research/drift_recovery/artifacts.py:58:            raise ValueError("fit_idx contains duplicates")
research/drift_recovery/artifacts.py:59:        for method, scores in self.per_query_scores.items():
research/drift_recovery/artifacts.py:60:            array = np.asarray(scores)
research/drift_recovery/artifacts.py:61:            if array.shape != (len(self.query_ids),):
research/drift_recovery/artifacts.py:62:                raise ValueError(f"{method} per-query score shape mismatch: {array.shape}")
research/drift_recovery/artifacts.py:63:            if not np.all(np.isfinite(array)):
research/drift_recovery/artifacts.py:64:                raise ValueError(f"{method} contains non-finite scores")
research/drift_recovery/artifacts.py:65:        missing = set(map(str, self.query_ids)).difference(map(str, self.qrels))
research/drift_recovery/artifacts.py:66:        if missing:
research/drift_recovery/artifacts.py:67:            raise ValueError(f"qrels missing eval queries: {sorted(missing)[:5]}")
research/drift_recovery/artifacts.py:68:
research/drift_recovery/artifacts.py:69:    def save(self, prefix: Path) -> Dict[str, str]:
research/drift_recovery/artifacts.py:70:        self.validate()
research/drift_recovery/artifacts.py:71:        prefix = Path(prefix)
research/drift_recovery/artifacts.py:72:        prefix.parent.mkdir(parents=True, exist_ok=True)
research/drift_recovery/artifacts.py:73:        arrays_path = prefix.with_suffix(".npz")
research/drift_recovery/artifacts.py:74:        metadata_path = prefix.with_suffix(".json")
research/drift_recovery/artifacts.py:75:        arrays: Dict[str, np.ndarray] = {
research/drift_recovery/artifacts.py:76:            "Do": np.asarray(self.Do),
research/drift_recovery/artifacts.py:77:            "Dor": np.asarray(self.Dor),
research/drift_recovery/artifacts.py:78:            "Qd": np.asarray(self.Qd),
research/drift_recovery/artifacts.py:79:            "doc_ids": np.asarray(self.doc_ids, dtype=str),
research/drift_recovery/artifacts.py:80:            "query_ids": np.asarray(self.query_ids, dtype=str),
research/drift_recovery/artifacts.py:81:            "fit_idx": np.asarray(self.fit_idx, dtype=np.int64),
research/drift_recovery/artifacts.py:82:        }
research/drift_recovery/artifacts.py:83:        arrays.update({f"score__{key}": np.asarray(value) for key, value in self.per_query_scores.items()})
research/drift_recovery/artifacts.py:84:        arrays.update({f"extra__{key}": np.asarray(value) for key, value in self.extra_arrays.items()})
research/drift_recovery/artifacts.py:85:        np.savez_compressed(arrays_path, **arrays)
research/drift_recovery/artifacts.py:86:        payload = {
research/drift_recovery/artifacts.py:87:            "record_type": "embedding_pack",
research/drift_recovery/artifacts.py:88:            "format_version": 1,
research/drift_recovery/artifacts.py:89:            "arrays_file": arrays_path.name,
research/drift_recovery/artifacts.py:90:            "qrels": _json_ready(self.qrels),
research/drift_recovery/artifacts.py:91:            "metadata": _json_ready(self.metadata),
research/drift_recovery/artifacts.py:92:            "array_shapes": {key: list(value.shape) for key, value in arrays.items()},
research/drift_recovery/artifacts.py:93:        }
research/drift_recovery/artifacts.py:94:        metadata_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
research/drift_recovery/artifacts.py:95:        digest = hashlib.sha256(arrays_path.read_bytes()).hexdigest()
research/drift_recovery/artifacts.py:96:        payload["arrays_sha256"] = digest
research/drift_recovery/artifacts.py:97:        metadata_path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
research/drift_recovery/artifacts.py:98:        return {"arrays": str(arrays_path), "metadata": str(metadata_path), "sha256": digest}
research/drift_recovery/artifacts.py:99:
research/drift_recovery/artifacts.py:100:    @classmethod
research/drift_recovery/artifacts.py:101:    def load(cls, prefix: Path) -> "EmbeddingPack":
research/drift_recovery/artifacts.py:102:        prefix = Path(prefix)
research/drift_recovery/artifacts.py:103:        arrays_path = prefix.with_suffix(".npz")
research/drift_recovery/artifacts.py:104:        metadata_path = prefix.with_suffix(".json")
research/drift_recovery/artifacts.py:105:        payload = json.loads(metadata_path.read_text(encoding="utf-8"))
research/drift_recovery/artifacts.py:106:        digest = hashlib.sha256(arrays_path.read_bytes()).hexdigest()
research/drift_recovery/artifacts.py:107:        if digest != payload.get("arrays_sha256"):
research/drift_recovery/artifacts.py:108:            raise ValueError("EmbeddingPack array checksum mismatch")
research/drift_recovery/artifacts.py:109:        with np.load(arrays_path, allow_pickle=False) as stored:
research/drift_recovery/artifacts.py:110:            scores = {key[7:]: stored[key].copy() for key in stored.files if key.startswith("score__")}
research/drift_recovery/artifacts.py:111:            extras = {key[7:]: stored[key].copy() for key in stored.files if key.startswith("extra__")}
research/drift_recovery/artifacts.py:112:            pack = cls(
research/drift_recovery/artifacts.py:113:                Do=stored["Do"].copy(),
research/drift_recovery/artifacts.py:114:                Dor=stored["Dor"].copy(),
research/drift_recovery/artifacts.py:115:                Qd=stored["Qd"].copy(),
research/drift_recovery/artifacts.py:116:                doc_ids=stored["doc_ids"].copy(),
research/drift_recovery/artifacts.py:117:                query_ids=stored["query_ids"].copy(),
research/drift_recovery/artifacts.py:118:                qrels=payload["qrels"],
research/drift_recovery/artifacts.py:119:                fit_idx=stored["fit_idx"].copy(),
research/drift_recovery/artifacts.py:120:                per_query_scores=scores,
research/drift_recovery/artifacts.py:121:                extra_arrays=extras,
research/drift_recovery/artifacts.py:122:                metadata=payload.get("metadata", {}),
research/drift_recovery/artifacts.py:123:            )
research/drift_recovery/artifacts.py:124:        pack.validate()
research/drift_recovery/artifacts.py:125:        return pack
research/drift_recovery/artifacts.py:126:
research/drift_recovery/tests/test_estimator.py:1:"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
research/drift_recovery/tests/test_estimator.py:2:
research/drift_recovery/tests/test_estimator.py:3:from __future__ import annotations
research/drift_recovery/tests/test_estimator.py:4:
research/drift_recovery/tests/test_estimator.py:5:import json
research/drift_recovery/tests/test_estimator.py:6:import unittest
research/drift_recovery/tests/test_estimator.py:7:from unittest.mock import patch
research/drift_recovery/tests/test_estimator.py:8:
research/drift_recovery/tests/test_estimator.py:9:import numpy as np
research/drift_recovery/tests/test_estimator.py:10:
research/drift_recovery/tests/test_estimator.py:11:from research.drift_recovery.artifacts import EmbeddingPack
research/drift_recovery/tests/test_estimator.py:12:from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
research/drift_recovery/tests/test_estimator.py:13:from research.drift_recovery.estimator.features import extract_regime_features
research/drift_recovery/tests/test_estimator.py:14:from research.drift_recovery.estimator.margin_bound import (
research/drift_recovery/tests/test_estimator.py:15:    order_is_preserved,
research/drift_recovery/tests/test_estimator.py:16:    order_preservation_slack,
research/drift_recovery/tests/test_estimator.py:17:    predict_inversion,
research/drift_recovery/tests/test_estimator.py:18:)
research/drift_recovery/tests/test_estimator.py:19:from research.drift_recovery.estimator.predictor import estimate_recovery
research/drift_recovery/tests/test_estimator.py:20:from research.drift_recovery.estimator.validation import leave_one_regime_out
research/drift_recovery/tests/test_estimator.py:21:
research/drift_recovery/tests/test_estimator.py:22:
research/drift_recovery/tests/test_estimator.py:23:def _tiny_pack() -> EmbeddingPack:
research/drift_recovery/tests/test_estimator.py:24:    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
research/drift_recovery/tests/test_estimator.py:25:    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
research/drift_recovery/tests/test_estimator.py:26:    oracle = np.asarray(
research/drift_recovery/tests/test_estimator.py:27:        [
research/drift_recovery/tests/test_estimator.py:28:            [1.0, 0.0],
research/drift_recovery/tests/test_estimator.py:29:            [0.6, 0.8],
research/drift_recovery/tests/test_estimator.py:30:            [-1.0, 0.0],
research/drift_recovery/tests/test_estimator.py:31:            [0.0, -1.0],
research/drift_recovery/tests/test_estimator.py:32:        ],
research/drift_recovery/tests/test_estimator.py:33:        dtype=np.float64,
research/drift_recovery/tests/test_estimator.py:34:    )
research/drift_recovery/tests/test_estimator.py:35:    old = np.asarray(
research/drift_recovery/tests/test_estimator.py:36:        [
research/drift_recovery/tests/test_estimator.py:37:            [0.8, 0.2],
research/drift_recovery/tests/test_estimator.py:38:            [0.4, 0.9],
research/drift_recovery/tests/test_estimator.py:39:            [-0.9, 0.1],
research/drift_recovery/tests/test_estimator.py:40:            [0.1, -0.8],
research/drift_recovery/tests/test_estimator.py:41:        ],
research/drift_recovery/tests/test_estimator.py:42:        dtype=np.float64,
research/drift_recovery/tests/test_estimator.py:43:    )
research/drift_recovery/tests/test_estimator.py:44:    return EmbeddingPack(
research/drift_recovery/tests/test_estimator.py:45:        Do=old,
research/drift_recovery/tests/test_estimator.py:46:        Dor=oracle,
research/drift_recovery/tests/test_estimator.py:47:        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
research/drift_recovery/tests/test_estimator.py:48:        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
research/drift_recovery/tests/test_estimator.py:49:        query_ids=np.asarray(["q1"]),
research/drift_recovery/tests/test_estimator.py:50:        qrels={"q1": {"relevant": 1.0}},
research/drift_recovery/tests/test_estimator.py:51:        # The legacy/literal index is deliberately contaminated.  D3 must use
research/drift_recovery/tests/test_estimator.py:52:        # the explicit safe index below and never pass qrels to ridge fitting.
research/drift_recovery/tests/test_estimator.py:53:        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
research/drift_recovery/tests/test_estimator.py:54:        per_query_scores={
research/drift_recovery/tests/test_estimator.py:55:            "floor": np.asarray([0.0]),
research/drift_recovery/tests/test_estimator.py:56:            "oracle": np.asarray([1.0]),
research/drift_recovery/tests/test_estimator.py:57:            "ridge": np.asarray([0.5]),
research/drift_recovery/tests/test_estimator.py:58:        },
research/drift_recovery/tests/test_estimator.py:59:        extra_arrays={
research/drift_recovery/tests/test_estimator.py:60:            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
research/drift_recovery/tests/test_estimator.py:61:            "method_documents__ridge": old.copy(),
research/drift_recovery/tests/test_estimator.py:62:        },
research/drift_recovery/tests/test_estimator.py:63:        metadata={
research/drift_recovery/tests/test_estimator.py:64:            "dataset": "synthetic",
research/drift_recovery/tests/test_estimator.py:65:            "seed": 7,
research/drift_recovery/tests/test_estimator.py:66:            "anchor_fraction": 0.4,
research/drift_recovery/tests/test_estimator.py:67:            "k": 10,
research/drift_recovery/tests/test_estimator.py:68:            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
research/drift_recovery/tests/test_estimator.py:69:            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
research/drift_recovery/tests/test_estimator.py:70:        },
research/drift_recovery/tests/test_estimator.py:71:    )
research/drift_recovery/tests/test_estimator.py:72:
research/drift_recovery/tests/test_estimator.py:73:
research/drift_recovery/tests/test_estimator.py:74:def _calibration_features(value: float):
research/drift_recovery/tests/test_estimator.py:75:    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
research/drift_recovery/tests/test_estimator.py:76:
research/drift_recovery/tests/test_estimator.py:77:
research/drift_recovery/tests/test_estimator.py:78:class TestMarginBound(unittest.TestCase):
research/drift_recovery/tests/test_estimator.py:79:    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
research/drift_recovery/tests/test_estimator.py:80:        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
research/drift_recovery/tests/test_estimator.py:81:        self.assertAlmostEqual(float(slack), 0.3)
research/drift_recovery/tests/test_estimator.py:82:        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
research/drift_recovery/tests/test_estimator.py:83:        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
research/drift_recovery/tests/test_estimator.py:84:
research/drift_recovery/tests/test_estimator.py:85:        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
research/drift_recovery/tests/test_estimator.py:86:        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
research/drift_recovery/tests/test_estimator.py:87:
research/drift_recovery/tests/test_estimator.py:88:    def test_negative_error_norm_is_rejected(self) -> None:
research/drift_recovery/tests/test_estimator.py:89:        with self.assertRaisesRegex(ValueError, "nonnegative"):
research/drift_recovery/tests/test_estimator.py:90:            predict_inversion(0.5, 1.0, -0.1, 0.2)
research/drift_recovery/tests/test_estimator.py:91:
research/drift_recovery/tests/test_estimator.py:92:
research/drift_recovery/tests/test_estimator.py:93:class TestEstimatorFeatures(unittest.TestCase):
research/drift_recovery/tests/test_estimator.py:94:    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
research/drift_recovery/tests/test_estimator.py:95:        first = extract_regime_features(_tiny_pack())
research/drift_recovery/tests/test_estimator.py:96:        second = extract_regime_features(_tiny_pack())
research/drift_recovery/tests/test_estimator.py:97:
research/drift_recovery/tests/test_estimator.py:98:        self.assertEqual(
research/drift_recovery/tests/test_estimator.py:99:            json.dumps(first, sort_keys=True, allow_nan=False),
research/drift_recovery/tests/test_estimator.py:100:            json.dumps(second, sort_keys=True, allow_nan=False),
research/drift_recovery/tests/test_estimator.py:101:        )
research/drift_recovery/tests/test_estimator.py:102:        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
research/drift_recovery/tests/test_estimator.py:103:        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
research/drift_recovery/tests/test_estimator.py:104:        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
research/drift_recovery/tests/test_estimator.py:105:
research/drift_recovery/tests/test_estimator.py:106:    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
research/drift_recovery/tests/test_estimator.py:10…16089 tokens truncated…map misordered the regimes."
research/drift_recovery/run_estimator.py:298:        )
research/drift_recovery/run_estimator.py:299:    lines = [
research/drift_recovery/run_estimator.py:300:        "# D3 recoverability estimator validation",
research/drift_recovery/run_estimator.py:301:        "",
research/drift_recovery/run_estimator.py:302:        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
research/drift_recovery/run_estimator.py:303:        "No regime's own measured recovery trains its held-out prediction.",
research/drift_recovery/run_estimator.py:304:        "",
research/drift_recovery/run_estimator.py:305:        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
research/drift_recovery/run_estimator.py:306:        "|---|---:|---:|---:|---|---|",
research/drift_recovery/run_estimator.py:307:    ]
research/drift_recovery/run_estimator.py:308:    for row in validation["rows"]:
research/drift_recovery/run_estimator.py:309:        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
research/drift_recovery/run_estimator.py:310:        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
research/drift_recovery/run_estimator.py:311:        lines.append(
research/drift_recovery/run_estimator.py:312:            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
research/drift_recovery/run_estimator.py:313:            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
research/drift_recovery/run_estimator.py:314:        )
research/drift_recovery/run_estimator.py:315:    lines.extend(
research/drift_recovery/run_estimator.py:316:        [
research/drift_recovery/run_estimator.py:317:            "",
research/drift_recovery/run_estimator.py:318:            f"- Held-out regimes: **{validation['held_out_count']}**",
research/drift_recovery/run_estimator.py:319:            f"- Spearman(R_hat, R): **{rho_text}**",
research/drift_recovery/run_estimator.py:320:            f"- Three-way bin accuracy: **{accuracy_text}** "
research/drift_recovery/run_estimator.py:321:            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
research/drift_recovery/run_estimator.py:322:            f"- Verdict: **{validation['verdict']}**",
research/drift_recovery/run_estimator.py:323:            "",
research/drift_recovery/run_estimator.py:324:            validation["scoping_note"],
research/drift_recovery/run_estimator.py:325:            "",
research/drift_recovery/run_estimator.py:326:            failure_note,
research/drift_recovery/run_estimator.py:327:            "",
research/drift_recovery/run_estimator.py:328:            "Negative/scoping boundary: the per-query inequality only flags absence of an "
research/drift_recovery/run_estimator.py:329:            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
research/drift_recovery/run_estimator.py:330:            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
research/drift_recovery/run_estimator.py:331:            "distortion alone does not determine NDCG@10.",
research/drift_recovery/run_estimator.py:332:            "",
research/drift_recovery/run_estimator.py:333:            "## Regime availability",
research/drift_recovery/run_estimator.py:334:            "",
research/drift_recovery/run_estimator.py:335:        ]
research/drift_recovery/run_estimator.py:336:    )
research/drift_recovery/run_estimator.py:337:    for item in manifest["regimes"]:
research/drift_recovery/run_estimator.py:338:        detail = item.get("reason") or item.get("pack_prefix") or ""
research/drift_recovery/run_estimator.py:339:        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
research/drift_recovery/run_estimator.py:340:    return "\n".join(lines) + "\n"
research/drift_recovery/run_estimator.py:341:
research/drift_recovery/run_estimator.py:342:
research/drift_recovery/run_estimator.py:343:def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
research/drift_recovery/run_estimator.py:344:    rho = validation["spearman"]
research/drift_recovery/run_estimator.py:345:    accuracy = validation["bin_accuracy"]
research/drift_recovery/run_estimator.py:346:    rho_text = "undefined" if rho is None else f"{rho:.3f}"
research/drift_recovery/run_estimator.py:347:    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
research/drift_recovery/run_estimator.py:348:    available_names = [str(item["name"]) for item in available_items]
research/drift_recovery/run_estimator.py:349:    inversion_saturated = bool(available_items) and all(
research/drift_recovery/run_estimator.py:350:        item.get("predicted_inversion_rate") == 1.0 for item in available_items
research/drift_recovery/run_estimator.py:351:    )
research/drift_recovery/run_estimator.py:352:    if inversion_saturated:
research/drift_recovery/run_estimator.py:353:        limitation = (
research/drift_recovery/run_estimator.py:354:            "Most important limitation: the sufficient bound saturated at a 100% predicted "
research/drift_recovery/run_estimator.py:355:            "violation rate in all four regimes despite measured recovery spanning roughly "
research/drift_recovery/run_estimator.py:356:            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
research/drift_recovery/run_estimator.py:357:        )
research/drift_recovery/run_estimator.py:358:    else:
research/drift_recovery/run_estimator.py:359:        limitation = (
research/drift_recovery/run_estimator.py:360:            "Most important limitation: this bound tracks one relevant/competitor order and "
research/drift_recovery/run_estimator.py:361:            "cannot certify the full top-k NDCG ordering."
research/drift_recovery/run_estimator.py:362:        )
research/drift_recovery/run_estimator.py:363:    return "\n".join(
research/drift_recovery/run_estimator.py:364:        [
research/drift_recovery/run_estimator.py:365:            "# D3 report — recoverability estimator",
research/drift_recovery/run_estimator.py:366:            "",
research/drift_recovery/run_estimator.py:367:            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
research/drift_recovery/run_estimator.py:368:            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
research/drift_recovery/run_estimator.py:369:            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
research/drift_recovery/run_estimator.py:370:            "training-only cross-fitted overprediction residuals produce its lower band.",
research/drift_recovery/run_estimator.py:371:            "",
research/drift_recovery/run_estimator.py:372:            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
research/drift_recovery/run_estimator.py:373:            "",
research/drift_recovery/run_estimator.py:374:            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
research/drift_recovery/run_estimator.py:375:            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
research/drift_recovery/run_estimator.py:376:            "",
research/drift_recovery/run_estimator.py:377:            validation["scoping_note"],
research/drift_recovery/run_estimator.py:378:            "",
research/drift_recovery/run_estimator.py:379:            limitation,
research/drift_recovery/run_estimator.py:380:            "",
research/drift_recovery/run_estimator.py:381:        ]
research/drift_recovery/run_estimator.py:382:    )
research/drift_recovery/run_estimator.py:383:
research/drift_recovery/run_estimator.py:384:
research/drift_recovery/run_estimator.py:385:def run_d3(
research/drift_recovery/run_estimator.py:386:    output_dir: Path = DEFAULT_OUTPUT_DIR,
research/drift_recovery/run_estimator.py:387:    device: str = "cuda",
research/drift_recovery/run_estimator.py:388:    rebuild_packs: bool = False,
research/drift_recovery/run_estimator.py:389:    only_existing: bool = False,
research/drift_recovery/run_estimator.py:390:) -> Dict[str, Any]:
research/drift_recovery/run_estimator.py:391:    output_dir = Path(output_dir)
research/drift_recovery/run_estimator.py:392:    packs_dir = output_dir / "packs"
research/drift_recovery/run_estimator.py:393:    packs_dir.mkdir(parents=True, exist_ok=True)
research/drift_recovery/run_estimator.py:394:    manifest: Dict[str, Any] = {
research/drift_recovery/run_estimator.py:395:        "record_type": "d3_estimator_regime_manifest",
research/drift_recovery/run_estimator.py:396:        "environment": {
research/drift_recovery/run_estimator.py:397:            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
research/drift_recovery/run_estimator.py:398:            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
research/drift_recovery/run_estimator.py:399:            "device": device,
research/drift_recovery/run_estimator.py:400:        },
research/drift_recovery/run_estimator.py:401:        "parity_tolerance": 1e-12,
research/drift_recovery/run_estimator.py:402:        "regimes": [],
research/drift_recovery/run_estimator.py:403:    }
research/drift_recovery/run_estimator.py:404:    validation_regimes = []
research/drift_recovery/run_estimator.py:405:    for spec in REGIMES:
research/drift_recovery/run_estimator.py:406:        if only_existing and spec.existing_pack is None:
research/drift_recovery/run_estimator.py:407:            manifest["regimes"].append(
research/drift_recovery/run_estimator.py:408:                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
research/drift_recovery/run_estimator.py:409:            )
research/drift_recovery/run_estimator.py:410:            continue
research/drift_recovery/run_estimator.py:411:        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
research/drift_recovery/run_estimator.py:412:        try:
research/drift_recovery/run_estimator.py:413:            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
research/drift_recovery/run_estimator.py:414:                print(f"[D3] reloading {spec.name}", flush=True)
research/drift_recovery/run_estimator.py:415:                pack = EmbeddingPack.load(prefix)
research/drift_recovery/run_estimator.py:416:            elif spec.existing_pack is not None:
research/drift_recovery/run_estimator.py:417:                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
research/drift_recovery/run_estimator.py:418:            else:
research/drift_recovery/run_estimator.py:419:                print(f"[D3] building {spec.name} sequentially", flush=True)
research/drift_recovery/run_estimator.py:420:                pack = build_regime_pack(spec, prefix, device=device)
research/drift_recovery/run_estimator.py:421:            parity = assert_harness_parity(pack, atol=1e-12)
research/drift_recovery/run_estimator.py:422:            ground_truth = safe_ridge_ground_truth(pack)
research/drift_recovery/run_estimator.py:423:            features = extract_regime_features(pack)
research/drift_recovery/run_estimator.py:424:            feature_payload = {
research/drift_recovery/run_estimator.py:425:                **features,
research/drift_recovery/run_estimator.py:426:                "regime_name": spec.name,
research/drift_recovery/run_estimator.py:427:                "ground_truth": ground_truth,
research/drift_recovery/run_estimator.py:428:                "pack_prefix": str(prefix),
research/drift_recovery/run_estimator.py:429:                "harness_parity": parity,
research/drift_recovery/run_estimator.py:430:            }
research/drift_recovery/run_estimator.py:431:            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
research/drift_recovery/run_estimator.py:432:            validation_regimes.append(
research/drift_recovery/run_estimator.py:433:                {
research/drift_recovery/run_estimator.py:434:                    "name": spec.name,
research/drift_recovery/run_estimator.py:435:                    "features": features,
research/drift_recovery/run_estimator.py:436:                    "recovery": ground_truth["ridge_recovery"],
research/drift_recovery/run_estimator.py:437:                }
research/drift_recovery/run_estimator.py:438:            )
research/drift_recovery/run_estimator.py:439:            manifest["regimes"].append(
research/drift_recovery/run_estimator.py:440:                {
research/drift_recovery/run_estimator.py:441:                    "name": spec.name,
research/drift_recovery/run_estimator.py:442:                    "dataset": spec.dataset,
research/drift_recovery/run_estimator.py:443:                    "swap_model": spec.swap_model,
research/drift_recovery/run_estimator.py:444:                    "anchor_fraction": spec.anchor_fraction,
research/drift_recovery/run_estimator.py:445:                    "status": "available",
research/drift_recovery/run_estimator.py:446:                    "pack_prefix": str(prefix),
research/drift_recovery/run_estimator.py:447:                    "pack_sha256": _pack_digest(prefix),
research/drift_recovery/run_estimator.py:448:                    "harness_parity": parity,
research/drift_recovery/run_estimator.py:449:                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
research/drift_recovery/run_estimator.py:450:                    "document_count": int(features["summary"]["document_count"]),
research/drift_recovery/run_estimator.py:451:                    "query_count": int(features["summary"]["query_count"]),
research/drift_recovery/run_estimator.py:452:                    "safe_fit_count": int(features["summary"]["fit_count"]),
research/drift_recovery/run_estimator.py:453:                    "predicted_inversion_rate": float(
research/drift_recovery/run_estimator.py:454:                        features["summary"]["predicted_inversion_rate"]
research/drift_recovery/run_estimator.py:455:                    ),
research/drift_recovery/run_estimator.py:456:                }
research/drift_recovery/run_estimator.py:457:            )
research/drift_recovery/run_estimator.py:458:        except Exception as exc:  # availability failures are part of the requested artifact
research/drift_recovery/run_estimator.py:459:            reason = f"{type(exc).__name__}: {exc}"
research/drift_recovery/run_estimator.py:460:            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
research/drift_recovery/run_estimator.py:461:            manifest["regimes"].append(
research/drift_recovery/run_estimator.py:462:                {
research/drift_recovery/run_estimator.py:463:                    "name": spec.name,
research/drift_recovery/run_estimator.py:464:                    "dataset": spec.dataset,
research/drift_recovery/run_estimator.py:465:                    "swap_model": spec.swap_model,
research/drift_recovery/run_estimator.py:466:                    "anchor_fraction": spec.anchor_fraction,
research/drift_recovery/run_estimator.py:467:                    "status": "skipped",
research/drift_recovery/run_estimator.py:468:                    "reason": reason,
research/drift_recovery/run_estimator.py:469:                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
research/drift_recovery/run_estimator.py:470:                }
research/drift_recovery/run_estimator.py:471:            )
research/drift_recovery/run_estimator.py:472:
research/drift_recovery/run_estimator.py:473:    validation = leave_one_regime_out(validation_regimes)
research/drift_recovery/run_estimator.py:474:    _write_json(output_dir / "validation.json", validation)
research/drift_recovery/run_estimator.py:475:    if validation_regimes:
research/drift_recovery/run_estimator.py:476:        calibrator = fit_calibration(
research/drift_recovery/run_estimator.py:477:            [item["features"] for item in validation_regimes],
research/drift_recovery/run_estimator.py:478:            [item["recovery"] for item in validation_regimes],
research/drift_recovery/run_estimator.py:479:            regime_names=[item["name"] for item in validation_regimes],
research/drift_recovery/run_estimator.py:480:        )
research/drift_recovery/run_estimator.py:481:        _write_json(output_dir / "calibration.json", calibrator.to_dict())
research/drift_recovery/run_estimator.py:482:    _write_json(output_dir / "regime_manifest.json", manifest)
research/drift_recovery/run_estimator.py:483:    (output_dir / "estimator_validation.md").write_text(
research/drift_recovery/run_estimator.py:484:        _render_validation(validation, manifest), encoding="utf-8"
research/drift_recovery/run_estimator.py:485:    )
research/drift_recovery/run_estimator.py:486:    available_names = [item["name"] for item in validation_regimes]
research/drift_recovery/run_estimator.py:487:    available_manifest_items = [
research/drift_recovery/run_estimator.py:488:        item for item in manifest["regimes"] if item["status"] == "available"
research/drift_recovery/run_estimator.py:489:    ]
research/drift_recovery/run_estimator.py:490:    (output_dir / "D3_REPORT.md").write_text(
research/drift_recovery/run_estimator.py:491:        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
research/drift_recovery/run_estimator.py:492:    )
research/drift_recovery/run_estimator.py:493:    return {
research/drift_recovery/run_estimator.py:494:        "output_dir": str(output_dir),
research/drift_recovery/run_estimator.py:495:        "available_regimes": available_names,
research/drift_recovery/run_estimator.py:496:        "validation": validation,
research/drift_recovery/run_estimator.py:497:    }
research/drift_recovery/run_estimator.py:498:
research/drift_recovery/run_estimator.py:499:
research/drift_recovery/run_estimator.py:500:def build_parser() -> argparse.ArgumentParser:
research/drift_recovery/run_estimator.py:501:    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
research/drift_recovery/run_estimator.py:502:    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
research/drift_recovery/run_estimator.py:503:    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
research/drift_recovery/run_estimator.py:504:    parser.add_argument("--rebuild-packs", action="store_true")
research/drift_recovery/run_estimator.py:505:    parser.add_argument("--only-existing", action="store_true")
research/drift_recovery/run_estimator.py:506:    return parser
research/drift_recovery/run_estimator.py:507:
research/drift_recovery/run_estimator.py:508:
research/drift_recovery/run_estimator.py:509:def main(argv=None) -> int:
research/drift_recovery/run_estimator.py:510:    args = build_parser().parse_args(argv)
research/drift_recovery/run_estimator.py:511:    result = run_d3(
research/drift_recovery/run_estimator.py:512:        output_dir=args.output_dir,
research/drift_recovery/run_estimator.py:513:        device=args.device,
research/drift_recovery/run_estimator.py:514:        rebuild_packs=args.rebuild_packs,
research/drift_recovery/run_estimator.py:515:        only_existing=args.only_existing,
research/drift_recovery/run_estimator.py:516:    )
research/drift_recovery/run_estimator.py:517:    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
research/drift_recovery/run_estimator.py:518:    return 0
research/drift_recovery/run_estimator.py:519:
research/drift_recovery/run_estimator.py:520:
research/drift_recovery/run_estimator.py:521:if __name__ == "__main__":
research/drift_recovery/run_estimator.py:522:    raise SystemExit(main())
rg: research/drift_recovery/estimator/*.py: The filename, directory name, or volume label syntax is incorrect. (os error 123)

codex
Focused gates are fully green: 14/14 D1+D3 tests, ruff, compilation, and an integrity pass over all four pack checksums, safe fit indices, and parity values. I’m now running the repository-wide `unittest discover` gate in offline mode; if unrelated/environment-dependent failures appear, I’ll separate them from the D3-focused result with exact counts.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python -m unittest discover -s . -p 'test_*.py'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The full repository suite is still running and has not emitted an early failure. D3’s own tests are already independently green; this broader gate is checking for regressions outside the new package.
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 153059ms:
WARNING: adaptive_percentile=150 out of range [0.0, 100.0], clamping.
WARNING: adaptive_window=0 must be >= 1, using 1.
WARNING: adaptive_min_samples=-5 must be >= 1, using 1.
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS

Phase C Analysis — 2026-07-10T03:35:45Z
Recommendation : review_required
Promotable     : ['guard_p85_t0.01']

Candidate                            NDCG@10     Delta    Win%  Status
----------------------------------------------------------------------
baseline                              0.5000   +0.0000   0.0%  baseline
guard_p85_t0.01                       0.5500   +0.0500  100.0%  PROMOTE

Analysis written to: C:\Users\mattm\AppData\Local\Temp\tmpjx4kb4q9\analysis.json

Phase C Analysis — 2026-07-10T03:35:45Z
Recommendation : review_required
Promotable     : ['guard_p85_t0.01']

Candidate                            NDCG@10     Delta    Win%  Status
----------------------------------------------------------------------
baseline                              0.5000   +0.0000   0.0%  baseline
guard_p85_t0.01                       0.5500   +0.0500  100.0%  PROMOTE

Analysis written to: C:\Users\mattm\AppData\Local\Temp\tmpzz7jj_mb\analysis.json
  invalid: expected 1-10, 's', or 'q'; got 'xyz'
  invalid: grade must be in [1, 10]; got 0
  invalid: grade must be in [1, 10]; got 11

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 2 graded, 0 skipped.
  Sample mean semantic grade: 6.50 (out of 10)
  Sample mean structural score: 92.50 (out of 100)
  Delta (structural - semantic*10): +27.50
  WARNING: 1 finding(s) scored structurally >= semantic*10 + 30 — potential rubric-gap (see C:\Users\mattm\AppData\Local\Temp\tmpi9tfutea\findings_audit_2026-07-09.json).
  Wrote C:\Users\mattm\AppData\Local\Temp\tmpi9tfutea\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmpi9tfutea\findings_audit_latest.json

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 1 graded, 1 skipped.
  Sample mean semantic grade: 7.00 (out of 10)
  Sample mean structural score: 85.00 (out of 100)
  Delta (structural - semantic*10): +15.00
  Wrote C:\Users\mattm\AppData\Local\Temp\tmp2829u0nu\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmp2829u0nu\findings_audit_latest.json

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 2 graded, 0 skipped.
  Sample mean semantic grade: 5.50 (out of 10)
  Sample mean structural score: 92.50 (out of 100)
  Delta (structural - semantic*10): +37.50
  WARNING: 1 finding(s) scored structurally >= semantic*10 + 30 — potential rubric-gap (see C:\Users\mattm\AppData\Local\Temp\tmp2cf23qth\findings_audit_2026-07-09.json).
  Wrote C:\Users\mattm\AppData\Local\Temp\tmp2cf23qth\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmp2cf23qth\findings_audit_latest.json
Warning: ID mapping failed, using raw IDs: Retrieve failed
  Evaluating pre-sedimentation NDCG...
  Running 10 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
  Evaluating pre-sedimentation NDCG...
  Running 5 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
  Evaluating pre-sedimentation NDCG...
  Running 5 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
ERROR: Task 'NonExistentTask' not found in MTEB registry!
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke FAIL (1 check(s)):
  - fake failure
[BHS] floor-tier smoke PASS
Created checkpoint: cp_0_20260101_000000
Created checkpoint: cp_1_20260101_000001
Created checkpoint: cp_2_20260101_000002
Created checkpoint: cp_3_20260101_000003
Created checkpoint: cp_4_20260101_000004
Deleted checkpoint: cp_0_20260101_000000
Deleted checkpoint: cp_1_20260101_000001
Deleted checkpoint: cp_2_20260101_000002
Cleaned up 3 old checkpoints
Created checkpoint: only_one_20260709_233547
Created checkpoint: valid_name-123_20260709_233547
Created checkpoint: backup_20260709_233547
Created checkpoint: backup_20260709_233547
Created checkpoint: extra_20260709_233547
Created checkpoint: test_cp_20260709_233547
Created checkpoint: my_backup_20260709_233547
Created checkpoint: backup_20260709_233547
Created checkpoint: no_adapter_20260709_233547
ERROR: Checkpoint 'no_such_checkpoint' not found
Created checkpoint: to_delete_20260709_233547
Deleted checkpoint: to_delete_20260709_233547
Created checkpoint: to_delete_20260709_233547
Deleted checkpoint: to_delete_20260709_233547
Created checkpoint: first_20260101_000000
Created checkpoint: second_20260101_000001
Deleted checkpoint: second_20260101_000001
Deleted checkpoint: first_20260101_000000
Created checkpoint: first_20260709_233547
Created checkpoint: second_20260709_233547
Created checkpoint: third_20260709_233547
Created checkpoint: persistent_20260709_233547
Created checkpoint: snap_20260709_233547
Restoring checkpoint: snap_20260709_233547
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpk9lxkn55\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_233547
Restoring checkpoint: snap_20260709_233547
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpk781qhy0\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_233547
Restoring checkpoint: snap_20260709_233547
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmptiasly1n\restored_adapter.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_233547
Restoring checkpoint: snap_20260709_233547
Created checkpoint: snap_20260709_233547
Restoring checkpoint: snap_20260709_233547
ERROR: Checkpoint file hash mismatch. Refusing restore.
Created checkpoint: snap_20260709_233547
ERROR: Checkpoint 'nonexistent_id' not found
ERROR: No checkpoints available
Created checkpoint: before_bad_train_20260709_233547
Operation failed or not marked successful, rolling back to checkpoint before_bad_train_20260709_233547
Restoring checkpoint: before_bad_train_20260709_233547
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpmamllc8a\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: before_no_rollback_20260709_233547
Operation failed but auto_rollback disabled
Created checkpoint: before_forgot_20260709_233547
Operation failed or not marked successful, rolling back to checkpoint before_forgot_20260709_233547
Restoring checkpoint: before_forgot_20260709_233547
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpb5137q_3\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: before_fail_both_20260709_233547
Operation failed or not marked successful, rolling back to checkpoint before_fail_both_20260709_233547
ERROR: Rollback failed: Simulated rollback failure
WARNING: Original exception preserved despite rollback failure
Created checkpoint: before_rollback_fail_20260709_233547
Operation failed or not marked successful, rolling back to checkpoint before_rollback_fail_20260709_233547
ERROR: Rollback failed: Simulated rollback failure
Created checkpoint: before_op1_20260709_233547
Operation 'op1' completed successfully
Created checkpoint: before_train_20260709_233547
Operation 'train' completed successfully
23:35:47 [ERROR] [error] Error message
23:35:48 [ERROR] [level_test] Message at ERROR
23:35:48 [ERROR] [error] Validation failed
23:35:48 [ERROR] [error] Connection timeout
23:35:48 [ERROR] [error] failing_operation failed after 0.010s
23:35:48 [ERROR] [error] error_op failed after 0.000s
23:35:48 [INFO] [test] Event from logger1
23:35:48 [INFO] [test] Event from logger2
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmpn36bw6zg\flash.img in raw mode...
>> Fetching data from silicon...
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmpreb8rgnj in raw mode...
>> Fetching data from silicon...
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmp5vgwe9vq in raw mode...
>> Fetching data from silicon...
Extracting weights for Computational Storage serialization...
23:35:56 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
[BHS] floor-tier smoke PASS
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_233600
Operation 'sedimentation_cycle_threshold_1' completed successfully

--- HIERARCHICAL SEDIMENTATION (Threshold=1, LR=0.001) ---
Collapse targets: 5 candidates.
23:36:02 [INFO] [training_start] Starting training on 5 samples
Phase 1: Per-cluster training (2 clusters, 2 epochs)...
Created checkpoint: before_hierarchical_sedimentation_threshold_1_20260709_233602
Phase 2: Global refinement (2 epochs, LR=0.000100)...
Adapter weights saved.
Syncing updated vectors to Qdrant...
23:36:05 [INFO] [hierarchical_sedimentation_success] Hierarchical training completed successfully. Updated 5 vectors.
Operation 'hierarchical_sedimentation_threshold_1' completed successfully
23:36:05 [INFO] [training_complete] Training complete | Updated: 5 | Failed: 0
Hierarchical sedimentation complete. Updated 5 vectors, 0 failed.
--- HIERARCHICAL SLEEP CYCLE COMPLETE ---
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_233608
Operation 'sedimentation_cycle_threshold_1' completed successfully
WARNING: teacher_weight=1.5 out of range [0.0, 1.0], clamping.
WARNING: teacher_weight=-0.5 out of range [0.0, 1.0], clamping.
WARNING: training_mode='invalid_mode' invalid. Valid options: ['baseline', 'offline', 'hybrid']. Using 'baseline'.
[model-scope] Observation enabled.
[model-scope] WARNING: Could not enable observation: no transformer model
Created checkpoint: before_model_scope_shadow_policy_20260709_233618
23:36:18 [INFO] [initialization] Initializing Antigravity Engine with model: ollama:nomic-embed-text
23:36:18 [INFO] [embedding_backend_init] Initializing Ollama backend: nomic-embed-text
23:36:22 [ERROR] [error] Ollama connection lost for doc 0
23:36:26 [ERROR] [error] Ollama connection lost for doc 0
23:36:30 [ERROR] [error] Ollama connection lost for doc 0
23:36:30 [ERROR] [error] Failed to embed doc 0 after retries
23:36:30 [INFO] [embedding_backend_ready] Ollama backend connected. Vector size: 768
23:36:30 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([192, 384]) from checkpoint, the shape in current model is torch.Size([384, 768]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([192]) from checkpoint, the shape in current model is torch.Size([384]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([384, 192]) from checkpoint, the shape in current model is torch.Size([768, 384]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([768]).
23:36:30 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:36:31 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:36:31 [INFO] [sedimentation_start] Running sedimentation cycle (Mode=baseline, Threshold=1, LR=0.001)
23:36:31 [INFO] [training_preparation] Found 1 collapsing node candidates
23:36:31 [INFO] [training_start] Starting training on 1 samples
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_233631
23:36:31 [INFO] [checkpoint] Checkpoint save: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:36:31 [INFO] [vector_update] Syncing updated vectors to Qdrant
23:36:31 [INFO] [sedimentation_success] Training cycle completed successfully. Updated 1 vectors.
Operation 'sedimentation_cycle_threshold_1' completed successfully
23:36:31 [INFO] [training_complete] Training complete | Updated: 1 | Failed: 0
23:36:31 [INFO] [initialization] Initializing Antigravity Engine with model: ollama:nomic-embed-text
23:36:31 [INFO] [embedding_backend_init] Initializing Ollama backend: nomic-embed-text
23:36:35 [ERROR] [error] Ollama connection lost for doc 0
23:36:39 [ERROR] [error] Ollama connection lost for doc 0
23:36:43 [ERROR] [error] Ollama connection lost for doc 0
23:36:43 [ERROR] [error] Failed to embed doc 0 after retries
23:36:43 [INFO] [embedding_backend_ready] Ollama backend connected. Vector size: 768
23:36:43 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:36:43 [INFO] [checkpoint] Checkpoint load: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:36:43 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:36:43 [INFO] [sedimentation_start] Running sedimentation cycle (Mode=baseline, Threshold=1, LR=0.001)
23:36:43 [INFO] [training_preparation] Found 2 collapsing node candidates
23:36:43 [INFO] [noise_injection] Enabled noise injection with base scale 0.1
23:36:43 [INFO] [training_start] Starting training on 2 samples
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_233643
23:36:44 [INFO] [checkpoint] Checkpoint save: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:36:44 [INFO] [vector_update] Syncing updated vectors to Qdrant
23:36:44 [INFO] [sedimentation_success] Training cycle completed successfully. Updated 2 vectors.
Operation 'sedimentation_cycle_threshold_1' completed successfully
23:36:44 [INFO] [training_complete] Training complete | Updated: 2 | Failed: 0
{
  "commands": [
    {
      "name": "overlay_model_scope_tests",
      "command": [
        "C:\\Program Files\\Python311\\python.exe",
        "-m",
        "unittest",
        "test_adaptive_overlay.py",
        "test_run_model_scope_campaign.py",
        "test_model_scope_overlay_smoke.py",
        "test_promotion_contract.py",
        "test_attnres_repeat_seed_decision.py",
        "-v"
      ]
    },
    {
      "name": "model_scope_overlay_smoke",
      "command": [
        "C:\\Program Files\\Python311\\python.exe",
        "run_model_scope_overlay_smoke.py",
        "--output-dir",
        "experiment_runs\\overlay-model-scope-validation\\latest\\smoke"
      ]
    }
  ]
}
Extracting weights for Computational Storage serialization...
Extracting weights for Computational Storage serialization...
23:36:49 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:36:49 [INFO] [phase_c_gates] Gate readiness
23:36:49 [INFO] [phase_c_dataset_load] Loading dataset: X
23:36:49 [INFO] [phase_c_candidate_start] Evaluating baseline on X
23:36:49 [INFO] [phase_c_candidate_done] baseline on X: NDCG@10=0.0000
23:36:49 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpaw8tgdd3\phase_c_results.json
23:36:49 [INFO] [phase_c_done] Phase C evaluation complete
23:36:49 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:36:49 [INFO] [phase_c_gates] Gate readiness
23:36:49 [INFO] [phase_c_dataset_load] Loading dataset: X
23:36:49 [INFO] [phase_c_candidate_start] Evaluating baseline on X
23:36:49 [INFO] [phase_c_candidate_done] baseline on X: NDCG@10=0.0000
23:36:49 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpyoakdgjb\phase_c_results.json
23:36:49 [INFO] [phase_c_done] Phase C evaluation complete
23:36:49 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:36:49 [INFO] [phase_c_gates] Gate readiness
23:36:49 [INFO] [phase_c_dataset_load] Loading dataset: TestDS
23:36:49 [INFO] [phase_c_candidate_start] Evaluating baseline on TestDS
23:36:49 [INFO] [phase_c_candidate_done] baseline on TestDS: NDCG@10=0.5000
23:36:49 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmp1syl2i5o\phase_c_results.json
23:36:49 [INFO] [phase_c_done] Phase C evaluation complete
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr6xo_ra_\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr6xo_ra_\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpaspa8ppz\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpaspa8ppz\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpe_1onlr8\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpe_1onlr8\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpgd7m04s5\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpgd7m04s5\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_a7ywjoq\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_a7ywjoq\\report.md"}
23:36:49 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:49 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate

--- HIERARCHICAL SEDIMENTATION (Threshold=3, LR=0.001) ---
Collapse targets: 0 candidates.
No sedimentation targets. Brain is stable.

--- HIERARCHICAL SEDIMENTATION (Threshold=3, LR=0.001) ---
Collapse targets: 10 candidates.
23:36:50 [INFO] [training_start] Starting training on 10 samples
Phase 1: Per-cluster training (2 clusters, 1 epochs)...
Phase 2: Global refinement (1 epochs, LR=0.000100)...
Adapter weights saved.
Syncing updated vectors to Qdrant...
23:36:50 [INFO] [hierarchical_sedimentation_success] Hierarchical training completed successfully. Updated 10 vectors.
23:36:50 [INFO] [training_complete] Training complete | Updated: 10 | Failed: 0
Hierarchical sedimentation complete. Updated 10 vectors, 0 failed.
--- HIERARCHICAL SLEEP CYCLE COMPLETE ---
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpn_qng73s\\calibration-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpn_qng73s\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr5x1dl1h\\sweep\\knob-sweep-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr5x1dl1h\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpmjxoedgt\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpmjxoedgt\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpe5_9gx3w\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpe5_9gx3w\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9lye1xlt\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9lye1xlt\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpxzjluutw\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpxzjluutw\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpbqz86z8x\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpbqz86z8x\\report.md"}
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps0m0pqc\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps0m0pqc\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps0m0pqc\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps0m0pqc\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
23:36:57 [WARNING] [gate_train_skipped] Pool has only 25 samples (< 9999); skipping gate training.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvu55qtht\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvu55qtht\\nested\\output\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvu55qtht\\nested\\output\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvu55qtht\\nested\\output\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
23:36:57 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:36:57 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:36:57 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:36:57 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkc7zk25v\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkc7zk25v\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkc7zk25v\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkc7zk25v\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpk9itis2e\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpk9itis2e\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpk9itis2e\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpk9itis2e\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9mqsayas\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9mqsayas\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9mqsayas\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9mqsayas\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1qr5w4pi\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1qr5w4pi\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1qr5w4pi\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1qr5w4pi\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnyt_5ndy\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnyt_5ndy\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnyt_5ndy\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnyt_5ndy\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp6q1worzt\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp6q1worzt\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp6q1worzt\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp6q1worzt\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpexgxp57n\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpexgxp57n\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpexgxp57n\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpexgxp57n\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr33q9j1r\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr33q9j1r\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr33q9j1r\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr33q9j1r\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpawvwmqz9\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpawvwmqz9\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpawvwmqz9\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpawvwmqz9\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpc8du5qpi\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpc8du5qpi\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpc8du5qpi\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpc8du5qpi\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr8_hxozy\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr8_hxozy\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr8_hxozy\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpr8_hxozy\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp2cp53fiv\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp2cp53fiv\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp2cp53fiv\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp2cp53fiv\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp4hlwxjt3\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp4hlwxjt3\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp4hlwxjt3\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp4hlwxjt3\\custom_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphf3_xhsp\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphf3_xhsp\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphf3_xhsp\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphf3_xhsp\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpscily9fb\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpscily9fb\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpscily9fb\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpscily9fb\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpordwkw4_\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpordwkw4_\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpordwkw4_\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpordwkw4_\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkh3mxfa9\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkh3mxfa9\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkh3mxfa9\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkh3mxfa9\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9bs_zgb2\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9bs_zgb2\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9bs_zgb2\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9bs_zgb2\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpge9qwicj\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpge9qwicj\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpge9qwicj\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpge9qwicj\\gate_train_summary.json"
}
23:36:57 [INFO] [gate_train_complete] Gate training complete.
{
  "run_dir": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpf2lx48qv\\test_run",
  "status": "completed",
  "finished_at": "2026-07-10T03:36:57.389013+00:00",
  "latest_report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpf2lx48qv\\test_run\\reports\\iteration_001.json",
  "terminal_decision": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpf2lx48qv\\test_run\\terminal_decision.json"
}
============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session-32-review
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session-32-review --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-overnight
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-overnight --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session32-verylonglabelverylonglabelverylonglabelverylonglabelve
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session32-verylonglabelverylonglabelverylonglabelverylonglabelve --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

23:36:57 [INFO] [phase_c_mask_gate_load] Loading mask gate: C:\Users\mattm\AppData\Local\Temp\tmpjadnhelh.json
23:36:57 [INFO] [phase_c_reform_gate_load] Loading reform gate: C:\Users\mattm\AppData\Local\Temp\tmpeou_u_dy.json
23:36:57 [INFO] [phase_c_reform_gate_skip] Attribution pool not found; learned_reform_gate_v1 will be fail-closed
23:36:57 [INFO] [phase_c_reform_gate_train] Training reform gate from: C:\Users\mattm\AppData\Local\Temp\tmpjf6mzr2f.json
23:36:57 [INFO] [phase_c_start] Phase C eval: 2 candidates × 1 datasets
23:36:57 [INFO] [phase_c_gates] Gate readiness
23:36:57 [INFO] [phase_c_dataset_load] Loading dataset: SciFact
23:36:57 [INFO] [phase_c_candidate_start] Evaluating baseline on SciFact
23:36:57 [INFO] [phase_c_candidate_done] baseline on SciFact: NDCG@10=1.0000
23:36:57 [INFO] [phase_c_candidate_start] Evaluating guard_p85_t0.01 on SciFact
23:36:57 [INFO] [phase_c_candidate_done] guard_p85_t0.01 on SciFact: NDCG@10=1.0000
23:36:57 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpp52wsuwc\phase_c_results.json
23:36:57 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpp52wsuwc\overlay_card_baseline.json
23:36:57 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpp52wsuwc\overlay_card_guard_p85_t0.01.json
23:36:57 [INFO] [phase_c_done] Phase C evaluation complete
23:36:57 [INFO] [phase_c_start] Phase C eval: 2 candidates × 1 datasets
23:36:57 [INFO] [phase_c_gates] Gate readiness
23:36:57 [INFO] [phase_c_dataset_load] Loading dataset: SciFact
23:36:57 [INFO] [phase_c_candidate_start] Evaluating baseline on SciFact
23:36:57 [INFO] [phase_c_candidate_done] baseline on SciFact: NDCG@10=0.8155
23:36:57 [INFO] [phase_c_candidate_start] Evaluating guard_p85_t0.01 on SciFact
23:36:57 [INFO] [phase_c_candidate_done] guard_p85_t0.01 on SciFact: NDCG@10=0.8155
23:36:57 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmp__7p3lts\phase_c_results.json
23:36:57 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmp__7p3lts\overlay_card_baseline.json
23:36:57 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmp__7p3lts\overlay_card_guard_p85_t0.01.json
23:36:57 [INFO] [phase_c_done] Phase C evaluation complete
line one
C:\Users\mattm\AppData\Local\Temp\tmplv9k_xzn
C:\Users\mattm\AppData\Local\Temp\tmpowlolt4v
SMOKE_PIPELINE PASS (mocked)
[BHS] ceiling-tier smoke PASS
SMOKE_PIPELINE FAIL: simulated
[BHS] ceiling-tier smoke FAIL: smoke_pipeline.py exit=1
23:36:58 [INFO] [initialization] Initializing Antigravity Engine with model: all-MiniLM-L6-v2
23:36:58 [INFO] [embedding_backend_init] Initializing local backend: all-MiniLM-L6-v2
23:36:58 [INFO] [initialization] Device Selected: cuda
23:36:59 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:36:59 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:36:59 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:36:59 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:36:59 [INFO] [ingestion_start] Ingesting 4 documents
23:36:59 [INFO] [ingestion_complete] Ingestion Complete
23:36:59 [INFO] [initialization] Initializing Antigravity Engine with model: all-MiniLM-L6-v2
23:36:59 [INFO] [embedding_backend_init] Initializing local backend: all-MiniLM-L6-v2
23:36:59 [INFO] [initialization] Device Selected: cuda
23:36:59 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:36:59 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:36:59 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:36:59 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:36:59 [INFO] [ingestion_start] Ingesting 4 documents
23:36:59 [INFO] [ingestion_complete] Ingestion Complete
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:36:59 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
TTS enabled: translation=True, transport=True, steering=False
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp207tkb2g\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
TTS enabled: translation=False, transport=True, steering=True
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmptd9_ab05\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
TTS enabled: translation=True, transport=False, steering=True
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp49c6ppw1\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp3ushquyj\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": false
  }
}
23:37:02 [INFO] [initialization] Initializing Antigravity Engine with model: sentence-transformers/all-MiniLM-L6-v2
23:37:02 [INFO] [embedding_backend_init] Initializing local backend: sentence-transformers/all-MiniLM-L6-v2
23:37:02 [INFO] [initialization] Device Selected: cuda
23:37:02 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:37:02 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:37:02 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:37:02 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:37:02 [INFO] [ingestion_start] Ingesting 2 documents
23:37:02 [INFO] [ingestion_complete] Ingestion Complete
23:37:02 [INFO] [query] Query: 'cat mat...' | Action: FAST
23:37:02 [INFO] [initialization] Initializing Antigravity Engine with model: sentence-transformers/all-MiniLM-L6-v2
23:37:02 [INFO] [embedding_backend_init] Initializing local backend: sentence-transformers/all-MiniLM-L6-v2
23:37:02 [INFO] [initialization] Device Selected: cuda
23:37:02 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:37:02 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:37:02 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:37:02 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:37:02 [INFO] [ingestion_start] Ingesting 2 documents
23:37:03 [INFO] [ingestion_complete] Ingestion Complete
23:37:03 [INFO] [tts_enabled] TTS pipeline enabled
23:37:03 [INFO] [query] Query: 'cat mat...' | Action: FAST
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([256, 512]) from checkpoint, the shape in current model is torch.Size([384, 768]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([256]) from checkpoint, the shape in current model is torch.Size([384]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([512, 256]) from checkpoint, the shape in current model is torch.Size([768, 384]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([512]) from checkpoint, the shape in current model is torch.Size([768]).
WARNING: chelation_p=150 out of range [0, 100], clamping.
WARNING: chelation_p=-10 out of range [0, 100], clamping.
WARNING: epochs=200 out of range [1, 100], clamping.
WARNING: epochs=0 out of range [1, 100], clamping.
WARNING: learning_rate=10.0 out of range [0.0001, 1.0], clamping.
WARNING: learning_rate=1e-05 out of range [0.0001, 1.0], clamping.
WARNING: max_depth=20 out of range [1, 10], clamping.
WARNING: max_depth=0 out of range [1, 10], clamping.
....................................................................................................................................................ERROR: Results file not found: does_not_exist.json
.............................................................................................................................................................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'fast' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'slow' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
.D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'cfg1' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
....D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'a' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'b' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'c' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
.D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 't' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
...D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_benchmark_beir.py:309: RuntimeWarning: BenchmarkResult for 'a' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  results = runner.run_single_dataset("TestDS", corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_benchmark_beir.py:309: RuntimeWarning: BenchmarkResult for 'b' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  results = runner.run_single_dataset("TestDS", corpus, queries, qrels,
..................................................................................................................................................................................................................................................................................................................................................................................................................................s.........s.............................................................................................................................................................................................................................................................................................................................Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
...........................................................................................................................................................................................................................................................................................ss........D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_model_scope_runtime.py:261: UserWarning: torch tensor protocol fallback triggered in hook (mock-tensor compatible path; unexpected in production): AttributeError("'float' object has no attribute 'item'")
  hook_fn(lm, None, tensor)
................................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_model_scope_steering.py:655: UserWarning: Skipping corrupt JSON line in C:\Users\mattm\AppData\Local\Temp\tmp8ivzzpe_\corrupt.jsonl: 'THIS IS NOT VALID JSON {{{'
  loaded = actuator2.load_records(path)
...........................................................................................................................................................................................ssssss..................................................................................s...................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\qwen_scope_adapter.py:43: FutureWarning: You are using `torch.load` with `weights_only=False` (the current default value), which uses the default pickle module implicitly. It is possible to construct malicious pickle data which will execute arbitrary code during unpickling (See https://github.com/pytorch/pytorch/blob/main/SECURITY.md#untrusted-models for more details). In a future release, the default value for `weights_only` will be flipped to `True`. This limits the functions that could be executed during unpickling. Arbitrary objects will no longer be allowed to be loaded via this mode unless they are explicitly allowlisted by the user via `torch.serialization.add_safe_globals`. We recommend you start setting `weights_only=True` for any use case where you don't have full control of the loaded file. Please open an issue on GitHub for any issues related to this experimental feature.
  state_dict = torch.load(Path(path), map_location="cpu")
..............................................................................................................................................................................WARNING: pool has 25 samples, below minimum 9999. Skipping training.
..ERROR: attribution pool file not found: nonexistent_pool.json
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.ERROR: attribution pool file not found: C:\Users\mattm\AppData\Local\Temp\tmpj_z18vba\does_not_exist.json
.ERROR: attribution pool file not found: C:\Users\mattm\AppData\Local\Temp\tmp1ab9g3di\does_not_exist.json
............................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................
----------------------------------------------------------------------
Ran 2754 tests in 123.100s

OK (skipped=11)

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'git status --short; Get-Item adapter_weights.pt -ErrorAction SilentlyContinue | Select-Object FullName,Length,LastWriteTime; git ls-files --stage adapter_weights.pt; Get-Content -Raw research/drift_recovery/out/estimator/D3_REPORT.md; Get-Content -Raw research/drift_recovery/out/estimator/estimator_validation.md' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1566ms:
?? research/

FullName                                                               Length LastWriteTime
--------                                                               ------ -------------
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt 2366160 7/9/2026 11:36:44 PM
# D3 report — recoverability estimator

Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space margin quantiles, leakage-safe ridge correction-error norms, and the per-query margin-bound violation rate. A strongly regularized linear map produces `R_hat`; training-only cross-fitted overprediction residuals produce its lower band.

Regimes used (4): `scifact_minilm_to_mpnet`, `nfcorpus_minilm_to_mpnet`, `scifact_minilm_to_bge_large`, `nfcorpus_minilm_to_bge_large`.

Leave-one-regime-out result: Spearman **-0.400**, three-way bin accuracy **50.0%**. Verdict: **VALIDATION-NEGATIVE**.

Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.

Most important limitation: the sufficient bound saturated at a 100% predicted violation rate in all four regimes despite measured recovery spanning roughly 0.65-0.79, so the core inversion-rate signal did not discriminate regimes.

# D3 recoverability estimator validation

Held-out axis: **regime** (dataset / encoder family / anchor setting). No regime's own measured recovery trains its held-out prediction.

| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |
|---|---:|---:|---:|---|---|
| scifact_minilm_to_mpnet | 0.7891 | 0.6840 | 0.5418 | high | high |
| nfcorpus_minilm_to_mpnet | 0.6471 | 0.7315 | 0.6412 | medium | high |
| scifact_minilm_to_bge_large | 0.7224 | 0.7289 | 0.2234 | high | high |
| nfcorpus_minilm_to_bge_large | 0.6640 | 0.6679 | 0.2125 | medium | high |

- Held-out regimes: **4**
- Spearman(R_hat, R): **-0.400**
- Three-way bin accuracy: **50.0%** (fixed bins: low <1/3, medium <2/3, high >=2/3)
- Verdict: **VALIDATION-NEGATIVE**

Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.

Smallest concrete failure: the bound predicted a violation for **100% of queries in every available regime**, so its inversion-rate feature did not separate observed recoveries ranging from 0.647 to 0.789.

Negative/scoping boundary: the per-query inequality only flags absence of an order-preservation guarantee for one oracle-best relevant/non-relevant pair. It does not prove an inversion, does not model the full top-k ordering, and Gram/margin distortion alone does not determine NDCG@10.

## Regime availability

- `scifact_minilm_to_mpnet`: **available** — research\drift_recovery\out\d1\scifact_evalsplit_pack
- `nfcorpus_minilm_to_mpnet`: **available** — research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_mpnet_pack
- `scifact_minilm_to_bge_large`: **available** — research\drift_recovery\out\estimator\packs\scifact_minilm_to_bge_large_pack
- `nfcorpus_minilm_to_bge_large`: **available** — research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_bge_large_pack



diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

collab: Wait
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a74e5324af42303b4dea5a9987a58c850e6a89aa
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,522 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The repository-wide gate also passed: `2,754` tests, `11` skipped, `0` failures in 123 seconds. One final disclosure I’m tightening in the report is the legacy D1 fit distinction: D3’s SciFact/mpnet target is the leakage-safe recovery `0.7891`, not the legacy literal-fit headline `0.8434`, because the literal index contains eval-positive documents.
apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..5e2ed55d63445aaaff37d6755403be3e0224ad7d
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,527 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 fit note: `scifact_minilm_to_mpnet` uses the pack's explicit "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` target (R=0.7891), not the literal "
+            "`fit_idx` headline (R=0.8434), because that legacy literal index contains 28 "
+            "eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..5e2ed55d63445aaaff37d6755403be3e0224ad7d
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,527 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 fit note: `scifact_minilm_to_mpnet` uses the pack's explicit "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` target (R=0.7891), not the literal "
+            "`fit_idx` headline (R=0.8434), because that legacy literal index contains 28 "
+            "eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python research/drift_recovery/run_estimator.py --device cuda" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..5e2ed55d63445aaaff37d6755403be3e0224ad7d
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,527 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 fit note: `scifact_minilm_to_mpnet` uses the pack's explicit "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` target (R=0.7891), not the literal "
+            "`fit_idx` headline (R=0.8434), because that legacy literal index contains 28 "
+            "eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

2026-07-10T03:39:33.527448Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 2.3 seconds
Output:
research/drift_recovery/out/estimator/validation.json:2:  "bin_accuracy": 0.5,
research/drift_recovery/out/estimator/validation.json:17:      "actual_recovery": 0.7891443001339904,
research/drift_recovery/out/estimator/validation.json:32:      "actual_recovery": 0.647119786542357,
research/drift_recovery/out/estimator/validation.json:47:      "actual_recovery": 0.7223707722283974,
research/drift_recovery/out/estimator/validation.json:62:      "actual_recovery": 0.6640397106924352,
research/drift_recovery/out/estimator/validation.json:75:  "scoping_note": "Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.",
research/drift_recovery/out/estimator/validation.json:77:  "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
research/drift_recovery/out/estimator/D3_REPORT.md:7:Leave-one-regime-out result: Spearman **-0.400**, three-way bin accuracy **50.0%**. Verdict: **VALIDATION-NEGATIVE**.
research/drift_recovery/out/estimator/D3_REPORT.md:9:Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.
research/drift_recovery/out/estimator/scifact_minilm_to_mpnet_features.json:17:        "ridge": {
research/drift_recovery/out/estimator/scifact_minilm_to_mpnet_features.json:107:    "ridge_recovery": 0.7891443001339904
research/drift_recovery/out/estimator/scifact_minilm_to_mpnet_features.json:115:    "ridge": 0.0
research/drift_recovery/out/estimator/estimator_validation.md:13:- Spearman(R_hat, R): **-0.400**
research/drift_recovery/out/estimator/estimator_validation.md:14:- Three-way bin accuracy: **50.0%** (fixed bins: low <1/3, medium <2/3, high >=2/3)
research/drift_recovery/out/estimator/estimator_validation.md:17:Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.
research/drift_recovery/out/d1/scifact_evalsplit_pack.json:151:      "ridge": 0.6811590403826876
research/drift_recovery/out/d1/scifact_evalsplit_pack.json:173:      "ridge": 0.8434286306657018
research/drift_recovery/out/d1/scifact_evalsplit_pack.json:184:        "ridge": 0.8434286306657018
research/drift_recovery/out/estimator/regime_manifest.json:20:        "ridge": 0.0
research/drift_recovery/out/estimator/regime_manifest.json:28:      "safe_ridge_recovery": 0.7891443001339904,
research/drift_recovery/out/estimator/regime_manifest.json:39:        "ridge": 0.0
research/drift_recovery/out/estimator/regime_manifest.json:47:      "safe_ridge_recovery": 0.647119786542357,
research/drift_recovery/out/estimator/regime_manifest.json:58:        "ridge": 0.0
research/drift_recovery/out/estimator/regime_manifest.json:66:      "safe_ridge_recovery": 0.7223707722283974,
research/drift_recovery/out/estimator/regime_manifest.json:77:        "ridge": 0.0
research/drift_recovery/out/estimator/regime_manifest.json:85:      "safe_ridge_recovery": 0.6640397106924352,
rg: regex parse error:
    (?:safe_ridge_ground_truth|fit_ridge_for_pack|ground_truth =|per_query_scores\[\)
    ^
error: unclosed group

 succeeded in 33255ms:
[D3] reloading scifact_minilm_to_mpnet
[D3] reloading nfcorpus_minilm_to_mpnet
[D3] reloading scifact_minilm_to_bge_large
[D3] reloading nfcorpus_minilm_to_bge_large
{
  "available_regimes": [
    "scifact_minilm_to_mpnet",
    "nfcorpus_minilm_to_mpnet",
    "scifact_minilm_to_bge_large",
    "nfcorpus_minilm_to_bge_large"
  ],
  "output_dir": "research\\drift_recovery\\out\\estimator",
  "validation": {
    "bin_accuracy": 0.5,
    "bin_thresholds": {
      "high": "R >= 0.666667",
      "low": "R < 0.333333",
      "medium": "0.333333 <= R < 0.666667"
    },
    "held_out_axis": "regime (dataset / encoder family / anchor setting)",
    "held_out_count": 4,
    "lower_band_coverage": 1.0,
    "record_type": "recoverability_leave_one_regime_out_validation",
    "regime_count": 4,
    "rows": [
      {
        "R_hat": 0.6840096562278455,
        "actual_bin": "high",
        "actual_recovery": 0.7891443001339904,
        "bin_correct": true,
        "lower_band": 0.5418130994796307,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_mpnet",
        "training_regimes": [
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7314921804097357,
        "actual_bin": "medium",
        "actual_recovery": 0.647119786542357,
        "bin_correct": false,
        "lower_band": 0.6412319974171363,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_mpnet",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7289212641928171,
        "actual_bin": "high",
        "actual_recovery": 0.7223707722283974,
        "bin_correct": true,
        "lower_band": 0.22337122509004526,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.667862322352701,
        "actual_bin": "medium",
        "actual_recovery": 0.6640397106924352,
        "bin_correct": false,
        "lower_band": 0.21251608512032283,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large"
        ]
      }
    ],
    "scoping_note": "Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.",
    "spearman": -0.39999999999999997,
    "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
    "verdict": "VALIDATION-NEGATIVE"
  }
}

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..5e2ed55d63445aaaff37d6755403be3e0224ad7d
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,527 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 fit note: `scifact_minilm_to_mpnet` uses the pack's explicit "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` target (R=0.7891), not the literal "
+            "`fit_idx` headline (R=0.8434), because that legacy literal index contains 28 "
+            "eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

collab: Wait
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..5e2ed55d63445aaaff37d6755403be3e0224ad7d
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,527 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def safe_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Run the full NDCG eval only to create validation ground truth."""
+
+    fit = fit_ridge_for_pack(pack)
+    ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ridge_per_query_scores": np.asarray(ridge_scores, dtype=np.float64).tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "fit_count": int(len(fit.fit_idx)),
+        "eval_positive_docs_in_fit": 0,
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["safe_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["safe_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 fit note: `scifact_minilm_to_mpnet` uses the pack's explicit "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` target (R=0.7891), not the literal "
+            "`fit_idx` headline (R=0.8434), because that legacy literal index contains 28 "
+            "eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = safe_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "safe_ridge_recovery": ground_truth["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The adversarial review found one real contract mismatch, and I’m accepting it: I used the D1 leakage-safe sensitivity value (`0.7891`) as SciFact/mpnet’s label, but the locked wording says each ground-truth label is the frozen pack’s D1-measured ridge recovery (`0.8434`). I’ll keep the estimator’s feature fit leakage-safe, but calibrate against the frozen D1 ridge label and disclose that this legacy label came from the contaminated literal D1 fit. The verdict remains negative; the reviewer’s independent recompute changes bin accuracy from 50% to 75%, still below 80%.
2026-07-10T03:40:21.360759Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.8 seconds
Output:
ParserError: 
Line |
   2 |  … rays=($j.array_shapes.PSObject.Properties.Name -join ',')}} | Format- …
     |                                                                ~
     | An empty pipe element is not allowed.

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..2bc0057d6d2b16d1ae9b0eea192d923c90b57379
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,550 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..2bc0057d6d2b16d1ae9b0eea192d923c90b57379
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,550 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            "violation rate in all four regimes despite measured recovery spanning roughly "
+            "0.65-0.79, so the core inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..ed6675edc0f1926babc331e0ff3273284c807860
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,162 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

2026-07-10T03:41:02.463770Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.2 seconds
Output:
# D3 recoverability estimator validation

Held-out axis: **regime** (dataset / encoder family / anchor setting). No regime's own measured recovery trains its held-out prediction.

| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |
|---|---:|---:|---:|---|---|
| scifact_minilm_to_mpnet | 0.7891 | 0.6840 | 0.5418 | high | high |
| nfcorpus_minilm_to_mpnet | 0.6471 | 0.7315 | 0.6412 | medium | high |
| scifact_minilm_to_bge_large | 0.7224 | 0.7289 | 0.2234 | high | high |
| nfcorpus_minilm_to_bge_large | 0.6640 | 0.6679 | 0.2125 | medium | high |

- Held-out regimes: **4**
- Spearman(R_hat, R): **-0.400**
- Three-way bin accuracy: **50.0%** (fixed bins: low <1/3, medium <2/3, high >=2/3)
- Verdict: **VALIDATION-NEGATIVE**

Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.

Smallest concrete failure: the bound predicted a violation for **100% of queries in every available regime**, so its inversion-rate feature did not separate observed recoveries ranging from 0.647 to 0.789.

Negative/scoping boundary: the per-query inequality only flags absence of an order-preservation guarantee for one oracle-best relevant/non-relevant pair. It does not prove an inversion, does not model the full top-k ordering, and Gram/margin distortion alone does not determine NDCG@10.

Legacy D1 fit note: `scifact_minilm_to_mpnet` uses the pack's explicit `extra_arrays["leakage_safe_fit_idx"]` target (R=0.7891), not the literal `fit_idx` headline (R=0.8434), because that legacy literal index contains 28 eval-positive documents.

## Regime availability

- `scifact_minilm_to_mpnet`: **available** — research\drift_recovery\out\d1\scifact_evalsplit_pack
- `nfcorpus_minilm_to_mpnet`: **available** — research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_mpnet_pack
- `scifact_minilm_to_bge_large`: **available** — research\drift_recovery\out\estimator\packs\scifact_minilm_to_bge_large_pack
- `nfcorpus_minilm_to_bge_large`: **available** — research\drift_recovery\out\estimator\packs\nfcorpus_minilm_to_bge_large_pack

# D3 report — recoverability estimator

Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space margin quantiles, leakage-safe ridge correction-error norms, and the per-query margin-bound violation rate. A strongly regularized linear map produces `R_hat`; training-only cross-fitted overprediction residuals produce its lower band.

Regimes used (4): `scifact_minilm_to_mpnet`, `nfcorpus_minilm_to_mpnet`, `scifact_minilm_to_bge_large`, `nfcorpus_minilm_to_bge_large`.

Leave-one-regime-out result: Spearman **-0.400**, three-way bin accuracy **50.0%**. Verdict: **VALIDATION-NEGATIVE**.

Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=50.0%; neither locked success threshold was met.

Most important limitation: the sufficient bound saturated at a 100% predicted violation rate in all four regimes despite measured recovery spanning roughly 0.65-0.79, so the core inversion-rate signal did not discriminate regimes.

research/drift_recovery/run_estimator.py:1:"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
research/drift_recovery/run_estimator.py:53:@dataclass(frozen=True)
research/drift_recovery/run_estimator.py:228:def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
research/drift_recovery/run_estimator.py:229:    """Use the pack's frozen D1 ridge scores as the validation target.
research/drift_recovery/run_estimator.py:231:    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
research/drift_recovery/run_estimator.py:236:    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
research/drift_recovery/run_estimator.py:240:        frozen_ridge_scores,
research/drift_recovery/run_estimator.py:263:                    "ridge": frozen_ridge_scores,
research/drift_recovery/run_estimator.py:275:        "ground_truth_source": "frozen pack per_query_scores.ridge",
research/drift_recovery/run_estimator.py:276:        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
research/drift_recovery/run_estimator.py:284:        "frozen_fit_audit": (
research/drift_recovery/run_estimator.py:307:        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:308:        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:353:            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
research/drift_recovery/run_estimator.py:356:            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
research/drift_recovery/run_estimator.py:378:        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:379:        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:395:            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
research/drift_recovery/run_estimator.py:445:                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
research/drift_recovery/run_estimator.py:450:            ground_truth = frozen_ridge_ground_truth(pack)
research/drift_recovery/run_estimator.py:455:                "ground_truth": ground_truth,
research/drift_recovery/run_estimator.py:464:                    "recovery": ground_truth["ridge_recovery"],
research/drift_recovery/run_estimator.py:477:                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
research/drift_recovery/run_estimator.py:478:                    "leakage_safe_feature_refit_recovery": ground_truth[
rg: research/drift_recovery/out/estimator/*.md: The filename, directory name, or volume label syntax is incorrect. (os error 123)

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; python -m unittest research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery/estimator research/drift_recovery/run_estimator.py research/drift_recovery/tests/test_estimator.py research/drift_recovery/harness_bridge.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1049ms:
All checks passed!

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 29698ms:
test_ground_truth_uses_frozen_pack_ridge_scores (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_ground_truth_uses_frozen_pack_ridge_scores) ... ok
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 7 tests in 0.039s

OK

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python research/drift_recovery/run_estimator.py --device cuda" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 42861ms:
[D3] reloading scifact_minilm_to_mpnet
[D3] reloading nfcorpus_minilm_to_mpnet
[D3] reloading scifact_minilm_to_bge_large
[D3] reloading nfcorpus_minilm_to_bge_large
{
  "available_regimes": [
    "scifact_minilm_to_mpnet",
    "nfcorpus_minilm_to_mpnet",
    "scifact_minilm_to_bge_large",
    "nfcorpus_minilm_to_bge_large"
  ],
  "output_dir": "research\\drift_recovery\\out\\estimator",
  "validation": {
    "bin_accuracy": 0.75,
    "bin_thresholds": {
      "high": "R >= 0.666667",
      "low": "R < 0.333333",
      "medium": "0.333333 <= R < 0.666667"
    },
    "held_out_axis": "regime (dataset / encoder family / anchor setting)",
    "held_out_count": 4,
    "lower_band_coverage": 0.75,
    "record_type": "recoverability_leave_one_regime_out_validation",
    "regime_count": 4,
    "rows": [
      {
        "R_hat": 0.6840096562278455,
        "actual_bin": "high",
        "actual_recovery": 0.8434286306657018,
        "bin_correct": true,
        "lower_band": 0.5418130994796307,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_mpnet",
        "training_regimes": [
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7635889655935123,
        "actual_bin": "medium",
        "actual_recovery": 0.647119786542357,
        "bin_correct": false,
        "lower_band": 0.6473716250700156,
        "lower_band_covers": false,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_mpnet",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7558645839863694,
        "actual_bin": "high",
        "actual_recovery": 0.7223707722283974,
        "bin_correct": true,
        "lower_band": 0.050617102091020216,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.6562146658656209,
        "actual_bin": "medium",
        "actual_recovery": 0.6640397106924352,
        "bin_correct": true,
        "lower_band": 0.0,
        "lower_band_covers": true,
        "predicted_bin": "medium",
        "regime": "nfcorpus_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large"
        ]
      }
    ],
    "scoping_note": "Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=75.0%; neither locked success threshold was met.",
    "spearman": -0.39999999999999997,
    "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
    "verdict": "VALIDATION-NEGATIVE"
  }
}

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

2026-07-10T03:42:23.836139Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.6 seconds
Output:
research/drift_recovery/run_estimator.py:228:def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
research/drift_recovery/run_estimator.py:307:        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:308:        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:378:        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:379:        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
research/drift_recovery/run_estimator.py:450:            ground_truth = frozen_ridge_ground_truth(pack)
research/drift_recovery/run_estimator.py:477:                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
research/drift_recovery/run_estimator.py:478:                    "leakage_safe_feature_refit_recovery": ground_truth[
research/drift_recovery/tests/test_estimator.py:21:from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
research/drift_recovery/tests/test_estimator.py:136:        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
research/drift_recovery/out/estimator\regime_manifest.json:14:      "frozen_ridge_recovery": 0.8434286306657018,
research/drift_recovery/out/estimator\regime_manifest.json:23:      "leakage_safe_feature_refit_recovery": 0.7891443001339904,
research/drift_recovery/out/estimator\regime_manifest.json:37:      "frozen_ridge_recovery": 0.647119786542357,
research/drift_recovery/out/estimator\regime_manifest.json:43:      "leakage_safe_feature_refit_recovery": 0.647119786542357,
research/drift_recovery/out/estimator\regime_manifest.json:57:      "frozen_ridge_recovery": 0.7223707722283974,
research/drift_recovery/out/estimator\regime_manifest.json:63:      "leakage_safe_feature_refit_recovery": 0.7223707722283974,
research/drift_recovery/out/estimator\regime_manifest.json:77:      "frozen_ridge_recovery": 0.6640397106924352,
research/drift_recovery/out/estimator\regime_manifest.json:83:      "leakage_safe_feature_refit_recovery": 0.6640397106924352,
[Errno 22] Invalid argument: 'research/drift_recovery/estimator/*.py'
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The frozen-label correction is now regenerated: four-regime Spearman remains `-0.400`; bin accuracy is `75%` (still below the locked `80%`); lower-band coverage is `75%`; verdict remains `VALIDATION-NEGATIVE`. The estimator feature fit is still leakage-safe, while the report now explicitly separates that fit from the legacy contaminated D1 label. I’m doing the final post-fix integrity and regression pass while the reviewer re-audits the regenerated artifacts.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; python -m unittest research.drift_recovery.tests.test_d1_stats research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery; python -m compileall -q research/drift_recovery; if($LASTEXITCODE -eq 0){'"'compileall: PASS'}" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; @'
import hashlib, json
from pathlib import Path
from research.drift_recovery.artifacts import EmbeddingPack
from research.drift_recovery.estimator.features import extract_regime_features, leakage_safe_fit_indices
from research.drift_recovery.harness_bridge import assert_harness_parity
root=Path('research/drift_recovery/out/estimator')
m=json.loads((root/'regime_manifest.json').read_text())
for item in m['regimes']:
    assert item['status']=='available', item
    p=Path(item['pack_prefix']); pack=EmbeddingPack.load(p)
    assert hashlib.sha256(p.with_suffix('.npz').read_bytes()).hexdigest()==item['pack_sha256']
    assert max(assert_harness_parity(pack, atol=1e-12).values()) <= 1e-12
    assert len(leakage_safe_fit_indices(pack))==item['safe_fit_count']
    frozen=item['frozen_ridge_recovery']; safe=item['leakage_safe_feature_refit_recovery']
    if item['name']=='scifact_minilm_to_mpnet':
        assert abs(frozen-0.8434286306657018)<1e-12
        assert abs(safe-0.7891443001339904)<1e-12
    else: assert abs(frozen-safe)<1e-12
    stored=json.loads((root/f\"{item['name']}_features.json\").read_text())
    assert extract_regime_features(pack)=={k:stored[k] for k in ('record_type','feature_version','regime','summary','per_query')}
cal=json.loads((root/'calibration.json').read_text()); val=json.loads((root/'validation.json').read_text())
assert cal['training_count']==4 and val['held_out_count']==4
assert val['verdict']=='VALIDATION-NEGATIVE'
assert abs(val['spearman']+0.4)<1e-12 and val['bin_accuracy']==0.75
assert val['lower_band_coverage']==0.75
reports=(root/'D3_REPORT.md').read_text()+(root/'estimator_validation.md').read_text()
assert '50.0%' not in reports and '75.0%' in reports and '28 eval-positive' in reports
print('artifact_integrity: PASS (4 deterministic packs/features, frozen D1 target, safe feature fits, parity <=1e-12, negative verdict)')
'@ | python -" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 1148ms:
All checks passed!
compileall: PASS

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 34143ms:
test_leakage_safe_ridge_recovery_is_below_literal_fit (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_leakage_safe_ridge_recovery_is_below_literal_fit) ... ok
test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_literal_and_safe_fit_leakage_counts_are_recomputed_from_pack) ... ok
test_real_pack_reloads_and_matches_harness_aggregates (research.drift_recovery.tests.test_d1_stats.TestFrozenPack.test_real_pack_reloads_and_matches_harness_aggregates) ... ok
test_prefixes_are_nested_and_seeded (research.drift_recovery.tests.test_d1_stats.TestLearningCurve.test_prefixes_are_nested_and_seeded) ... ok
test_floor_oracle_and_methods_share_bootstrap_indices (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_floor_oracle_and_methods_share_bootstrap_indices) ... ok
test_holm_is_monotone_in_sorted_order (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_holm_is_monotone_in_sorted_order) ... ok
test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_invalid_draw_gate_includes_epsilon_and_hard_fails_above_one_percent) ... ok
test_recovery_uses_ratio_of_resampled_means (research.drift_recovery.tests.test_d1_stats.TestPairedBootstrap.test_recovery_uses_ratio_of_resampled_means) ... ok
test_ground_truth_uses_frozen_pack_ridge_scores (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_ground_truth_uses_frozen_pack_ridge_scores) ... ok
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 15 tests in 8.679s

OK

 succeeded in 36702ms:
artifact_integrity: PASS (4 deterministic packs/features, frozen D1 target, safe feature fits, parity <=1e-12, negative verdict)

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

collab: Wait
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python -m unittest discover -s . -p 'test_*.py'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The reviewer’s second pass reports no blocker: pack hashes/parity/features/targets and regenerated validation all match, and the 15 drift tests pass independently. The final repository-wide rerun is still active; I’m leaving the concise D3 report focused on its required single limitation, with the legacy-label provenance fully disclosed in the detailed validation report.
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 159450ms:
WARNING: adaptive_percentile=150 out of range [0.0, 100.0], clamping.
WARNING: adaptive_window=0 must be >= 1, using 1.
WARNING: adaptive_min_samples=-5 must be >= 1, using 1.
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS

Phase C Analysis — 2026-07-10T03:44:41Z
Recommendation : review_required
Promotable     : ['guard_p85_t0.01']

Candidate                            NDCG@10     Delta    Win%  Status
----------------------------------------------------------------------
baseline                              0.5000   +0.0000   0.0%  baseline
guard_p85_t0.01                       0.5500   +0.0500  100.0%  PROMOTE

Analysis written to: C:\Users\mattm\AppData\Local\Temp\tmpp3hdtwq0\analysis.json

Phase C Analysis — 2026-07-10T03:44:41Z
Recommendation : review_required
Promotable     : ['guard_p85_t0.01']

Candidate                            NDCG@10     Delta    Win%  Status
----------------------------------------------------------------------
baseline                              0.5000   +0.0000   0.0%  baseline
guard_p85_t0.01                       0.5500   +0.0500  100.0%  PROMOTE

Analysis written to: C:\Users\mattm\AppData\Local\Temp\tmpk7jnztks\analysis.json
  invalid: expected 1-10, 's', or 'q'; got 'xyz'
  invalid: grade must be in [1, 10]; got 0
  invalid: grade must be in [1, 10]; got 11

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 2 graded, 0 skipped.
  Sample mean semantic grade: 6.50 (out of 10)
  Sample mean structural score: 92.50 (out of 100)
  Delta (structural - semantic*10): +27.50
  WARNING: 1 finding(s) scored structurally >= semantic*10 + 30 — potential rubric-gap (see C:\Users\mattm\AppData\Local\Temp\tmpkqaqjahg\findings_audit_2026-07-09.json).
  Wrote C:\Users\mattm\AppData\Local\Temp\tmpkqaqjahg\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmpkqaqjahg\findings_audit_latest.json

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 1 graded, 1 skipped.
  Sample mean semantic grade: 7.00 (out of 10)
  Sample mean structural score: 85.00 (out of 100)
  Delta (structural - semantic*10): +15.00
  Wrote C:\Users\mattm\AppData\Local\Temp\tmpqds5kcyx\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmpqds5kcyx\findings_audit_latest.json

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 2 graded, 0 skipped.
  Sample mean semantic grade: 5.50 (out of 10)
  Sample mean structural score: 92.50 (out of 100)
  Delta (structural - semantic*10): +37.50
  WARNING: 1 finding(s) scored structurally >= semantic*10 + 30 — potential rubric-gap (see C:\Users\mattm\AppData\Local\Temp\tmp2vocr929\findings_audit_2026-07-09.json).
  Wrote C:\Users\mattm\AppData\Local\Temp\tmp2vocr929\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmp2vocr929\findings_audit_latest.json
Warning: ID mapping failed, using raw IDs: Retrieve failed
  Evaluating pre-sedimentation NDCG...
  Running 10 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
  Evaluating pre-sedimentation NDCG...
  Running 5 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
  Evaluating pre-sedimentation NDCG...
  Running 5 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
ERROR: Task 'NonExistentTask' not found in MTEB registry!
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke FAIL (1 check(s)):
  - fake failure
[BHS] floor-tier smoke PASS
Created checkpoint: cp_0_20260101_000000
Created checkpoint: cp_1_20260101_000001
Created checkpoint: cp_2_20260101_000002
Created checkpoint: cp_3_20260101_000003
Created checkpoint: cp_4_20260101_000004
Deleted checkpoint: cp_0_20260101_000000
Deleted checkpoint: cp_1_20260101_000001
Deleted checkpoint: cp_2_20260101_000002
Cleaned up 3 old checkpoints
Created checkpoint: only_one_20260709_234453
Created checkpoint: valid_name-123_20260709_234453
Created checkpoint: backup_20260709_234453
Created checkpoint: backup_20260709_234453
Created checkpoint: extra_20260709_234453
Created checkpoint: test_cp_20260709_234453
Created checkpoint: my_backup_20260709_234453
Created checkpoint: backup_20260709_234453
Created checkpoint: no_adapter_20260709_234453
ERROR: Checkpoint 'no_such_checkpoint' not found
Created checkpoint: to_delete_20260709_234454
Deleted checkpoint: to_delete_20260709_234454
Created checkpoint: to_delete_20260709_234454
Deleted checkpoint: to_delete_20260709_234454
Created checkpoint: first_20260101_000000
Created checkpoint: second_20260101_000001
Deleted checkpoint: second_20260101_000001
Deleted checkpoint: first_20260101_000000
Created checkpoint: first_20260709_234454
Created checkpoint: second_20260709_234454
Created checkpoint: third_20260709_234454
Created checkpoint: persistent_20260709_234454
Created checkpoint: snap_20260709_234454
Restoring checkpoint: snap_20260709_234454
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpruzh7a4r\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_234454
Restoring checkpoint: snap_20260709_234454
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmp4iv3oudp\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_234454
Restoring checkpoint: snap_20260709_234454
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmp7ki4xy4f\restored_adapter.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_234454
Restoring checkpoint: snap_20260709_234454
Created checkpoint: snap_20260709_234454
Restoring checkpoint: snap_20260709_234454
ERROR: Checkpoint file hash mismatch. Refusing restore.
Created checkpoint: snap_20260709_234454
ERROR: Checkpoint 'nonexistent_id' not found
ERROR: No checkpoints available
Created checkpoint: before_bad_train_20260709_234454
Operation failed or not marked successful, rolling back to checkpoint before_bad_train_20260709_234454
Restoring checkpoint: before_bad_train_20260709_234454
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmp7ezv7jar\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: before_no_rollback_20260709_234454
Operation failed but auto_rollback disabled
Created checkpoint: before_forgot_20260709_234454
Operation failed or not marked successful, rolling back to checkpoint before_forgot_20260709_234454
Restoring checkpoint: before_forgot_20260709_234454
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmplt3cuuf7\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: before_fail_both_20260709_234454
Operation failed or not marked successful, rolling back to checkpoint before_fail_both_20260709_234454
ERROR: Rollback failed: Simulated rollback failure
WARNING: Original exception preserved despite rollback failure
Created checkpoint: before_rollback_fail_20260709_234454
Operation failed or not marked successful, rolling back to checkpoint before_rollback_fail_20260709_234454
ERROR: Rollback failed: Simulated rollback failure
Created checkpoint: before_op1_20260709_234454
Operation 'op1' completed successfully
Created checkpoint: before_train_20260709_234454
Operation 'train' completed successfully
23:44:54 [ERROR] [error] Error message
23:44:54 [ERROR] [level_test] Message at ERROR
23:44:54 [ERROR] [error] Validation failed
23:44:54 [ERROR] [error] Connection timeout
23:44:54 [ERROR] [error] failing_operation failed after 0.011s
23:44:54 [ERROR] [error] error_op failed after 0.000s
23:44:54 [INFO] [test] Event from logger1
23:44:54 [INFO] [test] Event from logger2
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmpp2_kwj7b\flash.img in raw mode...
>> Fetching data from silicon...
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmp_htf216h in raw mode...
>> Fetching data from silicon...
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmprwrsbmll in raw mode...
>> Fetching data from silicon...
Extracting weights for Computational Storage serialization...
23:45:08 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
[BHS] floor-tier smoke PASS
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_234514
Operation 'sedimentation_cycle_threshold_1' completed successfully

--- HIERARCHICAL SEDIMENTATION (Threshold=1, LR=0.001) ---
Collapse targets: 5 candidates.
23:45:17 [INFO] [training_start] Starting training on 5 samples
Phase 1: Per-cluster training (2 clusters, 2 epochs)...
Created checkpoint: before_hierarchical_sedimentation_threshold_1_20260709_234517
Phase 2: Global refinement (2 epochs, LR=0.000100)...
Adapter weights saved.
Syncing updated vectors to Qdrant...
23:45:22 [INFO] [hierarchical_sedimentation_success] Hierarchical training completed successfully. Updated 5 vectors.
Operation 'hierarchical_sedimentation_threshold_1' completed successfully
23:45:23 [INFO] [training_complete] Training complete | Updated: 5 | Failed: 0
Hierarchical sedimentation complete. Updated 5 vectors, 0 failed.
--- HIERARCHICAL SLEEP CYCLE COMPLETE ---
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_234526
Operation 'sedimentation_cycle_threshold_1' completed successfully
WARNING: teacher_weight=1.5 out of range [0.0, 1.0], clamping.
WARNING: teacher_weight=-0.5 out of range [0.0, 1.0], clamping.
WARNING: training_mode='invalid_mode' invalid. Valid options: ['baseline', 'offline', 'hybrid']. Using 'baseline'.
[model-scope] Observation enabled.
[model-scope] WARNING: Could not enable observation: no transformer model
Created checkpoint: before_model_scope_shadow_policy_20260709_234539
23:45:40 [INFO] [initialization] Initializing Antigravity Engine with model: ollama:nomic-embed-text
23:45:40 [INFO] [embedding_backend_init] Initializing Ollama backend: nomic-embed-text
23:45:44 [ERROR] [error] Ollama connection lost for doc 0
23:45:48 [ERROR] [error] Ollama connection lost for doc 0
23:45:52 [ERROR] [error] Ollama connection lost for doc 0
23:45:52 [ERROR] [error] Failed to embed doc 0 after retries
23:45:52 [INFO] [embedding_backend_ready] Ollama backend connected. Vector size: 768
23:45:52 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([192, 384]) from checkpoint, the shape in current model is torch.Size([384, 768]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([192]) from checkpoint, the shape in current model is torch.Size([384]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([384, 192]) from checkpoint, the shape in current model is torch.Size([768, 384]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([768]).
23:45:52 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:45:52 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:45:52 [INFO] [sedimentation_start] Running sedimentation cycle (Mode=baseline, Threshold=1, LR=0.001)
23:45:52 [INFO] [training_preparation] Found 1 collapsing node candidates
23:45:52 [INFO] [training_start] Starting training on 1 samples
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_234552
23:45:53 [INFO] [checkpoint] Checkpoint save: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:45:53 [INFO] [vector_update] Syncing updated vectors to Qdrant
23:45:53 [INFO] [sedimentation_success] Training cycle completed successfully. Updated 1 vectors.
Operation 'sedimentation_cycle_threshold_1' completed successfully
23:45:53 [INFO] [training_complete] Training complete | Updated: 1 | Failed: 0
23:45:53 [INFO] [initialization] Initializing Antigravity Engine with model: ollama:nomic-embed-text
23:45:53 [INFO] [embedding_backend_init] Initializing Ollama backend: nomic-embed-text
23:45:57 [ERROR] [error] Ollama connection lost for doc 0
23:46:01 [ERROR] [error] Ollama connection lost for doc 0
23:46:05 [ERROR] [error] Ollama connection lost for doc 0
23:46:05 [ERROR] [error] Failed to embed doc 0 after retries
23:46:05 [INFO] [embedding_backend_ready] Ollama backend connected. Vector size: 768
23:46:05 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:46:05 [INFO] [checkpoint] Checkpoint load: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:46:05 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:46:05 [INFO] [sedimentation_start] Running sedimentation cycle (Mode=baseline, Threshold=1, LR=0.001)
23:46:05 [INFO] [training_preparation] Found 2 collapsing node candidates
23:46:05 [INFO] [noise_injection] Enabled noise injection with base scale 0.1
23:46:05 [INFO] [training_start] Starting training on 2 samples
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_234605
23:46:06 [INFO] [checkpoint] Checkpoint save: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:46:06 [INFO] [vector_update] Syncing updated vectors to Qdrant
23:46:06 [INFO] [sedimentation_success] Training cycle completed successfully. Updated 2 vectors.
Operation 'sedimentation_cycle_threshold_1' completed successfully
23:46:06 [INFO] [training_complete] Training complete | Updated: 2 | Failed: 0
{
  "commands": [
    {
      "name": "overlay_model_scope_tests",
      "command": [
        "C:\\Program Files\\Python311\\python.exe",
        "-m",
        "unittest",
        "test_adaptive_overlay.py",
        "test_run_model_scope_campaign.py",
        "test_model_scope_overlay_smoke.py",
        "test_promotion_contract.py",
        "test_attnres_repeat_seed_decision.py",
        "-v"
      ]
    },
    {
      "name": "model_scope_overlay_smoke",
      "command": [
        "C:\\Program Files\\Python311\\python.exe",
        "run_model_scope_overlay_smoke.py",
        "--output-dir",
        "experiment_runs\\overlay-model-scope-validation\\latest\\smoke"
      ]
    }
  ]
}
Extracting weights for Computational Storage serialization...
Extracting weights for Computational Storage serialization...
23:46:10 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:46:10 [INFO] [phase_c_gates] Gate readiness
23:46:10 [INFO] [phase_c_dataset_load] Loading dataset: X
23:46:10 [INFO] [phase_c_candidate_start] Evaluating baseline on X
23:46:10 [INFO] [phase_c_candidate_done] baseline on X: NDCG@10=0.0000
23:46:10 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpf2ntarmq\phase_c_results.json
23:46:10 [INFO] [phase_c_done] Phase C evaluation complete
23:46:10 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:46:10 [INFO] [phase_c_gates] Gate readiness
23:46:10 [INFO] [phase_c_dataset_load] Loading dataset: X
23:46:10 [INFO] [phase_c_candidate_start] Evaluating baseline on X
23:46:10 [INFO] [phase_c_candidate_done] baseline on X: NDCG@10=0.0000
23:46:10 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpm8dagtxb\phase_c_results.json
23:46:10 [INFO] [phase_c_done] Phase C evaluation complete
23:46:10 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:46:10 [INFO] [phase_c_gates] Gate readiness
23:46:10 [INFO] [phase_c_dataset_load] Loading dataset: TestDS
23:46:10 [INFO] [phase_c_candidate_start] Evaluating baseline on TestDS
23:46:10 [INFO] [phase_c_candidate_done] baseline on TestDS: NDCG@10=0.5000
23:46:10 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpaaqplvyt\phase_c_results.json
23:46:10 [INFO] [phase_c_done] Phase C evaluation complete
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0p94kbvp\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0p94kbvp\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpaye8cn96\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpaye8cn96\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzgqeyfqs\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzgqeyfqs\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpi_a2_wga\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpi_a2_wga\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpss6uhrlt\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpss6uhrlt\\report.md"}
23:46:12 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:12 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate

--- HIERARCHICAL SEDIMENTATION (Threshold=3, LR=0.001) ---
Collapse targets: 0 candidates.
No sedimentation targets. Brain is stable.

--- HIERARCHICAL SEDIMENTATION (Threshold=3, LR=0.001) ---
Collapse targets: 10 candidates.
23:46:12 [INFO] [training_start] Starting training on 10 samples
Phase 1: Per-cluster training (2 clusters, 1 epochs)...
Phase 2: Global refinement (1 epochs, LR=0.000100)...
Adapter weights saved.
Syncing updated vectors to Qdrant...
23:46:12 [INFO] [hierarchical_sedimentation_success] Hierarchical training completed successfully. Updated 10 vectors.
23:46:12 [INFO] [training_complete] Training complete | Updated: 10 | Failed: 0
Hierarchical sedimentation complete. Updated 10 vectors, 0 failed.
--- HIERARCHICAL SLEEP CYCLE COMPLETE ---
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpz182pqba\\calibration-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpz182pqba\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwnad2fey\\sweep\\knob-sweep-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwnad2fey\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_jyqkz_9\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_jyqkz_9\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpev7do90s\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpev7do90s\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp74gq7s8f\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp74gq7s8f\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7mmjg5cp\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7mmjg5cp\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzzqr9esb\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzzqr9esb\\report.md"}
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwgsz334h\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwgsz334h\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwgsz334h\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwgsz334h\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
23:46:21 [WARNING] [gate_train_skipped] Pool has only 25 samples (< 9999); skipping gate training.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpp9302opg\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpp9302opg\\nested\\output\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpp9302opg\\nested\\output\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpp9302opg\\nested\\output\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
23:46:21 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:46:21 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:46:21 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:46:21 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksliotgj\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksliotgj\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksliotgj\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksliotgj\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp79042lfd\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp79042lfd\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp79042lfd\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp79042lfd\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx2jsztyn\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx2jsztyn\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx2jsztyn\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx2jsztyn\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppitrftij\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppitrftij\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppitrftij\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppitrftij\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpebn5leo5\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpebn5leo5\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpebn5leo5\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpebn5leo5\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprxllkk_2\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprxllkk_2\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprxllkk_2\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprxllkk_2\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpz0bz9iyg\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpz0bz9iyg\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpz0bz9iyg\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpz0bz9iyg\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpdiup4zn4\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpdiup4zn4\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpdiup4zn4\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpdiup4zn4\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7cpbyb47\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7cpbyb47\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7cpbyb47\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7cpbyb47\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0qbu1kb7\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0qbu1kb7\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0qbu1kb7\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0qbu1kb7\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9ld83sv9\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9ld83sv9\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9ld83sv9\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp9ld83sv9\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpuecyc9dl\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpuecyc9dl\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpuecyc9dl\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpuecyc9dl\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnno890y6\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnno890y6\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnno890y6\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnno890y6\\custom_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvd1wqyfh\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvd1wqyfh\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvd1wqyfh\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvd1wqyfh\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpyf475o9g\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpyf475o9g\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpyf475o9g\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpyf475o9g\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0g2wtljb\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0g2wtljb\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0g2wtljb\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0g2wtljb\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppyoiigi2\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppyoiigi2\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppyoiigi2\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmppyoiigi2\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmplflmxhtg\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmplflmxhtg\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmplflmxhtg\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmplflmxhtg\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphbpllj8_\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphbpllj8_\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphbpllj8_\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmphbpllj8_\\gate_train_summary.json"
}
23:46:21 [INFO] [gate_train_complete] Gate training complete.
{
  "run_dir": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpq8g8zjzt\\test_run",
  "status": "completed",
  "finished_at": "2026-07-10T03:46:21.622366+00:00",
  "latest_report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpq8g8zjzt\\test_run\\reports\\iteration_001.json",
  "terminal_decision": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpq8g8zjzt\\test_run\\terminal_decision.json"
}
============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session-32-review
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session-32-review --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-overnight
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-overnight --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session32-verylonglabelverylonglabelverylonglabelverylonglabelve
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session32-verylonglabelverylonglabelverylonglabelverylonglabelve --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

23:46:21 [INFO] [phase_c_mask_gate_load] Loading mask gate: C:\Users\mattm\AppData\Local\Temp\tmpir49abyp.json
23:46:21 [INFO] [phase_c_reform_gate_load] Loading reform gate: C:\Users\mattm\AppData\Local\Temp\tmpd09cx9js.json
23:46:21 [INFO] [phase_c_reform_gate_skip] Attribution pool not found; learned_reform_gate_v1 will be fail-closed
23:46:21 [INFO] [phase_c_reform_gate_train] Training reform gate from: C:\Users\mattm\AppData\Local\Temp\tmps3qxgrx_.json
23:46:21 [INFO] [phase_c_start] Phase C eval: 2 candidates × 1 datasets
23:46:21 [INFO] [phase_c_gates] Gate readiness
23:46:21 [INFO] [phase_c_dataset_load] Loading dataset: SciFact
23:46:21 [INFO] [phase_c_candidate_start] Evaluating baseline on SciFact
23:46:21 [INFO] [phase_c_candidate_done] baseline on SciFact: NDCG@10=1.0000
23:46:21 [INFO] [phase_c_candidate_start] Evaluating guard_p85_t0.01 on SciFact
23:46:21 [INFO] [phase_c_candidate_done] guard_p85_t0.01 on SciFact: NDCG@10=1.0000
23:46:21 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpx6tbsggc\phase_c_results.json
23:46:21 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpx6tbsggc\overlay_card_baseline.json
23:46:21 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpx6tbsggc\overlay_card_guard_p85_t0.01.json
23:46:21 [INFO] [phase_c_done] Phase C evaluation complete
23:46:21 [INFO] [phase_c_start] Phase C eval: 2 candidates × 1 datasets
23:46:21 [INFO] [phase_c_gates] Gate readiness
23:46:21 [INFO] [phase_c_dataset_load] Loading dataset: SciFact
23:46:21 [INFO] [phase_c_candidate_start] Evaluating baseline on SciFact
23:46:21 [INFO] [phase_c_candidate_done] baseline on SciFact: NDCG@10=0.8155
23:46:21 [INFO] [phase_c_candidate_start] Evaluating guard_p85_t0.01 on SciFact
23:46:21 [INFO] [phase_c_candidate_done] guard_p85_t0.01 on SciFact: NDCG@10=0.8155
23:46:21 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpw3uq6lx5\phase_c_results.json
23:46:21 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpw3uq6lx5\overlay_card_baseline.json
23:46:21 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpw3uq6lx5\overlay_card_guard_p85_t0.01.json
23:46:21 [INFO] [phase_c_done] Phase C evaluation complete
line one
C:\Users\mattm\AppData\Local\Temp\tmpj6dyb1wp
C:\Users\mattm\AppData\Local\Temp\tmpc15i4u2z
SMOKE_PIPELINE PASS (mocked)
[BHS] ceiling-tier smoke PASS
SMOKE_PIPELINE FAIL: simulated
[BHS] ceiling-tier smoke FAIL: smoke_pipeline.py exit=1
23:46:23 [INFO] [initialization] Initializing Antigravity Engine with model: all-MiniLM-L6-v2
23:46:23 [INFO] [embedding_backend_init] Initializing local backend: all-MiniLM-L6-v2
23:46:23 [INFO] [initialization] Device Selected: cuda
23:46:23 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:46:23 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:46:23 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:46:23 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:46:23 [INFO] [ingestion_start] Ingesting 4 documents
23:46:23 [INFO] [ingestion_complete] Ingestion Complete
23:46:23 [INFO] [initialization] Initializing Antigravity Engine with model: all-MiniLM-L6-v2
23:46:23 [INFO] [embedding_backend_init] Initializing local backend: all-MiniLM-L6-v2
23:46:23 [INFO] [initialization] Device Selected: cuda
23:46:24 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:46:24 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:46:24 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:46:24 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:46:24 [INFO] [ingestion_start] Ingesting 4 documents
23:46:24 [INFO] [ingestion_complete] Ingestion Complete
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:46:24 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
TTS enabled: translation=True, transport=True, steering=False
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprvc8h4qn\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
TTS enabled: translation=False, transport=True, steering=True
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpok32l23x\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
TTS enabled: translation=True, transport=False, steering=True
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp0klgt0lq\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_0ct4h50\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": false
  }
}
23:46:26 [INFO] [initialization] Initializing Antigravity Engine with model: sentence-transformers/all-MiniLM-L6-v2
23:46:26 [INFO] [embedding_backend_init] Initializing local backend: sentence-transformers/all-MiniLM-L6-v2
23:46:26 [INFO] [initialization] Device Selected: cuda
23:46:26 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:46:26 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:46:26 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:46:26 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:46:26 [INFO] [ingestion_start] Ingesting 2 documents
23:46:26 [INFO] [ingestion_complete] Ingestion Complete
23:46:26 [INFO] [query] Query: 'cat mat...' | Action: FAST
23:46:26 [INFO] [initialization] Initializing Antigravity Engine with model: sentence-transformers/all-MiniLM-L6-v2
23:46:26 [INFO] [embedding_backend_init] Initializing local backend: sentence-transformers/all-MiniLM-L6-v2
23:46:26 [INFO] [initialization] Device Selected: cuda
23:46:27 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:46:27 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:46:27 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:46:27 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:46:27 [INFO] [ingestion_start] Ingesting 2 documents
23:46:27 [INFO] [ingestion_complete] Ingestion Complete
23:46:27 [INFO] [tts_enabled] TTS pipeline enabled
23:46:27 [INFO] [query] Query: 'cat mat...' | Action: FAST
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([256, 512]) from checkpoint, the shape in current model is torch.Size([384, 768]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([256]) from checkpoint, the shape in current model is torch.Size([384]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([512, 256]) from checkpoint, the shape in current model is torch.Size([768, 384]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([512]) from checkpoint, the shape in current model is torch.Size([768]).
WARNING: chelation_p=150 out of range [0, 100], clamping.
WARNING: chelation_p=-10 out of range [0, 100], clamping.
WARNING: epochs=200 out of range [1, 100], clamping.
WARNING: epochs=0 out of range [1, 100], clamping.
WARNING: learning_rate=10.0 out of range [0.0001, 1.0], clamping.
WARNING: learning_rate=1e-05 out of range [0.0001, 1.0], clamping.
WARNING: max_depth=20 out of range [1, 10], clamping.
WARNING: max_depth=0 out of range [1, 10], clamping.
.....................................................................................................................................................ERROR: Results file not found: does_not_exist.json
.............................................................................................................................................................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'fast' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'slow' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
.D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'cfg1' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
....D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'a' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'b' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'c' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
.D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 't' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
...D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_benchmark_beir.py:309: RuntimeWarning: BenchmarkResult for 'a' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  results = runner.run_single_dataset("TestDS", corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_benchmark_beir.py:309: RuntimeWarning: BenchmarkResult for 'b' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  results = runner.run_single_dataset("TestDS", corpus, queries, qrels,
..................................................................................................................................................................................................................................................................................................................................................................................................................................s.........s.............................................................................................................................................................................................................................................................................................................................Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
...........................................................................................................................................................................................................................................................................................ss........D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_model_scope_runtime.py:261: UserWarning: torch tensor protocol fallback triggered in hook (mock-tensor compatible path; unexpected in production): AttributeError("'float' object has no attribute 'item'")
  hook_fn(lm, None, tensor)
................................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_model_scope_steering.py:655: UserWarning: Skipping corrupt JSON line in C:\Users\mattm\AppData\Local\Temp\tmp7_my_9ky\corrupt.jsonl: 'THIS IS NOT VALID JSON {{{'
  loaded = actuator2.load_records(path)
...........................................................................................................................................................................................ssssss..................................................................................s...................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\qwen_scope_adapter.py:43: FutureWarning: You are using `torch.load` with `weights_only=False` (the current default value), which uses the default pickle module implicitly. It is possible to construct malicious pickle data which will execute arbitrary code during unpickling (See https://github.com/pytorch/pytorch/blob/main/SECURITY.md#untrusted-models for more details). In a future release, the default value for `weights_only` will be flipped to `True`. This limits the functions that could be executed during unpickling. Arbitrary objects will no longer be allowed to be loaded via this mode unless they are explicitly allowlisted by the user via `torch.serialization.add_safe_globals`. We recommend you start setting `weights_only=True` for any use case where you don't have full control of the loaded file. Please open an issue on GitHub for any issues related to this experimental feature.
  state_dict = torch.load(Path(path), map_location="cpu")
..............................................................................................................................................................................WARNING: pool has 25 samples, below minimum 9999. Skipping training.
..ERROR: attribution pool file not found: nonexistent_pool.json
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.ERROR: attribution pool file not found: C:\Users\mattm\AppData\Local\Temp\tmpop0bp1zv\does_not_exist.json
.ERROR: attribution pool file not found: C:\Users\mattm\AppData\Local\Temp\tmpa14wg631\does_not_exist.json
............................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................
----------------------------------------------------------------------
Ran 2755 tests in 130.644s

OK (skipped=11)

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..a0fc6c8032e7a44a21ed062cd5519ac84176cab4
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,553 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+import traceback
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    case = load_retrieval_evalsplit(
+        spec.dataset,
+        seed=spec.seed,
+        anchor_fraction=spec.anchor_fraction,
+        max_queries=100,
+        sample_docs=1200,
+    )
+    encoded = encode_retrieval_case(
+        case,
+        swap_model=spec.swap_model,
+        seed=spec.seed,
+        device=device,
+        old_model=OLD_MODEL,
+        batch_size=64,
+    )
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+            parity = assert_harness_parity(pack, atol=1e-12)
+            ground_truth = frozen_ridge_ground_truth(pack)
+            features = extract_regime_features(pack)
+            feature_payload = {
+                **features,
+                "regime_name": spec.name,
+                "ground_truth": ground_truth,
+                "pack_prefix": str(prefix),
+                "harness_parity": parity,
+            }
+            _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+            validation_regimes.append(
+                {
+                    "name": spec.name,
+                    "features": features,
+                    "recovery": ground_truth["ridge_recovery"],
+                }
+            )
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "available",
+                    "pack_prefix": str(prefix),
+                    "pack_sha256": _pack_digest(prefix),
+                    "harness_parity": parity,
+                    "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                    "leakage_safe_feature_refit_recovery": ground_truth[
+                        "leakage_safe_feature_refit"
+                    ]["ridge_recovery"],
+                    "document_count": int(features["summary"]["document_count"]),
+                    "query_count": int(features["summary"]["query_count"]),
+                    "safe_fit_count": int(features["summary"]["fit_count"]),
+                    "predicted_inversion_rate": float(
+                        features["summary"]["predicted_inversion_rate"]
+                    ),
+                }
+            )
+        except Exception as exc:  # availability failures are part of the requested artifact
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                    "traceback_tail": traceback.format_exc().splitlines()[-8:],
+                }
+            )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The reviewer found a second real BHS issue in the runner: a broad `except Exception` could have mislabeled parity/checksum/feature bugs as “offline unavailable” skips. I’m narrowing that path so only explicit dataset/model-cache unavailability is skippable; parity, checksum, leakage, feature, and artifact errors will now fail the build loudly. The actual four packs already pass, but this closes failure masking in future reruns.
apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\run_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'Get-Content research/drift_recovery/run_estimator.py | Select-Object -Skip 430 -First 155' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 7163ms:
) -> Dict[str, Any]:
    output_dir = Path(output_dir)
    packs_dir = output_dir / "packs"
    packs_dir.mkdir(parents=True, exist_ok=True)
    manifest: Dict[str, Any] = {
        "record_type": "d3_estimator_regime_manifest",
        "environment": {
            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
            "device": device,
        },
        "parity_tolerance": 1e-12,
        "regimes": [],
    }
    validation_regimes = []
    for spec in REGIMES:
        if only_existing and spec.existing_pack is None:
            manifest["regimes"].append(
                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
            )
            continue
        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
        try:
            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
                print(f"[D3] reloading {spec.name}", flush=True)
                pack = EmbeddingPack.load(prefix)
            elif spec.existing_pack is not None:
                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
            else:
                print(f"[D3] building {spec.name} sequentially", flush=True)
                pack = build_regime_pack(spec, prefix, device=device)
        except RegimeUnavailableError as exc:
            reason = f"{type(exc).__name__}: {exc}"
            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
            manifest["regimes"].append(
                {
                    "name": spec.name,
                    "dataset": spec.dataset,
                    "swap_model": spec.swap_model,
                    "anchor_fraction": spec.anchor_fraction,
                    "status": "skipped",
                    "reason": reason,
                }
            )
            continue

        # From here on every failure is a build failure, never an availability
        # skip.  In particular, parity/checksum/leakage/feature assertions must
        # terminate the run rather than manufacture a scoping-negative result.
        parity = assert_harness_parity(pack, atol=1e-12)
        ground_truth = frozen_ridge_ground_truth(pack)
        features = extract_regime_features(pack)
        feature_payload = {
            **features,
            "regime_name": spec.name,
            "ground_truth": ground_truth,
            "pack_prefix": str(prefix),
            "harness_parity": parity,
        }
        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
        validation_regimes.append(
            {
                "name": spec.name,
                "features": features,
                "recovery": ground_truth["ridge_recovery"],
            }
        )
        manifest["regimes"].append(
            {
                "name": spec.name,
                "dataset": spec.dataset,
                "swap_model": spec.swap_model,
                "anchor_fraction": spec.anchor_fraction,
                "status": "available",
                "pack_prefix": str(prefix),
                "pack_sha256": _pack_digest(prefix),
                "harness_parity": parity,
                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
                "leakage_safe_feature_refit_recovery": ground_truth[
                    "leakage_safe_feature_refit"
                ]["ridge_recovery"],
                "document_count": int(features["summary"]["document_count"]),
                "query_count": int(features["summary"]["query_count"]),
                "safe_fit_count": int(features["summary"]["fit_count"]),
                "predicted_inversion_rate": float(
                    features["summary"]["predicted_inversion_rate"]
                ),
            }
        )

    validation = leave_one_regime_out(validation_regimes)
    _write_json(output_dir / "validation.json", validation)
    if validation_regimes:
        calibrator = fit_calibration(
            [item["features"] for item in validation_regimes],
            [item["recovery"] for item in validation_regimes],
            regime_names=[item["name"] for item in validation_regimes],
        )
        _write_json(output_dir / "calibration.json", calibrator.to_dict())
    _write_json(output_dir / "regime_manifest.json", manifest)
    (output_dir / "estimator_validation.md").write_text(
        _render_validation(validation, manifest), encoding="utf-8"
    )
    available_names = [item["name"] for item in validation_regimes]
    available_manifest_items = [
        item for item in manifest["regimes"] if item["status"] == "available"
    ]
    (output_dir / "D3_REPORT.md").write_text(
        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
    )
    return {
        "output_dir": str(output_dir),
        "available_regimes": available_names,
        "validation": validation,
    }


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
    parser.add_argument("--rebuild-packs", action="store_true")
    parser.add_argument("--only-existing", action="store_true")
    return parser


def main(argv=None) -> int:
    args = build_parser().parse_args(argv)
    result = run_d3(
        output_dir=args.output_dir,
        device=args.device,
        rebuild_packs=args.rebuild_packs,
        only_existing=args.only_existing,
    )
    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..fc3b9aa5aec473d89eb224773a938654cd23cf3f
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,170 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import unittest
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import frozen_ridge_ground_truth
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+
+if __name__ == "__main__":
+    unittest.main()

2026-07-10T03:48:23.726333Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 5 seconds
Output:
50:
51:
52:@dataclass(frozen=True)
53:class RegimeSpec:
54:    name: str
55:    dataset: str
56:    swap_model: str
57:    anchor_fraction: float = 0.4
58:    seed: int = 42
59:    existing_pack: Optional[Path] = None
60:
61:
62:class RegimeUnavailableError(RuntimeError):
63:    """An explicitly skippable offline dataset/model-cache miss."""
64:
65:
66:REGIMES = (
67:    RegimeSpec(
68:        name="scifact_minilm_to_mpnet",
69:        dataset="SciFact",
70:        swap_model=MPNET_MODEL,
71:        existing_pack=DEFAULT_D1_PACK,
72:    ),
73:    RegimeSpec(
74:        name="nfcorpus_minilm_to_mpnet",
75:        dataset="NFCorpus",
76:        swap_model=MPNET_MODEL,
77:    ),
78:    RegimeSpec(
79:        name="scifact_minilm_to_bge_large",
80:        dataset="SciFact",
81:        swap_model=BGE_MODEL,
82:    ),
83:    RegimeSpec(
84:        name="nfcorpus_minilm_to_bge_large",
85:        dataset="NFCorpus",
86:        swap_model=BGE_MODEL,
87:    ),
88:)
89:
90:
91:def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
92:    path.parent.mkdir(parents=True, exist_ok=True)
93:    path.write_text(
94:        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
95:        encoding="utf-8",
96:    )
97:
98:
99:def _safe_fit_index(
100:    doc_ids: np.ndarray,
101:    eval_qrels: Mapping[str, Mapping[str, float]],
102:    seed: int,
103:) -> np.ndarray:
104:    positives = {
105:        str(doc_id)
106:        for relevance in eval_qrels.values()
107:        for doc_id, score in relevance.items()
108:        if float(score) > 0.0
109:    }
110:    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
111:    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
112:    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
113:    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
114:    # regularized and remains well-defined below the embedding dimension; the
115:    # exact resulting n_anchor is a recorded regime axis rather than padded
116:    # with eval-positive pairs.
117:    fit_count = min(len(doc_ids) // 2, len(safe_order))
118:    if fit_count < 2:
119:        raise RuntimeError(
120:            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
121:        )
122:    selected = safe_order[:fit_count]
123:    return np.asarray(selected, dtype=np.int64)
124:
125:
126:def _recovery(
127:    floor_scores: Sequence[float],
128:    oracle_scores: Sequence[float],
129:    ridge_scores: Sequence[float],
130:) -> Dict[str, float]:
131:    floor = aggregate_ndcg(floor_scores)
132:    oracle = aggregate_ndcg(oracle_scores)
133:    ridge = aggregate_ndcg(ridge_scores)
134:    gap = oracle - floor
135:    if gap <= 1e-12:
136:        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
137:    return {
138:        "floor_ndcg": floor,
139:        "oracle_ndcg": oracle,
140:        "ridge_ndcg": ridge,
141:        "oracle_gap": gap,
142:        "ridge_recovery": (ridge - floor) / gap,
143:    }
144:
145:
146:def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
147:    """Build one additive D3 pack with a leakage-safe ridge headline."""
148:
149:    try:
150:        case = load_retrieval_evalsplit(
151:            spec.dataset,
152:            seed=spec.seed,
153:            anchor_fraction=spec.anchor_fraction,
154:            max_queries=100,
155:            sample_docs=1200,
156:        )
157:    except RuntimeError as exc:
158:        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
159:            raise RegimeUnavailableError(str(exc)) from exc
160:        raise
161:    try:
162:        encoded = encode_retrieval_case(
163:            case,
164:            swap_model=spec.swap_model,
165:            seed=spec.seed,
166:            device=device,
167:            old_model=OLD_MODEL,
168:            batch_size=64,
169:        )
170:    except (FileNotFoundError, OSError) as exc:
171:        raise RegimeUnavailableError(
172:            f"offline model cache unavailable for {spec.swap_model}: {exc}"
173:        ) from exc
174:    doc_ids = np.asarray(case["doc_ids"], dtype=str)
175:    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
176:    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
177:    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
178:    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
179:    score_args = {
180:        "query_vectors": encoded["Qd"],
430:    only_existing: bool = False,
431:) -> Dict[str, Any]:
432:    output_dir = Path(output_dir)
433:    packs_dir = output_dir / "packs"
434:    packs_dir.mkdir(parents=True, exist_ok=True)
435:    manifest: Dict[str, Any] = {
436:        "record_type": "d3_estimator_regime_manifest",
437:        "environment": {
438:            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
439:            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
440:            "device": device,
441:        },
442:        "parity_tolerance": 1e-12,
443:        "regimes": [],
444:    }
445:    validation_regimes = []
446:    for spec in REGIMES:
447:        if only_existing and spec.existing_pack is None:
448:            manifest["regimes"].append(
449:                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
450:            )
451:            continue
452:        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
453:        try:
454:            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
455:                print(f"[D3] reloading {spec.name}", flush=True)
456:                pack = EmbeddingPack.load(prefix)
457:            elif spec.existing_pack is not None:
458:                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
459:            else:
460:                print(f"[D3] building {spec.name} sequentially", flush=True)
461:                pack = build_regime_pack(spec, prefix, device=device)
462:        except RegimeUnavailableError as exc:
463:            reason = f"{type(exc).__name__}: {exc}"
464:            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
465:            manifest["regimes"].append(
466:                {
467:                    "name": spec.name,
468:                    "dataset": spec.dataset,
469:                    "swap_model": spec.swap_model,
470:                    "anchor_fraction": spec.anchor_fraction,
471:                    "status": "skipped",
472:                    "reason": reason,
473:                }
474:            )
475:            continue
476:
477:        # From here on every failure is a build failure, never an availability
478:        # skip.  In particular, parity/checksum/leakage/feature assertions must
479:        # terminate the run rather than manufacture a scoping-negative result.
480:        parity = assert_harness_parity(pack, atol=1e-12)

apply patch
patch: completed
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\research\drift_recovery\tests\test_estimator.py
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'python -m ruff check research/drift_recovery/run_estimator.py research/drift_recovery/tests/test_estimator.py' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; python -m unittest research.drift_recovery.tests.test_estimator -v" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 3791ms:
All checks passed!

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 28275ms:
[D3] reloading parity_failure
[D3] building missing_cache sequentially
[D3] skipped missing_cache: RegimeUnavailableError: offline model cache unavailable
test_ground_truth_uses_frozen_pack_ridge_scores (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_ground_truth_uses_frozen_pack_ridge_scores) ... ok
test_leave_one_regime_out_never_trains_on_held_out_target (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_leave_one_regime_out_never_trains_on_held_out_target) ... ok
test_predictor_returns_calibrated_shape_without_ndcg_eval (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_predictor_returns_calibrated_shape_without_ndcg_eval) ... ok
test_runner_does_not_mask_parity_failure_as_unavailable (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_runner_does_not_mask_parity_failure_as_unavailable) ... ok
test_runner_records_only_explicit_availability_failure_as_skip (research.drift_recovery.tests.test_estimator.TestEstimatorCalibration.test_runner_records_only_explicit_availability_failure_as_skip) ... ok
test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels) ... ok
test_synthetic_pack_margin_is_hand_checkable_and_deterministic (research.drift_recovery.tests.test_estimator.TestEstimatorFeatures.test_synthetic_pack_margin_is_hand_checkable_and_deterministic) ... ok
test_hand_checkable_margin_and_tie_semantics (research.drift_recovery.tests.test_estimator.TestMarginBound.test_hand_checkable_margin_and_tie_semantics) ... ok
test_negative_error_norm_is_rejected (research.drift_recovery.tests.test_estimator.TestMarginBound.test_negative_error_norm_is_rejected) ... ok

----------------------------------------------------------------------
Ran 9 tests in 0.065s

OK

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python research/drift_recovery/run_estimator.py --device cuda" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 48942ms:
[D3] reloading scifact_minilm_to_mpnet
[D3] reloading nfcorpus_minilm_to_mpnet
[D3] reloading scifact_minilm_to_bge_large
[D3] reloading nfcorpus_minilm_to_bge_large
{
  "available_regimes": [
    "scifact_minilm_to_mpnet",
    "nfcorpus_minilm_to_mpnet",
    "scifact_minilm_to_bge_large",
    "nfcorpus_minilm_to_bge_large"
  ],
  "output_dir": "research\\drift_recovery\\out\\estimator",
  "validation": {
    "bin_accuracy": 0.75,
    "bin_thresholds": {
      "high": "R >= 0.666667",
      "low": "R < 0.333333",
      "medium": "0.333333 <= R < 0.666667"
    },
    "held_out_axis": "regime (dataset / encoder family / anchor setting)",
    "held_out_count": 4,
    "lower_band_coverage": 0.75,
    "record_type": "recoverability_leave_one_regime_out_validation",
    "regime_count": 4,
    "rows": [
      {
        "R_hat": 0.6840096562278455,
        "actual_bin": "high",
        "actual_recovery": 0.8434286306657018,
        "bin_correct": true,
        "lower_band": 0.5418130994796307,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_mpnet",
        "training_regimes": [
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7635889655935123,
        "actual_bin": "medium",
        "actual_recovery": 0.647119786542357,
        "bin_correct": false,
        "lower_band": 0.6473716250700156,
        "lower_band_covers": false,
        "predicted_bin": "high",
        "regime": "nfcorpus_minilm_to_mpnet",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "scifact_minilm_to_bge_large",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.7558645839863694,
        "actual_bin": "high",
        "actual_recovery": 0.7223707722283974,
        "bin_correct": true,
        "lower_band": 0.050617102091020216,
        "lower_band_covers": true,
        "predicted_bin": "high",
        "regime": "scifact_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "nfcorpus_minilm_to_bge_large"
        ]
      },
      {
        "R_hat": 0.6562146658656209,
        "actual_bin": "medium",
        "actual_recovery": 0.6640397106924352,
        "bin_correct": true,
        "lower_band": 0.0,
        "lower_band_covers": true,
        "predicted_bin": "medium",
        "regime": "nfcorpus_minilm_to_bge_large",
        "training_regimes": [
          "scifact_minilm_to_mpnet",
          "nfcorpus_minilm_to_mpnet",
          "scifact_minilm_to_bge_large"
        ]
      }
    ],
    "scoping_note": "Across 4 held-out regimes, Spearman=-0.400 and 3-way bin accuracy=75.0%; neither locked success threshold was met.",
    "spearman": -0.39999999999999997,
    "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
    "verdict": "VALIDATION-NEGATIVE"
  }
}

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The adversarial reviewer’s final verdict is no blocker / Tier B 100: the explicit availability boundary works, all four artifacts independently reproduce, and the exact negative metrics match. I’m running one last repository-wide suite against this final code state; this is the final gate before handoff.
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command '$env:HF_HUB_OFFLINE='"'1'; "'$env:HF_DATASETS_OFFLINE='"'1'; "'$env:TRANSFORMERS_OFFLINE='"'1'; "'$env:PYTHONIOENCODING='"'utf-8'; python -m unittest discover -s . -p 'test_*.py'" in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
The final full suite is still active with no early failure. The focused final-state suite and reviewer recomputation are already green; I’m waiting for the repository-wide summary line so the handoff can include the exact final count.
diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

 succeeded in 200432ms:
[D3] reloading parity_failure
[D3] building missing_cache sequentially
[D3] skipped missing_cache: RegimeUnavailableError: offline model cache unavailable
WARNING: adaptive_percentile=150 out of range [0.0, 100.0], clamping.
WARNING: adaptive_window=0 must be >= 1, using 1.
WARNING: adaptive_min_samples=-5 must be >= 1, using 1.
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke PASS

Phase C Analysis — 2026-07-10T03:51:04Z
Recommendation : review_required
Promotable     : ['guard_p85_t0.01']

Candidate                            NDCG@10     Delta    Win%  Status
----------------------------------------------------------------------
baseline                              0.5000   +0.0000   0.0%  baseline
guard_p85_t0.01                       0.5500   +0.0500  100.0%  PROMOTE

Analysis written to: C:\Users\mattm\AppData\Local\Temp\tmp6wm92mey\analysis.json

Phase C Analysis — 2026-07-10T03:51:04Z
Recommendation : review_required
Promotable     : ['guard_p85_t0.01']

Candidate                            NDCG@10     Delta    Win%  Status
----------------------------------------------------------------------
baseline                              0.5000   +0.0000   0.0%  baseline
guard_p85_t0.01                       0.5500   +0.0500  100.0%  PROMOTE

Analysis written to: C:\Users\mattm\AppData\Local\Temp\tmpba3dhwpy\analysis.json
  invalid: expected 1-10, 's', or 'q'; got 'xyz'
  invalid: grade must be in [1, 10]; got 0
  invalid: grade must be in [1, 10]; got 11

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 2 graded, 0 skipped.
  Sample mean semantic grade: 6.50 (out of 10)
  Sample mean structural score: 92.50 (out of 100)
  Delta (structural - semantic*10): +27.50
  WARNING: 1 finding(s) scored structurally >= semantic*10 + 30 — potential rubric-gap (see C:\Users\mattm\AppData\Local\Temp\tmppvokpnw8\findings_audit_2026-07-09.json).
  Wrote C:\Users\mattm\AppData\Local\Temp\tmppvokpnw8\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmppvokpnw8\findings_audit_latest.json

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 1 graded, 1 skipped.
  Sample mean semantic grade: 7.00 (out of 10)
  Sample mean structural score: 85.00 (out of 100)
  Delta (structural - semantic*10): +15.00
  Wrote C:\Users\mattm\AppData\Local\Temp\tmpheszagny\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmpheszagny\findings_audit_latest.json

========================================================================
Finding 1/2 — id='F-rich' severity='HIGH' structural=100
========================================================================
  impact: Outage at server.py:120 returns wrong shape
  recommended_fix: Patch handler at api.py:55, see PR #244

========================================================================
Finding 2/2 — id='F-padded' severity='HIGH' structural=85
========================================================================
  impact: issue at a.py:1 with details after
  recommended_fix: xxxxxxxxxxxx

========================================================================
Audit complete: 2 graded, 0 skipped.
  Sample mean semantic grade: 5.50 (out of 10)
  Sample mean structural score: 92.50 (out of 100)
  Delta (structural - semantic*10): +37.50
  WARNING: 1 finding(s) scored structurally >= semantic*10 + 30 — potential rubric-gap (see C:\Users\mattm\AppData\Local\Temp\tmpr64o3e5y\findings_audit_2026-07-09.json).
  Wrote C:\Users\mattm\AppData\Local\Temp\tmpr64o3e5y\findings_audit_2026-07-09.json
  Updated C:\Users\mattm\AppData\Local\Temp\tmpr64o3e5y\findings_audit_latest.json
Warning: ID mapping failed, using raw IDs: Retrieve failed
  Evaluating pre-sedimentation NDCG...
  Running 10 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
  Evaluating pre-sedimentation NDCG...
  Running 5 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
  Evaluating pre-sedimentation NDCG...
  Running 5 training queries...
  Running sedimentation (epochs=5)...
  Evaluating post-sedimentation NDCG...
ERROR: Task 'NonExistentTask' not found in MTEB registry!
[BHS] floor-tier smoke PASS
[BHS] floor-tier smoke FAIL (1 check(s)):
  - fake failure
[BHS] floor-tier smoke PASS
Created checkpoint: cp_0_20260101_000000
Created checkpoint: cp_1_20260101_000001
Created checkpoint: cp_2_20260101_000002
Created checkpoint: cp_3_20260101_000003
Created checkpoint: cp_4_20260101_000004
Deleted checkpoint: cp_0_20260101_000000
Deleted checkpoint: cp_1_20260101_000001
Deleted checkpoint: cp_2_20260101_000002
Cleaned up 3 old checkpoints
Created checkpoint: only_one_20260709_235107
Created checkpoint: valid_name-123_20260709_235107
Created checkpoint: backup_20260709_235107
Created checkpoint: backup_20260709_235107
Created checkpoint: extra_20260709_235107
Created checkpoint: test_cp_20260709_235107
Created checkpoint: my_backup_20260709_235107
Created checkpoint: backup_20260709_235107
Created checkpoint: no_adapter_20260709_235107
ERROR: Checkpoint 'no_such_checkpoint' not found
Created checkpoint: to_delete_20260709_235107
Deleted checkpoint: to_delete_20260709_235107
Created checkpoint: to_delete_20260709_235107
Deleted checkpoint: to_delete_20260709_235107
Created checkpoint: first_20260101_000000
Created checkpoint: second_20260101_000001
Deleted checkpoint: second_20260101_000001
Deleted checkpoint: first_20260101_000000
Created checkpoint: first_20260709_235107
Created checkpoint: second_20260709_235107
Created checkpoint: third_20260709_235107
Created checkpoint: persistent_20260709_235107
Created checkpoint: snap_20260709_235107
Restoring checkpoint: snap_20260709_235107
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpcpan9j7b\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_235107
Restoring checkpoint: snap_20260709_235107
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpel5b6i3k\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_235107
Restoring checkpoint: snap_20260709_235107
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpooa9r521\restored_adapter.pt
Checkpoint restored successfully
Created checkpoint: snap_20260709_235107
Restoring checkpoint: snap_20260709_235107
Created checkpoint: snap_20260709_235107
Restoring checkpoint: snap_20260709_235107
ERROR: Checkpoint file hash mismatch. Refusing restore.
Created checkpoint: snap_20260709_235107
ERROR: Checkpoint 'nonexistent_id' not found
ERROR: No checkpoints available
Created checkpoint: before_bad_train_20260709_235107
Operation failed or not marked successful, rolling back to checkpoint before_bad_train_20260709_235107
Restoring checkpoint: before_bad_train_20260709_235107
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpqcnpwum6\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: before_no_rollback_20260709_235107
Operation failed but auto_rollback disabled
Created checkpoint: before_forgot_20260709_235107
Operation failed or not marked successful, rolling back to checkpoint before_forgot_20260709_235107
Restoring checkpoint: before_forgot_20260709_235107
Restored adapter weights to: C:\Users\mattm\AppData\Local\Temp\tmpm26kqa9_\adapter_weights.pt
Checkpoint restored successfully
Created checkpoint: before_fail_both_20260709_235107
Operation failed or not marked successful, rolling back to checkpoint before_fail_both_20260709_235107
ERROR: Rollback failed: Simulated rollback failure
WARNING: Original exception preserved despite rollback failure
Created checkpoint: before_rollback_fail_20260709_235107
Operation failed or not marked successful, rolling back to checkpoint before_rollback_fail_20260709_235107
ERROR: Rollback failed: Simulated rollback failure
Created checkpoint: before_op1_20260709_235107
Operation 'op1' completed successfully
Created checkpoint: before_train_20260709_235107
Operation 'train' completed successfully
23:51:07 [ERROR] [error] Error message
23:51:07 [ERROR] [level_test] Message at ERROR
23:51:07 [ERROR] [error] Validation failed
23:51:07 [ERROR] [error] Connection timeout
23:51:07 [ERROR] [error] failing_operation failed after 0.011s
23:51:07 [ERROR] [error] error_op failed after 0.000s
23:51:07 [INFO] [test] Event from logger1
23:51:07 [INFO] [test] Event from logger2
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmpz00xj32s\flash.img in raw mode...
>> Fetching data from silicon...
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmprkcvpsju in raw mode...
>> Fetching data from silicon...
Attempting to open C:\Users\mattm\AppData\Local\Temp\tmpk28mk_d0 in raw mode...
>> Fetching data from silicon...
Extracting weights for Computational Storage serialization...
23:51:15 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
[BHS] floor-tier smoke PASS
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([384, 768]) from checkpoint, the shape in current model is torch.Size([192, 384]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([192]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([768, 384]) from checkpoint, the shape in current model is torch.Size([384, 192]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([768]) from checkpoint, the shape in current model is torch.Size([384]).
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_235120
Operation 'sedimentation_cycle_threshold_1' completed successfully

--- HIERARCHICAL SEDIMENTATION (Threshold=1, LR=0.001) ---
Collapse targets: 5 candidates.
23:51:21 [INFO] [training_start] Starting training on 5 samples
Phase 1: Per-cluster training (2 clusters, 2 epochs)...
Created checkpoint: before_hierarchical_sedimentation_threshold_1_20260709_235121
Phase 2: Global refinement (2 epochs, LR=0.000100)...
Adapter weights saved.
Syncing updated vectors to Qdrant...
23:51:22 [INFO] [hierarchical_sedimentation_success] Hierarchical training completed successfully. Updated 5 vectors.
Operation 'hierarchical_sedimentation_threshold_1' completed successfully
23:51:22 [INFO] [training_complete] Training complete | Updated: 5 | Failed: 0
Hierarchical sedimentation complete. Updated 5 vectors, 0 failed.
--- HIERARCHICAL SLEEP CYCLE COMPLETE ---
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_235126
Operation 'sedimentation_cycle_threshold_1' completed successfully
WARNING: teacher_weight=1.5 out of range [0.0, 1.0], clamping.
WARNING: teacher_weight=-0.5 out of range [0.0, 1.0], clamping.
WARNING: training_mode='invalid_mode' invalid. Valid options: ['baseline', 'offline', 'hybrid']. Using 'baseline'.
[model-scope] Observation enabled.
[model-scope] WARNING: Could not enable observation: no transformer model
Created checkpoint: before_model_scope_shadow_policy_20260709_235139
23:51:40 [INFO] [initialization] Initializing Antigravity Engine with model: ollama:nomic-embed-text
23:51:40 [INFO] [embedding_backend_init] Initializing Ollama backend: nomic-embed-text
23:51:44 [ERROR] [error] Ollama connection lost for doc 0
23:51:48 [ERROR] [error] Ollama connection lost for doc 0
23:51:52 [ERROR] [error] Ollama connection lost for doc 0
23:51:52 [ERROR] [error] Failed to embed doc 0 after retries
23:51:52 [INFO] [embedding_backend_ready] Ollama backend connected. Vector size: 768
23:51:52 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([192, 384]) from checkpoint, the shape in current model is torch.Size([384, 768]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([192]) from checkpoint, the shape in current model is torch.Size([384]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([384, 192]) from checkpoint, the shape in current model is torch.Size([768, 384]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([384]) from checkpoint, the shape in current model is torch.Size([768]).
23:51:52 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:51:52 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:51:52 [INFO] [sedimentation_start] Running sedimentation cycle (Mode=baseline, Threshold=1, LR=0.001)
23:51:52 [INFO] [training_preparation] Found 1 collapsing node candidates
23:51:52 [INFO] [training_start] Starting training on 1 samples
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_235152
23:51:52 [INFO] [checkpoint] Checkpoint save: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:51:52 [INFO] [vector_update] Syncing updated vectors to Qdrant
23:51:52 [INFO] [sedimentation_success] Training cycle completed successfully. Updated 1 vectors.
Operation 'sedimentation_cycle_threshold_1' completed successfully
23:51:52 [INFO] [training_complete] Training complete | Updated: 1 | Failed: 0
23:51:52 [INFO] [initialization] Initializing Antigravity Engine with model: ollama:nomic-embed-text
23:51:52 [INFO] [embedding_backend_init] Initializing Ollama backend: nomic-embed-text
23:51:56 [ERROR] [error] Ollama connection lost for doc 0
23:52:01 [ERROR] [error] Ollama connection lost for doc 0
23:52:05 [ERROR] [error] Ollama connection lost for doc 0
23:52:05 [ERROR] [error] Failed to embed doc 0 after retries
23:52:05 [INFO] [embedding_backend_ready] Ollama backend connected. Vector size: 768
23:52:05 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:52:05 [INFO] [checkpoint] Checkpoint load: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:52:05 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:52:05 [INFO] [sedimentation_start] Running sedimentation cycle (Mode=baseline, Threshold=1, LR=0.001)
23:52:05 [INFO] [training_preparation] Found 2 collapsing node candidates
23:52:05 [INFO] [noise_injection] Enabled noise injection with base scale 0.1
23:52:05 [INFO] [training_start] Starting training on 2 samples
Created checkpoint: before_sedimentation_cycle_threshold_1_20260709_235205
23:52:05 [INFO] [checkpoint] Checkpoint save: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\adapter_weights.pt
23:52:05 [INFO] [vector_update] Syncing updated vectors to Qdrant
23:52:05 [INFO] [sedimentation_success] Training cycle completed successfully. Updated 2 vectors.
Operation 'sedimentation_cycle_threshold_1' completed successfully
23:52:05 [INFO] [training_complete] Training complete | Updated: 2 | Failed: 0
{
  "commands": [
    {
      "name": "overlay_model_scope_tests",
      "command": [
        "C:\\Program Files\\Python311\\python.exe",
        "-m",
        "unittest",
        "test_adaptive_overlay.py",
        "test_run_model_scope_campaign.py",
        "test_model_scope_overlay_smoke.py",
        "test_promotion_contract.py",
        "test_attnres_repeat_seed_decision.py",
        "-v"
      ]
    },
    {
      "name": "model_scope_overlay_smoke",
      "command": [
        "C:\\Program Files\\Python311\\python.exe",
        "run_model_scope_overlay_smoke.py",
        "--output-dir",
        "experiment_runs\\overlay-model-scope-validation\\latest\\smoke"
      ]
    }
  ]
}
Extracting weights for Computational Storage serialization...
Extracting weights for Computational Storage serialization...
23:52:15 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:52:15 [INFO] [phase_c_gates] Gate readiness
23:52:15 [INFO] [phase_c_dataset_load] Loading dataset: X
23:52:15 [INFO] [phase_c_candidate_start] Evaluating baseline on X
23:52:15 [INFO] [phase_c_candidate_done] baseline on X: NDCG@10=0.0000
23:52:15 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmp68304tpx\phase_c_results.json
23:52:15 [INFO] [phase_c_done] Phase C evaluation complete
23:52:15 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:52:15 [INFO] [phase_c_gates] Gate readiness
23:52:15 [INFO] [phase_c_dataset_load] Loading dataset: X
23:52:15 [INFO] [phase_c_candidate_start] Evaluating baseline on X
23:52:15 [INFO] [phase_c_candidate_done] baseline on X: NDCG@10=0.0000
23:52:15 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpqb1_cdfm\phase_c_results.json
23:52:15 [INFO] [phase_c_done] Phase C evaluation complete
23:52:15 [INFO] [phase_c_start] Phase C eval: 1 candidates × 1 datasets
23:52:15 [INFO] [phase_c_gates] Gate readiness
23:52:15 [INFO] [phase_c_dataset_load] Loading dataset: TestDS
23:52:15 [INFO] [phase_c_candidate_start] Evaluating baseline on TestDS
23:52:16 [INFO] [phase_c_candidate_done] baseline on TestDS: NDCG@10=0.5000
23:52:16 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpqy3nisfy\phase_c_results.json
23:52:16 [INFO] [phase_c_done] Phase C evaluation complete
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpfirdqjpj\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpfirdqjpj\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpklug8lyo\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpklug8lyo\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1se38dlb\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1se38dlb\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpktzxvdky\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpktzxvdky\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp94p1d6gk\\post-bank-headtohead-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp94p1d6gk\\report.md"}
23:52:17 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:17 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate

--- HIERARCHICAL SEDIMENTATION (Threshold=3, LR=0.001) ---
Collapse targets: 0 candidates.
No sedimentation targets. Brain is stable.

--- HIERARCHICAL SEDIMENTATION (Threshold=3, LR=0.001) ---
Collapse targets: 10 candidates.
23:52:17 [INFO] [training_start] Starting training on 10 samples
Phase 1: Per-cluster training (2 clusters, 1 epochs)...
Phase 2: Global refinement (1 epochs, LR=0.000100)...
Adapter weights saved.
Syncing updated vectors to Qdrant...
23:52:17 [INFO] [hierarchical_sedimentation_success] Hierarchical training completed successfully. Updated 10 vectors.
23:52:17 [INFO] [training_complete] Training complete | Updated: 10 | Failed: 0
Hierarchical sedimentation complete. Updated 10 vectors, 0 failed.
--- HIERARCHICAL SLEEP CYCLE COMPLETE ---
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnkzieakm\\calibration-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnkzieakm\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpawaurcfh\\sweep\\knob-sweep-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpawaurcfh\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpoop0shpr\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpoop0shpr\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnw8btrta\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnw8btrta\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwfeeg01h\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpwfeeg01h\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1we9f4ej\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp1we9f4ej\\report.md"}
{"manifest": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp339cudte\\swap\\swap-campaign-manifest-2026-06.json", "report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp339cudte\\report.md"}
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvcu6necf\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvcu6necf\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvcu6necf\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpvcu6necf\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
23:52:32 [WARNING] [gate_train_skipped] Pool has only 25 samples (< 9999); skipping gate training.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_uudde9s\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_uudde9s\\nested\\output\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_uudde9s\\nested\\output\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp_uudde9s\\nested\\output\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
23:52:32 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:52:32 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:52:32 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
23:52:32 [WARNING] [gate_train_skipped] Pool has only 3 samples (< 10); skipping gate training.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qv9_iwt\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qv9_iwt\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qv9_iwt\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qv9_iwt\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkguo94vl\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkguo94vl\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkguo94vl\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpkguo94vl\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprbqo0xj8\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprbqo0xj8\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprbqo0xj8\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprbqo0xj8\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpfi96bg04\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpfi96bg04\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpfi96bg04\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpfi96bg04\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp04ccsbtf\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp04ccsbtf\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp04ccsbtf\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp04ccsbtf\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpbzd6o78_\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpbzd6o78_\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpbzd6o78_\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpbzd6o78_\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzcmbvru2\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzcmbvru2\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzcmbvru2\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpzcmbvru2\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp74dqnuop\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp74dqnuop\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp74dqnuop\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp74dqnuop\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qt9qcps\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qt9qcps\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qt9qcps\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp7qt9qcps\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpt8g0yjyq\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpt8g0yjyq\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpt8g0yjyq\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpt8g0yjyq\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx80hlii1\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx80hlii1\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx80hlii1\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpx80hlii1\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpa40g3q4r\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpa40g3q4r\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpa40g3q4r\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpa40g3q4r\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp955u_gjc\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp955u_gjc\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp955u_gjc\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp955u_gjc\\custom_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprikjihn3\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprikjihn3\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprikjihn3\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmprikjihn3\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnpiovdwq\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnpiovdwq\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnpiovdwq\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpnpiovdwq\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpsmd374_s\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpsmd374_s\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpsmd374_s\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpsmd374_s\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps01to0y\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps01to0y\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps01to0y\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpps01to0y\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5_cso_f1\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5_cso_f1\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5_cso_f1\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5_cso_f1\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "pool": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksseqlbx\\attribution_pool.json",
  "n_total_samples": 25,
  "reform_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksseqlbx\\reform_gate_v1.json",
  "mask_gate_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksseqlbx\\mask_gate_v1.json",
  "reform_gate_present": true,
  "mask_gate_present": true,
  "summary_path": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmpksseqlbx\\gate_train_summary.json"
}
23:52:32 [INFO] [gate_train_complete] Gate training complete.
{
  "run_dir": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5io4pmsf\\test_run",
  "status": "completed",
  "finished_at": "2026-07-10T03:52:32.993285+00:00",
  "latest_report": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5io4pmsf\\test_run\\reports\\iteration_001.json",
  "terminal_decision": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp5io4pmsf\\test_run\\terminal_decision.json"
}
============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session-32-review
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session-32-review --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-overnight
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-overnight --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

============================================================
Overnight Weight Refinement Campaign
============================================================
Run directory: D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session32-verylonglabelverylonglabelverylonglabelverylonglabelve
Query budget: 100
Distillation: 5 cycles x 50 queries x 5 epochs
Learning rate: 0.01
Teacher model: sentence-transformers/all-mpnet-base-v2
Large sweep: no

Fixes applied in this campaign:
  - use_quantization=True (chelation path enabled)
  - threshold=1 (not 3)
  - LR=0.01 (not 0.001)
  - Procrustes init: randn (not zeros)
  - Low-rank init: std=0.01 (not 0.001)
  - All 3 adapter types tested (mlp, procrustes, low_rank)
  - Different teacher model (768-dim) for real distillation signal
============================================================

Launching: C:\Program Files\Python311\python.exe -u run_weight_refinement_campaign.py --run-dir D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\experiment_runs\weight-refinement-20260424-210000-000000-session32-verylonglabelverylonglabelverylonglabelverylonglabelve --model sentence-transformers/all-MiniLM-L6-v2 --max-queries 100 --distill-cycles 5 --distill-queries-per-cycle 50 --distill-epochs 5 --learning-rate 0.01 --teacher sentence-transformers/all-mpnet-base-v2 --adapter-types mlp,procrustes,low_rank

23:52:33 [INFO] [phase_c_mask_gate_load] Loading mask gate: C:\Users\mattm\AppData\Local\Temp\tmp4z94dumw.json
23:52:33 [INFO] [phase_c_reform_gate_load] Loading reform gate: C:\Users\mattm\AppData\Local\Temp\tmp5ri5xtuu.json
23:52:33 [INFO] [phase_c_reform_gate_skip] Attribution pool not found; learned_reform_gate_v1 will be fail-closed
23:52:33 [INFO] [phase_c_reform_gate_train] Training reform gate from: C:\Users\mattm\AppData\Local\Temp\tmp90zzi4l8.json
23:52:33 [INFO] [phase_c_start] Phase C eval: 2 candidates × 1 datasets
23:52:33 [INFO] [phase_c_gates] Gate readiness
23:52:33 [INFO] [phase_c_dataset_load] Loading dataset: SciFact
23:52:33 [INFO] [phase_c_candidate_start] Evaluating baseline on SciFact
23:52:33 [INFO] [phase_c_candidate_done] baseline on SciFact: NDCG@10=1.0000
23:52:33 [INFO] [phase_c_candidate_start] Evaluating guard_p85_t0.01 on SciFact
23:52:33 [INFO] [phase_c_candidate_done] guard_p85_t0.01 on SciFact: NDCG@10=1.0000
23:52:33 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpian6j5cu\phase_c_results.json
23:52:33 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpian6j5cu\overlay_card_baseline.json
23:52:33 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpian6j5cu\overlay_card_guard_p85_t0.01.json
23:52:33 [INFO] [phase_c_done] Phase C evaluation complete
23:52:33 [INFO] [phase_c_start] Phase C eval: 2 candidates × 1 datasets
23:52:33 [INFO] [phase_c_gates] Gate readiness
23:52:33 [INFO] [phase_c_dataset_load] Loading dataset: SciFact
23:52:33 [INFO] [phase_c_candidate_start] Evaluating baseline on SciFact
23:52:33 [INFO] [phase_c_candidate_done] baseline on SciFact: NDCG@10=0.8155
23:52:33 [INFO] [phase_c_candidate_start] Evaluating guard_p85_t0.01 on SciFact
23:52:33 [INFO] [phase_c_candidate_done] guard_p85_t0.01 on SciFact: NDCG@10=0.8155
23:52:33 [INFO] [phase_c_results_written] Results written: C:\Users\mattm\AppData\Local\Temp\tmpmkzyv8dq\phase_c_results.json
23:52:33 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpmkzyv8dq\overlay_card_baseline.json
23:52:33 [INFO] [phase_c_overlay_card_written] Overlay card written: C:\Users\mattm\AppData\Local\Temp\tmpmkzyv8dq\overlay_card_guard_p85_t0.01.json
23:52:33 [INFO] [phase_c_done] Phase C evaluation complete
line one
C:\Users\mattm\AppData\Local\Temp\tmpb9jgcihg
C:\Users\mattm\AppData\Local\Temp\tmpwmeq255b
SMOKE_PIPELINE PASS (mocked)
[BHS] ceiling-tier smoke PASS
SMOKE_PIPELINE FAIL: simulated
[BHS] ceiling-tier smoke FAIL: smoke_pipeline.py exit=1
23:52:36 [INFO] [initialization] Initializing Antigravity Engine with model: all-MiniLM-L6-v2
23:52:36 [INFO] [embedding_backend_init] Initializing local backend: all-MiniLM-L6-v2
23:52:36 [INFO] [initialization] Device Selected: cuda
23:52:37 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:52:37 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:52:37 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:52:37 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:52:37 [INFO] [ingestion_start] Ingesting 4 documents
23:52:37 [INFO] [ingestion_complete] Ingestion Complete
23:52:37 [INFO] [initialization] Initializing Antigravity Engine with model: all-MiniLM-L6-v2
23:52:37 [INFO] [embedding_backend_init] Initializing local backend: all-MiniLM-L6-v2
23:52:37 [INFO] [initialization] Device Selected: cuda
23:52:38 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:52:38 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:52:38 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:52:38 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:52:38 [INFO] [ingestion_start] Ingesting 4 documents
23:52:38 [INFO] [ingestion_complete] Ingestion Complete
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
23:52:38 [INFO] [quantization_gate_evaluated] Evaluated quantization promotion gate
TTS enabled: translation=True, transport=True, steering=False
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp2ax2klvs\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
TTS enabled: translation=False, transport=True, steering=True
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp41bbncya\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
TTS enabled: translation=True, transport=False, steering=True
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp54g4ni_5\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": true,
    "profiles_with_tts": 0,
    "total_tts_queries": 0
  }
}
{
  "output": "C:\\Users\\mattm\\AppData\\Local\\Temp\\tmp6kyrjv_5\\out.json",
  "task": "SciFact",
  "model": "sentence-transformers/all-MiniLM-L6-v2",
  "corpus_size": 2,
  "query_count": 1,
  "default_recommendation": {
    "recommended_profile": "baseline",
    "baseline_ndcg_at_10": 0.5,
    "best_ndcg_at_10": 0.5,
    "delta_vs_baseline": 0.0,
    "default_change_allowed": false,
    "reason": "baseline_remains_best"
  },
  "tts_summary": {
    "enabled": false
  }
}
23:52:41 [INFO] [initialization] Initializing Antigravity Engine with model: sentence-transformers/all-MiniLM-L6-v2
23:52:41 [INFO] [embedding_backend_init] Initializing local backend: sentence-transformers/all-MiniLM-L6-v2
23:52:41 [INFO] [initialization] Device Selected: cuda
23:52:42 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:52:42 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:52:42 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:52:42 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:52:42 [INFO] [ingestion_start] Ingesting 2 documents
23:52:42 [INFO] [ingestion_complete] Ingestion Complete
23:52:42 [INFO] [query] Query: 'cat mat...' | Action: FAST
23:52:42 [INFO] [initialization] Initializing Antigravity Engine with model: sentence-transformers/all-MiniLM-L6-v2
23:52:42 [INFO] [embedding_backend_init] Initializing local backend: sentence-transformers/all-MiniLM-L6-v2
23:52:42 [INFO] [initialization] Device Selected: cuda
23:52:42 [INFO] [embedding_backend_ready] Local backend loaded. Vector size: 384
23:52:42 [INFO] [adapter_init] Initializing Dynamic Chelation Adapter
23:52:42 [INFO] [adapter_init] Created new adapter (Identity initialization)
23:52:42 [INFO] [checkpoint_manager_init] CheckpointManager initialized for safe training
23:52:42 [INFO] [ingestion_start] Ingesting 2 documents
23:52:42 [INFO] [ingestion_complete] Ingestion Complete
23:52:42 [INFO] [tts_enabled] TTS pipeline enabled
23:52:42 [INFO] [query] Query: 'cat mat...' | Action: FAST
Warning: Failed to load adapter weights (Dimension Mismatch?): Error(s) in loading state_dict for ChelationAdapter:
	size mismatch for correction_net.0.weight: copying a param with shape torch.Size([256, 512]) from checkpoint, the shape in current model is torch.Size([384, 768]).
	size mismatch for correction_net.0.bias: copying a param with shape torch.Size([256]) from checkpoint, the shape in current model is torch.Size([384]).
	size mismatch for correction_net.2.weight: copying a param with shape torch.Size([512, 256]) from checkpoint, the shape in current model is torch.Size([768, 384]).
	size mismatch for correction_net.2.bias: copying a param with shape torch.Size([512]) from checkpoint, the shape in current model is torch.Size([768]).
WARNING: chelation_p=150 out of range [0, 100], clamping.
WARNING: chelation_p=-10 out of range [0, 100], clamping.
WARNING: epochs=200 out of range [1, 100], clamping.
WARNING: epochs=0 out of range [1, 100], clamping.
WARNING: learning_rate=10.0 out of range [0.0001, 1.0], clamping.
WARNING: learning_rate=1e-05 out of range [0.0001, 1.0], clamping.
WARNING: max_depth=20 out of range [1, 10], clamping.
WARNING: max_depth=0 out of range [1, 10], clamping.
.......................................................................................................................................................ERROR: Results file not found: does_not_exist.json
.............................................................................................................................................................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'fast' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'slow' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
.D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'cfg1' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
....D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'a' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'b' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 'c' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
.D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\benchmark_beir.py:557: RuntimeWarning: BenchmarkResult for 't' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  self.run_single_dataset(ds_name, corpus, queries, qrels,
...D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_benchmark_beir.py:309: RuntimeWarning: BenchmarkResult for 'a' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  results = runner.run_single_dataset("TestDS", corpus, queries, qrels,
D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_benchmark_beir.py:309: RuntimeWarning: BenchmarkResult for 'b' uses synthetic retrieval (unranked first-10 corpus docs, 0.0 ms latency). This result is tagged is_synthetic=True and must not appear in production reports or promotion decisions.
  results = runner.run_single_dataset("TestDS", corpus, queries, qrels,
..................................................................................................................................................................................................................................................................................................................................................................................................................................s.........s.............................................................................................................................................................................................................................................................................................................................Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'default' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\default\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:14:01 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'corpus' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\corpus\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:39 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
Using the latest cached version of the dataset since mteb/scifact couldn't be found on the Hugging Face Hub (offline mode is enabled).
Found the latest cached dataset configuration 'queries' at C:\Users\mattm\.cache\huggingface\datasets\mteb___scifact\queries\0.0.0\d56462d0e63a25450459c4f213e49ffdb866f7f9 (last modified on Thu Jan  1 20:16:08 2026).
...........................................................................................................................................................................................................................................................................................ss........D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_model_scope_runtime.py:261: UserWarning: torch tensor protocol fallback triggered in hook (mock-tensor compatible path; unexpected in production): AttributeError("'float' object has no attribute 'item'")
  hook_fn(lm, None, tensor)
................................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\test_model_scope_steering.py:655: UserWarning: Skipping corrupt JSON line in C:\Users\mattm\AppData\Local\Temp\tmp61856vqr\corrupt.jsonl: 'THIS IS NOT VALID JSON {{{'
  loaded = actuator2.load_records(path)
...........................................................................................................................................................................................ssssss..................................................................................s...................................D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build\qwen_scope_adapter.py:43: FutureWarning: You are using `torch.load` with `weights_only=False` (the current default value), which uses the default pickle module implicitly. It is possible to construct malicious pickle data which will execute arbitrary code during unpickling (See https://github.com/pytorch/pytorch/blob/main/SECURITY.md#untrusted-models for more details). In a future release, the default value for `weights_only` will be flipped to `True`. This limits the functions that could be executed during unpickling. Arbitrary objects will no longer be allowed to be loaded via this mode unless they are explicitly allowlisted by the user via `torch.serialization.add_safe_globals`. We recommend you start setting `weights_only=True` for any use case where you don't have full control of the loaded file. Please open an issue on GitHub for any issues related to this experimental feature.
  state_dict = torch.load(Path(path), map_location="cpu")
..............................................................................................................................................................................WARNING: pool has 25 samples, below minimum 9999. Skipping training.
..ERROR: attribution pool file not found: nonexistent_pool.json
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.WARNING: pool has 3 samples, below minimum 10. Skipping training.
.ERROR: attribution pool file not found: C:\Users\mattm\AppData\Local\Temp\tmplqum3j2p\does_not_exist.json
.ERROR: attribution pool file not found: C:\Users\mattm\AppData\Local\Temp\tmph9btvxmc\does_not_exist.json
............................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................
----------------------------------------------------------------------
Ran 2757 tests in 172.145s

OK (skipped=11)

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

All four offline regimes were built/reloaded with parity and leakage audits, the frozen-target contract and failure-masking review findings were fixed, artifacts regenerated, adversarial review cleared, and the final full repository suite passed.
  ✓ Inspect repo rules, D1 pack/harness/stats contracts, existing artifacts, and relevant prior CHELATEDAI notes
  ✓ Implement estimator package and focused tests inside research/drift_recovery
  ✓ Discover/build offline regimes, assert parity, and emit estimator artifacts
  ✓ Run focused and broader validation, then finalize D3 report with honest verdict
exec
"C:\\Program Files\\WindowsApps\\Microsoft.PowerShell_7.6.3.0_x64__8wekyb3d8bbwe\\pwsh.exe" -Command 'git status --short; rg -n "def extract_regime_features|def fit_calibration|def leave_one_regime_out|def estimate_recovery|class TestMarginBound|class TestEstimatorCalibration|def run_d3" research/drift_recovery/estimator research/drift_recovery/tests/test_estimator.py research/drift_recovery/run_estimator.py; rg -n "Verdict|Smallest concrete failure|Legacy D1 label note|Most important limitation" research/drift_recovery/out/estimator/estimator_validation.md research/drift_recovery/out/estimator/D3_REPORT.md; $p='"'C:\\Users\\mattm\\.codex\\memories\\rollout_summaries\\2026-06-11T15-10-39-PIMu-drift_recovery_pr6_pr8_calibration_knob_sweep_and_bhs_harden.md'; Select-String -Path "'$p -Pattern '"'thread_id|successful verification path|gitignored|adversarial BHS review' | ForEach-Object {\""'$($_.LineNumber):$($_.Line)"}' in D:\GITHUB\CHELATEDAI\.claude\worktrees\agent-build
 succeeded in 3230ms:
?? research/
research/drift_recovery/run_estimator.py:426:def run_d3(
research/drift_recovery/tests/test_estimator.py:86:class TestMarginBound(unittest.TestCase):
research/drift_recovery/tests/test_estimator.py:141:class TestEstimatorCalibration(unittest.TestCase):
research/drift_recovery/estimator\calibration.py:125:def fit_calibration(
research/drift_recovery/estimator\features.py:152:def extract_regime_features(
research/drift_recovery/estimator\validation.py:54:def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
research/drift_recovery/estimator\predictor.py:29:def estimate_recovery(
research/drift_recovery/out/estimator/D3_REPORT.md:7:Leave-one-regime-out result: Spearman **-0.400**, three-way bin accuracy **75.0%**. Verdict: **VALIDATION-NEGATIVE**.
research/drift_recovery/out/estimator/D3_REPORT.md:11:Most important limitation: the sufficient bound saturated at a 100% predicted violation rate in all 4 regimes despite measured recovery spanning 0.65-0.84, so the core inversion-rate signal did not discriminate regimes.
research/drift_recovery/out/estimator/estimator_validation.md:15:- Verdict: **VALIDATION-NEGATIVE**
research/drift_recovery/out/estimator/estimator_validation.md:19:Smallest concrete failure: the bound predicted a violation for **100% of queries in every available regime**, so its inversion-rate feature did not separate observed recoveries ranging from 0.647 to 0.843.
research/drift_recovery/out/estimator/estimator_validation.md:23:Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack ridge target R=0.8434. Its estimator feature map is fitted only on `extra_arrays["leakage_safe_fit_idx"]` (safe-refit diagnostic R=0.7891), because the legacy literal D1 target fit contains 28 eval-positive documents.
1:thread_id: 019eb73c-3da3-7f70-9035-afd5a56ecb86
9:Rollout context: The active objective was to follow `docs/waypoint-research-2026-06-09/goal-day-2026-06-13.md` exactly: diagnose the mixed drift-recovery result, run a severity-calibration campaign into the 8–20% drop zone, execute a pre-registered C3 knob sweep with all cells reported plus 3-seed confirmation, avoid touching parked PRs #256/#257, and finish by appending the `Day 2026-06-13` section to the local-only waypoint notes. The repo uses a strict brutal-honesty/BHS process with required PR-body fields, fresh adversarial Tier B review, and `scripts/smoke_pipeline.py` as the release gate. The waypoint folder is gitignored/local-only and must not be committed.
78:- The first local/online smoke attempts surfaced a host-specific Hugging Face SSL issue; the successful verification path on this machine was `HF_HUB_OFFLINE=1` plus the cached model/dataset.
79:- A major blocker was that `experiment_runs/drift-recovery/knob-sweep/` was gitignored. The artifacts were real but not durable until force-added. Future evidence-bearing experiment runs in ignored directories must be force-added or moved to a tracked path before PR creation.
104:- Spawned fresh reviewer sub-agents for adversarial BHS review: Kant, Kierkegaard, Aquinas, and Archimedes.

diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

codex
Implemented D3 with an honest **VALIDATION-NEGATIVE** verdict.

- Four regimes validated: SciFact/NFCorpus × mpnet/bge-large.
- LOO Spearman: **−0.400**
- Three-way bin accuracy: **75%** — below the locked 80% threshold.
- Lower-band coverage: **75%**
- Harness parity: **0.0 error for every pack**, within `1e-12`.
- Core failure: predicted inversion rate saturated at **100% in every regime**, despite recovery ranging from `0.647` to `0.843`.

Key outputs:

- [estimator_validation.md](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/out/estimator/estimator_validation.md:1)
- [D3_REPORT.md](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/out/estimator/D3_REPORT.md:1)
- [regime_manifest.json](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/out/estimator/regime_manifest.json)
- [predictor.py](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/estimator/predictor.py:29)
- [test_estimator.py](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/tests/test_estimator.py:86)

Validation:

- **PASS:** repository-wide suite — `2,757 tests`, `11 skipped`, `0 failures`
- **PASS:** ruff and compilation
- **PASS:** deterministic artifact/checksum/leakage/parity audit
- **PASS:** fresh adversarial review; no blockers remained

The frozen D1 pack was not regenerated. Its locked `0.8434` target is preserved, while estimator feature fitting uses the leakage-safe index; the legacy target’s 28 eval-positive-document contamination is explicitly disclosed. The pre-existing `research/` tree remains untracked in this detached worktree.


diff --git a/research/drift_recovery/estimator/__init__.py b/research/drift_recovery/estimator/__init__.py
new file mode 100644
index 0000000000000000000000000000000000000000..9535d256fa1267e26b6aa5783f1660071ad5ab64
--- /dev/null
+++ b/research/drift_recovery/estimator/__init__.py
@@ -0,0 +1,5 @@
+"""Calibrated, explicitly non-certifying recoverability estimator."""
+
+from research.drift_recovery.estimator.predictor import estimate_recovery
+
+__all__ = ["estimate_recovery"]
diff --git a/research/drift_recovery/estimator/calibration.py b/research/drift_recovery/estimator/calibration.py
new file mode 100644
index 0000000000000000000000000000000000000000..0db4d27d785ebe38a3e262ced30db1a77ee2d780
--- /dev/null
+++ b/research/drift_recovery/estimator/calibration.py
@@ -0,0 +1,184 @@
+"""Small cross-fittable linear calibration map for regime-level recovery."""
+
+from __future__ import annotations
+
+from dataclasses import asdict, dataclass
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+
+# Fixed before validation.  The strong ridge penalty is intentional because
+# outer-fold training sets contain only a handful of regimes.
+FEATURE_NAMES = (
+    "predicted_preservation_rate",
+    "oracle_margin_sign_rate",
+    "oracle_margin_q10",
+    "oracle_margin_q50",
+    "fit_error_norm_q50",
+    "fit_error_norm_q90",
+    "bound_slack_q10",
+    "bound_slack_q50",
+)
+DEFAULT_REGULARIZATION = 10.0
+DEFAULT_LOWER_COVERAGE = 0.80
+
+
+def feature_vector(features: Mapping[str, Any]) -> np.ndarray:
+    """Return the predeclared calibration vector from a feature record."""
+
+    summary = features.get("summary", features)
+    missing = [name for name in FEATURE_NAMES if name not in summary]
+    if missing:
+        raise ValueError(f"feature record is missing calibration fields: {missing}")
+    vector = np.asarray([summary[name] for name in FEATURE_NAMES], dtype=np.float64)
+    if not np.all(np.isfinite(vector)):
+        raise ValueError("calibration features must be finite")
+    return vector
+
+
+@dataclass(frozen=True)
+class LinearCalibration:
+    feature_names: Sequence[str]
+    feature_mean: Sequence[float]
+    feature_scale: Sequence[float]
+    coefficients: Sequence[float]
+    intercept: float
+    regularization: float
+    lower_coverage: float
+    lower_offset: float
+    training_count: int
+    training_regimes: Sequence[str]
+    prediction_clip_low: float = 0.0
+    prediction_clip_high: float = 1.0
+    record_type: str = "recoverability_linear_calibration"
+    format_version: int = 1
+
+    def to_dict(self) -> Dict[str, Any]:
+        return asdict(self)
+
+    @classmethod
+    def from_dict(cls, payload: Mapping[str, Any]) -> "LinearCalibration":
+        expected = payload.get("record_type", "recoverability_linear_calibration")
+        if expected != "recoverability_linear_calibration":
+            raise ValueError(f"unexpected calibration record type: {expected}")
+        fields = {
+            key: payload[key]
+            for key in cls.__dataclass_fields__
+            if key in payload
+        }
+        return cls(**fields)
+
+    def predict_vector(self, vector: np.ndarray) -> float:
+        values = np.asarray(vector, dtype=np.float64)
+        if values.shape != (len(self.feature_names),):
+            raise ValueError("calibration vector has the wrong shape")
+        mean = np.asarray(self.feature_mean, dtype=np.float64)
+        scale = np.asarray(self.feature_scale, dtype=np.float64)
+        coefficients = np.asarray(self.coefficients, dtype=np.float64)
+        raw = float(self.intercept + ((values - mean) / scale) @ coefficients)
+        return float(np.clip(raw, self.prediction_clip_low, self.prediction_clip_high))
+
+    def predict(self, features: Mapping[str, Any]) -> float:
+        return self.predict_vector(feature_vector(features))
+
+    def lower_band(self, features: Mapping[str, Any]) -> float:
+        prediction = self.predict(features)
+        return float(max(self.prediction_clip_low, prediction - self.lower_offset))
+
+
+def _fit_core(
+    matrix: np.ndarray,
+    targets: np.ndarray,
+    regularization: float,
+) -> tuple:
+    mean = np.mean(matrix, axis=0)
+    scale = np.std(matrix, axis=0, ddof=0)
+    scale = np.where(scale > 1e-12, scale, 1.0)
+    standardized = (matrix - mean) / scale
+    target_mean = float(np.mean(targets))
+    centered_targets = targets - target_mean
+    penalty = float(regularization) * np.eye(matrix.shape[1], dtype=np.float64)
+    coefficients = np.linalg.solve(
+        standardized.T @ standardized + penalty,
+        standardized.T @ centered_targets,
+    )
+    return mean, scale, coefficients, target_mean
+
+
+def _predict_core(
+    vector: np.ndarray,
+    mean: np.ndarray,
+    scale: np.ndarray,
+    coefficients: np.ndarray,
+    intercept: float,
+) -> float:
+    return float(intercept + ((vector - mean) / scale) @ coefficients)
+
+
+def _higher_quantile(values: np.ndarray, coverage: float) -> float:
+    ordered = np.sort(np.asarray(values, dtype=np.float64))
+    index = min(len(ordered) - 1, max(0, int(np.ceil(float(coverage) * len(ordered))) - 1))
+    return float(ordered[index])
+
+
+def fit_calibration(
+    feature_records: Sequence[Mapping[str, Any]],
+    recoveries: Sequence[float],
+    regime_names: Sequence[str] = (),
+    regularization: float = DEFAULT_REGULARIZATION,
+    lower_coverage: float = DEFAULT_LOWER_COVERAGE,
+) -> LinearCalibration:
+    """Fit a regularized linear map and a training-only one-sided lower offset."""
+
+    if len(feature_records) != len(recoveries) or len(feature_records) == 0:
+        raise ValueError("features and recoveries must have the same non-zero length")
+    if regime_names and len(regime_names) != len(feature_records):
+        raise ValueError("regime_names length mismatch")
+    if not np.isfinite(regularization) or regularization <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    if not 0.0 < lower_coverage < 1.0:
+        raise ValueError("lower_coverage must lie strictly between zero and one")
+
+    matrix = np.vstack([feature_vector(record) for record in feature_records])
+    targets = np.asarray(recoveries, dtype=np.float64)
+    if not np.all(np.isfinite(targets)):
+        raise ValueError("recoveries must be finite")
+    mean, scale, coefficients, intercept = _fit_core(matrix, targets, regularization)
+
+    if len(targets) < 2:
+        # A single regime cannot calibrate residual uncertainty.  A full recovery
+        # point is deliberately subtracted so the lower output is non-assertive.
+        lower_offset = 1.0
+    else:
+        cross_fitted = []
+        for held_out in range(len(targets)):
+            train = np.arange(len(targets)) != held_out
+            fold = _fit_core(matrix[train], targets[train], regularization)
+            cross_fitted.append(_predict_core(matrix[held_out], *fold))
+        overprediction = np.maximum(np.asarray(cross_fitted) - targets, 0.0)
+        lower_offset = _higher_quantile(overprediction, lower_coverage)
+
+    names = tuple(str(name) for name in regime_names) if regime_names else tuple()
+    return LinearCalibration(
+        feature_names=FEATURE_NAMES,
+        feature_mean=mean.tolist(),
+        feature_scale=scale.tolist(),
+        coefficients=coefficients.tolist(),
+        intercept=float(intercept),
+        regularization=float(regularization),
+        lower_coverage=float(lower_coverage),
+        lower_offset=float(lower_offset),
+        training_count=int(len(targets)),
+        training_regimes=names,
+    )
+
+
+__all__ = [
+    "DEFAULT_LOWER_COVERAGE",
+    "DEFAULT_REGULARIZATION",
+    "FEATURE_NAMES",
+    "LinearCalibration",
+    "feature_vector",
+    "fit_calibration",
+]
diff --git a/research/drift_recovery/estimator/features.py b/research/drift_recovery/estimator/features.py
new file mode 100644
index 0000000000000000000000000000000000000000..d2f85d1b649750043c7d7043bfde52a880eda689
--- /dev/null
+++ b/research/drift_recovery/estimator/features.py
@@ -0,0 +1,262 @@
+"""Pure-NumPy margin-bound features over frozen embedding packs.
+
+The calculations intentionally operate in the cosine-normalized geometry used
+by the merged retrieval harness.  Fitting uses document pairs selected by the
+pack's leakage-safe fit index; eval qrels are used only after fitting to choose
+the relevant/competitor pair for each diagnostic inequality.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+from typing import Any, Dict, Mapping
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.margin_bound import (
+    order_preservation_slack,
+    predict_inversion,
+)
+
+
+RIDGE_REGULARIZATION = 1.0
+QUANTILES = (0.01, 0.05, 0.10, 0.25, 0.50, 0.75, 0.90, 0.95, 0.99)
+
+
+def _normalize_rows(matrix: np.ndarray) -> np.ndarray:
+    values = np.asarray(matrix, dtype=np.float64)
+    if values.ndim != 2:
+        raise ValueError("expected a two-dimensional matrix")
+    norms = np.linalg.norm(values, axis=1, keepdims=True)
+    return values / np.where(norms > 0.0, norms, 1.0)
+
+
+def _positive_eval_doc_ids(pack: EmbeddingPack) -> set:
+    positives = set()
+    for query_id in np.asarray(pack.query_ids, dtype=str):
+        relevance = pack.qrels.get(str(query_id), {})
+        positives.update(
+            str(doc_id) for doc_id, score in relevance.items() if float(score) > 0.0
+        )
+    return positives
+
+
+def leakage_safe_fit_indices(pack: EmbeddingPack) -> np.ndarray:
+    """Return the audited safe index, rejecting eval-positive contamination."""
+
+    if "leakage_safe_fit_idx" in pack.extra_arrays:
+        indices = np.asarray(pack.extra_arrays["leakage_safe_fit_idx"], dtype=np.int64)
+        source = "extra_arrays.leakage_safe_fit_idx"
+    else:
+        indices = np.asarray(pack.fit_idx, dtype=np.int64)
+        source = "fit_idx"
+    if indices.ndim != 1 or len(indices) == 0:
+        raise ValueError(f"{source} must be a non-empty one-dimensional index")
+    if np.any(indices < 0) or np.any(indices >= len(pack.doc_ids)):
+        raise ValueError(f"{source} contains an out-of-bounds document index")
+    if len(np.unique(indices)) != len(indices):
+        raise ValueError(f"{source} contains duplicate document indices")
+
+    positive_ids = _positive_eval_doc_ids(pack)
+    fit_ids = set(np.asarray(pack.doc_ids, dtype=str)[indices].tolist())
+    overlap = sorted(fit_ids.intersection(positive_ids))
+    if overlap:
+        raise ValueError(
+            f"{source} is not leakage-safe: {len(overlap)} eval-positive documents in fit"
+        )
+    return indices.copy()
+
+
+def fit_ridge_map(
+    source: np.ndarray,
+    target: np.ndarray,
+    fit_idx: np.ndarray,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> np.ndarray:
+    """Fit the same no-intercept ridge map used by D1 on selected document pairs."""
+
+    source_values = np.asarray(source, dtype=np.float64)
+    target_values = np.asarray(target, dtype=np.float64)
+    indices = np.asarray(fit_idx, dtype=np.int64)
+    if source_values.shape != target_values.shape or source_values.ndim != 2:
+        raise ValueError("ridge source and target must be same-shape matrices")
+    if not np.isfinite(regularization) or float(regularization) <= 0.0:
+        raise ValueError("regularization must be finite and positive")
+    selected_source = source_values[indices]
+    selected_target = target_values[indices]
+    dimension = source_values.shape[1]
+    return np.linalg.solve(
+        selected_source.T @ selected_source
+        + float(regularization) * np.eye(dimension, dtype=np.float64),
+        selected_source.T @ selected_target,
+    )
+
+
+@dataclass(frozen=True)
+class RidgeFit:
+    fit_idx: np.ndarray
+    ridge_map: np.ndarray
+    corrected_documents: np.ndarray
+    normalized_corrected_documents: np.ndarray
+    normalized_oracle_documents: np.ndarray
+    error_norms: np.ndarray
+
+
+def fit_ridge_for_pack(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> RidgeFit:
+    """Fit from safe document pairs without passing qrels into the optimizer."""
+
+    pack.validate()
+    fit_idx = leakage_safe_fit_indices(pack)
+    ridge_map = fit_ridge_map(pack.Do, pack.Dor, fit_idx, regularization=regularization)
+    corrected = np.asarray(pack.Do, dtype=np.float64) @ ridge_map
+    corrected_normalized = _normalize_rows(corrected)
+    oracle_normalized = _normalize_rows(pack.Dor)
+    errors = np.linalg.norm(corrected_normalized - oracle_normalized, axis=1)
+    return RidgeFit(
+        fit_idx=fit_idx,
+        ridge_map=ridge_map,
+        corrected_documents=corrected,
+        normalized_corrected_documents=corrected_normalized,
+        normalized_oracle_documents=oracle_normalized,
+        error_norms=errors,
+    )
+
+
+def _quantile_summary(values: np.ndarray, prefix: str) -> Dict[str, float]:
+    array = np.asarray(values, dtype=np.float64)
+    if array.ndim != 1 or len(array) == 0 or not np.all(np.isfinite(array)):
+        raise ValueError(f"{prefix} values must be a finite non-empty vector")
+    result = {
+        f"{prefix}_mean": float(np.mean(array)),
+        f"{prefix}_std": float(np.std(array, ddof=0)),
+        f"{prefix}_min": float(np.min(array)),
+        f"{prefix}_max": float(np.max(array)),
+    }
+    for quantile in QUANTILES:
+        label = int(round(100 * quantile))
+        result[f"{prefix}_q{label:02d}"] = float(np.quantile(array, quantile))
+    return result
+
+
+def _relevance(pack: EmbeddingPack, query_id: str) -> Mapping[str, float]:
+    relevance = pack.qrels.get(str(query_id))
+    if relevance is None:
+        raise ValueError(f"qrels missing eval query {query_id}")
+    return relevance
+
+
+def extract_regime_features(
+    pack: EmbeddingPack,
+    regularization: float = RIDGE_REGULARIZATION,
+) -> Dict[str, Any]:
+    """Extract deterministic per-query and aggregate margin-bound features."""
+
+    fit = fit_ridge_for_pack(pack, regularization=regularization)
+    queries = _normalize_rows(pack.Qd)
+    query_norms = np.linalg.norm(queries, axis=1)
+    oracle_scores = queries @ fit.normalized_oracle_documents.T
+    document_ids = np.asarray(pack.doc_ids, dtype=str)
+    doc_lookup = {doc_id: index for index, doc_id in enumerate(document_ids)}
+
+    margins = []
+    relevant_errors = []
+    competitor_errors = []
+    relevant_indices = []
+    competitor_indices = []
+    for row, query_id in enumerate(np.asarray(pack.query_ids, dtype=str)):
+        relevant = [
+            doc_lookup[str(doc_id)]
+            for doc_id, score in _relevance(pack, query_id).items()
+            if float(score) > 0.0 and str(doc_id) in doc_lookup
+        ]
+        if not relevant:
+            raise ValueError(f"eval query {query_id} has no in-pack positive document")
+        relevant_array = np.asarray(sorted(set(relevant)), dtype=np.int64)
+        relevant_score_values = oracle_scores[row, relevant_array]
+        relevant_index = int(relevant_array[int(np.argmax(relevant_score_values))])
+
+        non_relevant_mask = np.ones(len(document_ids), dtype=bool)
+        non_relevant_mask[relevant_array] = False
+        if not np.any(non_relevant_mask):
+            raise ValueError(f"eval query {query_id} has no non-relevant competitor")
+        non_relevant_indices = np.flatnonzero(non_relevant_mask)
+        competitor_index = int(
+            non_relevant_indices[int(np.argmax(oracle_scores[row, non_relevant_indices]))]
+        )
+
+        margin = oracle_scores[row, relevant_index] - oracle_scores[row, competitor_index]
+        margins.append(float(margin))
+        relevant_errors.append(float(fit.error_norms[relevant_index]))
+        competitor_errors.append(float(fit.error_norms[competitor_index]))
+        relevant_indices.append(relevant_index)
+        competitor_indices.append(competitor_index)
+
+    margin_array = np.asarray(margins, dtype=np.float64)
+    relevant_error_array = np.asarray(relevant_errors, dtype=np.float64)
+    competitor_error_array = np.asarray(competitor_errors, dtype=np.float64)
+    rhs = query_norms * (relevant_error_array + competitor_error_array)
+    slack = order_preservation_slack(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+    inversions = predict_inversion(
+        margin_array,
+        query_norms,
+        relevant_error_array,
+        competitor_error_array,
+    )
+
+    summary: Dict[str, Any] = {
+        "query_count": int(len(pack.query_ids)),
+        "document_count": int(len(pack.doc_ids)),
+        "embedding_dimension": int(pack.Do.shape[1]),
+        "fit_count": int(len(fit.fit_idx)),
+        "fit_index_source": (
+            "extra_arrays.leakage_safe_fit_idx"
+            if "leakage_safe_fit_idx" in pack.extra_arrays
+            else "fit_idx"
+        ),
+        "eval_positive_docs_in_fit": 0,
+        "ridge_regularization": float(regularization),
+        "oracle_margin_sign_rate": float(np.mean(margin_array > 0.0)),
+        "predicted_inversion_rate": float(np.mean(inversions)),
+        "predicted_preservation_rate": float(np.mean(~inversions)),
+    }
+    summary.update(_quantile_summary(margin_array, "oracle_margin"))
+    summary.update(_quantile_summary(fit.error_norms[fit.fit_idx], "fit_error_norm"))
+    summary.update(_quantile_summary(fit.error_norms, "all_doc_error_norm"))
+    summary.update(_quantile_summary(relevant_error_array, "relevant_error_norm"))
+    summary.update(_quantile_summary(competitor_error_array, "competitor_error_norm"))
+    summary.update(_quantile_summary(rhs, "bound_rhs"))
+    summary.update(_quantile_summary(np.asarray(slack), "bound_slack"))
+
+    return {
+        "record_type": "recoverability_margin_features",
+        "feature_version": 1,
+        "regime": {
+            "dataset": str(pack.metadata.get("dataset", "unknown")),
+            "old_model": str(pack.metadata.get("models", {}).get("old", "unknown")),
+            "swap_model": str(pack.metadata.get("models", {}).get("swap", "unknown")),
+            "anchor_fraction": float(pack.metadata.get("anchor_fraction", float("nan"))),
+            "seed": int(pack.metadata.get("seed", 0)),
+        },
+        "summary": summary,
+        "per_query": {
+            "query_ids": np.asarray(pack.query_ids, dtype=str).tolist(),
+            "relevant_doc_ids": document_ids[np.asarray(relevant_indices)].tolist(),
+            "competitor_doc_ids": document_ids[np.asarray(competitor_indices)].tolist(),
+            "oracle_margin": margin_array.tolist(),
+            "query_norm": query_norms.tolist(),
+            "relevant_error_norm": relevant_error_array.tolist(),
+            "competitor_error_norm": competitor_error_array.tolist(),
+            "bound_rhs": rhs.tolist(),
+            "bound_slack": np.asarray(slack, dtype=np.float64).tolist(),
+            "predicted_inversion": np.asarray(inversions, dtype=bool).tolist(),
+        },
+    }
diff --git a/research/drift_recovery/estimator/predictor.py b/research/drift_recovery/estimator/predictor.py
new file mode 100644
index 0000000000000000000000000000000000000000..12a1421abe607159b658e2b4554ca374ec69960e
--- /dev/null
+++ b/research/drift_recovery/estimator/predictor.py
@@ -0,0 +1,62 @@
+"""Public estimator entry point backed by a serialized regime calibrator."""
+
+from __future__ import annotations
+
+import json
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import LinearCalibration
+from research.drift_recovery.estimator.features import extract_regime_features
+
+
+DEFAULT_CALIBRATION_PATH = (
+    Path(__file__).resolve().parents[1] / "out" / "estimator" / "calibration.json"
+)
+
+
+def load_calibration(path: Path = DEFAULT_CALIBRATION_PATH) -> LinearCalibration:
+    selected = Path(path)
+    if not selected.exists():
+        raise FileNotFoundError(
+            f"calibration artifact not found at {selected}; run the D3 estimator build first"
+        )
+    payload = json.loads(selected.read_text(encoding="utf-8"))
+    return LinearCalibration.from_dict(payload)
+
+
+def estimate_recovery(
+    pack: EmbeddingPack,
+    calibration: Optional[Mapping[str, Any]] = None,
+) -> Dict[str, Any]:
+    """Estimate ridge oracle-gap recovery without calculating corrected NDCG.
+
+    Validation of packs used to train the default calibration must use the
+    separate leave-one-regime-out path; applying the all-regime artifact back to
+    a training pack is an in-sample prediction and is not validation evidence.
+    """
+
+    features = extract_regime_features(pack)
+    calibrator = (
+        LinearCalibration.from_dict(calibration)
+        if calibration is not None
+        else load_calibration()
+    )
+    estimate = calibrator.predict(features)
+    lower = calibrator.lower_band(features)
+    return {
+        "record_type": "recoverability_estimate",
+        "R_hat": estimate,
+        "lower_band": lower,
+        "features": features,
+        "calibration_training_count": calibrator.training_count,
+        "calibration_training_regimes": list(calibrator.training_regimes),
+        "scope_note": (
+            "Margin-bound violations are predictors, not proven inversions; "
+            "Gram/margin distortion alone does not determine NDCG@k."
+        ),
+    }
+
+
+__all__ = ["DEFAULT_CALIBRATION_PATH", "estimate_recovery", "load_calibration"]
diff --git a/research/drift_recovery/estimator/validation.py b/research/drift_recovery/estimator/validation.py
new file mode 100644
index 0000000000000000000000000000000000000000..b3be69e784304b25f30e529c3aa4e735c3f55b1d
--- /dev/null
+++ b/research/drift_recovery/estimator/validation.py
@@ -0,0 +1,169 @@
+"""Leave-one-regime-out validation for the recoverability estimator."""
+
+from __future__ import annotations
+
+from typing import Any, Dict, Mapping, Sequence
+
+import numpy as np
+
+from research.drift_recovery.estimator.calibration import fit_calibration
+
+
+LOW_MEDIUM_THRESHOLD = 1.0 / 3.0
+MEDIUM_HIGH_THRESHOLD = 2.0 / 3.0
+MIN_VALIDATED_REGIMES = 3
+
+
+def recovery_bin(value: float) -> str:
+    """Map recovery to fixed, predeclared low/medium/high thirds."""
+
+    if float(value) < LOW_MEDIUM_THRESHOLD:
+        return "low"
+    if float(value) < MEDIUM_HIGH_THRESHOLD:
+        return "medium"
+    return "high"
+
+
+def _average_ranks(values: np.ndarray) -> np.ndarray:
+    order = np.argsort(values, kind="mergesort")
+    ranks = np.empty(len(values), dtype=np.float64)
+    start = 0
+    while start < len(values):
+        end = start + 1
+        while end < len(values) and values[order[end]] == values[order[start]]:
+            end += 1
+        ranks[order[start:end]] = 0.5 * (start + end - 1) + 1.0
+        start = end
+    return ranks
+
+
+def spearman_correlation(left: Sequence[float], right: Sequence[float]):
+    """Compute Spearman rho with average tie ranks, or None if undefined."""
+
+    x = np.asarray(left, dtype=np.float64)
+    y = np.asarray(right, dtype=np.float64)
+    if x.shape != y.shape or x.ndim != 1 or len(x) < 2:
+        return None
+    x_rank = _average_ranks(x)
+    y_rank = _average_ranks(y)
+    if np.std(x_rank) == 0.0 or np.std(y_rank) == 0.0:
+        return None
+    return float(np.corrcoef(x_rank, y_rank)[0, 1])
+
+
+def leave_one_regime_out(regimes: Sequence[Mapping[str, Any]]) -> Dict[str, Any]:
+    """Predict each regime using calibrator targets from every other regime only."""
+
+    names = [str(item["name"]) for item in regimes]
+    if len(set(names)) != len(names):
+        raise ValueError("regime names must be unique")
+    rows = []
+    actual = []
+    predicted = []
+    for held_out, regime in enumerate(regimes):
+        train = [item for index, item in enumerate(regimes) if index != held_out]
+        if not train:
+            rows.append(
+                {
+                    "regime": names[held_out],
+                    "actual_recovery": float(regime["recovery"]),
+                    "R_hat": None,
+                    "lower_band": None,
+                    "actual_bin": recovery_bin(float(regime["recovery"])),
+                    "predicted_bin": None,
+                    "bin_correct": False,
+                    "training_regimes": [],
+                }
+            )
+            continue
+        calibrator = fit_calibration(
+            [item["features"] for item in train],
+            [float(item["recovery"]) for item in train],
+            regime_names=[str(item["name"]) for item in train],
+        )
+        estimate = calibrator.predict(regime["features"])
+        lower = calibrator.lower_band(regime["features"])
+        actual_value = float(regime["recovery"])
+        actual_bin = recovery_bin(actual_value)
+        predicted_bin = recovery_bin(estimate)
+        rows.append(
+            {
+                "regime": names[held_out],
+                "actual_recovery": actual_value,
+                "R_hat": estimate,
+                "lower_band": lower,
+                "actual_bin": actual_bin,
+                "predicted_bin": predicted_bin,
+                "bin_correct": predicted_bin == actual_bin,
+                "lower_band_covers": lower <= actual_value,
+                "training_regimes": list(calibrator.training_regimes),
+            }
+        )
+        actual.append(actual_value)
+        predicted.append(estimate)
+
+    held_out_count = len(predicted)
+    rho = spearman_correlation(predicted, actual)
+    accuracy = (
+        float(np.mean([row["bin_correct"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    coverage = (
+        float(np.mean([row["lower_band_covers"] for row in rows if row["R_hat"] is not None]))
+        if held_out_count
+        else None
+    )
+    enough_regimes = held_out_count >= MIN_VALIDATED_REGIMES
+    success = bool(
+        enough_regimes
+        and ((rho is not None and rho >= 0.7) or (accuracy is not None and accuracy >= 0.80))
+    )
+    if success:
+        verdict = "SHIP-POSITIVE"
+        note = (
+            "Leave-one-regime-out validation meets the locked success bar. "
+            "This remains a calibrated estimator, not a certificate that the bound controls NDCG."
+        )
+    elif not enough_regimes:
+        verdict = "SCOPING-NEGATIVE"
+        note = (
+            f"Only {held_out_count} held-out regimes were available; at least "
+            f"{MIN_VALIDATED_REGIMES} are required for the locked cross-regime verdict."
+        )
+    else:
+        verdict = "VALIDATION-NEGATIVE"
+        rho_text = "undefined" if rho is None else f"{rho:.3f}"
+        accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+        note = (
+            f"Across {held_out_count} held-out regimes, Spearman={rho_text} and "
+            f"3-way bin accuracy={accuracy_text}; neither locked success threshold was met."
+        )
+
+    return {
+        "record_type": "recoverability_leave_one_regime_out_validation",
+        "held_out_axis": "regime (dataset / encoder family / anchor setting)",
+        "regime_count": int(len(regimes)),
+        "held_out_count": int(held_out_count),
+        "spearman": rho,
+        "bin_accuracy": accuracy,
+        "lower_band_coverage": coverage,
+        "bin_thresholds": {
+            "low": f"R < {LOW_MEDIUM_THRESHOLD:.6f}",
+            "medium": (
+                f"{LOW_MEDIUM_THRESHOLD:.6f} <= R < {MEDIUM_HIGH_THRESHOLD:.6f}"
+            ),
+            "high": f"R >= {MEDIUM_HIGH_THRESHOLD:.6f}",
+        },
+        "success_bar": "Spearman >= 0.7 OR bin accuracy >= 0.80, across >=3 regimes",
+        "verdict": verdict,
+        "scoping_note": note,
+        "rows": rows,
+    }
+
+
+__all__ = [
+    "leave_one_regime_out",
+    "recovery_bin",
+    "spearman_correlation",
+]
diff --git a/research/drift_recovery/harness_bridge.py b/research/drift_recovery/harness_bridge.py
index 5d795fbc6c3068e3b8e8b68552b0c470a8bdc62e..9d95013d53660a59406600bd63c06366f626b012
--- a/research/drift_recovery/harness_bridge.py
+++ b/research/drift_recovery/harness_bridge.py
@@ -196,6 +196,125 @@
     }
 
 
+def load_retrieval_evalsplit(
+    dataset: str,
+    seed: int = 42,
+    anchor_fraction: float = 0.4,
+    max_queries: int = 100,
+    sample_docs: int = 1200,
+) -> Dict[str, Any]:
+    """Load a deterministic, generic frozen-pack case through merged helpers.
+
+    Unlike :func:`load_scifact_evalsplit`, this generic path does not construct
+    C3a or learning-curve supervision.  It is intentionally limited to the
+    document/query/qrels arrays needed by the D3 ridge estimator regimes.
+    """
+
+    corpus, queries, qrels = load_mteb_data(str(dataset))
+    if corpus is None or queries is None or qrels is None:
+        raise RuntimeError(f"offline {dataset} cache is unavailable")
+    sliced_corpus, sliced_queries, sliced_qrels = select_road_course_slice(
+        corpus,
+        queries,
+        qrels,
+        max_queries=int(max_queries),
+        sample_docs=int(sample_docs),
+        seed=int(seed),
+    )
+    anchor_ids, eval_ids = _split_anchor_eval(
+        sliced_queries,
+        float(anchor_fraction),
+        int(seed),
+        active=True,
+    )
+    anchor_set = set(map(canonicalize_id, anchor_ids))
+    eval_set = set(map(canonicalize_id, eval_ids))
+    if anchor_set.intersection(eval_set):
+        raise AssertionError("merged split leaked anchor IDs into eval IDs")
+
+    query_by_id = {
+        canonicalize_id(query_id): str(text) for query_id, text in sliced_queries.items()
+    }
+    qrels_by_id = {
+        canonicalize_id(query_id): {
+            canonicalize_id(doc_id): float(score) for doc_id, score in relevance.items()
+        }
+        for query_id, relevance in sliced_qrels.items()
+    }
+    eval_query_ids = [
+        query_id
+        for query_id in sorted(query_by_id)
+        if query_id in eval_set and qrels_by_id.get(query_id)
+    ]
+    if not eval_query_ids:
+        raise RuntimeError(f"{dataset} split produced no evaluable queries")
+    return {
+        "dataset": str(dataset),
+        "corpus_texts": [str(text) for text in sliced_corpus.values()],
+        "doc_ids": [canonicalize_id(value) for value in sliced_corpus.keys()],
+        "eval_query_texts": [query_by_id[query_id] for query_id in eval_query_ids],
+        "eval_query_ids": eval_query_ids,
+        "eval_qrels": {query_id: qrels_by_id[query_id] for query_id in eval_query_ids},
+        "anchor_ids": list(anchor_ids),
+        "anchor_fraction": float(anchor_fraction),
+        "max_queries": int(max_queries),
+        "sample_docs": int(sample_docs),
+    }
+
+
+def encode_retrieval_case(
+    case: Mapping[str, Any],
+    swap_model: str,
+    seed: int = 42,
+    device: str = "cuda",
+    old_model: str = "sentence-transformers/all-MiniLM-L6-v2",
+    batch_size: int = 64,
+) -> Dict[str, Any]:
+    """Encode one generic D3 regime with cached local models only."""
+
+    import gc
+
+    from sentence_transformers import SentenceTransformer
+
+    old = SentenceTransformer(str(old_model), device=device)
+    Do = np.asarray(
+        old.encode(
+            list(case["corpus_texts"]),
+            batch_size=int(batch_size),
+            show_progress_bar=True,
+        ),
+        dtype=np.float64,
+    )
+    drift = QueryEncoderDrift(
+        store_dim=int(Do.shape[1]),
+        swap_model_name=str(swap_model),
+        seed=int(seed),
+    )
+    # The first call fixes the seeded frozen projection.  The document oracle
+    # positions reuse that exact projection instance.
+    Qd = np.asarray(drift.embed_queries(list(case["eval_query_texts"])), dtype=np.float64)
+    Dor = np.asarray(drift.embed_queries(list(case["corpus_texts"])), dtype=np.float64)
+    manifest = drift.manifest()
+    del old
+    del drift
+    gc.collect()
+    try:
+        import torch
+
+        if torch.cuda.is_available():
+            torch.cuda.empty_cache()
+    except ImportError:
+        pass
+    return {
+        "Do": Do,
+        "Dor": Dor,
+        "Qd": Qd,
+        "drift_manifest": manifest,
+        "old_model": str(old_model),
+        "swap_model": str(swap_model),
+    }
+
+
 def encode_scifact_case(case: Mapping[str, Any], seed: int = 42, device: str = "cuda") -> Dict[str, Any]:
     """Run both cached encoders once and return arrays ready for freezing."""
 
diff --git a/research/drift_recovery/run_estimator.py b/research/drift_recovery/run_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..c45fd2423ae172da493d5a73735d140463bcfb87
--- /dev/null
+++ b/research/drift_recovery/run_estimator.py
@@ -0,0 +1,570 @@
+"""Build D3 frozen regimes, calibrated estimator artifacts, and validation."""
+
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import os
+import sys
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Dict, Mapping, Optional, Sequence
+
+import numpy as np
+
+os.environ.setdefault("HF_HUB_OFFLINE", "1")
+os.environ.setdefault("HF_DATASETS_OFFLINE", "1")
+os.environ.setdefault("TOKENIZERS_PARALLELISM", "false")
+
+if __package__ in {None, ""}:
+    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
+
+from research.drift_recovery.artifacts import EmbeddingPack  # noqa: E402
+from research.drift_recovery.estimator.calibration import fit_calibration  # noqa: E402
+from research.drift_recovery.estimator.features import (  # noqa: E402
+    extract_regime_features,
+    fit_ridge_for_pack,
+    fit_ridge_map,
+)
+from research.drift_recovery.estimator.validation import leave_one_regime_out  # noqa: E402
+from research.drift_recovery.harness_bridge import (  # noqa: E402
+    aggregate_ndcg,
+    assert_harness_parity,
+    encode_retrieval_case,
+    load_retrieval_evalsplit,
+    per_query_ndcg,
+)
+from research.drift_recovery.stats.paired_bootstrap import (  # noqa: E402
+    BootstrapDraws,
+    paired_query_bootstrap,
+    strip_draws,
+)
+
+
+OLD_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
+MPNET_MODEL = "all-mpnet-base-v2"
+BGE_MODEL = "BAAI/bge-large-en-v1.5"
+DEFAULT_OUTPUT_DIR = Path("research/drift_recovery/out/estimator")
+DEFAULT_D1_PACK = Path("research/drift_recovery/out/d1/scifact_evalsplit_pack")
+
+
+@dataclass(frozen=True)
+class RegimeSpec:
+    name: str
+    dataset: str
+    swap_model: str
+    anchor_fraction: float = 0.4
+    seed: int = 42
+    existing_pack: Optional[Path] = None
+
+
+class RegimeUnavailableError(RuntimeError):
+    """An explicitly skippable offline dataset/model-cache miss."""
+
+
+REGIMES = (
+    RegimeSpec(
+        name="scifact_minilm_to_mpnet",
+        dataset="SciFact",
+        swap_model=MPNET_MODEL,
+        existing_pack=DEFAULT_D1_PACK,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_mpnet",
+        dataset="NFCorpus",
+        swap_model=MPNET_MODEL,
+    ),
+    RegimeSpec(
+        name="scifact_minilm_to_bge_large",
+        dataset="SciFact",
+        swap_model=BGE_MODEL,
+    ),
+    RegimeSpec(
+        name="nfcorpus_minilm_to_bge_large",
+        dataset="NFCorpus",
+        swap_model=BGE_MODEL,
+    ),
+)
+
+
+def _write_json(path: Path, payload: Mapping[str, Any]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    path.write_text(
+        json.dumps(payload, indent=2, sort_keys=True, allow_nan=False),
+        encoding="utf-8",
+    )
+
+
+def _safe_fit_index(
+    doc_ids: np.ndarray,
+    eval_qrels: Mapping[str, Mapping[str, float]],
+    seed: int,
+) -> np.ndarray:
+    positives = {
+        str(doc_id)
+        for relevance in eval_qrels.values()
+        for doc_id, score in relevance.items()
+        if float(score) > 0.0
+    }
+    order = np.random.default_rng(int(seed)).permutation(len(doc_ids))
+    safe_order = [index for index in order if str(doc_ids[index]) not in positives]
+    # SciFact supports the D1 half-corpus count.  NFCorpus has much denser
+    # relevance judgments, so its honest safe pool can be smaller.  Ridge is
+    # regularized and remains well-defined below the embedding dimension; the
+    # exact resulting n_anchor is a recorded regime axis rather than padded
+    # with eval-positive pairs.
+    fit_count = min(len(doc_ids) // 2, len(safe_order))
+    if fit_count < 2:
+        raise RuntimeError(
+            f"only {fit_count} non-eval-positive documents remain for leakage-safe ridge fit"
+        )
+    selected = safe_order[:fit_count]
+    return np.asarray(selected, dtype=np.int64)
+
+
+def _recovery(
+    floor_scores: Sequence[float],
+    oracle_scores: Sequence[float],
+    ridge_scores: Sequence[float],
+) -> Dict[str, float]:
+    floor = aggregate_ndcg(floor_scores)
+    oracle = aggregate_ndcg(oracle_scores)
+    ridge = aggregate_ndcg(ridge_scores)
+    gap = oracle - floor
+    if gap <= 1e-12:
+        raise RuntimeError(f"oracle gap is non-positive ({gap:.6g})")
+    return {
+        "floor_ndcg": floor,
+        "oracle_ndcg": oracle,
+        "ridge_ndcg": ridge,
+        "oracle_gap": gap,
+        "ridge_recovery": (ridge - floor) / gap,
+    }
+
+
+def build_regime_pack(spec: RegimeSpec, prefix: Path, device: str) -> EmbeddingPack:
+    """Build one additive D3 pack with a leakage-safe ridge headline."""
+
+    try:
+        case = load_retrieval_evalsplit(
+            spec.dataset,
+            seed=spec.seed,
+            anchor_fraction=spec.anchor_fraction,
+            max_queries=100,
+            sample_docs=1200,
+        )
+    except RuntimeError as exc:
+        if "offline" in str(exc).lower() and "cache is unavailable" in str(exc).lower():
+            raise RegimeUnavailableError(str(exc)) from exc
+        raise
+    try:
+        encoded = encode_retrieval_case(
+            case,
+            swap_model=spec.swap_model,
+            seed=spec.seed,
+            device=device,
+            old_model=OLD_MODEL,
+            batch_size=64,
+        )
+    except (FileNotFoundError, OSError) as exc:
+        raise RegimeUnavailableError(
+            f"offline model cache unavailable for {spec.swap_model}: {exc}"
+        ) from exc
+    doc_ids = np.asarray(case["doc_ids"], dtype=str)
+    query_ids = np.asarray(case["eval_query_ids"], dtype=str)
+    fit_idx = _safe_fit_index(doc_ids, case["eval_qrels"], spec.seed)
+    ridge_map = fit_ridge_map(encoded["Do"], encoded["Dor"], fit_idx)
+    ridge_documents = np.asarray(encoded["Do"], dtype=np.float64) @ ridge_map
+    score_args = {
+        "query_vectors": encoded["Qd"],
+        "query_ids": query_ids,
+        "doc_ids": doc_ids,
+        "qrels": case["eval_qrels"],
+        "k": 10,
+    }
+    floor_scores = per_query_ndcg(doc_vectors=encoded["Do"], **score_args)
+    oracle_scores = per_query_ndcg(doc_vectors=encoded["Dor"], **score_args)
+    ridge_scores = per_query_ndcg(doc_vectors=ridge_documents, **score_args)
+    recovery = _recovery(floor_scores, oracle_scores, ridge_scores)
+    aggregate = {
+        "floor": recovery["floor_ndcg"],
+        "oracle": recovery["oracle_ndcg"],
+        "ridge": recovery["ridge_ndcg"],
+    }
+    pack = EmbeddingPack(
+        Do=encoded["Do"],
+        Dor=encoded["Dor"],
+        Qd=encoded["Qd"],
+        doc_ids=doc_ids,
+        query_ids=query_ids,
+        qrels=case["eval_qrels"],
+        fit_idx=fit_idx,
+        per_query_scores={
+            "floor": floor_scores,
+            "oracle": oracle_scores,
+            "ridge": ridge_scores,
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": fit_idx.copy(),
+            "method_documents__ridge": ridge_documents,
+        },
+        metadata={
+            "dataset": spec.dataset,
+            "seed": spec.seed,
+            "max_queries": 100,
+            "sample_docs": 1200,
+            "anchor_fraction": spec.anchor_fraction,
+            "k": 10,
+            "ndcg": "binary merged-harness ndcg_at_k",
+            "models": {"old": encoded["old_model"], "swap": encoded["swap_model"]},
+            "drift_manifest": encoded["drift_manifest"],
+            "harness_aggregate_ndcg": aggregate,
+            "recovery": {"ridge": recovery["ridge_recovery"]},
+            "fit_contract": {
+                "index": "fit_idx and extra_arrays.leakage_safe_fit_idx",
+                "fit_count": int(len(fit_idx)),
+                "eval_positive_docs_in_fit": 0,
+                "regularization": 1.0,
+                "eval_qrels_used_by_optimizer": False,
+            },
+        },
+    )
+    saved = pack.save(prefix)
+    parity = assert_harness_parity(pack, atol=1e-12)
+    if max(parity.values()) > 1e-12:
+        raise AssertionError(f"harness parity exceeded tolerance: {parity}")
+    print(f"[D3] froze {spec.name}: {saved['sha256']}", flush=True)
+    return pack
+
+
+def frozen_ridge_ground_truth(pack: EmbeddingPack) -> Dict[str, Any]:
+    """Use the pack's frozen D1 ridge scores as the validation target.
+
+    The estimator feature fit remains leakage-safe.  For the legacy D1 pack,
+    the separate safe refit below is diagnostic only because the locked target
+    contract calls for the pack's already-measured ridge recovery.
+    """
+
+    frozen_ridge_scores = np.asarray(pack.per_query_scores["ridge"], dtype=np.float64)
+    metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        frozen_ridge_scores,
+    )
+    fit = fit_ridge_for_pack(pack)
+    safe_ridge_scores = per_query_ndcg(
+        pack.Qd,
+        pack.query_ids,
+        fit.corrected_documents,
+        pack.doc_ids,
+        pack.qrels,
+        k=int(pack.metadata.get("k", 10)),
+    )
+    safe_metrics = _recovery(
+        pack.per_query_scores["floor"],
+        pack.per_query_scores["oracle"],
+        safe_ridge_scores,
+    )
+    try:
+        draws = BootstrapDraws.create(len(pack.query_ids), draws=2000, seed=20260709)
+        bootstrap = strip_draws(
+            paired_query_bootstrap(
+                {
+                    "floor": pack.per_query_scores["floor"],
+                    "oracle": pack.per_query_scores["oracle"],
+                    "ridge": frozen_ridge_scores,
+                },
+                methods=("ridge",),
+                bootstrap_draws=draws,
+            )
+        )
+        bootstrap_error = None
+    except (RuntimeError, ValueError) as exc:
+        bootstrap = None
+        bootstrap_error = f"{type(exc).__name__}: {exc}"
+    return {
+        **metrics,
+        "ground_truth_source": "frozen pack per_query_scores.ridge",
+        "ridge_per_query_scores": frozen_ridge_scores.tolist(),
+        "paired_query_bootstrap": bootstrap,
+        "bootstrap_error": bootstrap_error,
+        "leakage_safe_feature_refit": {
+            **safe_metrics,
+            "fit_count": int(len(fit.fit_idx)),
+            "eval_positive_docs_in_fit": 0,
+        },
+        "frozen_fit_audit": (
+            pack.metadata.get("fit_contract")
+            or pack.metadata.get("waypoint_audit")
+            or {"status": "not recorded"}
+        ),
+    }
+
+
+def _pack_digest(prefix: Path) -> str:
+    return hashlib.sha256(prefix.with_suffix(".npz").read_bytes()).hexdigest()
+
+
+def _render_validation(validation: Mapping[str, Any], manifest: Mapping[str, Any]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    if available_items and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    ):
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        failure_note = (
+            "Smallest concrete failure: the bound predicted a violation for **100% of "
+            "queries in every available regime**, so its inversion-rate feature did not "
+            f"separate observed recoveries ranging from {minimum_recovery:.3f} to "
+            f"{maximum_recovery:.3f}."
+        )
+    else:
+        failure_note = (
+            "Smallest concrete failure: the held-out feature map misordered the regimes."
+        )
+    lines = [
+        "# D3 recoverability estimator validation",
+        "",
+        "Held-out axis: **regime** (dataset / encoder family / anchor setting). "
+        "No regime's own measured recovery trains its held-out prediction.",
+        "",
+        "| Held-out regime | Actual R | R_hat | Lower band | Actual bin | Predicted bin |",
+        "|---|---:|---:|---:|---|---|",
+    ]
+    for row in validation["rows"]:
+        estimate = "n/a" if row["R_hat"] is None else f"{row['R_hat']:.4f}"
+        lower = "n/a" if row["lower_band"] is None else f"{row['lower_band']:.4f}"
+        lines.append(
+            f"| {row['regime']} | {row['actual_recovery']:.4f} | {estimate} | {lower} | "
+            f"{row['actual_bin']} | {row['predicted_bin'] or 'n/a'} |"
+        )
+    lines.extend(
+        [
+            "",
+            f"- Held-out regimes: **{validation['held_out_count']}**",
+            f"- Spearman(R_hat, R): **{rho_text}**",
+            f"- Three-way bin accuracy: **{accuracy_text}** "
+            "(fixed bins: low <1/3, medium <2/3, high >=2/3)",
+            f"- Verdict: **{validation['verdict']}**",
+            "",
+            validation["scoping_note"],
+            "",
+            failure_note,
+            "",
+            "Negative/scoping boundary: the per-query inequality only flags absence of an "
+            "order-preservation guarantee for one oracle-best relevant/non-relevant pair. It "
+            "does not prove an inversion, does not model the full top-k ordering, and Gram/margin "
+            "distortion alone does not determine NDCG@10.",
+            "",
+            "Legacy D1 label note: `scifact_minilm_to_mpnet` uses the locked frozen-pack "
+            "ridge target R=0.8434. Its estimator feature map is fitted only on "
+            "`extra_arrays[\"leakage_safe_fit_idx\"]` (safe-refit diagnostic R=0.7891), "
+            "because the legacy literal D1 target fit contains 28 eval-positive documents.",
+            "",
+            "## Regime availability",
+            "",
+        ]
+    )
+    for item in manifest["regimes"]:
+        detail = item.get("reason") or item.get("pack_prefix") or ""
+        lines.append(f"- `{item['name']}`: **{item['status']}** — {detail}")
+    return "\n".join(lines) + "\n"
+
+
+def _render_d3_report(validation: Mapping[str, Any], available_items: Sequence[Mapping[str, Any]]) -> str:
+    rho = validation["spearman"]
+    accuracy = validation["bin_accuracy"]
+    rho_text = "undefined" if rho is None else f"{rho:.3f}"
+    accuracy_text = "undefined" if accuracy is None else f"{100.0 * accuracy:.1f}%"
+    available_names = [str(item["name"]) for item in available_items]
+    inversion_saturated = bool(available_items) and all(
+        item.get("predicted_inversion_rate") == 1.0 for item in available_items
+    )
+    if inversion_saturated:
+        minimum_recovery = min(item["frozen_ridge_recovery"] for item in available_items)
+        maximum_recovery = max(item["frozen_ridge_recovery"] for item in available_items)
+        limitation = (
+            "Most important limitation: the sufficient bound saturated at a 100% predicted "
+            f"violation rate in all {len(available_items)} regimes despite measured recovery "
+            f"spanning {minimum_recovery:.2f}-{maximum_recovery:.2f}, so the core "
+            "inversion-rate signal did not discriminate regimes."
+        )
+    else:
+        limitation = (
+            "Most important limitation: this bound tracks one relevant/competitor order and "
+            "cannot certify the full top-k NDCG ordering."
+        )
+    return "\n".join(
+        [
+            "# D3 report — recoverability estimator",
+            "",
+            "Built a pure-NumPy feature path over frozen `EmbeddingPack` data: oracle-space "
+            "margin quantiles, leakage-safe ridge correction-error norms, and the per-query "
+            "margin-bound violation rate. A strongly regularized linear map produces `R_hat`; "
+            "training-only cross-fitted overprediction residuals produce its lower band.",
+            "",
+            f"Regimes used ({len(available_names)}): " + ", ".join(f"`{name}`" for name in available_names) + ".",
+            "",
+            f"Leave-one-regime-out result: Spearman **{rho_text}**, three-way bin accuracy "
+            f"**{accuracy_text}**. Verdict: **{validation['verdict']}**.",
+            "",
+            validation["scoping_note"],
+            "",
+            limitation,
+            "",
+        ]
+    )
+
+
+def run_d3(
+    output_dir: Path = DEFAULT_OUTPUT_DIR,
+    device: str = "cuda",
+    rebuild_packs: bool = False,
+    only_existing: bool = False,
+) -> Dict[str, Any]:
+    output_dir = Path(output_dir)
+    packs_dir = output_dir / "packs"
+    packs_dir.mkdir(parents=True, exist_ok=True)
+    manifest: Dict[str, Any] = {
+        "record_type": "d3_estimator_regime_manifest",
+        "environment": {
+            "HF_HUB_OFFLINE": os.environ.get("HF_HUB_OFFLINE", ""),
+            "HF_DATASETS_OFFLINE": os.environ.get("HF_DATASETS_OFFLINE", ""),
+            "device": device,
+        },
+        "parity_tolerance": 1e-12,
+        "regimes": [],
+    }
+    validation_regimes = []
+    for spec in REGIMES:
+        if only_existing and spec.existing_pack is None:
+            manifest["regimes"].append(
+                {"name": spec.name, "status": "skipped", "reason": "--only-existing requested"}
+            )
+            continue
+        prefix = Path(spec.existing_pack) if spec.existing_pack else packs_dir / f"{spec.name}_pack"
+        try:
+            if prefix.with_suffix(".npz").exists() and not rebuild_packs:
+                print(f"[D3] reloading {spec.name}", flush=True)
+                pack = EmbeddingPack.load(prefix)
+            elif spec.existing_pack is not None:
+                raise FileNotFoundError(f"required frozen D1 pack not found: {prefix}")
+            else:
+                print(f"[D3] building {spec.name} sequentially", flush=True)
+                pack = build_regime_pack(spec, prefix, device=device)
+        except RegimeUnavailableError as exc:
+            reason = f"{type(exc).__name__}: {exc}"
+            print(f"[D3] skipped {spec.name}: {reason}", flush=True)
+            manifest["regimes"].append(
+                {
+                    "name": spec.name,
+                    "dataset": spec.dataset,
+                    "swap_model": spec.swap_model,
+                    "anchor_fraction": spec.anchor_fraction,
+                    "status": "skipped",
+                    "reason": reason,
+                }
+            )
+            continue
+
+        # From here on every failure is a build failure, never an availability
+        # skip.  In particular, parity/checksum/leakage/feature assertions must
+        # terminate the run rather than manufacture a scoping-negative result.
+        parity = assert_harness_parity(pack, atol=1e-12)
+        ground_truth = frozen_ridge_ground_truth(pack)
+        features = extract_regime_features(pack)
+        feature_payload = {
+            **features,
+            "regime_name": spec.name,
+            "ground_truth": ground_truth,
+            "pack_prefix": str(prefix),
+            "harness_parity": parity,
+        }
+        _write_json(output_dir / f"{spec.name}_features.json", feature_payload)
+        validation_regimes.append(
+            {
+                "name": spec.name,
+                "features": features,
+                "recovery": ground_truth["ridge_recovery"],
+            }
+        )
+        manifest["regimes"].append(
+            {
+                "name": spec.name,
+                "dataset": spec.dataset,
+                "swap_model": spec.swap_model,
+                "anchor_fraction": spec.anchor_fraction,
+                "status": "available",
+                "pack_prefix": str(prefix),
+                "pack_sha256": _pack_digest(prefix),
+                "harness_parity": parity,
+                "frozen_ridge_recovery": ground_truth["ridge_recovery"],
+                "leakage_safe_feature_refit_recovery": ground_truth[
+                    "leakage_safe_feature_refit"
+                ]["ridge_recovery"],
+                "document_count": int(features["summary"]["document_count"]),
+                "query_count": int(features["summary"]["query_count"]),
+                "safe_fit_count": int(features["summary"]["fit_count"]),
+                "predicted_inversion_rate": float(
+                    features["summary"]["predicted_inversion_rate"]
+                ),
+            }
+        )
+
+    validation = leave_one_regime_out(validation_regimes)
+    _write_json(output_dir / "validation.json", validation)
+    if validation_regimes:
+        calibrator = fit_calibration(
+            [item["features"] for item in validation_regimes],
+            [item["recovery"] for item in validation_regimes],
+            regime_names=[item["name"] for item in validation_regimes],
+        )
+        _write_json(output_dir / "calibration.json", calibrator.to_dict())
+    _write_json(output_dir / "regime_manifest.json", manifest)
+    (output_dir / "estimator_validation.md").write_text(
+        _render_validation(validation, manifest), encoding="utf-8"
+    )
+    available_names = [item["name"] for item in validation_regimes]
+    available_manifest_items = [
+        item for item in manifest["regimes"] if item["status"] == "available"
+    ]
+    (output_dir / "D3_REPORT.md").write_text(
+        _render_d3_report(validation, available_manifest_items), encoding="utf-8"
+    )
+    return {
+        "output_dir": str(output_dir),
+        "available_regimes": available_names,
+        "validation": validation,
+    }
+
+
+def build_parser() -> argparse.ArgumentParser:
+    parser = argparse.ArgumentParser(description="Build the D3 recoverability estimator")
+    parser.add_argument("--output-dir", type=Path, default=DEFAULT_OUTPUT_DIR)
+    parser.add_argument("--device", choices=("cpu", "cuda"), default="cuda")
+    parser.add_argument("--rebuild-packs", action="store_true")
+    parser.add_argument("--only-existing", action="store_true")
+    return parser
+
+
+def main(argv=None) -> int:
+    args = build_parser().parse_args(argv)
+    result = run_d3(
+        output_dir=args.output_dir,
+        device=args.device,
+        rebuild_packs=args.rebuild_packs,
+        only_existing=args.only_existing,
+    )
+    print(json.dumps(result, indent=2, sort_keys=True, allow_nan=False))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/research/drift_recovery/tests/test_estimator.py b/research/drift_recovery/tests/test_estimator.py
new file mode 100644
index 0000000000000000000000000000000000000000..7deed0253a9e197d71ad3280a8638270090b839b
--- /dev/null
+++ b/research/drift_recovery/tests/test_estimator.py
@@ -0,0 +1,217 @@
+"""Focused D3 estimator tests; runnable with the repository's unittest gate."""
+
+from __future__ import annotations
+
+import json
+import tempfile
+import unittest
+from pathlib import Path
+from unittest.mock import patch
+
+import numpy as np
+
+from research.drift_recovery.artifacts import EmbeddingPack
+from research.drift_recovery.estimator.calibration import FEATURE_NAMES, fit_calibration
+from research.drift_recovery.estimator.features import extract_regime_features
+from research.drift_recovery.estimator.margin_bound import (
+    order_is_preserved,
+    order_preservation_slack,
+    predict_inversion,
+)
+from research.drift_recovery.estimator.predictor import estimate_recovery
+from research.drift_recovery.estimator.validation import leave_one_regime_out
+from research.drift_recovery.run_estimator import (
+    RegimeSpec,
+    RegimeUnavailableError,
+    frozen_ridge_ground_truth,
+    run_d3,
+)
+
+
+def _tiny_pack() -> EmbeddingPack:
+    # q=[1,0], oracle relevant=[1,0], and oracle competitor=[0.6,0.8],
+    # hence the hand-checkable clean cosine margin is 1.0 - 0.6 = 0.4.
+    oracle = np.asarray(
+        [
+            [1.0, 0.0],
+            [0.6, 0.8],
+            [-1.0, 0.0],
+            [0.0, -1.0],
+        ],
+        dtype=np.float64,
+    )
+    old = np.asarray(
+        [
+            [0.8, 0.2],
+            [0.4, 0.9],
+            [-0.9, 0.1],
+            [0.1, -0.8],
+        ],
+        dtype=np.float64,
+    )
+    return EmbeddingPack(
+        Do=old,
+        Dor=oracle,
+        Qd=np.asarray([[1.0, 0.0]], dtype=np.float64),
+        doc_ids=np.asarray(["relevant", "competitor", "fit-a", "fit-b"]),
+        query_ids=np.asarray(["q1"]),
+        qrels={"q1": {"relevant": 1.0}},
+        # The legacy/literal index is deliberately contaminated.  D3 must use
+        # the explicit safe index below and never pass qrels to ridge fitting.
+        fit_idx=np.asarray([0, 1, 2], dtype=np.int64),
+        per_query_scores={
+            "floor": np.asarray([0.0]),
+            "oracle": np.asarray([1.0]),
+            "ridge": np.asarray([0.5]),
+        },
+        extra_arrays={
+            "leakage_safe_fit_idx": np.asarray([1, 2, 3], dtype=np.int64),
+            "method_documents__ridge": old.copy(),
+        },
+        metadata={
+            "dataset": "synthetic",
+            "seed": 7,
+            "anchor_fraction": 0.4,
+            "k": 10,
+            "models": {"old": "synthetic-old", "swap": "synthetic-swap"},
+            "harness_aggregate_ndcg": {"floor": 0.0, "oracle": 1.0, "ridge": 0.5},
+        },
+    )
+
+
+def _calibration_features(value: float):
+    return {"summary": {name: float(value) for name in FEATURE_NAMES}}
+
+
+class TestMarginBound(unittest.TestCase):
+    def test_hand_checkable_margin_and_tie_semantics(self) -> None:
+        slack = order_preservation_slack(0.6, 1.0, 0.1, 0.2)
+        self.assertAlmostEqual(float(slack), 0.3)
+        self.assertTrue(bool(order_is_preserved(0.6, 1.0, 0.1, 0.2)))
+        self.assertFalse(bool(predict_inversion(0.6, 1.0, 0.1, 0.2)))
+
+        self.assertAlmostEqual(float(order_preservation_slack(0.3, 1.0, 0.1, 0.2)), 0.0)
+        self.assertTrue(bool(predict_inversion(0.3, 1.0, 0.1, 0.2)))
+
+    def test_negative_error_norm_is_rejected(self) -> None:
+        with self.assertRaisesRegex(ValueError, "nonnegative"):
+            predict_inversion(0.5, 1.0, -0.1, 0.2)
+
+
+class TestEstimatorFeatures(unittest.TestCase):
+    def test_synthetic_pack_margin_is_hand_checkable_and_deterministic(self) -> None:
+        first = extract_regime_features(_tiny_pack())
+        second = extract_regime_features(_tiny_pack())
+
+        self.assertEqual(
+            json.dumps(first, sort_keys=True, allow_nan=False),
+            json.dumps(second, sort_keys=True, allow_nan=False),
+        )
+        self.assertAlmostEqual(first["per_query"]["oracle_margin"][0], 0.4)
+        self.assertEqual(first["per_query"]["relevant_doc_ids"], ["relevant"])
+        self.assertEqual(first["per_query"]["competitor_doc_ids"], ["competitor"])
+
+    def test_ridge_fit_receives_only_safe_arrays_and_index_not_qrels(self) -> None:
+        captured = {}
+
+        def fake_fit(source, target, fit_idx, regularization=1.0):
+            captured["source"] = np.asarray(source).copy()
+            captured["target"] = np.asarray(target).copy()
+            captured["fit_idx"] = np.asarray(fit_idx).copy()
+            captured["regularization"] = regularization
+            return np.eye(np.asarray(source).shape[1], dtype=np.float64)
+
+        with patch(
+            "research.drift_recovery.estimator.features.fit_ridge_map",
+            side_effect=fake_fit,
+        ) as mocked:
+            features = extract_regime_features(_tiny_pack())
+
+        np.testing.assert_array_equal(captured["fit_idx"], np.asarray([1, 2, 3]))
+        np.testing.assert_array_equal(captured["source"], _tiny_pack().Do)
+        np.testing.assert_array_equal(captured["target"], _tiny_pack().Dor)
+        self.assertNotIn("qrels", mocked.call_args.kwargs)
+        self.assertEqual(features["summary"]["eval_positive_docs_in_fit"], 0)
+        self.assertEqual(
+            features["summary"]["fit_index_source"],
+            "extra_arrays.leakage_safe_fit_idx",
+        )
+
+
+class TestEstimatorCalibration(unittest.TestCase):
+    def test_ground_truth_uses_frozen_pack_ridge_scores(self) -> None:
+        ground_truth = frozen_ridge_ground_truth(_tiny_pack())
+
+        self.assertEqual(ground_truth["ground_truth_source"], "frozen pack per_query_scores.ridge")
+        self.assertAlmostEqual(ground_truth["ridge_recovery"], 0.5)
+        self.assertIn("leakage_safe_feature_refit", ground_truth)
+
+    def test_leave_one_regime_out_never_trains_on_held_out_target(self) -> None:
+        regimes = [
+            {"name": f"r{index}", "features": _calibration_features(value), "recovery": recovery}
+            for index, (value, recovery) in enumerate(
+                ((0.1, 0.15), (0.3, 0.35), (0.7, 0.72), (0.9, 0.88)), start=1
+            )
+        ]
+        result = leave_one_regime_out(regimes)
+
+        self.assertEqual(result["held_out_count"], 4)
+        self.assertEqual(len(result["rows"]), 4)
+        for row in result["rows"]:
+            self.assertNotIn(row["regime"], row["training_regimes"])
+            self.assertEqual(len(row["training_regimes"]), 3)
+            self.assertIsInstance(row["R_hat"], float)
+
+    def test_predictor_returns_calibrated_shape_without_ndcg_eval(self) -> None:
+        records = [_calibration_features(0.2), _calibration_features(0.8)]
+        calibration = fit_calibration(records, [0.25, 0.75], regime_names=["a", "b"])
+        result = estimate_recovery(_tiny_pack(), calibration=calibration.to_dict())
+
+        self.assertIn("R_hat", result)
+        self.assertIn("lower_band", result)
+        self.assertIn("features", result)
+        self.assertLessEqual(result["lower_band"], result["R_hat"])
+
+    def test_runner_does_not_mask_parity_failure_as_unavailable(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            prefix = root / "existing_pack"
+            prefix.with_suffix(".npz").write_bytes(b"present")
+            spec = RegimeSpec(
+                name="parity_failure",
+                dataset="synthetic",
+                swap_model="synthetic",
+                existing_pack=prefix,
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.EmbeddingPack.load",
+                return_value=_tiny_pack(),
+            ), patch(
+                "research.drift_recovery.run_estimator.assert_harness_parity",
+                side_effect=AssertionError("parity must fail loudly"),
+            ):
+                with self.assertRaisesRegex(AssertionError, "parity must fail loudly"):
+                    run_d3(output_dir=root / "out", device="cpu")
+
+    def test_runner_records_only_explicit_availability_failure_as_skip(self) -> None:
+        with tempfile.TemporaryDirectory() as temporary:
+            root = Path(temporary)
+            spec = RegimeSpec(
+                name="missing_cache",
+                dataset="synthetic",
+                swap_model="synthetic",
+            )
+            with patch("research.drift_recovery.run_estimator.REGIMES", (spec,)), patch(
+                "research.drift_recovery.run_estimator.build_regime_pack",
+                side_effect=RegimeUnavailableError("offline model cache unavailable"),
+            ):
+                result = run_d3(output_dir=root / "out", device="cpu")
+
+            self.assertEqual(result["available_regimes"], [])
+            manifest = json.loads((root / "out" / "regime_manifest.json").read_text())
+            self.assertEqual(manifest["regimes"][0]["status"], "skipped")
+            self.assertIn("RegimeUnavailableError", manifest["regimes"][0]["reason"])
+
+
+if __name__ == "__main__":
+    unittest.main()

hook: Stop
hook: Stop Completed
tokens used
300,923
Implemented D3 with an honest **VALIDATION-NEGATIVE** verdict.

- Four regimes validated: SciFact/NFCorpus × mpnet/bge-large.
- LOO Spearman: **−0.400**
- Three-way bin accuracy: **75%** — below the locked 80% threshold.
- Lower-band coverage: **75%**
- Harness parity: **0.0 error for every pack**, within `1e-12`.
- Core failure: predicted inversion rate saturated at **100% in every regime**, despite recovery ranging from `0.647` to `0.843`.

Key outputs:

- [estimator_validation.md](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/out/estimator/estimator_validation.md:1)
- [D3_REPORT.md](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/out/estimator/D3_REPORT.md:1)
- [regime_manifest.json](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/out/estimator/regime_manifest.json)
- [predictor.py](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/estimator/predictor.py:29)
- [test_estimator.py](D:/GITHUB/CHELATEDAI/.claude/worktrees/agent-build/research/drift_recovery/tests/test_estimator.py:86)

Validation:

- **PASS:** repository-wide suite — `2,757 tests`, `11 skipped`, `0 failures`
- **PASS:** ruff and compilation
- **PASS:** deterministic artifact/checksum/leakage/parity audit
- **PASS:** fresh adversarial review; no blockers remained

The frozen D1 pack was not regenerated. Its locked `0.8434` target is preserved, while estimator feature fitting uses the leakage-safe index; the legacy target’s 28 eval-positive-document contamination is explicitly disclosed. The pre-existing `research/` tree remains untracked in this detached worktree.


