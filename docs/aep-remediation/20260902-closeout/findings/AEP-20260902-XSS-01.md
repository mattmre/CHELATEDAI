# AEP-20260902-XSS-01 — fix record (P1, closeout)

Full descriptor: `AEP-20260902-XSS-01|L9|High|dashboard/index.html:481`
Layer: L9 (presentation) | Severity: High | Status: fixed (pending review) | Tier: 2
File: `dashboard/index.html` (theme: escape-helper)
Branch: `aep/High/AEP-20260902-XSS-01/escape-helper` (sanitized from
`aep/<sev>/AEP-20260902-XSS-01|L9|High|dashboard/index.html:481/<theme>`; `|`/`/`/`:` are not valid in branch/file paths)
Prior finding: `docs/aep-remediation/20260902-r4/findings/AEP-20260902-XSS-01.md`
Disposition carried: CONFIRMED (per `docs/aep-remediation/20260902-closeout/00-disposition-log.md`; evidence + disprove-attempt + challenger Alex recorded there, not repeated here)

## Root cause
Server-controlled JSON strings interpolated unescaped into `innerHTML` sinks:
L481-482 `${data.reason}` (/api/test_results), L502-506 `${t.outcome}`/`${t.nodeid}`/duration loop, L919-930 `${r.config.*}`/`${r.metrics.*}` sweep table, plus error-path concat `String(e.message || e)` (L822). No escape helper existed (`rg escapeHTML` = 0 pre-fix); `rg -c innerHTML` = 26.

## Fix (minimal, this finding only)
- Added `escapeHTML()` helper (L474-481: `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;`, `"`→`&quot;`, `'`→`&#39;`) next to `asObject`/`asArray`.
- Escaped all server-string interpolations inside `innerHTML` templates: L481 reason, L502-506 outcome/nodeid/duration, L919-930 six config fields + three metric fields, L822 error concat (now `escapeHTML(e.message || e)`).
- Deliberately untouched: static-literal `innerHTML` rows (no interpolation, safe), `textContent`/`replaceChildren` paths (already safe), all other findings (STATIC-01, AUTH-01, CORS-01, CDN-01, LIMIT-01, etc.), `dashboard_server.py` (no BE change).
- Diff: `dashboard/index.html` only, +22/−13.

## Acceptance criteria
- AC1 (probe payload renders inert): PASS — `node /tmp/aep-xss01-verify.js` escapes `<script>`, `<img onerror>`, `<svg onload>`, quote-breakout payloads to inert entities (e.g. `<script>` → `&lt;script&gt;`); exact-escape assertion passes.
- AC2 (zero unescaped server interpolations): PASS — all `${...}` inside `innerHTML` templates are `escapeHTML(...)` or allowlisted non-server literals (`color` ternary literals, loop index `i+1`, `'+'` literal); sole `innerHTML`-with-`+` concat (L831) is escaped; remaining `${...}` elsewhere are non-DOM (`Error`, URL `encodeURIComponent`), `textContent`, or numeric style — verified by `rg -n '\$\{'` audit.
- AC3 (benign render unchanged): PASS — benign strings/numbers/`null` assertions pass; loopback `GET /dashboard` → 200 (50547 bytes) contains `escapeHTML` and intact `test-rows`/`top-configs` markers; `test_dashboard_server.py` 65 passed, 2 skipped (no BE regression).

## Verification commands (executed)
- `rg -n "innerHTML" dashboard/index.html` (26 hits, all static or escaped)
- `rg -n "escapeHTML" dashboard/index.html` (helper + 14 escaped sites)
- `rg -n '\$\{' dashboard/index.html` + allowlist audit (see AC2)
- `node /tmp/aep-xss01-verify.js` → RESULT PASS (25 checks)
- inline-JS extract → `node --check` → JS syntax OK
- loopback serve `python3 dashboard_server.py --host 127.0.0.1 --port 8137`, `GET /dashboard` → 200
- `python3 -m pytest test_dashboard_server.py -x -q` → 65 passed, 2 skipped
- Full log appended to `docs/aep-remediation/20260902-closeout/verification-log.md` (§P1-XSS-01).

## BHS v3.7.1 (no self-score)
No Tier A/B score claimed for this fix. Scoring deferred to a fresh/different Tier-B agent per closeout rule (OFFICIAL=min carried 76, NO-MERGE; only 100 ships; max 5 Tier-A iters). Challenger for the fix must be an agent distinct from this fix agent and all prior Tier-B scorers.
